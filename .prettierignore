# Checksum-immutable migration ladder: reformatting = breaking (golden rule 6).
db/migrations/
db/schema.fingerprint

# Real exported CR payloads, kept verbatim for provenance.
fixtures/

# Build output and lockfile.
dist/
package-lock.json

# Hand-authored prose; prettier pairs glob asterisks as emphasis and corrupts them.
*.md

# Upstream mirror (git subtree of jthingelstad/cr-agent-api-docs). Verbatim
# or every subtree pull conflicts -- same rule as fixtures/.
docs/cr-api-docs/
