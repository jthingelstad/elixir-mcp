{
  "name": "@elixir-mcp/site",
  "version": "0.1.0",
  "description": "elixir.poapkings.com static half - home, docs, updates, changelog, and the machine-readable surfaces (llms.txt, tools.json, sitemap). Real HTML for readers, crawlers, and agents; the app owns everything behind a session.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "test": "node ../../infra/scripts/build-site.mjs --skip-stats && node --test"
  },
  "dependencies": {
    "@elixir-mcp/contracts": "*",
    "@elixir-mcp/design": "*",
    "marked": "^18.0.11"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "jsdom": "^30.0.1",
    "lucide-static": "^1.44.0"
  }
}
