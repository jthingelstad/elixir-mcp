---
layout: doc.njk
title: "Docs"
description: "Documentation for Elixir MCP: connecting your agent, the difference between users, agents and integrations, the full tool reference, how the numbers are made, and how the service treats your data."
permalink: /docs/index.html
canonical: /docs
navActive: docs
---
<h1>Documentation</h1>

<p>Elixir MCP records the Clash Royale history the official API doesn't keep,
and serves it to your own agent. If you just want that working, the
<a href="/docs/quickstart">quickstart</a> is five minutes. If you are trying to
work out whether you want a personal connection or an agent for your clan,
read <a href="/docs/connections">users, agents and integrations</a> first —
it's the distinction everything else rests on.</p>

{%- for s in collections.docSections %}
<h2>{{ s.label }}</h2>
<dl class="outline">
  {%- for d in s.pages %}
  <dt><a href="{{ d | pageUrl }}">{{ d.data.title }}</a></dt>
  <dd>{{ d.data.description }}</dd>
  {%- endfor %}
</dl>
{%- endfor %}

<h2>For agents</h2>
<dl class="outline">
  <dt><a href="/llms.txt">/llms.txt</a></dt>
  <dd>What this service is and where everything lives, in one short file.</dd>
  <dt><a href="/llms-full.txt">/llms-full.txt</a></dt>
  <dd>Every documentation page and the whole tool reference in one fetch.</dd>
  <dt><a href="/tools.json">/tools.json</a></dt>
  <dd>The tool registry as JSON: names, groups, descriptions, arguments, and the capability each one requires.</dd>
  <dt><a href="/api/public/stats">/api/public/stats</a></dt>
  <dd>Live corpus totals and the full-history daily series. No authentication.</dd>
  <dt><a href="/data/changelog">Contract changelog</a></dt>
  <dd>Every contract version and what shipped in it — the same list <code>elixir_changelog</code> returns.</dd>
</dl>
