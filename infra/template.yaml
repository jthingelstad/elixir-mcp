AWSTemplateFormatVersion: "2010-09-09"
Description: elixir-mcp - Clash Royale history recorder and MCP server (unofficial, not endorsed by Supercell)

# One stack (docs/ENGINEERING.md). NAT-free VPC: DB-touching Lambdas reach
# SQS through an interface endpoint; the email relay lives OUTSIDE the VPC
# and is the only compute with internet egress. Secrets arrive via
# CloudFormation dynamic references from the app secret (created by
# bootstrap); CR API tokens exist only on gateway operator machines.

Parameters:
  CodeBucket:
    Type: String
    Description: S3 bucket holding Lambda bundles (created by bootstrap)
  WebApiCodeKey:
    Type: String
  McpCodeKey:
    Type: String
  SchedulerCodeKey:
    Type: String
  EmailRelayCodeKey:
    Type: String
  MigrateCodeKey:
    Type: String
  JobsCodeKey:
    Type: String
  AppSecretName:
    Type: String
    Default: elixir-mcp/app
    Description: Secrets Manager secret with db_password, session_secret, jmap_token (URL-safe values)
  SiteCertificateArn:
    Type: String
    Default: ""
    Description: ACM cert for elixir.poapkings.com (us-east-1); empty deploys without the alias
  MonthlyCostAlarmUsd:
    Type: Number
    Default: 40
  SchedulerTickMinutes:
    Type: Number
    Default: 5
    MinValue: 1
    MaxValue: 60
    Description: Planner tick cadence; tune with a parameter-only stack update, no code change
  CollectorMinEnforce:
    Type: String
    Default: "0"
    AllowedValues: ["0", "1"]
    Description: Refuse lease and submit to a collector below min_client_version. Config is never refused, since it is the channel a stale client updates through. Flip only when every collector reports a version at or above the minimum.
  EmailTransport:
    Type: String
    Default: jmap
    AllowedValues: [jmap, ses]
    Description: >-
      How the relay sends mail from elixir@poapkings.com. jmap is the
      Fastmail account; ses is the poapkings.com identity below, usable
      once its DKIM CNAMEs have verified at Namecheap and the account is
      out of the SES sandbox (2026-09-16). PRESERVED: a deploy never
      flips it back.
  DbSnapshotIdentifier:
    Type: String
    Default: ""
    Description: >-
      Set ONCE (2026-09-06 encryption migration) to restore the instance
      from an encrypted snapshot; PRESERVED thereafter. Changing it
      REPLACES the database - never let it reset to default.
  OriginSecret:
    Type: String
    NoEcho: true
    Default: ""
    Description: >-
      Shared secret CloudFront attaches to every origin request as the
      x-elixir-origin header; both API Lambdas refuse requests without it,
      so a direct execute-api hit cannot forge the viewer headers the doors
      record. Generated by deploy.mjs on its first deploy and PRESERVED.

Conditions:
  HasSiteCert: !Not [!Equals [!Ref SiteCertificateArn, ""]]
  HasDbSnapshot: !Not [!Equals [!Ref DbSnapshotIdentifier, ""]]
  HasOriginSecret: !Not [!Equals [!Ref OriginSecret, ""]]
  TickIsOneMinute: !Equals [!Ref SchedulerTickMinutes, 1]

Resources:
  # ------------------------------------------------------------ networking
  Vpc:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: 10.42.0.0/24
      EnableDnsSupport: true
      EnableDnsHostnames: true
      Tags: [{ Key: Name, Value: elixir-mcp }]

  PrivateSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref Vpc
      CidrBlock: 10.42.0.0/26
      AvailabilityZone: !Select [0, !GetAZs ""]
      Tags: [{ Key: Name, Value: elixir-mcp-private-a }]

  PrivateSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref Vpc
      CidrBlock: 10.42.0.64/26
      AvailabilityZone: !Select [1, !GetAZs ""]
      Tags: [{ Key: Name, Value: elixir-mcp-private-b }]

  LambdaSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: elixir-mcp VPC Lambdas
      VpcId: !Ref Vpc

  DbSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: elixir-mcp RDS; Postgres from Lambdas only
      VpcId: !Ref Vpc
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 5432
          ToPort: 5432
          SourceSecurityGroupId: !Ref LambdaSecurityGroup

  EndpointSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: elixir-mcp SQS interface endpoint; HTTPS from Lambdas
      VpcId: !Ref Vpc
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 443
          ToPort: 443
          SourceSecurityGroupId: !Ref LambdaSecurityGroup

  SqsEndpoint:
    Type: AWS::EC2::VPCEndpoint
    Properties:
      VpcId: !Ref Vpc
      ServiceName: !Sub com.amazonaws.${AWS::Region}.sqs
      VpcEndpointType: Interface
      PrivateDnsEnabled: true
      SubnetIds: [!Ref PrivateSubnetA, !Ref PrivateSubnetB]
      SecurityGroupIds: [!Ref EndpointSecurityGroup]

  # S3 reachability for the VPC Lambdas (payload archive). A Gateway
  # endpoint is free; it needs an explicit route table, so the private
  # subnets move off the VPC main table (local route is implicit).
  PrivateRouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref Vpc
      Tags: [{ Key: Name, Value: elixir-mcp-private }]

  PrivateSubnetARouteAssoc:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref PrivateSubnetA
      RouteTableId: !Ref PrivateRouteTable

  PrivateSubnetBRouteAssoc:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref PrivateSubnetB
      RouteTableId: !Ref PrivateRouteTable

  S3Endpoint:
    Type: AWS::EC2::VPCEndpoint
    Properties:
      VpcId: !Ref Vpc
      ServiceName: !Sub com.amazonaws.${AWS::Region}.s3
      VpcEndpointType: Gateway
      RouteTableIds: [!Ref PrivateRouteTable]

  # -------------------------------------------------------------- database
  DbSubnetGroup:
    Type: AWS::RDS::DBSubnetGroup
    Properties:
      DBSubnetGroupDescription: elixir-mcp private subnets
      SubnetIds: [!Ref PrivateSubnetA, !Ref PrivateSubnetB]

  Database:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    DependsOn: DbPostgresLogGroup
    Properties:
      # Renamed at the 2026-09-06 encryption migration: restoring from
      # the encrypted snapshot is a REPLACEMENT, and the retained old
      # instance still holds the original identifier.
      DBInstanceIdentifier: elixir-mcp-enc
      Engine: postgres
      EngineVersion: "17"
      DBInstanceClass: db.t4g.micro
      AllocatedStorage: "20"
      MaxAllocatedStorage: 100
      StorageType: gp3
      DBSnapshotIdentifier:
        !If [HasDbSnapshot, !Ref DbSnapshotIdentifier, !Ref AWS::NoValue]
      # Snapshot restores take name/encryption from the snapshot itself.
      DBName: !If [HasDbSnapshot, !Ref AWS::NoValue, elixir_mcp]
      StorageEncrypted: !If [HasDbSnapshot, !Ref AWS::NoValue, true]
      MasterUsername: !If [HasDbSnapshot, !Ref AWS::NoValue, elixir]
      MasterUserPassword: !Sub "{{resolve:secretsmanager:${AppSecretName}:SecretString:db_password}}"
      DBSubnetGroupName: !Ref DbSubnetGroup
      VPCSecurityGroups: [!Ref DbSecurityGroup]
      MultiAZ: false
      PubliclyAccessible: false
      BackupRetentionPeriod: 7
      DeletionProtection: true
      EnablePerformanceInsights: false
      AutoMinorVersionUpgrade: true
      # Enhanced Monitoring at one minute (review 2026-09-11, item 12):
      # the two memory recoveries that day were invisible from the
      # instance metrics alone - FreeableMemory and SwapUsage say the box
      # is squeezed, not by whom. This is the OS-level split (Postgres vs
      # page cache vs RDS agent) the instance-class decision waits on.
      # About $0.50/month of CloudWatch Logs.
      MonitoringInterval: 60
      MonitoringRoleArn: !GetAtt DbMonitoringRole.Arn
      # The statement side of a stall (2026-09-12). Enhanced Monitoring
      # says the box is squeezed; the Postgres log says by WHICH query,
      # and whether it was waiting on a lock. Exported to CloudWatch so
      # it survives the instance and can be queried beside the Lambda
      # lines. The custom parameter group only takes effect after one
      # manual reboot (a group swap is applied at reboot even when every
      # parameter in it is dynamic) - docs/NOTES.md 2026-09-12.
      DBParameterGroupName: !Ref DbParameterGroup
      EnableCloudwatchLogsExports: [postgresql]

  DbParameterGroup:
    Type: AWS::RDS::DBParameterGroup
    Properties:
      Family: postgres17
      Description: elixir-mcp postgres17 - slow statements and lock waits to the log
      Parameters:
        # One second: at this volume the whole log is a few lines an hour
        # in steady state and every line is one worth reading.
        log_min_duration_statement: 1000
        log_lock_waits: 1
        # Autovacuum runs over one second are the other thing that eats
        # a micro's memory and I/O without any request naming it.
        log_autovacuum_min_duration: 1000
        # 88 MB, in 8 kB pages. The engine default on a micro is 180 MB;
        # RDS cut it to 88 MB itself under memory pressure on 2026-09-11
        # and the ten hours at 88 MB were the only swap-free window the
        # instance has had (9-27 MB against 56-100 MB). Static: applies
        # at the next reboot. docs/NOTES.md 2026-09-12.
        shared_buffers: 11264

  # Declared so retention is set before RDS starts writing; left to RDS
  # the group is created with never-expire.
  DbPostgresLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/rds/instance/elixir-mcp-enc/postgresql
      RetentionInDays: 14

  DbMonitoringRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: elixir-mcp-rds-monitoring
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: monitoring.rds.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonRDSEnhancedMonitoringRole

  # ---------------------------------------------------------------- queues
  EmailQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: elixir-mcp-email
      VisibilityTimeout: 120
      MessageRetentionPeriod: 345600
      SqsManagedSseEnabled: true
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt EmailDlq.Arn
        maxReceiveCount: 5

  EmailDlq:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: elixir-mcp-email-dlq
      MessageRetentionPeriod: 1209600
      SqsManagedSseEnabled: true

  # ------------------------------------------------------------------- iam
  # One role per function (#24). These five used to share
  # elixir-mcp-vpc-lambda, so the two internet-facing Lambdas carried
  # the union of everything any of them needed - including archive
  # Get/Put/List that only three of them use at all. Each role below
  # grants exactly what its function's code reaches for, verified
  # against the ARCHIVE_BUCKET / EMAIL_QUEUE_URL environment each one
  # actually receives.
  #
  # The shared PostgreSQL credential is NOT split here. Doing that
  # properly means separate database roles with migration-managed
  # GRANT/REVOKE, which is a larger and riskier change than an IAM
  # rewrite and wants its own pass.

  # Public web + collector door: mails, reads the email DLQ for the
  # status page, and archives collector submissions at admission.
  WebApiRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: elixir-mcp-web-api
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole
      Policies:
        - PolicyName: elixir-mcp-web-api
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: [sqs:SendMessage, sqs:GetQueueUrl]
                Resource: !GetAtt EmailQueue.Arn
              - Effect: Allow
                Action: [sqs:GetQueueAttributes, sqs:GetQueueUrl]
                Resource: !GetAtt EmailDlq.Arn
              - Effect: Allow
                Action: [s3:PutObject, s3:GetObject]
                Resource: !Sub
                  - ${Arn}/payloads/*
                  - Arn: !GetAtt ArchiveBucket.Arn
              # Captured tool calls (review 2026-09-10 Part 5): the
              # console's call record reads one body at a time through
              # the session check. Read only; the MCP door writes them.
              - Effect: Allow
                Action: s3:GetObject
                Resource: !Sub
                  - ${Arn}/calls/*
                  - Arn: !GetAtt ArchiveBucket.Arn
              - Effect: Allow
                Action: s3:ListBucket
                Resource: !GetAtt ArchiveBucket.Arn

  # The MCP/OAuth door sends login mail and, since 0063, writes the
  # request/response of every tool call under calls/ (nothing else on
  # the bucket: the payload archive is the collector door's).
  McpRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: elixir-mcp-mcp
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole
      Policies:
        - PolicyName: elixir-mcp-mcp
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: [sqs:SendMessage, sqs:GetQueueUrl]
                Resource: !GetAtt EmailQueue.Arn
              - Effect: Allow
                Action: s3:PutObject
                Resource: !Sub
                  - ${Arn}/calls/*
                  - Arn: !GetAtt ArchiveBucket.Arn

  # The tick only touches Postgres and stdout (ledger health rides EMF,
  # not the CloudWatch API). No queues, no bucket.
  SchedulerRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: elixir-mcp-scheduler
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole

  # Migrations plus the archive export/verify ops.
  MigrateRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: elixir-mcp-migrate
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole
      Policies:
        - PolicyName: elixir-mcp-migrate
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: [s3:PutObject, s3:GetObject]
                Resource: !Sub
                  - ${Arn}/payloads/*
                  - Arn: !GetAtt ArchiveBucket.Arn
              - Effect: Allow
                Action: s3:ListBucket
                Resource: !GetAtt ArchiveBucket.Arn

  # The weekly sweep HEAD-verifies an S3 twin before deleting the
  # Postgres row. HeadObject is authorized by s3:GetObject - READ only,
  # deliberately: the job that deletes the database copy must not be
  # able to write or overwrite the copy it is checking against.
  JobsRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: elixir-mcp-jobs
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole
      Policies:
        - PolicyName: elixir-mcp-jobs
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: s3:GetObject
                Resource: !Sub
                  - ${Arn}/payloads/*
                  - Arn: !GetAtt ArchiveBucket.Arn
              - Effect: Allow
                Action: s3:ListBucket
                Resource: !GetAtt ArchiveBucket.Arn

  EmailRelayRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: elixir-mcp-email-relay
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: elixir-mcp-email-relay
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  [
                    sqs:ReceiveMessage,
                    sqs:DeleteMessage,
                    sqs:GetQueueAttributes,
                    sqs:GetQueueUrl,
                  ]
                Resource: !GetAtt EmailQueue.Arn
              # Send as elixir@poapkings.com and nothing else: the identity
              # and the configuration set are the only resources, and the
              # From address is pinned by condition. SES authorizes a send
              # against the ADDRESS identity ARN even when only the domain
              # is verified (first send, 2026-09-17: AccessDenied on
              # identity/elixir@poapkings.com), so both spellings are here.
              - Effect: Allow
                Action: [ses:SendEmail, ses:SendRawEmail]
                Resource:
                  - !Sub arn:aws:ses:${AWS::Region}:${AWS::AccountId}:identity/poapkings.com
                  - !Sub arn:aws:ses:${AWS::Region}:${AWS::AccountId}:identity/elixir@poapkings.com
                  - !Sub arn:aws:ses:${AWS::Region}:${AWS::AccountId}:configuration-set/elixir-mcp
                Condition:
                  StringEquals:
                    ses:FromAddress: elixir@poapkings.com

  # ----------------------------------------------------------------- email
  # Sending as elixir@poapkings.com through SES (2026-09-16; Fastmail is a
  # mailbox, not a sender, and RECEIVING stays there - nothing here touches
  # the apex MX). Easy DKIM signs with SES-managed keys: the three CNAMEs
  # in Outputs go to Namecheap by hand. bounce.poapkings.com is the custom
  # MAIL FROM so SPF aligns with the From domain (Gmail/Yahoo bulk rules);
  # it needs the MX and TXT in Outputs. Bounces and complaints go to the
  # alarm topic, so they reach the ops queue like everything else; open
  # and click are NOT in the event list, which is what keeps the mail
  # pixel-free and the links unwrapped. Forwarding bounce notifications to
  # the From mailbox is off for the same reason: the queue is the record.
  SenderConfigurationSet:
    Type: AWS::SES::ConfigurationSet
    Properties:
      Name: elixir-mcp
      SendingOptions: { SendingEnabled: true }
      ReputationOptions: { ReputationMetricsEnabled: true }
      SuppressionOptions: { SuppressedReasons: [BOUNCE, COMPLAINT] }

  SenderEvents:
    Type: AWS::SES::ConfigurationSetEventDestination
    Properties:
      ConfigurationSetName: !Ref SenderConfigurationSet
      EventDestination:
        Name: ops-alerts
        Enabled: true
        MatchingEventTypes:
          [BOUNCE, COMPLAINT, REJECT, RENDERING_FAILURE, DELIVERY_DELAY]
        SnsDestination: { TopicARN: !Ref AlarmTopic }

  SenderIdentity:
    Type: AWS::SES::EmailIdentity
    Properties:
      EmailIdentity: poapkings.com
      DkimAttributes: { SigningEnabled: true }
      DkimSigningAttributes: { NextSigningKeyLength: RSA_2048_BIT }
      MailFromAttributes:
        MailFromDomain: bounce.poapkings.com
        BehaviorOnMxFailure: USE_DEFAULT_VALUE
      ConfigurationSetAttributes:
        ConfigurationSetName: !Ref SenderConfigurationSet
      FeedbackAttributes: { EmailForwardingEnabled: false }

  # --------------------------------------------------------------- lambdas
  WebApiLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/lambda/elixir-mcp-web-api
      RetentionInDays: 30

  WebApiFunction:
    Type: AWS::Lambda::Function
    DependsOn: WebApiLogGroup
    Properties:
      FunctionName: elixir-mcp-web-api
      # A ceiling, not a target (#23). Each invocation opens its own
      # PostgreSQL client against a db.t4g.micro, so unbounded Lambda
      # concurrency makes the DATABASE the thing that fails first.
      # Measured traffic is a handful of requests a minute; 20 is orders
      # of magnitude above that and still a hard stop on a runaway
      # client or a bill.
      ReservedConcurrentExecutions: 20
      Runtime: nodejs24.x
      Architectures: [arm64]
      Handler: index.handler
      MemorySize: 512
      Timeout: 20
      Role: !GetAtt WebApiRole.Arn
      Code: { S3Bucket: !Ref CodeBucket, S3Key: !Ref WebApiCodeKey }
      VpcConfig:
        SubnetIds: [!Ref PrivateSubnetA, !Ref PrivateSubnetB]
        SecurityGroupIds: [!Ref LambdaSecurityGroup]
      Environment:
        Variables:
          DATABASE_URL: !Sub "postgres://elixir:{{resolve:secretsmanager:${AppSecretName}:SecretString:db_password}}@${Database.Endpoint.Address}:5432/elixir_mcp?sslmode=no-verify"
          SESSION_SECRET: !Sub "{{resolve:secretsmanager:${AppSecretName}:SecretString:session_secret}}"
          ARCHIVE_BUCKET: !Ref ArchiveBucket
          EMAIL_QUEUE_URL: !Ref EmailQueue
          COLLECTOR_MIN_ENFORCE: !Ref CollectorMinEnforce
          ORIGIN_SECRET: !Ref OriginSecret
          OWNER_NOTIFY_EMAIL: jamie@thingelstad.com

  McpLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/lambda/elixir-mcp-mcp
      RetentionInDays: 30

  McpFunction:
    Type: AWS::Lambda::Function
    DependsOn: McpLogGroup
    Properties:
      FunctionName: elixir-mcp-mcp
      # Same reasoning as the web API: bound the database, not just the
      # function (#23).
      ReservedConcurrentExecutions: 20
      Runtime: nodejs24.x
      Architectures: [arm64]
      Handler: index.handler
      MemorySize: 512
      Timeout: 25
      Role: !GetAtt McpRole.Arn
      Code: { S3Bucket: !Ref CodeBucket, S3Key: !Ref McpCodeKey }
      VpcConfig:
        SubnetIds: [!Ref PrivateSubnetA, !Ref PrivateSubnetB]
        SecurityGroupIds: [!Ref LambdaSecurityGroup]
      Environment:
        Variables:
          DATABASE_URL: !Sub "postgres://elixir:{{resolve:secretsmanager:${AppSecretName}:SecretString:db_password}}@${Database.Endpoint.Address}:5432/elixir_mcp?sslmode=no-verify"
          OAUTH_ISSUER: https://elixir.poapkings.com
          EMAIL_QUEUE_URL: !Ref EmailQueue
          ORIGIN_SECRET: !Ref OriginSecret
          # Call capture is on when this is set (services/mcp/src/capture.mjs).
          ARCHIVE_BUCKET: !Ref ArchiveBucket
          # The Status page's "work waiting" gauge re-runs the scheduler's
          # eligibility query, so it must see the same A/B arm and know the
          # tick cadence. Keep ELIXIR_LOSS_BOUND identical to the scheduler's.
          ELIXIR_LOSS_BOUND: half
          SCHEDULER_TICK_MINUTES: !Ref SchedulerTickMinutes
          OWNER_NOTIFY_EMAIL: jamie@thingelstad.com
          # Consent honours the site session and mints one (0083). Same
          # secret as the site API, so a cookie either origin set resolves
          # at the other.
          SESSION_SECRET: !Sub "{{resolve:secretsmanager:${AppSecretName}:SecretString:session_secret}}"

  SchedulerLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/lambda/elixir-mcp-scheduler
      RetentionInDays: 30

  SchedulerFunction:
    Type: AWS::Lambda::Function
    DependsOn: SchedulerLogGroup
    Properties:
      FunctionName: elixir-mcp-scheduler
      Runtime: nodejs24.x
      Architectures: [arm64]
      Handler: index.handler
      MemorySize: 256
      Timeout: 50
      ReservedConcurrentExecutions: 1
      Role: !GetAtt SchedulerRole.Arn
      Code: { S3Bucket: !Ref CodeBucket, S3Key: !Ref SchedulerCodeKey }
      VpcConfig:
        SubnetIds: [!Ref PrivateSubnetA, !Ref PrivateSubnetB]
        SecurityGroupIds: [!Ref LambdaSecurityGroup]
      Environment:
        Variables:
          # Loss-aware battlelog bound A/B arm (docs/FETCH-LOOP-AUDIT-2026-09-09.md):
          # off | half | all. `half` = the stable hash-selected half of subjects;
          # promote to `all` once ab_yield shows the treated arm's gap rate under
          # 1.5% of polls against the control arm over the same clock hours.
          ELIXIR_LOSS_BOUND: half
          DATABASE_URL: !Sub "postgres://elixir:{{resolve:secretsmanager:${AppSecretName}:SecretString:db_password}}@${Database.Endpoint.Address}:5432/elixir_mcp?sslmode=no-verify"

  SchedulerRule:
    Type: AWS::Events::Rule
    Properties:
      Name: elixir-mcp-scheduler-tick
      # EventBridge insists on the singular unit for exactly 1.
      ScheduleExpression: !If
        - TickIsOneMinute
        - rate(1 minute)
        - !Sub rate(${SchedulerTickMinutes} minutes)
      State: ENABLED
      Targets:
        - Id: scheduler
          Arn: !GetAtt SchedulerFunction.Arn

  SchedulerInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref SchedulerFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt SchedulerRule.Arn

  EmailRelayLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/lambda/elixir-mcp-email-relay
      RetentionInDays: 30

  EmailRelayFunction:
    Type: AWS::Lambda::Function
    DependsOn: EmailRelayLogGroup
    Properties:
      FunctionName: elixir-mcp-email-relay
      Runtime: nodejs24.x
      Architectures: [arm64]
      Handler: index.handler
      MemorySize: 256
      Timeout: 60
      Role: !GetAtt EmailRelayRole.Arn
      Code: { S3Bucket: !Ref CodeBucket, S3Key: !Ref EmailRelayCodeKey }
      Environment:
        Variables:
          JMAP_TOKEN: !Sub "{{resolve:secretsmanager:${AppSecretName}:SecretString:jmap_token}}"
          FROM_EMAIL: elixir@poapkings.com
          EMAIL_TRANSPORT: !Ref EmailTransport
          SES_CONFIGURATION_SET: !Ref SenderConfigurationSet
          TINYLYTICS_API_TOKEN: !Sub "{{resolve:secretsmanager:${AppSecretName}:SecretString:tinylytics_api_token}}"
          TINYLYTICS_SITE_ID: "3762"
          BUTTONDOWN_API_TOKEN: !Sub "{{resolve:secretsmanager:${AppSecretName}:SecretString:buttondown_api_token}}"

  EmailEventSource:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !Ref EmailRelayFunction
      EventSourceArn: !GetAtt EmailQueue.Arn
      BatchSize: 5
      FunctionResponseTypes: [ReportBatchItemFailures]

  MigrateLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/lambda/elixir-mcp-migrate
      RetentionInDays: 30

  MigrateFunction:
    Type: AWS::Lambda::Function
    DependsOn: MigrateLogGroup
    Properties:
      FunctionName: elixir-mcp-migrate
      Runtime: nodejs24.x
      Architectures: [arm64]
      Handler: index.handler
      MemorySize: 1024
      Timeout: 300
      ReservedConcurrentExecutions: 1
      Role: !GetAtt MigrateRole.Arn
      Code: { S3Bucket: !Ref CodeBucket, S3Key: !Ref MigrateCodeKey }
      VpcConfig:
        SubnetIds: [!Ref PrivateSubnetA, !Ref PrivateSubnetB]
        SecurityGroupIds: [!Ref LambdaSecurityGroup]
      Environment:
        Variables:
          DATABASE_URL: !Sub "postgres://elixir:{{resolve:secretsmanager:${AppSecretName}:SecretString:db_password}}@${Database.Endpoint.Address}:5432/elixir_mcp?sslmode=no-verify"
          ARCHIVE_BUCKET: !Ref ArchiveBucket

  # Scheduled product jobs (review item 5): the timer-driven work —
  # clan pulse, weekly sweeps — runs in its own Lambda so the
  # schema-capable migrate function never fires on a schedule.
  JobsLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/lambda/elixir-mcp-jobs
      RetentionInDays: 30

  JobsFunction:
    Type: AWS::Lambda::Function
    DependsOn: JobsLogGroup
    Properties:
      FunctionName: elixir-mcp-jobs
      Runtime: nodejs24.x
      Architectures: [arm64]
      Handler: index.handler
      MemorySize: 1024
      # 900 since 0121: a full-month season rollup is minutes on the
      # micro, and the nightly job may fill one past season after the
      # running one. Reserved concurrency 1 keeps runs from overlapping.
      Timeout: 900
      ReservedConcurrentExecutions: 1
      Role: !GetAtt JobsRole.Arn
      Code: { S3Bucket: !Ref CodeBucket, S3Key: !Ref JobsCodeKey }
      VpcConfig:
        SubnetIds: [!Ref PrivateSubnetA, !Ref PrivateSubnetB]
        SecurityGroupIds: [!Ref LambdaSecurityGroup]
      Environment:
        Variables:
          DATABASE_URL: !Sub "postgres://elixir:{{resolve:secretsmanager:${AppSecretName}:SecretString:db_password}}@${Database.Endpoint.Address}:5432/elixir_mcp?sslmode=no-verify"
          ARCHIVE_BUCKET: !Ref ArchiveBucket

  # Hourly Postgres payload sweep (DATA-TOOLS section 1): superseded
  # payload rows leave Postgres once their S3 twin is HEAD-verified, and
  # the JSON of rows not fetched for two hours is nulled the same way
  # (0071/0072: the column is a cache of the archive; the recorder
  # refetches most entities daily, so a daily sweep kept nearly all of
  # it resident). Hourly since 2026-09-11; was weekly.
  PayloadSweepRule:
    Type: AWS::Events::Rule
    Properties:
      Name: elixir-mcp-payload-sweep
      ScheduleExpression: cron(15 * * * ? *)
      Targets:
        - Arn: !GetAtt JobsFunction.Arn
          Id: payload-sweep
          Input: '{"sweep_payloads": true, "sweep_operational": true}'

  PayloadSweepPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref JobsFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt PayloadSweepRule.Arn

  # Nightly battle-activity histogram (0084): one row per recorded player,
  # a decayed 24x7 rhythm and a year of UTC daily counts, rebuilt from
  # battle_participant. 05:30Z is the fleet's quietest hour (2026-09-13
  # read: 91-100 battlelog fetches/hr at 06-09Z against 150 in the US
  # evening) and after the UTC day has closed, so yesterday's count is
  # final. Read by the console today and by the scheduler later.
  ActivityHistogramRule:
    Type: AWS::Events::Rule
    Properties:
      Name: elixir-mcp-activity-histogram
      ScheduleExpression: cron(30 5 * * ? *)
      Targets:
        - Arn: !GetAtt JobsFunction.Arn
          Id: activity-histogram
          Input: '{"activity_histogram": true}'

  ActivityHistogramPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref JobsFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt ActivityHistogramRule.Arn

  # The season meta rollups (0121): nightly, the running season rebuilt
  # from the raw rows (the only writer of distinct players and card
  # pairs) and any ended season filled in; hourly at :45, the counters
  # incremented from battles created since the cursor. 04:40Z sits in
  # the quiet hours before the histogram and after the UTC day closes;
  # the two nightly jobs never overlap (reserved concurrency 1 queues,
  # but the rollup is ~1 min a season on the micro).
  MetaRollupNightlyRule:
    Type: AWS::Events::Rule
    Properties:
      Name: elixir-mcp-meta-rollup-nightly
      ScheduleExpression: cron(40 4 * * ? *)
      Targets:
        - Arn: !GetAtt JobsFunction.Arn
          Id: meta-rollup-nightly
          Input: '{"meta_rollup_nightly": true}'

  MetaRollupNightlyPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref JobsFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt MetaRollupNightlyRule.Arn

  MetaRollupHourlyRule:
    Type: AWS::Events::Rule
    Properties:
      Name: elixir-mcp-meta-rollup-hourly
      ScheduleExpression: cron(45 * * * ? *)
      Targets:
        - Arn: !GetAtt JobsFunction.Arn
          Id: meta-rollup-hourly
          Input: '{"meta_rollup_hourly": true}'

  MetaRollupHourlyPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref JobsFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt MetaRollupHourlyRule.Arn

  # The nightly payload shape census (time-series review 2.7, 0132):
  # twenty archived objects per endpoint from the last day against the
  # field manifest; findings filed into feedback (surface recorder), the
  # PayloadShapeFindings metric emitted. 05:05Z: after the 04:40Z rollup,
  # before the 05:30Z histogram, the UTC day closed.
  ShapeCensusRule:
    Type: AWS::Events::Rule
    Properties:
      Name: elixir-mcp-shape-census
      ScheduleExpression: cron(5 5 * * ? *)
      Targets:
        - Arn: !GetAtt JobsFunction.Arn
          Id: shape-census
          Input: '{"shape_census": true}'

  ShapeCensusPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref JobsFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt ShapeCensusRule.Arn

  # ------------------------------------------------------------ front doors
  SiteApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: elixir-mcp-site-api
      ProtocolType: HTTP
      Target: !GetAtt WebApiFunction.Arn

  SiteApiPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref WebApiFunction
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${SiteApi}/*

  McpApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: elixir-mcp-mcp-api
      ProtocolType: HTTP
      Target: !GetAtt McpFunction.Arn

  McpApiPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref McpFunction
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${McpApi}/*

  # The payload archive (DATA-TOOLS): every admitted payload's raw bytes,
  # Hive-partitioned, content-addressed. S3 is the system of record for
  # raw observations; Postgres keeps the hot set.
  ArchiveBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !Sub elixir-mcp-archive-${AWS::AccountId}
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      # Encryption stated rather than inherited from the account default.
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault: { SSEAlgorithm: AES256 }
            BucketKeyEnabled: true
      # This bucket is the system of record: canonical projections are
      # rebuilt FROM it. Without versioning a PutObject over a known key
      # is silent and unrecoverable - and PutObject is exactly what the
      # internet-facing ingest path holds (#24). Deleting still needs
      # s3:DeleteObject, which nothing is granted.
      VersioningConfiguration:
        Status: Enabled
      LifecycleConfiguration:
        Rules:
          - Id: payloads-to-ia
            Status: Enabled
            Prefix: payloads/
            Transitions:
              - StorageClass: STANDARD_IA
                TransitionInDays: 30
          # Captured tool calls follow the call log's own rule: the
          # arguments leave the audit row after 90 days, so the bodies
          # do too (docs/limits retention table). The housekeeping job
          # flips mcp_call_audit.captured on the same clock.
          - Id: calls-expire
            Status: Enabled
            Prefix: calls/
            Transitions:
              - StorageClass: STANDARD_IA
                TransitionInDays: 30
            ExpirationInDays: 90
          # Payloads are content-addressed, so an overwrite means
          # something went wrong rather than something changed. Keep a
          # year of superseded versions - long enough to notice and
          # recover, bounded so a loop cannot grow the bill forever.
          - Id: expire-superseded-versions
            Status: Enabled
            NoncurrentVersionExpiration:
              NoncurrentDays: 365
          - Id: abort-incomplete-uploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 7

  GlueArchiveDatabase:
    Type: AWS::Glue::Database
    Properties:
      CatalogId: !Ref AWS::AccountId
      DatabaseInput:
        Name: elixir_mcp_archive
        Description: Athena over the S3 payload archive (raw CR API payloads, one JSON doc per object)

  # One raw-JSON-lines table with partition projection - no crawler, no
  # maintenance. Queries pin entity (injected projection); cross-entity
  # archaeology is DuckDB's job (DATA-TOOLS section 2).
  GlueArchivePayloadsTable:
    Type: AWS::Glue::Table
    Properties:
      CatalogId: !Ref AWS::AccountId
      DatabaseName: !Ref GlueArchiveDatabase
      TableInput:
        Name: payloads
        TableType: EXTERNAL_TABLE
        Parameters:
          classification: json
          projection.enabled: "true"
          projection.endpoint.type: enum
          projection.endpoint.values: player_battlelog,player,clan,currentriverrace,riverracelog,cards
          projection.entity.type: injected
          projection.dt.type: date
          projection.dt.range: 2026-02-01,NOW
          projection.dt.format: yyyy-MM-dd
          storage.location.template: !Sub s3://elixir-mcp-archive-${AWS::AccountId}/payloads/endpoint=${!endpoint}/entity=${!entity}/dt=${!dt}
        PartitionKeys:
          - { Name: endpoint, Type: string }
          - { Name: entity, Type: string }
          - { Name: dt, Type: string }
        StorageDescriptor:
          Location: !Sub s3://elixir-mcp-archive-${AWS::AccountId}/payloads/
          InputFormat: org.apache.hadoop.mapred.TextInputFormat
          OutputFormat: org.apache.hadoop.hive.ql.io.HiveIgnoreKeyTextOutputFormat
          SerdeInfo:
            # LazySimpleSerDe's default field delimiter is \u0001, which
            # never appears in JSON text - the whole line lands in `json`.
            SerializationLibrary: org.apache.hadoop.hive.serde2.lazy.LazySimpleSerDe
          Columns:
            - { Name: json, Type: string }

  # CloudFront standard (access) logs for the one hostname. Turned on
  # 2026-09-12 after a browser-side stall left no trace at the origin:
  # Lambda, API Gateway and CloudFront metrics were all clean, so the
  # edge's own request log is the only evidence that can place the next
  # one. ACLs stay enabled on this bucket because standard logging is
  # delivered through the awslogsdelivery ACL grant; 30 days matches the
  # Lambda log groups.
  EdgeLogBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub elixir-mcp-edge-logs-${AWS::AccountId}
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      OwnershipControls:
        Rules:
          - ObjectOwnership: BucketOwnerPreferred
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault: { SSEAlgorithm: AES256 }
      LifecycleConfiguration:
        Rules:
          - Id: expire-30d
            Status: Enabled
            ExpirationInDays: 30

  SiteBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub elixir-mcp-site-${AWS::AccountId}
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  SiteBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref SiteBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: cloudfront.amazonaws.com }
            Action: s3:GetObject
            Resource: !Sub ${SiteBucket.Arn}/*
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub arn:aws:cloudfront::${AWS::AccountId}:distribution/${SiteDistribution}

  SiteOac:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: elixir-mcp-site-oac
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  # Cookies and the contract header flow to the site API; caching off.
  # user-agent is forwarded for the Profile page's device list (a short
  # label is derived from it; the string itself is never stored).
  SiteApiOriginRequestPolicy:
    Type: AWS::CloudFront::OriginRequestPolicy
    Properties:
      OriginRequestPolicyConfig:
        Name: elixir-mcp-site-api
        CookiesConfig: { CookieBehavior: all }
        HeadersConfig:
          HeaderBehavior: whitelist
          Headers:
            [
              x-elixir-client,
              content-type,
              authorization,
              x-collector-version,
              user-agent,
              cloudfront-viewer-address,
              cloudfront-viewer-country,
            ]
        QueryStringsConfig: { QueryStringBehavior: all }

  # The ONE door route that sees the session cookie: the consent page.
  # A person signed in to the site consents without a code, and a person
  # who consents by code is signed in to the site (0083). Only that
  # cookie, and only on /oauth/authorize; /mcp, /oauth/token and every
  # other door route keep the no-cookie policy below.
  OauthAuthorizeOriginRequestPolicy:
    Type: AWS::CloudFront::OriginRequestPolicy
    Properties:
      OriginRequestPolicyConfig:
        Name: elixir-mcp-oauth-authorize
        CookiesConfig:
          CookieBehavior: whitelist
          Cookies: [__Host-elixir_session]
        HeadersConfig:
          HeaderBehavior: whitelist
          Headers:
            [
              content-type,
              user-agent,
              cloudfront-viewer-address,
              cloudfront-viewer-country,
            ]
        QueryStringsConfig: { QueryStringBehavior: all }

  # The structural guarantee: the MCP door forwards NO cookies, ever.
  McpOriginRequestPolicy:
    Type: AWS::CloudFront::OriginRequestPolicy
    Properties:
      OriginRequestPolicyConfig:
        Name: elixir-mcp-mcp-door
        CookiesConfig: { CookieBehavior: none }
        HeadersConfig:
          HeaderBehavior: whitelist
          # viewer-address and viewer-country are what let an owner see WHERE
          # a credential is being used from. CloudFront adds them; the origin
          # cannot derive either (sourceIp is an edge node). Still no cookies.
          Headers:
            [
              authorization,
              content-type,
              mcp-protocol-version,
              idempotency-key,
              cloudfront-viewer-address,
              cloudfront-viewer-country,
            ]
        QueryStringsConfig: { QueryStringBehavior: all }

  # Browser security headers on every response (#25). Before this, the
  # site shipped no CSP, no frame-ancestors, no nosniff, no
  # Referrer-Policy and no Permissions-Policy.
  #
  # script-src has NO 'unsafe-inline': the static half's scripts are all
  # external files for this reason (the access-request form was the last
  # inline block; it moved to the app entirely on 2026-09-10). tinylytics.app is allowed because BOTH halves load it:
  # #25 briefly kept the embed off app.html and that protected nothing,
  # since the session cookie is Path=/ and the embed still ran on every
  # static page of the same origin (reverted 2026-09-08). So this list
  # IS the boundary, not a backstop behind one - connect-src names
  # tinylytics.app for the same reason, because the app's route bridge
  # beacons navigation to the collector.
  #
  # style-src does allow 'unsafe-inline', stated rather than hidden:
  # the Eleventy templates carry ~70 style="" attributes and the OAuth
  # consent page is one self-contained document with a <style> block.
  # Inline STYLE cannot execute; the exposure is CSS-based exfiltration
  # of attacker-injected markup, which is a much smaller thing than
  # inline script, and tightening it means refactoring markup rather
  # than changing a header. Recorded as a deliberate trade.
  #
  # img-src needs Supercell's asset CDN: collector avatars are the
  # cards' own iconUrls, served straight from api-assets.clashroyale.com.
  # fonts.googleapis.com/fonts.gstatic.com are the site's webfont, which
  # both halves link. Every origin here was taken from the BUILT output,
  # not from memory - a CSP written from what you think the page loads
  # is a CSP that breaks the page.
  SecurityHeadersPolicy:
    Type: AWS::CloudFront::ResponseHeadersPolicy
    Properties:
      ResponseHeadersPolicyConfig:
        Name: elixir-mcp-security-headers
        Comment: CSP, HSTS, nosniff, referrer and permissions policy
        SecurityHeadersConfig:
          # NOT overridden, and that is load-bearing. The OAuth consent pages
          # set their own CSP because they must permit a cross-origin
          # form-action: the browser applies form-action to the REDIRECT of a
          # form POST, so `form-action 'self'` silently blocks the 303 back to
          # the client's callback. WebKit enforces this; the tap simply does
          # nothing, with no error anywhere. Forcing this header from the edge
          # replaced the origin's deliberate `form-action https:` and broke
          # every attempt to connect an MCP client from an iPhone
          # (2026-09-09). Origins that send no CSP -- the static site, the JSON
          # APIs -- still get this one.
          ContentSecurityPolicy:
            Override: false
            ContentSecurityPolicy: >-
              default-src 'self';
              base-uri 'self';
              object-src 'none';
              frame-ancestors 'none';
              form-action 'self';
              script-src 'self' https://tinylytics.app;
              connect-src 'self' https://tinylytics.app;
              img-src 'self' data: https://api-assets.clashroyale.com https://tinylytics.app;
              font-src 'self' https://fonts.gstatic.com;
              style-src 'self' 'unsafe-inline' https://fonts.googleapis.com
          ContentTypeOptions:
            Override: true
          FrameOptions:
            Override: true
            FrameOption: DENY
          ReferrerPolicy:
            Override: true
            ReferrerPolicy: strict-origin-when-cross-origin
          # Two years, subdomains included. NOT preloaded: preload is
          # effectively irreversible and belongs to a domain decision,
          # not a deploy.
          StrictTransportSecurity:
            Override: true
            AccessControlMaxAgeSec: 63072000
            IncludeSubdomains: true
            Preload: false
        CustomHeadersConfig:
          Items:
            # Permissions-Policy has no first-class field in the managed
            # security config, so it rides as a custom header. Nothing
            # here needs any of these.
            - Header: Permissions-Policy
              Override: true
              Value: >-
                accelerometer=(), camera=(), display-capture=(),
                geolocation=(), gyroscope=(), magnetometer=(),
                microphone=(), payment=(), usb=()

  SiteDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        Comment: elixir.poapkings.com
        HttpVersion: http2and3
        Logging:
          Bucket: !GetAtt EdgeLogBucket.RegionalDomainName
          Prefix: cloudfront/
          IncludeCookies: false
        Aliases: !If [HasSiteCert, [elixir.poapkings.com], !Ref "AWS::NoValue"]
        ViewerCertificate: !If
          - HasSiteCert
          - AcmCertificateArn: !Ref SiteCertificateArn
            SslSupportMethod: sni-only
            MinimumProtocolVersion: TLSv1.2_2021
          - CloudFrontDefaultCertificate: true
        DefaultRootObject: index.html
        Origins:
          - Id: site
            DomainName: !GetAtt SiteBucket.RegionalDomainName
            OriginAccessControlId: !Ref SiteOac
            S3OriginConfig: { OriginAccessIdentity: "" }
          - Id: api
            DomainName: !Sub ${SiteApi}.execute-api.${AWS::Region}.amazonaws.com
            OriginCustomHeaders: !If
              - HasOriginSecret
              - - HeaderName: x-elixir-origin
                  HeaderValue: !Ref OriginSecret
              - !Ref AWS::NoValue
            CustomOriginConfig:
              OriginProtocolPolicy: https-only
              OriginSSLProtocols: [TLSv1.2]
          - Id: mcpapi
            DomainName: !Sub ${McpApi}.execute-api.${AWS::Region}.amazonaws.com
            OriginCustomHeaders: !If
              - HasOriginSecret
              - - HeaderName: x-elixir-origin
                  HeaderValue: !Ref OriginSecret
              - !Ref AWS::NoValue
            CustomOriginConfig:
              OriginProtocolPolicy: https-only
              OriginSSLProtocols: [TLSv1.2]
        DefaultCacheBehavior:
          TargetOriginId: site
          ViewerProtocolPolicy: redirect-to-https
          # AWS managed CachingOptimized
          CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6
          ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
          Compress: true
          # Site routing lives HERE, on the site behavior only, so it
          # cannot reach the API, MCP or OAuth behaviors below.
          FunctionAssociations:
            - EventType: viewer-request
              FunctionARN: !GetAtt SpaRouter.FunctionMetadata.FunctionARN
        CacheBehaviors:
          # The two public read endpoints, CACHED (#23). Both run real
          # aggregation - status does several time-series queries,
          # stats groups the whole corpus - and both already emit
          # Cache-Control (60s and 3600s). They were being served under
          # the /api/* CachingDisabled behaviour, so every hit recomputed
          # them and x-cache said Miss every time. These behaviours are
          # listed FIRST because CloudFront takes the first pattern that
          # matches, not the most specific one.
          #
          # GET/HEAD only, and no origin request policy: no cookie, no
          # header and no query string reaches the origin, so one cached
          # object serves everyone. That is correct here precisely
          # because these responses are identical for every caller -
          # the same property that makes them safe to cache is what
          # makes them public.
          - PathPattern: /api/public/status
            TargetOriginId: api
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD]
            # AWS managed CachingOptimized: honours the origin's own
            # Cache-Control, forwards nothing.
            CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            Compress: true
          - PathPattern: /api/public/stats
            TargetOriginId: api
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD]
            CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            Compress: true
          - PathPattern: /api/v1/*
            TargetOriginId: api
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            OriginRequestPolicyId: !Ref McpOriginRequestPolicy
          - PathPattern: /api/*
            TargetOriginId: api
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            # AWS managed CachingDisabled
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            OriginRequestPolicyId: !Ref SiteApiOriginRequestPolicy
          # The MCP/OAuth door, path-split on the one hostname. The
          # no-cookie origin request policy is the structural guarantee:
          # session cookies can never reach these routes (docs/ENGINEERING.md).
          - PathPattern: /mcp
            TargetOriginId: mcpapi
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            OriginRequestPolicyId: !Ref McpOriginRequestPolicy
          # Agent and integration doors (0054). Same origin, same no-cookie
          # policy as /mcp -- these are the same protected resource served
          # under a per-principal identity, and a session cookie must never
          # reach one. Without these the Lambda's routing is correct and
          # unreachable: the edge answers /a/* with its own 403 HTML and the
          # door never sees the request.
          - PathPattern: /a/*
            TargetOriginId: mcpapi
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            OriginRequestPolicyId: !Ref McpOriginRequestPolicy
          - PathPattern: /i/*
            TargetOriginId: mcpapi
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            OriginRequestPolicyId: !Ref McpOriginRequestPolicy
          # Listed before /oauth/*: CloudFront takes the first pattern
          # that matches. GET and POST both carry the cookie (the consent
          # form posts back here); SameSite=Lax keeps a cross-site POST
          # from carrying it.
          - PathPattern: /oauth/authorize
            TargetOriginId: mcpapi
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            OriginRequestPolicyId: !Ref OauthAuthorizeOriginRequestPolicy
          - PathPattern: /oauth/*
            TargetOriginId: mcpapi
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            OriginRequestPolicyId: !Ref McpOriginRequestPolicy
          - PathPattern: /.well-known/*
            TargetOriginId: mcpapi
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
            OriginRequestPolicyId: !Ref McpOriginRequestPolicy
        # No CustomErrorResponses: those are DISTRIBUTION-wide, so
        # rewriting 403/404 to a 200 app shell also rewrote the API's
        # own refusals. An unauthenticated /api/admin/requests came back
        # 200 text/html, and the web client read that as success. Site
        # routing is done by SpaRouter on the site behavior instead,
        # and a missing page or asset is now honestly a miss.

  # Routes each path to the half of the site that owns it, without
  # touching status codes anywhere else on the distribution.
  #
  # The site is two builds in one bucket (2026-09-07). apps/site emits
  # real documents for the content pages; apps/web is the application,
  # served from /app.html. Before the split every extension-less path
  # was rewritten to the one shared shell, so all ten sitemap URLs
  # returned byte-identical HTML and no crawler or agent could read the
  # docs at all.
  #
  # STATIC_PAGES below must stay in step with what apps/site emits and
  # with STATIC_LINKS in apps/web/src/App.jsx. A test pins all three.
  SpaRouter:
    Type: AWS::CloudFront::Function
    Properties:
      # Logical id and Name are historical: this was the SPA router
      # before the site was split. Changing either forces a replacement
      # of a function attached to the live distribution, which is not
      # worth a better name.
      Name: elixir-mcp-spa-router
      AutoPublish: true
      FunctionConfig:
        Comment: Route content paths to the static site and app paths to the app shell
        Runtime: cloudfront-js-2.0
      FunctionCode: |
        // The Data section is five static pages; /data/dashboard is the
        // app's charts and must NOT match, which is why these are listed
        // rather than prefixed. /updates/ is prefixed: every update is
        // its own page and there is no app route under it.
        var STATIC_PAGES = {
          '/data': 1,
          '/data/growth': 1,
          '/data/collect': 1,
          '/data/now': 1,
          '/data/machines': 1,
          '/docs': 1,
          '/family': 1,
          '/updates': 1,
          '/support': 1
        };
        var STATIC_PREFIXES = ['/docs/', '/examples/', '/family/', '/updates/'];

        function handler(event) {
          var request = event.request;
          var uri = request.uri;

          // A path whose last segment has an extension is asking for a
          // real file; if it is not there, that is a 404 and must stay
          // one. This covers every asset, llms.txt, tools.json,
          // sitemap.xml and robots.txt.
          if (uri.substring(uri.lastIndexOf('/') + 1).indexOf('.') !== -1) {
            return request;
          }

          if (uri === '/' || uri === '') {
            request.uri = '/index.html';
            return request;
          }

          // Match without a trailing slash: both spellings of a page
          // resolve to the same document.
          var key = uri.charAt(uri.length - 1) === '/'
            ? uri.substring(0, uri.length - 1)
            : uri;

          var isStatic = STATIC_PAGES[key] === 1;
          for (var i = 0; i < STATIC_PREFIXES.length && !isStatic; i++) {
            if (key.indexOf(STATIC_PREFIXES[i]) === 0) isStatic = true;
          }

          // A static page that does not exist is an honest miss, the
          // same as a missing asset. Everything else is an application
          // route and gets the app shell.
          request.uri = isStatic ? key + '/index.html' : '/app.html';
          return request;
        }

  # ------------------------------------------------------------ monitoring
  AlarmTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: elixir-mcp-alarms
      # Routing of record is the sysadmin projects-ops-alerts queue; that
      # subscription is managed in the projects-sysadmin repo. No email.

  # A topic policy REPLACES the default one, so the account-owner
  # statement SNS would have written is restated first; the second lets
  # SES publish sending events from this stack's configuration set.
  AlarmTopicPolicy:
    Type: AWS::SNS::TopicPolicy
    Properties:
      Topics: [!Ref AlarmTopic]
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: owner
            Effect: Allow
            Principal: { AWS: "*" }
            Action:
              [
                SNS:Publish,
                SNS:Subscribe,
                SNS:Receive,
                SNS:GetTopicAttributes,
                SNS:SetTopicAttributes,
                SNS:ListSubscriptionsByTopic,
                SNS:AddPermission,
                SNS:RemovePermission,
                SNS:DeleteTopic,
              ]
            Resource: !Ref AlarmTopic
            Condition:
              StringEquals: { AWS:SourceOwner: !Ref AWS::AccountId }
          - Sid: ses-events
            Effect: Allow
            Principal: { Service: ses.amazonaws.com }
            Action: SNS:Publish
            Resource: !Ref AlarmTopic
            Condition:
              StringEquals: { aws:SourceAccount: !Ref AWS::AccountId }
              ArnLike:
                aws:SourceArn: !Sub arn:aws:ses:${AWS::Region}:${AWS::AccountId}:configuration-set/elixir-mcp

  DlqAlarmEmail:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-dlq-email
      Namespace: AWS/SQS
      MetricName: ApproximateNumberOfMessagesVisible
      Dimensions: [{ Name: QueueName, Value: !GetAtt EmailDlq.QueueName }]
      Statistic: Maximum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 0
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  JobsErrorsAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-jobs-errors
      Namespace: AWS/Lambda
      MetricName: Errors
      Dimensions: [{ Name: FunctionName, Value: !Ref JobsFunction }]
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  # A migrate invocation that runs long is a diagnostics op scanning the
  # production database (the 2026-09-11 14:02Z recovery followed thirteen
  # two-minute probe runs), or a migration that needs a human watching.
  # Migrations themselves take seconds.
  MigrateDurationAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-migrate-duration
      AlarmDescription: A migrate Lambda invocation ran longer than 90 s; a diagnostics op is scanning production
      Namespace: AWS/Lambda
      MetricName: Duration
      Dimensions: [{ Name: FunctionName, Value: !Ref MigrateFunction }]
      Statistic: Maximum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 90000
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  LedgerOldestJobAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-ledger-oldest-job
      AlarmDescription: Oldest queued job is stale; the fleet is down or drowning
      Namespace: ElixirMCP/Ledger
      MetricName: OldestQueuedAgeSeconds
      Statistic: Maximum
      Period: 900
      EvaluationPeriods: 2
      Threshold: 1800
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  LedgerDeadJobsAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-ledger-dead-jobs
      AlarmDescription: Jobs exhausted their retries; inspect the job table
      Namespace: ElixirMCP/Ledger
      MetricName: DeadJobs
      Statistic: Maximum
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  # The season row's derived war number disagreed with a riverracelog
  # entry (0104; services/ingest/src/season.mjs): the calendar the record
  # files everything under is wrong for that month. Nothing relabels
  # itself; a human reads the season table and the log entry.
  SeasonMismatchAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-season-war-id-mismatch
      AlarmDescription: A river race log entry carried a seasonId the season calendar does not derive; check the season table against the log
      Namespace: ElixirMCP/Record
      MetricName: SeasonWarIdMismatch
      Statistic: Sum
      Period: 3600
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  # A war-typed battle in the last seven days outside every war_period
  # row (the diagnostics probe's UNRESOLVED bucket, emitted hourly by the
  # meta rollup job): the scheduler's season upsert failed and the war
  # readers are blind to those battles. Fires at one.
  WarUnresolvedAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-war-battle-unresolved
      AlarmDescription: A recent war battle falls in no war_period row; the season and period calendar has a gap (check the scheduler tick and the season table)
      Namespace: ElixirMCP/Record
      MetricName: WarBattleUnresolved
      Statistic: Maximum
      Period: 3600
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  SchedulerErrorsAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-scheduler-errors
      Namespace: AWS/Lambda
      MetricName: Errors
      Dimensions: [{ Name: FunctionName, Value: !Ref SchedulerFunction }]
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 2
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  SchedulerMissingAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-scheduler-missing
      AlarmDescription: The planner stopped ticking; recording is stalled
      Namespace: AWS/Lambda
      MetricName: Invocations
      Dimensions: [{ Name: FunctionName, Value: !Ref SchedulerFunction }]
      Statistic: Sum
      # 15-minute windows so any tick cadence up to rate(15 minutes) is
      # covered without retuning the alarm alongside SchedulerTickMinutes.
      Period: 900
      EvaluationPeriods: 2
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      TreatMissingData: breaching
      AlarmActions: [!Ref AlarmTopic]

  # The user-facing doors (review 2026-09-05): the pipeline was fully
  # alarmed but a 500ing MCP or web-api lambda was invisible - users
  # would find out before the ops queue did.
  McpErrorsAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-mcp-errors
      Namespace: AWS/Lambda
      MetricName: Errors
      Dimensions: [{ Name: FunctionName, Value: !Ref McpFunction }]
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 2
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  WebApiErrorsAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-web-api-errors
      Namespace: AWS/Lambda
      MetricName: Errors
      Dimensions: [{ Name: FunctionName, Value: !Ref WebApiFunction }]
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 2
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  McpLatencyAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-mcp-latency-p95
      Namespace: AWS/Lambda
      MetricName: Duration
      Dimensions: [{ Name: FunctionName, Value: !Ref McpFunction }]
      ExtendedStatistic: p95
      Period: 300
      EvaluationPeriods: 3
      Threshold: 8000
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  # Per-call latency as the TOOL layer sees it (ElixirMCP/Tools, emitted
  # by the invoker as EMF), which the Lambda Duration above cannot split
  # from OAuth and metadata traffic. The invoker emits every metric both
  # with the Tool dimension and without; per-tool alarms are too many,
  # so this one watches the undimensioned p95.
  ToolLatencyP95Alarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-tool-latency-p95
      Namespace: ElixirMCP/Tools
      MetricName: DurationMs
      ExtendedStatistic: p95
      Period: 300
      EvaluationPeriods: 3
      DatapointsToAlarm: 2
      Threshold: 10000
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  DbCpuAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-db-cpu
      Namespace: AWS/RDS
      MetricName: CPUUtilization
      Dimensions: [{ Name: DBInstanceIdentifier, Value: !Ref Database }]
      Statistic: Average
      Period: 300
      EvaluationPeriods: 3
      Threshold: 85
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  DbStorageAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-db-storage
      Namespace: AWS/RDS
      MetricName: FreeStorageSpace
      Dimensions: [{ Name: DBInstanceIdentifier, Value: !Ref Database }]
      Statistic: Minimum
      Period: 900
      EvaluationPeriods: 1
      Threshold: 2147483648
      ComparisonOperator: LessThanThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  DbConnectionsAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-db-connections
      Namespace: AWS/RDS
      MetricName: DatabaseConnections
      Dimensions: [{ Name: DBInstanceIdentifier, Value: !Ref Database }]
      Statistic: Maximum
      Period: 300
      EvaluationPeriods: 2
      Threshold: 60
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  EstimatedChargesAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: elixir-mcp-estimated-charges
      AlarmDescription: Account-wide estimated charges guard for the hobby account
      Namespace: AWS/Billing
      MetricName: EstimatedCharges
      Dimensions: [{ Name: Currency, Value: USD }]
      Statistic: Maximum
      Period: 21600
      EvaluationPeriods: 1
      Threshold: !Ref MonthlyCostAlarmUsd
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions: [!Ref AlarmTopic]

  # The operations dashboard (2026-09-17): one page read top to bottom as
  # "is it up, is it recording, is it answering, is the database fine,
  # what is it costing". Every threshold drawn is an alarm's own; a
  # widget is never a second alarm. Beyond AWS's metrics it reads the
  # EMF lines the services already write on stdout: ElixirMCP/Ledger
  # (the scheduler tick, with the pace and fleet numbers added the same
  # day), ElixirMCP/Ingest (the collector door), ElixirMCP/Tools (the
  # MCP door), ElixirMCP/Record (the jobs Lambda) and ElixirMCP/Series
  # (the nightly series line). The body is generated JSON under !Sub;
  # services/jobs/test/dashboard.test.mjs parses it and checks that
  # every name it substitutes is a resource or parameter of this stack.
  Dashboard:
    Type: AWS::CloudWatch::Dashboard
    Properties:
      DashboardName: elixir-mcp
      DashboardBody: !Sub |
        {
          "start": "-PT24H",
          "periodOverride": "auto",
          "widgets": [
            {
              "type": "alarm",
              "x": 0,
              "y": 0,
              "width": 24,
              "height": 4,
              "properties": {
                "title": "Elixir MCP alarms (17; the ops queue gets every state change)",
                "alarms": [
                  "${DlqAlarmEmail.Arn}",
                  "${JobsErrorsAlarm.Arn}",
                  "${MigrateDurationAlarm.Arn}",
                  "${LedgerOldestJobAlarm.Arn}",
                  "${LedgerDeadJobsAlarm.Arn}",
                  "${SeasonMismatchAlarm.Arn}",
                  "${WarUnresolvedAlarm.Arn}",
                  "${SchedulerErrorsAlarm.Arn}",
                  "${SchedulerMissingAlarm.Arn}",
                  "${McpErrorsAlarm.Arn}",
                  "${WebApiErrorsAlarm.Arn}",
                  "${McpLatencyAlarm.Arn}",
                  "${ToolLatencyP95Alarm.Arn}",
                  "${DbCpuAlarm.Arn}",
                  "${DbStorageAlarm.Arn}",
                  "${DbConnectionsAlarm.Arn}",
                  "${EstimatedChargesAlarm.Arn}"
                ]
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 4,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "ElixirMCP/Ledger",
                    "FetchesHour",
                    {
                      "stat": "Maximum",
                      "label": "fetches, last hour"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "CeilingHour",
                    {
                      "stat": "Maximum",
                      "label": "ceiling per hour",
                      "color": "#d62728"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "FetchErrorsHour",
                    {
                      "stat": "Maximum",
                      "label": "fetch errors, last hour"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "Tokens",
                    {
                      "stat": "Minimum",
                      "label": "bucket tokens",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Recorder pace: fetches this hour against the budget's ceiling",
                "period": 60,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 12,
              "y": 4,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "ElixirMCP/Ledger",
                    "PlannedJobs",
                    {
                      "stat": "Sum",
                      "label": "planned"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "QueuedJobs",
                    {
                      "stat": "Maximum",
                      "label": "queued"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "DeadJobs",
                    {
                      "stat": "Maximum",
                      "label": "dead (alarm at 1)",
                      "color": "#d62728"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "OldestQueuedAgeSeconds",
                    {
                      "stat": "Maximum",
                      "label": "oldest queued, s (alarm at 1800)",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Job ledger: planned per tick, queued, oldest queued age, dead",
                "period": 60,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": 1800,
                      "label": "oldest queued alarm",
                      "yAxis": "right"
                    }
                  ]
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 10,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "ElixirMCP/Ledger",
                    "CollectorsActive",
                    {
                      "stat": "Minimum",
                      "label": "active, heard in 5 min"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "CollectorsDraining",
                    {
                      "stat": "Maximum",
                      "label": "draining",
                      "color": "#d62728"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "RequestedProfileJobs",
                    {
                      "stat": "Sum",
                      "label": "arena refresh requests",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "LossBoundedJobs",
                    {
                      "stat": "Sum",
                      "label": "loss bounded",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "ElixirMCP/Ledger",
                    "ReadCappedJobs",
                    {
                      "stat": "Sum",
                      "label": "reader capped",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Collector fleet: heard in the last 5 minutes, draining",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 12,
              "y": 10,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "ElixirMCP/Ingest",
                    "Admitted",
                    {
                      "stat": "Sum",
                      "label": "admitted"
                    }
                  ],
                  [
                    "ElixirMCP/Ingest",
                    "Rejected",
                    {
                      "stat": "Sum",
                      "label": "rejected",
                      "color": "#d62728"
                    }
                  ],
                  [
                    "ElixirMCP/Ingest",
                    "Duplicate",
                    {
                      "stat": "Sum",
                      "label": "duplicate"
                    }
                  ],
                  [
                    "ElixirMCP/Ingest",
                    "NewFacts",
                    {
                      "stat": "Sum",
                      "label": "new facts",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "ElixirMCP/Ingest",
                    "IngestMs",
                    {
                      "stat": "p95",
                      "label": "ingest p95 ms",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Admission at the collector door, per 5 minutes",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 16,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "ElixirMCP/Tools",
                    "DurationMs",
                    {
                      "stat": "SampleCount",
                      "label": "calls"
                    }
                  ],
                  [
                    "ElixirMCP/Tools",
                    "Errors",
                    {
                      "stat": "Sum",
                      "label": "errors",
                      "color": "#d62728"
                    }
                  ],
                  [
                    "ElixirMCP/Tools",
                    "DurationMs",
                    {
                      "stat": "p50",
                      "label": "p50 ms",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "ElixirMCP/Tools",
                    "DurationMs",
                    {
                      "stat": "p95",
                      "label": "p95 ms (alarm at 10000)",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "ElixirMCP/Tools",
                    "DbMs",
                    {
                      "stat": "p95",
                      "label": "db p95 ms",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "MCP door: calls, latency and errors",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": 10000,
                      "label": "tool p95 alarm",
                      "yAxis": "right"
                    }
                  ]
                }
              }
            },
            {
              "type": "metric",
              "x": 12,
              "y": 16,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "AWS/Lambda",
                    "Errors",
                    "FunctionName",
                    "${McpFunction}",
                    {
                      "stat": "Sum",
                      "label": "errors",
                      "color": "#d62728"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "Throttles",
                    "FunctionName",
                    "${McpFunction}",
                    {
                      "stat": "Sum",
                      "label": "throttles"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "ConcurrentExecutions",
                    "FunctionName",
                    "${McpFunction}",
                    {
                      "stat": "Maximum",
                      "label": "concurrent"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "Duration",
                    "FunctionName",
                    "${McpFunction}",
                    {
                      "stat": "p95",
                      "label": "duration p95 ms (alarm at 8000)",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "MCP Lambda: duration, errors, throttles, concurrency (reserved 20)",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": 20,
                      "label": "reserved concurrency"
                    }
                  ]
                }
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 22,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${McpLogGroup}' | fields Tool, DurationMs | filter ispresent(Tool) | stats pct(DurationMs, 95) as p95_ms, count() as calls by Tool | sort p95_ms desc | limit 10",
                "region": "${AWS::Region}",
                "title": "Slowest tools in the dashboard's window (p95 ms, calls)",
                "view": "table"
              }
            },
            {
              "type": "metric",
              "x": 12,
              "y": 22,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "AWS/ApiGateway",
                    "Count",
                    "ApiId",
                    "${SiteApi}",
                    {
                      "stat": "Sum",
                      "label": "requests"
                    }
                  ],
                  [
                    "AWS/ApiGateway",
                    "4xx",
                    "ApiId",
                    "${SiteApi}",
                    {
                      "stat": "Sum",
                      "label": "4xx"
                    }
                  ],
                  [
                    "AWS/ApiGateway",
                    "5xx",
                    "ApiId",
                    "${SiteApi}",
                    {
                      "stat": "Sum",
                      "label": "5xx",
                      "color": "#d62728"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "ConcurrentExecutions",
                    "FunctionName",
                    "${WebApiFunction}",
                    {
                      "stat": "Maximum",
                      "label": "web-api concurrent"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "Throttles",
                    "FunctionName",
                    "${WebApiFunction}",
                    {
                      "stat": "Sum",
                      "label": "web-api throttles"
                    }
                  ],
                  [
                    "AWS/ApiGateway",
                    "IntegrationLatency",
                    "ApiId",
                    "${SiteApi}",
                    {
                      "stat": "p95",
                      "label": "latency p95 ms",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Site and collector door: requests, errors, latency; web-api concurrency (reserved 20)",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": 20,
                      "label": "reserved concurrency"
                    }
                  ]
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 28,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "AWS/RDS",
                    "CPUUtilization",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Average",
                      "label": "cpu %"
                    }
                  ],
                  [
                    "AWS/RDS",
                    "CPUCreditBalance",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Average",
                      "label": "cpu credit balance",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "The micro: CPU (alarm at 85) and the burst credits that predict the afternoon",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": 85,
                      "label": "cpu alarm"
                    }
                  ]
                }
              }
            },
            {
              "type": "metric",
              "x": 12,
              "y": 28,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "AWS/RDS",
                    "FreeableMemory",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Average",
                      "label": "freeable memory, bytes"
                    }
                  ],
                  [
                    "AWS/RDS",
                    "SwapUsage",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Average",
                      "label": "swap, bytes"
                    }
                  ],
                  [
                    "AWS/RDS",
                    "DatabaseConnections",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Maximum",
                      "label": "connections",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "The micro: memory, swap and connections (alarm at 60 of 79)",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": 60,
                      "label": "connections alarm",
                      "yAxis": "right"
                    },
                    {
                      "value": 79,
                      "label": "max_connections",
                      "yAxis": "right"
                    }
                  ]
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 34,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "AWS/RDS",
                    "FreeStorageSpace",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Minimum",
                      "label": "free storage, bytes"
                    }
                  ],
                  [
                    "AWS/RDS",
                    "ReadIOPS",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Average",
                      "label": "read iops",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "AWS/RDS",
                    "WriteIOPS",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Average",
                      "label": "write iops",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "AWS/RDS",
                    "DiskQueueDepth",
                    "DBInstanceIdentifier",
                    "${Database}",
                    {
                      "stat": "Average",
                      "label": "queue depth",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "The micro: free storage (alarm at 2 GB, auto-scales to 100 GB) and disk activity",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": 2147483648,
                      "label": "storage alarm"
                    }
                  ]
                }
              }
            },
            {
              "type": "metric",
              "x": 12,
              "y": 34,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "AWS/Lambda",
                    "Duration",
                    "FunctionName",
                    "${JobsFunction}",
                    {
                      "stat": "Maximum",
                      "label": "jobs duration max ms"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "Duration",
                    "FunctionName",
                    "${MigrateFunction}",
                    {
                      "stat": "Maximum",
                      "label": "migrate duration max ms"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "Errors",
                    "FunctionName",
                    "${JobsFunction}",
                    {
                      "stat": "Sum",
                      "label": "jobs errors",
                      "color": "#d62728",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "Errors",
                    "FunctionName",
                    "${MigrateFunction}",
                    {
                      "stat": "Sum",
                      "label": "migrate errors",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "Errors",
                    "FunctionName",
                    "${SchedulerFunction}",
                    {
                      "stat": "Sum",
                      "label": "scheduler errors",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "AWS/Lambda",
                    "Invocations",
                    "FunctionName",
                    "${SchedulerFunction}",
                    {
                      "stat": "Sum",
                      "label": "scheduler ticks per 5 min",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Batch Lambdas: jobs (900 s), migrate (300 s), scheduler ticks and errors",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": 900000,
                      "label": "jobs timeout"
                    },
                    {
                      "value": 300000,
                      "label": "migrate timeout"
                    }
                  ]
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 40,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "ElixirMCP/Record",
                    "SeasonWarIdMismatch",
                    {
                      "stat": "Sum",
                      "label": "season war id mismatch (alarm)"
                    }
                  ],
                  [
                    "ElixirMCP/Record",
                    "WarBattleUnresolved",
                    {
                      "stat": "Maximum",
                      "label": "war battles outside the calendar (alarm)"
                    }
                  ],
                  [
                    "ElixirMCP/Record",
                    "PayloadShapeFindings",
                    {
                      "stat": "Maximum",
                      "label": "payload shape findings (nightly, filed to feedback)"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Record integrity: every line should sit at zero",
                "period": 3600,
                "yAxis": {
                  "left": {
                    "min": 0
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 12,
              "y": 40,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "ElixirMCP/Series",
                    "SnapshotRowsToday",
                    {
                      "stat": "Maximum",
                      "label": "snapshot rows"
                    }
                  ],
                  [
                    "ElixirMCP/Series",
                    "RosterRowsToday",
                    {
                      "stat": "Maximum",
                      "label": "written by the roster"
                    }
                  ],
                  [
                    "ElixirMCP/Series",
                    "ProfileRowsToday",
                    {
                      "stat": "Maximum",
                      "label": "written by the profile"
                    }
                  ],
                  [
                    "ElixirMCP/Series",
                    "ClanRowsToday",
                    {
                      "stat": "Maximum",
                      "label": "clan rows"
                    }
                  ],
                  [
                    "ElixirMCP/Series",
                    "ProgressRowsToday",
                    {
                      "stat": "Maximum",
                      "label": "progress rows"
                    }
                  ],
                  [
                    "ElixirMCP/Series",
                    "SnapshotTableMB",
                    {
                      "stat": "Maximum",
                      "label": "snapshot table MB",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "ElixirMCP/Series",
                    "ProgressTableMB",
                    {
                      "stat": "Maximum",
                      "label": "progress table MB",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Daily series: rows on the game day by writer (nightly), and the tables' size",
                "period": 86400,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 46,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "AWS/SQS",
                    "ApproximateNumberOfMessagesVisible",
                    "QueueName",
                    "${EmailQueue.QueueName}",
                    {
                      "stat": "Maximum",
                      "label": "queued"
                    }
                  ],
                  [
                    "AWS/SQS",
                    "ApproximateNumberOfMessagesVisible",
                    "QueueName",
                    "${EmailDlq.QueueName}",
                    {
                      "stat": "Maximum",
                      "label": "dead letters",
                      "color": "#d62728"
                    }
                  ],
                  [
                    "AWS/SQS",
                    "ApproximateAgeOfOldestMessage",
                    "QueueName",
                    "${EmailQueue.QueueName}",
                    {
                      "stat": "Maximum",
                      "label": "oldest, s",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Mail: the relay queue and its dead letters (alarm at 1)",
                "period": 300,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 12,
              "y": 46,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  [
                    "AWS/Billing",
                    "EstimatedCharges",
                    "Currency",
                    "USD",
                    {
                      "stat": "Maximum",
                      "label": "estimated charges, USD",
                      "region": "us-east-1"
                    }
                  ],
                  [
                    "AWS/S3",
                    "BucketSizeBytes",
                    "BucketName",
                    "${ArchiveBucket}",
                    "StorageType",
                    "StandardStorage",
                    {
                      "stat": "Average",
                      "label": "archive bytes",
                      "yAxis": "right"
                    }
                  ],
                  [
                    "AWS/S3",
                    "NumberOfObjects",
                    "BucketName",
                    "${ArchiveBucket}",
                    "StorageType",
                    "AllStorageTypes",
                    {
                      "stat": "Average",
                      "label": "archive objects",
                      "yAxis": "right"
                    }
                  ]
                ],
                "view": "timeSeries",
                "stacked": false,
                "region": "${AWS::Region}",
                "title": "Cost: month to date (alarm at the stack's MonthlyCostAlarmUsd) and the archive",
                "period": 86400,
                "yAxis": {
                  "left": {
                    "min": 0
                  },
                  "right": {
                    "min": 0
                  }
                },
                "annotations": {
                  "horizontal": [
                    {
                      "value": ${MonthlyCostAlarmUsd},
                      "label": "cost alarm"
                    }
                  ]
                }
              }
            }
          ]
        }

Outputs:
  DatabaseEndpoint:
    Value: !GetAtt Database.Endpoint.Address
  SiteDistributionDomain:
    Value: !GetAtt SiteDistribution.DomainName
    Description: CNAME target for elixir.poapkings.com at Namecheap (serves site, /api, /mcp, /oauth, /.well-known)
  SiteBucketName:
    Value: !Ref SiteBucket
  MigrateFunctionName:
    Value: !Ref MigrateFunction
  AlarmTopicArn:
    Value: !Ref AlarmTopic
    Description: Subscribe from projects-sysadmin to projects-ops-alerts
  # The DNS for sending as elixir@poapkings.com, all at Namecheap by hand.
  SenderDkimCname1:
    Value: !Sub "${SenderIdentity.DkimDNSTokenName1} CNAME ${SenderIdentity.DkimDNSTokenValue1}"
  SenderDkimCname2:
    Value: !Sub "${SenderIdentity.DkimDNSTokenName2} CNAME ${SenderIdentity.DkimDNSTokenValue2}"
  SenderDkimCname3:
    Value: !Sub "${SenderIdentity.DkimDNSTokenName3} CNAME ${SenderIdentity.DkimDNSTokenValue3}"
  SenderMailFromMx:
    Value: !Sub "bounce.poapkings.com MX 10 feedback-smtp.${AWS::Region}.amazonses.com"
  SenderMailFromSpf:
    Value: 'bounce.poapkings.com TXT "v=spf1 include:amazonses.com ~all"'
    Description: SPF for the MAIL FROM subdomain. The apex SPF, if any, also needs include:amazonses.com
