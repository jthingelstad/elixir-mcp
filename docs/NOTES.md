# Elixir MCP

Purpose: _One-line purpose statement TBD — rethinking elixir-bot as an MCP offering._

## Context: current Elixir

- Elixir is the clan agent for the POAP KINGS Clash Royale clan: a deterministic data engine (Clash Royale API → receipts → event streams → projections) with an hourly LLM awareness loop that is the sole proactive poster.
- It speaks through destination-specific Discord lanes (#elixir, #announcements, #actions, #ask-elixir, #leaders, etc.) with scoped memory, plus `/elixir` member commands and `/clanops` leadership operations.
- Clan management is deterministic (state + leader action cards, no model-decided promotions/kicks); runs on Python 3.14 / uv / SQLite, operated via launchd and `scripts/admin.sh`.

## Brainstorm

**Core idea:** Decompose the Elixir agent and simultaneously create a new offering: give Clash Royale players an MCP interface to explore their Clash data.

**Two components:**

1. **Elixir MCP** — the service/website.
   - Player registers with email via magic link (same pattern as other projects).
   - Registers their Clash Royale tag.
   - Possibly sets up billing/payment.
   - Possibly provisions their own Elixir MCP Gateway.
   - Service then collects data for that user and provides an MCP interface they can bring to Claude or any agent.
   - Can be focused on a player OR a clan.
2. **Elixir MCP Gateway** — solves the IP restriction issue of the Clash Royale API. Could be a multitude of gateways distributing load to the CR API across dozens of egress points.

**Future direction:** The Elixir agent sheds most of its data-collecting capabilities, relies on Elixir MCP for data, and becomes a lightweight layer on top providing clan agent services and clan management functions.

**Precedent:** This split already exists with Elixir Drop / Elixir Drop Gateway — the difference is many Elixir MCP Gateways instead of one.

**Scope clarifications:**

- Brand-new project. Does NOT reuse anything from elixir-bot. (May at some point backfill historical data for Jamie personally or for POAP KINGS.)
- Multiplayer and multi-clan from day one.
- The service stands alone without the Elixir agent — you could use Elixir MCP and never attach the agent. The agent is a V2 consideration, one possible consumer later.

**Core problem/value:** The Clash Royale API only provides current state, no history. Elixir MCP's job is to record history and expose a really robust MCP interface over Clash Royale data. A player says "record all my games"; a clan setup says "record all battles for all clan members." Substantial data collection; hundreds of players could attach, each making MCP requests via their favorite agent.

**Patterns already proven in-house:**

- MCP side (auth'd): very similar to the thingy MCP just built — auth patterns all exist.
- Gateway: looks a lot like the Clash Royale Drop bridge running on Jamie's home machine (queue-connected). Queue is the right model because there are two constraints: IP restriction AND rate limiting.

**Web interface scope:** Deliberately small. Register (magic link), identify your player tag, opt in to recording, and maybe a small dashboard showing what data is being collected for you. No chat interface — the agent experience lives entirely in the user's own MCP client.

**Monetization (later, not launch):** Launch is free, just player registration. But the shape of future monetization is clear: a player might pay a couple bucks ($20/year range) to have their data recorded; a clan could pay for collection across all members as a provisioned service for the clan.

**Key data-model insight:** Recording is shared, not per-user. If a clan is recording all members' battles and a member also signs up for their own data, that's the same data — record once, both parties get access. So the data model separates the recorded facts (battles/observations, stored once, keyed by player) from access entitlements (who subscribed to what). "A pretty robust data model backs the MCP."

## Open Questions

- Supercell CR API terms — commercial use, key ownership. Does a user-provisioned gateway mean the user brings their own API key?
- Multi-tenancy: what does per-user/per-clan data look like? (Greenfield — no elixir-bot code reuse.)
- MCP auth model — magic link to token for remote MCP connection (OAuth?).
- Data retention/collection start point for new users.
- Polling cadence vs CR battle-log window — the battle log endpoint only returns recent battles, so recorder scheduling determines whether games get missed.
- Queue/scheduler design for fan-out across many gateways with per-gateway rate budgets.
- MCP tool surface — raw queries vs aggregates (deck stats, win rates, trophy history, clan war history)?
- Canonical battle identity/dedup — the same battle appears in both participants' battle logs (and via clan recording), so battles need a canonical ID (player pair + battle time?) to store once regardless of how many recording paths observe it.

### Resolved / deferred

- ~~Migration path for elixir-bot to consume Elixir MCP~~ — agent is V2, one possible consumer later; no code reuse.

## Decisions

**AGENT-TEAM ACTIVATED IN CODEX (2026-09-06):** Registered the saved `elixir-mcp` project as four standalone local-project scheduled tasks from `AGENT-TEAM/automations.toml`: `run-elixir-mcp` (Run Elixir MCP, 12-hourly at :40, terra/high), `keep-the-record-true` (daily 06:50, terra/high), `close-the-loop` (daily 08:35, sol/xhigh; moved from 08:20 to avoid the installed Run the Librarian collision), and `guard-the-door` (Sunday 07:30, terra/high). The manifest now stores the installed canonical IDs, exact durable prompts, and local execution mode. Every task runs preflight before evidence gathering, treats a healthy no-op as success, claims its objective lease only before repository mutation, never clears another holder's lease, and owns authorized work through verification, deployment, and live acceptance. First-run outputs still need review before the feedback-response handoff is considered confirmed.

From the CR API terms review (full doc: [elixir-mcp-terms-review.md](../elixir-mcp-terms-review.md)):

- Launch free; any paid tier requires express Supercell approval per Fan Content Policy (fees prohibited; donations can't gate benefits).
- Gateway fleet is for redundancy/egress resilience, NOT quota multiplication; conservative global rate budget (~one key's limit) to avoid circumvention reading.
- No blockchain/POAP/ENS anything in this product (prohibited fan content).
- No Supercell marks in domain/branding; include unofficiality disclaimer.
- Non-subscriber data exposed only within a subscriber's own context; no arbitrary-tag historical lookup.

Jamie, 2026-09-03 (design session inputs):

- **Gated access:** account creation is "request access" with owner approval, not open signup. Expecting demand; approval queue in the web UI.
- **User-attached gateways:** gateway instances (elixir-mcp-gw) belong to users. Launch with Jamie's only, but the website must let a user raise their hand to run a gateway and walk them through setup. (Implies per-user gateway enrollment, key/IP registration, and a trust/ops story — still one global rate budget.)
- **Domains (revised same session):** web UI at `elixir.poapkings.com`, MCP/OAuth door at `mcp.poapkings.com` (two-hostname cookie separation, librarian pattern).
- **Stack:** TypeScript/Node 24 everywhere; repo `elixir-mcp`, monorepo in Drop's workspace shape.
- **Database:** RDS Postgres, single-AZ db.t4g.micro (24/7 recorder keeps serverless engines awake — fixed small instance wins).
- **Recorder compute:** fully serverless — EventBridge scheduler Lambda + SQS request/results queues + ingest Lambda; NAT-free VPC, email via queue-fed non-VPC relay Lambda.
- **Email:** Fastmail JMAP from `elixir@poapkings.com` (already configured for elixir-bot). Re-evaluated vs SES 2026-09-03 and kept: the relay-queue weight is a VPC/RDS consequence (librarian's non-VPC Lambdas send JMAP directly), SES's VPC endpoint costs more than the relay, and warm sender reputation matters most on the magic-link path. Relay transport is swappable to SES later behind the same queue.
- **Local time (2026-09-03):** `account.timezone` (IANA, migration 0002) — tools render timestamps in the player's local zone alongside UTC and resolve relative windows locally; storage stays UTC; UTC-day rollups labeled honestly. Set in web UI.
- **DNS workflow (2026-09-03):** poapkings.com DNS stays at Namecheap, Jamie applies records by hand. When infra reaches the front-door step, relay the exact records (CNAMEs for `elixir.` + `mcp.`, ACM validation records) to Jamie — never assume programmatic DNS.
- **Identity discipline:** CR tags (player + clan) are the primary keys for all game entities, one canonical normalized form, no surrogate IDs; game-data tables never reference accounts (`claim` is the sole join). Battles write participant rows symmetrically for claimed and unclaimed tags; a new user's claim *unlocks* pre-existing history recorded about them as opponents — entitlement change, never data migration. DESIGN.md §4.1.
- **Approvals:** owner-scoped admin page on the site (request queue, approve/deny).
- **Gateway phasing:** self-serve enrollment ships V2; V1 schema models gateways first-class and Jamie's gateway enrolls through the real path. Keys issued from Jamie's Supercell account (`key_source` column keeps operator-BYO open).
- **Visual design:** web UI follows the Elixir Drop / poapkings.com conventions — Clash Royale-themed look, dark background; a sibling of drop.poapkings.com, not a new brand.
- **Infrastructure:** design must include a mapped-out AWS architecture. Data volume exceeds prior projects (DynamoDB/SQLite territory ends here); a real SQL server — managed Postgres — is the working assumption. Engine/sizing choice (RDS vs Aurora Serverless vs external managed Postgres) to be decided in the design pass.

## Next Steps

- [x] Review Clash Royale API terms of service for compliance with the design (recording/retention, multi-key gateways, commercial use, other-players' data) — done; see Decisions and [elixir-mcp-terms-review.md](../elixir-mcp-terms-review.md).
- [x] Deep design work — elixir-bot data model study, CR API surface analysis, #ask-elixir question mining — producing [DESIGN.md](DESIGN.md).
- [x] Design v2 (2026-09-03): prior-art surveys (librarian auth/OAuth/MCP, Drop bridge, elixir-bot recorder), Jamie's decisions folded in, AWS infrastructure map (§7), prior-art map (§10).
- [ ] Create the `elixir-mcp` repo (monorepo skeleton, Drop workspace shape) and move these docs into it.
- [ ] Verify Fastmail JMAP send-as for `elixir@poapkings.com` works from a non-elixir-bot credential, or mint a dedicated API token for the relay Lambda.
- [x] CR token landed in `elixir-mcp/.env` (2026-09-03, mode 0600, canonical var name **`CR_API_TOKEN`** — note: Drop uses `CR_API_KEY`; our gateway config uses ours). If this is the borrowed Drop key, still swap in the dedicated key tonight; either way, delete the Fastmail draft that temporarily held a token. Do NOT borrow Drop's production key — decided against 2026-09-03 (shared 403 blast radius, and nothing needs a key before the gateway code exists).
- [ ] Confirm poapkings.com DNS/ACM path for `elixir.` + `mcp.` CNAMEs (Namecheap; certs in us-east-1).
- [ ] Privacy policy page for the site (we store emails + behavioral history incl. non-registered players; GDPR/deletion path per terms review §2/§4) — plain-language, one page.
- [ ] Apply to Supercell for the `verifytoken` scope (parallel track; swap into the claim ladder if granted).
- [ ] Postgres schema draft (DDL for §4) — first build artifact.

## V1 build order (the standing work order — check off as chunks land green)

- [x] Repo, gitignore-first, AGENTS.md/CLAUDE.md, workspace root
- [x] packages/contracts core (tags, errors, deck_hash, meta, version)
- [x] Migration runner + fingerprint + 0001 recorder core + 0002 timezone
- [x] Fixtures (12 real payloads + cross-observer pair) + admission + battle/roster ingest
- [x] Pipeline glue: results-message → txn(payload/receipt → admission → projections); poll_state freshness advance; ingest Lambda handler shape (queue contract v1 in packages/contracts; receipt redelivery dedup = 0003)
- [x] Scheduler: heat model, fairness floors, budget settle, job planning; EventBridge handler shape (0004: epoch-anchored decay + last_known_clan_tag auto-follow stamp; bulk share only, live reserve untouched)
- [x] Snapshots + events + rollups projectors (profile → snapshot + donation_reset; roster → clan events w/ evidence, first-sight silent; battles → daily rollups; completeness = day-level estimate from bracketing snapshots — refine if live gaps show)
- [x] Gateway (services/gateway): lease loop (live-first), gzip results + loud overflow, 403 breaker w/ half-open probe, split heartbeats, queue-name indirection, launchd installer, .env.example
- [x] Auth core: 0005 auth tables + magic-link/code + sessions (gate enforced in resolveSession) + rate limit (services/auth)
- [x] OAuth 2.1 logic (services/auth/oauth.mjs): DCR, PKCE, single-use codes, rotation + family replay revocation, 90d absolute lifetime, gate-enforcing validateAccessToken. HTTP shell (routes/HTML/discovery docs) rides the MCP-server chunk; /authorize state = magic_login purpose='oauth'.
- [x] MCP server: protocol layer (compact-JSON results, version cache-buster) + first tools (list_my_players, get_coverage, get_player, query_battles w/ local-time windows) + fail-open quota + audited invoker + bearer handler
- [x] Remaining V1 tools — all 11 declared and tested. Open threads: live lane wiring (returns live_unavailable until infra), upgrade-gap fields need packages/game-data reference table (no invented constants), timeline granularity 'season' deferred to V2 war work.
- [x] OAuth 2.1 HTTP shell + discovery on the MCP door (two-step authorize over the magic core; consent = entering the code; oracle-resistant; RFC 9207/8414/9728)
- [x] Web-api service: full journey tested (request → approve → sign in → claim → record); CSRF contract header; neutral gate responses everywhere
- [x] Web UI (apps/web): landing/sign-in/dashboard/admin/connect in Drop's token palette; 4 RTL tests; 49KB gzipped
- [x] Email relay (JMAP sender + templates + SQS consumer; EmailMessage contract) — transport swappable behind the queue
- [x] Season-roll watcher (shared pre-reset window fn; scheduler forces in-window profile polls; projector pins season_roll rows)
- [x] infra/ DEPLOYED 2026-09-03 (Jamie's go): stack live, migrations 5/5, smoke 7/7. Fixed en route: RDS force-SSL (sslmode=no-verify) and gateway .env→process.env export for launchd. Owner account + jamie-mac gateway seeded via the migrate Lambda's explicit seed payload; gateway running under launchd (com.poapkings.elixir-mcp-gw).
- **Hostname consolidation (Jamie, 2026-09-03 post-launch):** connect URL is `https://elixir.poapkings.com/mcp` — ONE hostname, path-split CloudFront behaviors (/mcp, /oauth/*, /.well-known/* forward no cookies). The mcp. distribution deleted before DNS existed; its issued cert sits unused (free). OAuth issuer = https://elixir.poapkings.com.
- **Owner identity fixed (2026-09-03):** owner login is jamie@thingelstad.com; the AWS-only address was purged from prod DB, local dev, repo, and git history (filter-repo + force push).
- [ ] DNS at Namecheap (Jamie): ONE record once I confirm aliases live — elixir → d3nauaye5ioxmf.cloudfront.net.
- [ ] Jamie: sign in at https://d3nauaye5ioxmf.cloudfront.net, claim #20JJJ2CCRU, opt into recording (the real product flow — also live-tests the JMAP relay).
- [ ] projects-sysadmin: subscribe elixir-mcp-alarms to projects-ops-alerts; add the gateway launchd service + backup verification to the rotations.

**Launch-day incidents (2026-09-03, all resolved same hour):** (1) RDS force-SSL → sslmode=no-verify; (2) gateway .env not exported to process.env under launchd; (3) stale CloudFront cache after web sync → deploys now invalidate; (4) API GW v2 base64 form bodies → 'unknown client_id' on first OAuth connect → rawBody() decode everywhere; (5) gateway had no instantaneous pacing → 1.5s fetch floor added; (6) first CR token was endpoint-limited (clans 200, players 403 accessDenied — the 'insufficient token scope' 403 flavor is REAL, watch for it) → replaced with a correct key. **STEADY STATE reached 18:30Z:** 92-job clan fan-out drained to 0, 177 fetches/6min, breaker silent, ingest clean; ~46 POAP KINGS members + Jamie recording.
- [ ] End-to-end on real recording (Jamie's tag via his gateway) — **GATE: dedicated CR key + DNS records from Jamie**

## V2 build order (the standing work order — check off as chunks land green)

Start every iteration with a recorder health glance (gateway FetchSucceeded/BreakerOpen, DLQs, a get_coverage call) — we babysit while we build. Deploys are routine (`deploy.mjs --skip-web` unless web changed); no billable gates in V2. Jamie-manual steps get queued in NOTES, never blocked on.

**Multi-tenant discipline (Jamie, 2026-09-03): elixir-bot's war/clan code was single-tenant — the pattern, never the assumption, transfers.** Every clan-scoped table leads its PK with the observing clan_tag; the war clock/season inference is per-clan; standings are view-scoped per observer (duplication across recorded clans in one race is by design); entitlements scope clan reads to the member's OWN clan. Any new clan-activity feature gets this checked explicitly.

- [x] War clock (services/ingest/war-clock.mjs): period grid + season inference + observed-anchor-with-fallback + resolveWarKeys from battle time (cross-section = honest nulls); verified against the three real captured riverrace payloads
- [x] Migration 0006 (deployed): war_period_anchor / war_week / war_week_clan / war_participation (**points**) / war_attendance_day + claim.share_battles_with_clan
- [x] War projector (deployed): anchors first-observation-wins; honest anchor_only genesis pending backfill; MAX-merge throughout; war keys stamped from battle time via per-clan clock; stale-payload-at-roll edge documented
- [x] riverracelog backfill (deployed): the log is a daily recorded endpoint; enrollment backfill = first poll; items carry seasonId (genesis unlock); 0007 adds rank/trophy_change; participation clan-scoped to own members
- [x] Entitlements module (deployed): rules 2-4; INTERIM soft claims count for clan scope until liveness ships (CLAN_SCOPE_REQUIRES flips then); owner administers all recorded clans, member consent still applies to owner
- [x] Clan enrollment UI (Jamie's ask, deployed): owner-only /api/admin/clans + Recorded Clans admin panel; leader self-serve stays a later item
- [x] Tools (deployed): get_war, get_war_history, get_clan, compare_players; all 15 tools entitlement-aware (clanmate=summary, war-only battles without consent + scope_note); timeline 'season' granularity deferred to a contract-minor later. Ops stats read added to the migrate Lambda ({stats:true}).
- **OPS GAP found live: the gateway runs launchd-from-source and does NOT pick up deploys — a gateway-code change needs `launchctl kickstart -k gui/$UID/com.poapkings.elixir-mcp-gw` (bit us: riverracelog jobs unleasable by the old process, 1 DLQ'd). Queue a proper fix (gateway self-update or a deploy-script reminder). Session MCP tool rosters are also start-frozen — new tools appear on reconnect (the serverInfo.version story working as designed).**
- Prod observation 20:40Z: 7 active recordings (Jamie appears to be enrolling clans via the new panel), 1,572 players, 1,533 battles.
- [x] Live lane (deployed + verified with a REAL end-to-end fetch from Claude Code: live colosseum data through the gateway in one tool call): enqueue → receipt poll; opportunistic recording confirmed; 50/day per-account live cap (owner exempt)
- [x] ~~Liveness-proof verification~~ RETIRED same day (Jamie, 2026-09-03): currentFavouriteCard is not reliably player-settable in-game, so the challenge could never be completed (cr-agent-api-docs patched). **Claims are trust-based**: owner approval is the gate, a claim is taken at its word. claim.status + verification_challenge (0008) stay in the ladder unused; never rebuild verification on favourite-card.
- [x] Web clan page (deployed): /clan — war standings (final rank order, our row highlighted), roster with trophies/donations/last-battle recency, share_battles toggle per claim. Recency policy mirrors get_clan: activity recency is roster-level; consent gates battle DETAIL only. Members reach it via open membership of a claimed tag in a recorded clan; owner falls back to first recorded clan. Leader self-serve clan-enroll folds into the gateway self-serve item below (same raise-hand pattern).
- [x] Gateway self-serve enrollment (deployed): POST /api/gateways raise-hand (name+static IP, owner notified) + Dashboard panel; owner lifecycle actions (pending→probation→activate→drain→revoke, forward-only) in the Admin gateways panel; ingest now ENFORCES the lifecycle (refuses revoked/unknown gateway_ids, cleanly — no FK-throw retry loop) and stamps last_heartbeat_at on any valid message / last_success_at on admission, so the health panel is real; docs/OPERATORS.md is the public guided install (linked from README). **Jamie-manual per operator: create the IP-allowlisted Supercell key + a per-gateway IAM user (recv/delete on request queues, send on results, PutMetricData) and hand both over out of band; then click "Begin probation".**
- [x] V2 smoke on live data (2026-09-03 21:16Z): smoke.mjs all green; ops stats 8,008 players / 7,442 battles / 64 war weeks / 2,975 participation rows / 5 war anchors across 12 recordings; live MCP get_coverage answered with 31s freshness and pre-claim battle capture. DESIGN/NOTES/AGENTS + memory updated.

**CI note (2026-09-03):** validate had NEVER passed — `node --test test/` needs the directory-positional support local Node 26 has and CI/Lambda Node 24 lacks, so every workspace failed on every run. Fixed with no-arg `node --test` (9a298ee); first green run confirmed. Lesson: dev machine runs Node 26, prod runs Node 24 — CI is the version referee, so check it after the first push, not days later.

**MCP agent-in-the-loop testing phase (Jamie, 2026-09-04, replaces the manual Claude<->Claude Code markdown relay):** fresh-context subagents ARE the naive users — they inherit the session's authed MCP connection but none of the implementation knowledge. Each round: spawn 4-5 persona agents (casual "how am I doing", deck tinkerer, clan leader, data-honesty nerd, adversarial edge-poker), each with a bounded mission (max ~12 tool calls, cr_api_live at most once) returning STRUCTURED feedback (task outcome / friction with exact tool+field names / delights / wishlist). Triage into three lanes: (1) fix now — descriptions, error messages, response shapes (contract-versioned); (2) feature wishlist -> this file for Jamie; (3) rejected with reason. Deploy fixes, spawn a FRESH round (never reuse a contaminated agent), repeat until a round is dry. Measure rounds objectively from mcp_call_audit error/truncation rates during test windows. Runs after the archive import + R1 land. **Jamie-manual: reconnect /mcp in the session when convenient — the session tool roster is start-frozen at the 11 V1 tools, so war tools (get_war, get_clan, get_war_history, compare_players) are invisible to testers until reconnect.**

**Archive import COMPLETE (2026-09-04 06:40Z):** all 13,809 elixir-bot raw payloads replayed through the real pipeline — final state 37,725 players / 35,120 battles / 2,730 snapshots / 134 war weeks / 5,109 participation rows; Jamie's own record now May 14 -> present (235 appearances). Verified via MCP door post-import: played-time ordering correct with interleaved inserts, display-scale levels, coverage honest. One mid-run FK crash (war projector vs never-seen clan) fixed forward. Perf census final in docs/DB-AUDIT.md: projector = 93-94% of cost, career-length scaling confirmed -> R1 next (batch projector writes + player/time index).

**MCP tester wishlist (pilot round 1, for Jamie's triage):** per-week win-rate series on get_performance (group_by: week — the tester approximated trend with one before/after split); mode filter or PoL-medal series on get_player_timeline; gap disclosure on timeline (gaps array or per-point flag); get_deck_performance sort/min_battles/share_of_battles; a single headline summary call (current trophies + 30d W/L + top deck). Pilot verdict: harness works — 6 tool calls, task answered, feedback citable; six friction fixes shipped same night (contract 0.2.1).

**PHANTOM-SEASON INCIDENT (found + fixed 2026-09-04, round-2 agent playtest):** the war-nerd tester cross-checked member_weeks against the battle log and caught colosseum rows frozen at day-1. Root cause: inferSeasonId's stateful roll (+1 when live section < logged section) ran away during the archive replay — logged state was FUTURE relative to replayed payloads, scattering 9 real weeks (true S134-135) across phantom seasons 136-144, then hijacking live polls into "145". Fix: seasons derive STATELESSLY from the CR calendar (first Monday -> first Monday, ~09:30Z; war-clock seasonFromDate, anchored + verified against riverracelog createdDate). 0021 purged season>135 rows; 132-135 history pristine; current week self-heals via MAX-merge. CLASS LESSON: time-derived identity must come from the calendar, never from a state machine that assumes in-order processing — same family as the insert-order/battle-time cursor bug. Round-2 honesty batch SHIPPED 2026-09-04 (contract 0.6.0, verified live): war_days_battled null-when-no-attendance-coverage (unknown never zero), seasons arg scopes member_weeks, get_war participants carry in_clan, standings payload-mirror note (zero-fame opponents can be real), member_since_observed -> first_observed_in_clan, compare_players recorded-window note. Still parked for later: periodLogs harvest (the proper fix for null week ranks/fame — daily endOfDayRank/pointsEarned).

**S3 payload archive BUILT 2026-09-04** (DATA-TOOLS.md has status + crib sheet): archive-at-admission live-verified (first live object 16:05Z), weekly sweep scheduled (MON 08:15Z). Free S3 gateway endpoint added (new private route table — the VPC lambdas had NO S3 route before this). **Migration COMPLETE same day:** history export 15,367/15,367 rows -> S3 (1:1 reconciled, 216MB gz), first sweep drove Postgres api_payload down to the 545-row hot set (14,822 superseded rows retired, 0 missing twins). Athena verified live (partition-projected query over Jamie's profile versions). Disk note: the 405MB table file shrinks only via vacuum full — pages are reused instead; RDS allocation floor is 20GB so reclaiming buys nothing. dt partition for replayed archive content = replay date (first time WE held the content), original fetch times live in receipts.

**Collector repo split DONE 2026-09-04:** github jthingelstad/elixir-mcp-collector (public); services/gateway removed from this repo; both local instances migrated in place (installer re-run from the new checkout, same labels/env files) and verified up; self-update now tracks the collector repo's main — deploying collector code = push THERE. Queue contract canonical here; the collector test pins the produced shape so drift fails collector-side first.

**MCP testing round 3 COMPLETE (2026-09-04, contract 0.7.0 + 0022):** three personas (war auditor / casual / adversarial), 13 fixes shipped same day — see commit ce5986e. The auditor's attendance cross-check exposed a REAL bug chain: attendance now unions decksUsedToday polls with recorded war battles, which exposed that (a) 0021 never purged phantom season stamps from battle ROWS (136-145 lived on) and (b) early-section battles stamped with null war_day are never revisited since stampWarKeys only selects season_id-is-null rows -> 0022 nulls both classes for re-stamping; boatBattle joined the stampable types. CLASS LESSON: a purge must sweep EVERY table carrying the poisoned key (0021 got the war tables, missed battle), and any stamp-once field needs a revisit story for rows stamped wrong. Adversarial: validation now symmetric (empty tag, forged cursor, >4 compare tags, seasons>12 all refuse loudly); full-verbosity limit capped at 25 since opponents now carry decks+tower_hp. Casual: week_of = ISO Monday, trophy_battles per weekly row, draws + best_deck + inclusion note on summary, names stamped from battlelogs (fill-null, forward-only — pre-existing opponents stay null until re-observed; S3 archive replay could backfill if wanted). cr-agent-api-docs patched (tower HP can be null).

**Round-3 wishlist parked for Jamie:** per-war-day per-member breakdown in get_war; decks_possible alongside decks_used; opponent tower HP / margin / close-losses filter on query_battles; roster tenure distinct from first_observed_in_clan; per-block as_of stamps; participants filter (in_clan_only/min_points); coverage hint inline in every summary tool; mode split inside weekly rows.

**ARCHITECTURE ASSESSMENT delivered (2026-09-04 night, Jamie's multi-clan thesis):** elixir-mcp = system of record for GAME facts; elixir-bot = system of record for COMMUNITY facts (voice/Discord/memory/policy verdicts). Unique-to-POAP stays in each fork (SOUL/CLAN/lanes, policy constants, spend); generic ALGORITHMS (archetype classifier, band math, awards mechanics) belong in the bot template as shared code, NOT in MCP; recording/fleet/corpus-intelligence/history belong in MCP and compound per clan (each clan brings a collector = capacity flywheel). Path: (1) stats reads DONE; (2) intelligence into bot voice (rivals->war narration, standings/pilot->answers, meta tools replace web-scraped meta_decks); (3) THE RECORDER SWAP - bot keeps emitters/baselines, sources observations from MCP recorded reads; when proven equivalent, bot's CR key/poller/raw layer retire (this makes clan #2 cheap; needs its own design doc before code); (4) productized fork + enrollment + service token + collector handshake. Risks on record: shared fate (fallbacks must survive phase 3), contract stability (no more alias-free renames once clan #2 connects), per-clan quota/budget policy.

**CLAN NOTIFICATIONS FIXED + CAPTURE AUDIT (2026-09-06, 0036, contract 0.21.1, commit 06f1072):** Jamie: "clan notifications aren't working - what triggers one?" INVESTIGATION: working as designed, design covered ~nothing - the ONLY clan topic was clan_war_week_finished (fires WEEKLY at the section boundary; none had passed since the lane shipped, so zero clan events ever). FIX: roster projector bridges observed diffs into the feed - member_joined / member_left / member_role_changed to accounts that ADDED the clan (firstSight silent, post-commit flush, fan-out pinned by test). CAPTURE AUDIT (Jamie approved): capture_audit table - one row per fresh battlelog poll with prior coverage; gap=true when the payload's OLDEST battle was previously unseen (log rolled past unseen battles). First polls = history arriving, never gaps. 24h gaps/polls PUBLIC on Data > Status - "no gaps" is now a measurement. QUEUED (Jamie, this session, in order): (1) DETAIL-VIEWS AUDIT - "click into an item" pages across the site: feedback items deserve dedicated pages; Collections editing should happen INSIDE an opened collection (current add/remove UI "very odd"); sweep the whole interface for detail-view opportunities. (2) POAP KINGS WEBSITE: add Elixir MCP alongside Elixir Drop + Elixir Agent. HELD: elixir-bot recorder swap.

**CLAN PULSE SHIPPED (2026-09-06, contract 0.22.0, commit 545799b):** Jamie ratified the design ("a good v1") and said to CLEAR THE QUEUE autonomously. Shipped same day: clan_pulse daily digest (EventBridge 07:00Z rule elixir-mcp-clan-pulse -> migrate {clan_pulse}; idempotent per clan per UTC day; notify-gated fan-out; facts only - battles_24h, active/total, top 3, quiet >=5d + never_recorded, war decks counts, roster changes), war_day_open on first sight of a war-day period anchor (recency-guarded, in projectRiverRace's new feedEvents), war_current decks_today (named untouched/partial/finished, attendance UNION battles, nominal-period-end staleness guard - a stale anchor says nothing). First live pulse verified: {clans:1, emitted:1}. Routine recipe in Docs > Tools. LESSON: warClock cross-checks periodIndex vs sectionIndex - mutated fixtures must keep them consistent. DEPLOY NOTE: deploy.mjs needs AWS_PROFILE=jamie in env (CLI profile flag isn't enough for the SDK's provider chain).

**CLAN PULSE - DESIGN (2026-09-06, docs/CLAN-PULSE.md, ratified):** Jamie: clan notifications should summarize player battle activity too; "design the solution first by looking at the kinds of things elixir-bot does" - intent is a scheduled Claude routine doing clan management against Elixir MCP. Design doc written, NOT built. Proposes: (1) `clan_pulse` daily digest topic (one per added clan per UTC day ~07:00Z via EventBridge -> migrate op; battles_24h, active/total members, top 3, quiet list >=5d recorded-quiet, war-day decks untouched/partial/finished, roster change counts - FACTS never judgments); (2) `war_day_open` real-time topic from the war_period_anchor first-insert; (3) war_current gains `decks_today` named lists (elixir-bot's _remaining_decks, computed attendance-union style); (4) routine recipe ships in site docs. No schema change; contract 0.22.0 when built.

**TINYLYTICS + BUTTONDOWN SHIPPED (2026-09-05, commits d351b24 + 5f91c13):** Jamie provisioned both tokens in .env and said clear the queue. TINYLYTICS (site Yzx8dUUvUPn9AEJpTMeU / numeric 3762, NO kudos): client embed loader (hits+countries+events+beacon; skips /signin entirely - magic tokens ride that URL; scrubs login_token; localhost never tracks) + SERVER-SIDE events - VPC lambdas enqueue {v:1,kind:"tinylytics_event",event,value} on the EMAIL queue, the non-VPC relay batch-posts to api/v1/sites/3762/events/batch (Bearer, mandatory User-Agent for Cloudflare, 1000 req/hr limit). Taxonomy: mcp.tool_call / explore.tool_call (tool name only), site.access_request, site.signin (magic_link|code), site.feedback (category). VERIFIED LIVE: one tool call -> mcp.tool_call visible via Tinylytics API seconds later. BUTTONDOWN (Drop's model, adapted): NO plaintext email exists in any table, so enrollment happens RELAY-SIDE on kind=login sends (only approved accounts ever get login email); idempotent POST, 400=exists=untouched (unsubscribes never overridden), failures log-and-drop (retry would RESEND the login email). RELAY SEMANTICS SPLIT (pinned by tests): email retries hard -> DLQ; analytics + enrollment are best-effort and NEVER dead-letter. Both tokens merged into elixir-mcp/app secret via new infra/scripts/secret-add-keys.mjs (file->AWS in-process, values never printed). Privacy page discloses both; newsletter pixel-free per ratified policy. JAMIE-MANUAL: (1) confirm tracking pixels are OFF in the Buttondown newsletter settings; (2) if the Buttondown account hosts multiple newsletters, tell me the newsletter id and I'll set BUTTONDOWN_NEWSLETTER_ID (currently unset - token-scoped assumed). WATCH: first real sign-in should appear as a Buttondown subscriber.

**QUEUE (Jamie, 2026-09-05, clear autonomously):** (1) DONE clan pulse; (2) DONE tinylytics; (3) DONE buttondown; (4) DONE docs refresh (architecture depth: collectors/pulse/feedback-loop/relay + repo family links; README repo-shape fixed); (5) DONE detail-views v1; (6) DONE POAP KINGS website (poapkings.com commit 6cde47f, Pages deploy green, /elixir-mcp/ live: family-pattern page + third home tool card + llms.txt/llms-full + agent-page cross-link; honest framing - request-approved, one shared budget). QUEUE EMPTY 2026-09-05 - back to standing watch (magic-pines heartbeat, health pulse, feedback loop). Detail-views audit items (user collections UI gap etc.) await Jamie's triage.

**VERSION GATE ARMED + BRIDGE-ERA FILES PURGED (2026-09-06, Jamie: magic-pines confirmed v2.0.22):** COLLECTOR_MIN_ENFORCE is now a TEMPLATE PARAMETER (CollectorMinEnforce, "0"|"1", PRESERVED so a routine deploy can never silently flip it either way) and is set to 1 in production. Turning it on or off is now a parameter-only stack update: `node infra/scripts/deploy.mjs --param=CollectorMinEnforce=0`. NOTE the first deploy of a new PRESERVED parameter needs that `--param=` override, because UsePreviousValue has nothing to point at - this repo's parameters.mjs already documents and handles that (Drop's did NOT, and I had to fix it there). POST-FLIP VERIFICATION, worth repeating if it is ever re-flipped: heartbeats stay fresh under refusal (authGateway bumps last_heartbeat_at BEFORE the version check), so heartbeat is NOT the signal - watch `last_success_at`. It climbed on all three for ~5 min after the flip and I nearly called an incident; it then RESET to 23s, i.e. the climb was just the gap between admission batches. Zero `client_too_old` in the door log, both local collectors logging jobs done with 0 fetch errors. Wall Breakers (bulk channel) ages longer between admissions by design - live heartbeat + old data means IDLE, which is exactly what the status page now says. PURGED from the collector checkout: .env.gw2 and .env.local-go (dead IAM-era + dev-build credentials) and bin/collector-v2 (14MB retired dev binary). Nothing referenced them; the live pair runs from ~/elixir-collectors/{ram-rider,tesla}/. The remaining .env in that checkout is unreferenced by any launch agent - it is the operator-facing example config, not a live credential path.

**DROP MIGRATION COMPLETE: 7.2 + WAR CLOCK + BRIDGE RETIRED (2026-09-06, Jamie: "do the complete plan", run unattended):** (1) 7.2 PROJECTION (migration 0044, contract 0.25.0): player gains last_known_clan_role, clan gains badge_id, player_snapshot_daily gains arena_id/best_trophies/favorite_card_id, plus a new player_badge CURRENT-STATE table upserted only when a badge actually moved (139 rows/player/day would be the largest thing we write, to say nothing). years_played/account_age_days ALREADY existed since 0024 and only needed exposing. players_profile now answers clan{badge_id,role}, an attributes block and badges - IDS ONLY, never icon URLs. BACKFILLED from retained api_payload because the player endpoint polls on a 3-DAY floor; waiting for traffic would have starved every recorded player for days. (2) DROP WAR CLOCK -> war_current: the bridge polled 2 CR endpoints every 5 min forever (~576 calls/day, Drop's biggest Supercell consumer). Now read on demand behind a 5-min TTL with the stored clock as the cache, so it costs a call only when someone is using Drop. THE ANCHOR IMPROVED: Drop derived seasonStartsAt from an ASSUMED 10:00 UTC reset corrected by the last riverracelog close; the hub reports the OBSERVED period open. Verified live with the bridge stopped - season/section/period/type identical, seasonStartsAt 09:57:37 (observed) vs 09:34:00 (derived). (3) BRIDGE RETIRED: services/cr-api-bridge deleted, request queue + DLQ + backlog/heartbeat alarms + IAM policy removed from the template, launch agent removed, IAM user elixir-drop-cr-bridge DELETED (key, inline policy, user). services/api no longer depends on @aws-sdk/client-sqs - knip caught that, and it is the tidy proof no producer remains. DROP MAKES ZERO CR CALLS AT RUNTIME; the only Supercell credential left is the manual card-snapshot refresher. DELIBERATE DEPARTURE from "retire both queue pairs": the RESULT pair STAYS. podium-finalize is a manual season-repair command with no in-repo producer and is now that queue's only rider; deleting it would have removed a working repair path and replaced it with nothing. Drop's golden rules 2 and 3 ("only the bridge may call CR", "the CR token lives on the allowlisted host") were rewritten rather than left wrong. CLOSED SAME SESSION: enrichment now reads players_profile FIRST and falls back to live_fetch only when the record cannot answer - the ordinary cold case (a just-linked tag), whose live result the hub records opportunistically, so that player reads from history from then on. `not_recorded` is therefore not logged as a failure. Drop's steady state now spends zero CR budget and zero login latency on enrichment. Also swept the 13 stored CR_PLAYER snapshots that still carried the bridge-era `cards` arrays - unused, unsent since the enrichment swap, now removed (note the key shape is CR_PLAYER##TAG, double hash, because the tag carries its own). DROP MIGRATION COMPLETE.

**DROP ENRICHMENT MOVED TO THE HUB + THE 7.2 GAP NAMED (2026-09-06):** Drop's player enrichment no longer goes API -> SQS -> fixed-IP bridge -> Supercell -> SQS -> Lambda. It is one `live_fetch /players/{tag}` through the hub, normalized in Lambda with the SAME rules the bridge used (clan needs tag+name+badgeId or it is dropped whole; account age is the YearsPlayed badge, progress = days), so the stored snapshot shape did not move. The 6h claim still dedupes, so a returning player inside the window costs nothing. CARDS DROPPED - collected, stored, shipped to the browser on every /me, read by nothing (confirmed in code and by 3 doc prohibitions + a regression test). CONSEQUENCE knip caught: services/api no longer imports @aws-sdk/client-sqs at all, because that refresh was its ONLY producer - the request queue and the bridge's player worker are now dead; the war clock is the last thing keeping the bridge alive. THE GAP JAMIE FLAGGED, now written into CONSUMER-SURFACES §7.2 with the priority raised: `players_profile` carries name + clan{clan_tag,name} and NOTHING else about the player as a game entity, so the hub cannot answer clan badgeId, clan role, or account age - the three fields Drop renders. They arrive in every player payload we already record and are thrown away. That is why Drop pays 1-3s and a slice of the CR budget for a LIVE read of facts we own. They pass the §1 test on their own (game facts about a player, not Drop's presentation). Closing 7.2 lets Drop's login path read recorded data instead of live-fetching. NEXT CHUNKS, in order: (1) 7.2 attributes + badges projection, (2) Drop's war clock -> war_current (verified it returns everything Drop derives; ~576 CR calls/day), (3) retire the bridge + both queue pairs + IAM user + Mac host.

**ELIXIR DROP READS THE HUB, STEP 1 (2026-09-06, Jamie: "we are ready to move forward"):** The consumer-surfaces plan (docs/CONSUMER-SURFACES.md, ratified) is the design of record and it is no longer unbuilt. SHIPPED: `collections_edit` (contract 0.24.0) - add/remove/set on a collection you own, 500 tags a call, idempotent, malformed tag refuses the WHOLE call (a silent skip during a roster sync quietly drops somebody). Domain-shaped per §1: an action on a domain resource, never `drop_add_player`. It is the FIRST write tool outside the service domain, and war-tools.test.mjs pinned "the service domain owns all write tools" - the invariant is now "service domain plus curating your own collections", because editing a collection you own is editing the domain, not administering the service. Recorded game history stays read-only to every tool. DROP SIDE: services/api/src/elixir-mcp.ts (JSON-RPC client, injectable fetcher per Drop's convention) + elixir-collection.ts; membership asserted on tag save (PATCH /me) and on every login (idempotent, and how pre-existing tags get picked up); best-effort so a hub outage cannot fail a login whose magic link is already spent. Drop's golden rule 2 amended: Elixir MCP is NOT the CR API, so Lambda may call it - the hub holds recorded history, not a live Supercell connection. INITIAL SYNC DONE: 13 tags, 11 new recordings, via scripts/sync-elixir-collection.mjs (adds, never sets - Drop is one source of members and a wholesale set would evict hand-curated ones). THE FINDING THAT CHANGED THE FEATURE: "anyone who signs up" is impossible - Drop's CR tag is OPTIONAL, out of setup by design, and UNVERIFIED (SPEC.md, CLAUDE.md). 26 accounts, 14 with a tag, 13 distinct. MY OWN ERROR WORTH REMEMBERING: my first count said 5,337 accounts because I filtered on `begins_with(pk,'PLAYER#')` without `sk='PROFILE'` - runs live under the same partition key. Always constrain sk on that table. INFRA TRAP FIXED IN DROP: its parameters.mjs preserve-by-default discipline sent UsePreviousValue for every preserved parameter, which CloudFormation REJECTS for a parameter it has never stored - the first deploy carrying a new preserved parameter would have failed outright. deploy.mjs now reads the deployed parameter keys and a never-seen parameter falls through to its template Default. STILL TO DO (Drop): live_fetch for player enrichment, war_current for the war clock (verified it returns everything Drop derives; ~576 CR calls/day saved), drop the cards[] persistence (CONFIRMED unused - collected, stored, shipped to the browser, read by nothing; the collection-dealt practice mode lived 24h in July 2026 and is now prohibited in CLAUDE.md, SPEC.md x2 and a regression test), then retire the bridge + both queue pairs + the IAM user + the Mac host.

**COLLECTIONS RECORD, AND DEPTH BECOMES ONE IDEA (2026-09-06, Jamie, migration 0043, contract 0.23.0):** Jamie was double-entering: adding a player/clan to his account AND to a collection, because a collection recorded NOTHING - it wrote a collection_member row and stopped. Now a tag named in a collection is a REASON TO RECORD, the third alongside a claim and an ops recording, and packages/claims owns the decision in ONE function (reconcileRecording): a subject is recorded while any reason holds, and stops when the last one goes. removePlayer and every collection edit call it, so the two can never disagree. DEPTH IS NOW ONE VOCABULARY ACROSS BOTH KINDS: `recording.clan_scope` -> `recording.scope`, now NOT NULL for players too, because the question was never clan-specific - it is "do we go and get the battles". clan activity = roster/war/standings, clan comprehensive = + every member's battles and profile; player activity = profile only, player comprehensive = + every battle. THE SCHEDULER HONOURS IT: plan.mjs only seeds/keeps the player_battlelog poll_state row when scope='comprehensive' (an activity player still gets its 'player' profile row). `collection.scope` carries the curator's choice, default comprehensive - a collection is usually assembled to study the play. RULES THAT MATTER: depth is only ever UPGRADED on a shared subject (two collections at different depths -> deeper wins; a shallow collection re-saving never takes battles away from someone else's comprehensive), and a CLAIM always means comprehensive because adding a player to your account is an explicit ask for their data. UI: membership is now ONE TEXTAREA, one tag per line, saved as a SET (the old add-a-box/remove-per-row shape made you think in diffs, not membership); an invalid tag REFUSES the whole set rather than silently deleting curation. Descriptions 500 -> 2000 chars. Owner and admin routes both go through setCollectionMembers. STILL OPEN, Jamie's item 3: externally managed collections (Elixir Drop adding players as they sign up). The path is an MCP tool calling setCollectionMembers, authenticated by a SERVICE TOKEN - services/mcp already validates those (validateServiceToken), so no new auth surface is needed, and set-semantics is already the right shape for "here is the current roster". CAPACITY IS THE OPEN QUESTION THERE: a large comprehensive collection fans out to every member's battles against ONE global rate budget; scope='activity' is the lever, and a size cap may be needed before Drop's collection lands.

**CROSS-ACCOUNT RECORDING RACE + FILTERED-EVENT ACK (2026-09-06, issues #12/#13):** #12 IS A FOLLOW-ON TO MY OWN #9 FIX, filed P1 and correct: packages/claims locked the ACCOUNT row, but a recording and its subscriber count are keyed by TAG and shared by everyone watching it, so two accounts could decide one recording's fate at once. Two interleavings both broke: (a) A removes its last claim and stops the recording while B adds the same player - B cannot see A's uncommitted stop, finds an active recording, creates none, and commits: one subscriber, nothing recorded; (b) A and B each remove their claim, each still sees the other's uncommitted claim, neither stops it: no subscribers, recording still running. FIX: pg_advisory_xact_lock(hashtext(tag)) taken AFTER the account lock. ONE FIXED ORDER, account then subject - every mutation touches at most one of each, so that order alone rules out a deadlock cycle; do not reverse it in a new caller. #13: elixir_events applied the topics filter to the READ but advanced the single global events_seen_through to the last RETURNED event, acknowledging everything the filter skipped over - a war-only routine could clear an account's unread feedback reply for good. Acknowledgement now stops at the first excluded event (one extra query, only when topics is set), and the response carries seen_through plus a note when it stopped early. mark_seen:false still acknowledges nothing. Contract 0.22.3. TEST NOTE worth keeping: my first #12 test read `select status from recording where subject_tag = $1` and took rows[0] - a re-add after a stop creates a SECOND recording row, so that helper was ambiguous and failed a correct fix. "Is this recorded?" is a question about the ACTIVE row, never the first one. Regression proof: each old behaviour restored in turn fails exactly its own test (3 for #12, 1 for #13).

**CLAIM MUTATIONS: THREE BUGS, ONE SHARED FUNCTION (2026-09-06, issues #8/#9/#10):** All three lived in the add/remove-player logic, which existed as NEAR-DUPLICATE COPIES in services/mcp/src/tools/elixir.mjs and services/web-api/src/handler.mjs - the copies drifted and both carried all three defects. Fixed once in a new `packages/claims` workspace that both services now import; do not reintroduce a second copy. Every mutation runs in ONE transaction that opens by locking the account row (`select ... from account where account_id = $1 for update`), which is what makes the slot cap real (#10: check and insert were separate autocommit statements, so three concurrent adds each saw 2 of 3 used and all three won, leaving 5 claims against a limit of 3). #8: the new primary was INSERTED before the old one was cleared, so the partial unique index claim_one_primary_per_account rejected it and the switch never ran - now the old primary is cleared first, and make_primary works on an already-claimed tag; removing a primary with others left now PROMOTES the oldest remaining one (it used to leave the account with no primary, and every default-player tool answered not_found). #9: MIGRATION 0042 adds recording.origin ('claim'|'ops'). Stopping a recording used to require the remover to be its original requested_by, so with two subscribers the wrong removal order orphaned it forever; the stop now keys on origin='claim' + zero remaining claims, regardless of who removes last, while origin='ops' (pros, clan fan-out - deliberately recorded with no subscribers) is never stopped by a removal. BACKFILL IS CONSERVATIVE ON PURPOSE: everything became 'ops' except player recordings with a matching 'recording_started' account_event, which is positive evidence a claim created it and survives the claim's deletion. Wrongly marking a pro recording claim-origin would silently stop collecting it, so the safe direction is to preserve. CONSEQUENCE: existing orphaned recordings (zero claims, no start event) stay active - they are indistinguishable from deliberate pro recordings and need a human eye, not a migration. Contract 0.22.2 (additive: primary_player_tag on remove). Tests: packages/claims/test/claims.test.mjs, 11 against a real scratch Postgres including the 3-connection concurrency race; each of the three old behaviours was restored in turn and fails exactly its own test.

**RELEASES ARE CANDIDATES UNTIL NAMED (2026-09-06, Jamie: "we should also be marking releases in the collector repo"):** Closes the soak gap in one mechanism instead of a written rule nobody reads. release.yml now publishes every green build as a PRERELEASE with notes saying it is not named; infra/scripts/name-collector-release.mjs promotes the release it names to Latest (`gh release edit --prerelease=false --latest`) with a note recording the timestamp and platform count. WHY THIS IS THE WHOLE FIX: install.sh, install.ps1 and the README all fetch `releases/latest`, and GitHub defines that as the newest NON-prerelease - so promoting on naming means a fresh operator install gets exactly the version the fleet runs, and an unsoaked build is invisible to them. VERIFIED: v2.0.22 published prerelease=true while releases/latest still resolved to the named v2.0.19. Script changes that came with it: a prerelease is now the EXPECTED input (it used to be refused); with no tag it takes the newest release of ANY kind via `release list`, because `release view` with no tag resolves to the last PROMOTED one and would re-name what is already live; a promotion failure WARNS rather than reporting failure, since the DB rows are already written and the fleet will update regardless. TRAP HIT AND FIXED: the first version of the notes used a multi-line `--notes` whose second line started at column 0, which ends a YAML block scalar - two pushes produced runs that failed before any job started, with no log. Notes now come from one printf into a file; validate a workflow edit by extracting the run block and running `bash -n` on it, don't push and hope.

**min_client_version MADE REAL (2026-09-06, Jamie, item 5):** The field had sat dead on the wire since the zero-trust cutover because it compared two different KINDS of number: the door asked for "2.0.0" (the client generation that speaks config/lease/submit) while the Go binaries called themselves v0.1.x (a CI build counter) and the Python twin hardcoded "py-2.0.0" regardless of what code was running. STAGE 1 DONE: collector release.yml now builds v2.0.<run>, the Python stamp inherits it (py-v2.0.<run>), v2.0.19 published and NAMED for all 7 platforms. Ram Rider self-updated v0.1.16 -> v2.0.19 on a kickstart (log shows "update authority names v2.0.19; self-updating" then "updated; exiting for supervisor restart" - the documented signature); Tesla re-downloaded to py-v2.0.19 (Python never self-updates, by design). magic-pines follows on its next hourly config call. STAGE 2 SHIPPED BUT OFF: parseClientVersion + clientMeetsMinimum in collector-door.mjs, gating lease and submit with HTTP 426 client_too_old. THREE DELIBERATE PROPERTIES, do not "fix" any of them: (1) CONFIG IS NEVER REFUSED - it is the channel a stale client self-updates through, so locking it out is how a version gate becomes a brick; (2) FAIL OPEN - an unparseable version on either side is ALLOWED, because this retires stale clients and does not authenticate anyone (the bearer token does), so a formatting slip must not become a fleet outage; (3) OFF unless COLLECTOR_MIN_ENFORCE=1, so the minimum can move and be observed before it bites. TO TURN ON: confirm every collector reports 2.0.x (admin Collectors, Version column), then set COLLECTOR_MIN_ENFORCE=1 on web-api and deploy. Tests: 3 in collector-door.test.mjs incl. numeric-not-lexical ordering (2.0.10 > 2.0.9) and the config exemption.

**PUBLIC OPERATOR CREDIT ON /data/status (2026-09-06, Jamie: "give credit where due"):** The public status page now names the player running each collector, beside its card. RATIFIED, DO NOT RE-LITIGATE: I built it opt-in first (a credit_public column, default false) because /data/status is unauthenticated, has carried a "no account data, no machine labels" rule since it shipped, and the raise-hand flow never asks permission to name someone publicly. Jamie's answer was explicit - "no, just show it, no opt-in" - so the migration was dropped and credit is unconditional. WHAT IS PUBLISHED: the operator's primary claimed PLAYER name + tag (claim -> player, is_primary then verified then oldest), which is public game data. WHAT IS NOT, and must never be: the account row. The web-api test still asserts no email_hash and no static_ip in the public blob and still passes - that boundary is intact and is the line to hold. A collector with no owner, or an owner who claimed no player, simply renders no credit (magic-pines today). The AUTHENTICATED ladder still shows no operator at all; only the status page credits.

**LOCAL COLLECTORS OFF DEV BUILDS + ADMIN SAYS WHO (2026-09-06, Jamie, one-at-a-time list):** (1) RAM RIDER MIGRATED to a released binary: v0.1.16 collector_darwin_arm64 downloaded to ~/elixir-collectors/ram-rider/, SHA-256 verified against the release SHA256SUMS, .env COPIED BY PATH from .env.local-go (never read), plist repointed + full unload/load. It self-updates now, so door changes no longer need a hand-bounce on this machine. The stale dev binary at elixir-mcp-collector/bin/collector-v2 and .env.local-go are now unreferenced - Jamie's to purge. CAUTION LEARNED: running the freshly staged ./collector to check its version started a SECOND live collector under Ram Rider's identity for ~90s (the .env was already beside it); check a version with a binary that has no config next to it, or not at all. (2) RETIRED NODE PLISTS DELETED (see the correction above). (3) TESLA still runs python/collector.py FROM THE WORKING TREE - an edit reaches production on its next restart. Open decision: pin it to a copy outside the repo (keeps the Go/Python diversity rule) vs convert to the released Go binary (all three collectors then share one binary, and one bad release silences the whole fleet). Recommended: pin the Python. (4) ADMIN GATEWAYS now identifies collectors and operators: card name + icon over the machine name (the card is the identity every other surface uses; the machine name is what you SSH into), and an Operator column. The account table stores ONLY email_hash by design, so the readable half is the operator's primary claimed player via claim->player (is_primary, then verified, then oldest), with the hash prefix as the stable id and an explicit "unowned" for collectors never provisioned to an account. Tests: apps/web/test/admin-gateways.test.jsx (5), including a header/cell count guard - that table had drifted to 6 <th> over 8 <td> before today.

**CAPTURE CHARTS STACKED BY COLLECTOR + 24H WINDOW (2026-09-06, Jamie):** Status page charts now answer "who fetched this". BACKEND: /api/public/status gained capture_24h (hourly, 24 buckets) and per-collector `by` maps on both series, plus capture_series - the STABLE stack/colour order, ordered by gateway.enrolled_at NOT by rank, so a busier week cannot repaint the fleet. Both series are gap-filled with generate_series: a quiet stretch is now a visible zero instead of a missing bucket that silently compressed the axis (the old 5m chart did this). FRONTEND: one CaptureChart component drives both windows - stacked segments with a 2px surface gap, rounded cap on the data-end only, hover/focus tooltip with the per-collector split and a "N rejected of M" footer, legend per chart. Rejections used to recolour the whole bar amber, which collides with stacking; they are now a red tick under the bucket plus the tooltip line (status colour never carries meaning alone). PALETTE: --series-1..6 in styles.css, validated with the dataviz skill's checker against the --well plot surface (dark band L 0.48-0.67, chroma floor, CVD dE on every ADJACENT pair since stack neighbours touch, 3:1 contrast) - the ORDER is part of the validation, do not reorder or eyeball substitutes. Past six collectors the tail folds into --neutral. Also fixed: secsSince now clamps at 0 - browser/server clock skew was rendering "-1s ago" for a collector that had just heartbeated. TESTS: apps/web/test/status.test.jsx (6) covers both windows rendering, the stack, hover AND keyboard-focus breakdown, the empty-bucket case, and that the queue panel stays gone.

**COLLECTOR RELEASES NOW NAMED + STATUS CLOCKS RECONCILED (2026-09-06, Jamie):** Three asks, one session. (1) RELEASE NAMING - the update authority was fully built since 0039 (table, config handler, idempotent upsert op at services/migrate collectorReleaseOp, dispatch on {collector_release}) and NOBODY EVER CALLED IT, so every collector got `update: {}` and no released binary had ever self-updated. Closed with infra/scripts/name-collector-release.mjs: reads a tagged GH release from the collector repo, pulls its SHA256SUMS, maps assets to platform keys, upserts one row per platform through the migrate lambda; --dry-run prints without writing; idempotent. NAMED v0.1.16 for all 7 platforms. VERIFIED END TO END 2026-09-06 ~15:05Z: magic-pines (Wall Breakers, the only release build in the fleet - both Macs are dev builds) moved v0.1.14 -> v0.1.16 on its own, on its next hourly config call, roughly an hour after install as designed. The authority chain is proven, not just wired: server names version+sha, client installs only that hash, supervisor restarts it on the new code. THE MAPPING TRAP the script encodes: the client builds its key as `go-${GOOS}-${GOARCH}`, so the armv7 asset (collector_linux_armv7, built GOARCH=arm GOARM=7) answers to **go-linux-arm**, and the Windows keys drop the .exe the asset carries - name those wrong and the collector looks up a key that is not in the response and silently never updates. SOAK IS THE OPERATOR'S JOB: every green push to collector main publishes a release, so a tag existing means nothing; naming is what ships it, within the hour. (2) STATUS PAGE DROPPED THE QUEUES - 0040 retired the SQS collector queues but Status.jsx still hardcoded 7 of them, so 6 rendered "unavailable" indefinitely; panel removed (the email DLQ still feeds the health verdict via queueStats, and h.dlq_messages still renders). (3) THE TIMES DISAGREED because the two surfaces read DIFFERENT COLUMNS: public showed last_success_at (advances only on ADMISSION, ingest-owned) while admin showed last_heartbeat_at (any authed door call, every few seconds) as an absolute browser-local toLocaleTimeString with no date. An idle-but-healthy collector therefore read "seconds" in admin and "minutes" in public and both were right. FIX: both surfaces now show BOTH, labelled, on one shared relative clock (apps/web/src/lib/time.js - ago/secsSince/freshCls/beatCls, extracted from three near-duplicate copies in Status/Dashboard/Admin). Heartbeat gets a tighter freshness band (5 min, matching the client watchdog) than data (15 min). Admin's gateways table also had 6 <th> against 8 <td> - now 9 and 9. OPEN/NOTED, not actioned: min_client_version is the static string "2.0.0" in collector-door.mjs while collectors report versions like "v0.1.16", and NEITHER client enforces it - wiring enforcement naively would compare those two formats and could exit the whole fleet at once. Decide the format before enforcing.

**AGENT-TEAM SCAFFOLDED (2026-09-05, Jamie: build it, run in CODEX like the others - Codex does final task setup):** Four objective owners in AGENT-TEAM/ following the house pattern (elixir-bot/drop READ as pattern, written fresh): Run Elixir MCP (12h, :40 offset), Keep the Record True (daily 06:50), Close the Loop (daily 08:20 + Friday deep pass -> summaries/), Guard the Door (Sunday 07:30, independent control). automations.toml in the Codex v2 schema (terra/high; sol/xhigh for loop); scripts/objective-lease.mjs (claim/check/release/status/clear-stale; objectives incl 'session' for interactive work; exercised: claim + conflict-refusal + dirty-release-refusal all correct) + scripts/preflight.sh (main-only, clean, synced, lease-free + live status snapshot). Restore rehearsal (review item 2) now lives in Run's quarterly checklist. HANDOFFS WHEN TEAM GOES LIVE: (1) Close the Loop takes the daily feedback-response duty - interactive sessions STOP draining {feedback_pending} once Jamie confirms first runs; (2) interactive sessions claim the lease (objective 'session') before multi-commit work. JAMIE-MANUAL: register the four Codex tasks from automations.toml.

**COLLECTOR SECRET LEAK + OPERATOR-EXPERIENCE OVERHAUL (2026-09-06):** Reviewing the collector repo as an outside operator, found `.env.v2-go` (my zero-trust cutover file for jamie-mac) TRACKED + PUSHED to the PUBLIC collector repo - .gitignore covered .env and .env.gw* but not that name. CONTAINED: jamie-mac Elixir token ROTATED (leaked hash overwritten in DB -> dead), file untracked+deleted, .gitignore hardened to `.env.*` + `!.env.example`, plist repointed to gitignored .env.local-go, Go collector reloaded (LESSON: a plist env-path change needs launchctl unload/load, NOT kickstart - kickstart reuses the loaded definition). RESOLVED: Jamie decided (2026-09-06) to LEAVE the CR key as-is - it's IP-allowlisted to jamie-mac so the exposed value can't fetch from any other IP, and the remotely-usable Elixir token is already rotated dead. No open action; do not re-raise CR-key rotation. OPERATOR OVERHAUL (collector repo, same review): README was describing the dead pre-zero-trust world (Node, IAM users, static-IP submission, AWS creds in .env) - fully rewritten around raise-hand(name-only)->reveal-copy-token->2-line .env->binary-or-python->supervise, with a cold-visitor "what/why" open; deleted the retired Node collector entirely; .env.example -> 2 real secrets; scripts/install.sh (platform binary + SHA verify + launchd/systemd); validate.yml -> Go+Python CI; AGENTS.md refreshed (CLAUDE.md symlink intact); untracked committed binary+pycache; MIT LICENSE added; GitHub description/topics/homepage set. FOLLOW-UP (not operator-blocking): collector_release table is empty, so released binaries won't self-update until it's populated server-side. RESOLVED 2026-09-06: infra/scripts/name-collector-release.mjs names a release; v0.1.16 named for all 7 platforms.

**JOB LEDGER PHASE 2 + COLLECTOR DURABILITY (2026-09-06, migration 0040 complete, commits across both repos):** AWS creds refreshed -> cleared all blocked items. VERIFIED: scheduled clan_pulse self-fired 07:00:02Z ({clans:1,emitted:1}); feedback queue clear. PHASE 2 TEARDOWN deployed (all 6 collector queues confirmed 0 depth+inflight first): deleted RequestsLive/Bulk + DLQs, ResultsQueue + DLQ, ingest Lambda + event-source + log group, and their alarms (bulk-age, 3 DLQ, ingest-errors); VpcLambdaRole SQS grants collapsed to EmailQueue only; mcp/scheduler queue-URL env vars removed; STATUS_QUEUES -> email_dlq only; build/deploy/params dropped the ingest bundle; dead ingest index.mjs + makeHandler removed (makeArchive stays as shared lib). VERIFIED post-deploy: 3 request queues NonExistent, email queue present, ingest lambda ResourceNotFound, pipeline healthy on ledger. Email queue stays = VPC->internet bridge. COLLECTOR DURABILITY (Jamie's asks from the wedge incident): (1) PROGRESS WATCHDOG in both v2 clients - track last successful door round-trip, exit(1) after 5 min no-contact so the supervisor restarts clean; automates the manual kickstart that recovered the phase-1 wedge, root-cause-independent (launchd KeepAlive only restarts a process that EXITS, not one wedged-but-alive). (2) 5-MINUTE ACTIVITY SUMMARY in the log - jobs done / fetch errors / channel - the logs were startup+errors only, so healthy work looked silent. Log files: ~/Library/Logs/elixir-mcp-gw-go.log (Go) + elixir-mcp-gw2-py.log (Python), JSON lines, documented in collector README. Both local collectors rebuilt + bounced with watchdog+activity-log live. WEDGE LESSON RECORDED: dev-build collectors don't self-update, so a door redeploy under running dev collectors needs a bounce - now the watchdog does it automatically after 5 min. RDS 1yr RI on db.t4g.micro: PURCHASED 2026-09-07 (see the RI entry below).

**JOB LEDGER PHASE 1 LIVE + VALIDATED (2026-09-06, contract still 0.22.1, migration 0040):** Jamie approved the SQS->Postgres job ledger from the collector-architecture analysis, built all the way through. SHIPPED+DEPLOYED: 0040 job table (queued/leased/done/dead, partial-unique one-queued-per-subject, SKIP LOCKED lease, timestamp expiry w/ 5-attempt cap); scheduler plans+enqueues in ONE txn (F5 class gone) + publishes ledger metrics (namespace-pinned PutMetricData); door leases from the ledger (500ms re-check loop replaces SQS long poll) with lease=row-id (HMAC codec deleted); SUBMIT RUNS INLINE INGEST (processResult in-request, answers after commit, surfaces admission outcome); MCP live enqueues rows directly; status page + health verdict carry ledger stats; ledger alarms added; sweeps prune done/dead. VALIDATED LIVE: bulk admissions fresh within minutes; LIVE FETCH returned a real full player payload through door->collector->inline-ingest. LESSON (operational, not code): the phase-1 door redeploy landed UNDER the running collectors' in-flight lease/submit calls and WEDGED them - live fetches timed out until `launchctl kickstart` bounced both. Local end-to-end repro proved the code correct; the fix was a restart. Released binaries self-update (clean restart); our Macs run DEV builds that don't - so ANY door-contract/backing change under running dev collectors needs a manual bounce. Bulk kept flowing throughout (collectors' bulk cycle survived; only the live path wedged). PHASE 2 (teardown: remove request/results queues + DLQs + ingest ESM + old alarms) PENDING - needs a template change + deploy, and AWS creds EXPIRED mid-session (blocks redeploy + CloudWatch inspection until Jamie refreshes). Keeping SQS one more day IS the rollback, so the delay is safe. The email queue stays regardless (VPC->internet bridge).

**PROVISIONING = REVEAL-AND-COPY (2026-09-06 morning, Jamie):** the one-time .env FILE download was IAM-era complexity - it is just a token now. Admin gateways gained "Provision token" (owner-only): server mints emcg_*, stores hash, stages raw for the operator's ONE-TIME reveal; the operator page shows the token once with Copy + the two-line .env snippet (CR key line beside it). Raw token lives in the DB only between click and look. magic-pines restaged token-only for the new flow (fresh token; bulk channel). Future operators never touch a file download or any CLI - raise hand, get approved+provisioned in admin, reveal, copy, run.

**ZERO-TRUST COLLECTOR TRANSITION EXECUTED (overnight 2026-09-06 ~04:15-05:15Z, Jamie's full go):** COLLECTOR-ZERO-TRUST.md is LIVE end to end. SERVER (0039 + collector door): gateway token_hash/channel/counters/missed_streak, gateway_lease (2-cap), collector_release (update authority); /api/collector/config|lease|submit on web-api (Bearer vs sha256, channel-aware lease with server-computed cr_path via contracts crPathForJob, HMAC-signed lease binding gateway+job+receipt, server-stamped envelope identity, yield accounting, streak-10 quarantine -> draining + owner notify); VpcLambdaRole internal SQS grants; 7-test door suite. CLIENTS: Go v2 (internal/v2, HTTP-only mode when ELIXIR_API_TOKEN set; per-platform update keys go-GOOS-GOARCH) + Python twin (python/collector.py, stdlib only) - both pin lease->fetch->submit/breaker/429-hold. CUTOVER: tokens minted LOCALLY (hash->{collector_token} op, raw->local .env file-to-file, never printed/staged); jamie-mac=Go(Ram Rider), jamie-mac-2=Python(Tesla), BOTH channel=live; old Node launchd services unloaded (plists retained; DELETED 2026-09-06 - they still had RunAtLoad+KeepAlive pointing at the removed src/index.mjs, so the next login would have respawn-looped them every 10s); admissions verified within minutes; LIVE LANE verified with one owner live_fetch (currentriverrace, real payload) AFTER fixing the cutover bug: SQS WaitTimeSeconds=0 is a SHORT poll that samples servers and MISSES messages - live peeks now wait 1s (long poll all servers). TEARDOWN: IAM users elixir-mcp-gw-jamie + elixir-mcp-gw-magic-pines DELETED (keys/policies/users) - ZERO collector principals remain in the tenant; magic-pines RESTAGED as v2 bulk-channel token download (cabin never receives AWS creds; CR key placeholder in the one-time env); raise-hand is name-only (static_ip nullable, gone from form/admin; channel shown instead); OPERATORS.md rewritten; architecture + What's-new updated; collector AGENTS.md v2 rules + GO-PORT postscript. SOAK AT HANDOFF: both cards heartbeating <60s, ~18 fetches/hr each, DLQs 0, capture 50 polls/0 gaps. MORNING FOLLOW-UPS: (1) cabin: download Go binary + one-time env + paste CR key - no AWS steps anymore; (2) collector_release rows empty until the next tagged GH release (Macs run dev builds that never self-update - fine) [DONE 2026-09-06: v0.1.16 named for all 7 platforms via infra/scripts/name-collector-release.mjs]; (3) old .env/.env.gw2 hold dead IAM keys + duplicate CR keys - purge after a healthy day; (4) delete retired Node src/ + release.yml git-pull era bits in a calm follow-up. ROLLBACK: old plists retained but IAM is gone - rollback is now fix-forward on the door (SQS path still exists for the door itself).

**FIVE-PERSONA MCP TEST PASSES + 0.22.1 HARDENING (2026-09-06):** Jamie asked for multi-pass subagent testing of the MCP surface. Five parallel personas (war auditor / casual / adversarial / cold-onboarding / clan-routine), all read-only + refusal-paths, cursors untouched. HEADLINE ENVIRONMENTAL FINDING (all five agents): the claude.ai session connector serves a STALE 21-tool 0.18-era registry (watch_* present; no events/changelog/nickname/meta tools) while responses stamp 0.22.x - server signals are CORRECT (serverInfo.version varies with the contract, tools.listChanged declared), so the fix is Jamie-side: RECONNECT the elixir-mcp connector on claude.ai, and RETIRE the legacy claude_ai_Elixir_MCP connector (11 old get_* tools with drifted schemas - an agent landing there gets weaker contracts). WAR AUDITOR: exact battle-level reconstruction of colosseum weeks (participants sum = boat fame to the point); found the FAME-CAP TRUNCATION trap - regular weeks that finish at 10,000 stop earning member points while decks_used keeps counting. ADVERSARIAL: 40 probes, one crash path (players_timeline dates), systemic unknown-enum->silent-empty class; cursors/tag-folding/precedence-notes all held. SHIPPED AS 0.22.1 (same night): requireEnum refusals (mode/outcome/sort/metric/granularity), with_card id sanity, timeline date guards, last_n_battles falsy-zero refusal, limit_applied echo, war_history finished_early + history_starts_at + note, period base documentation, member_left name stamped at source. LESSONS: (1) MODE_GROUPS is an ARRAY - Object.keys() on it silently yields indices; (2) heredoc-fed scripts in background Bash deadlock on stdin - write to a file first; (3) my replace-loop hung on a needle contained in its own replacement - one-pass str.replace. REMAINING PRODUCT PICKS (for Jamie): (a) mode_mix/dominant_mode per deck row - mode-blind deck summaries INVERTED advice (war-hot deck read as ladder-best); (b) activity recency on war_current rows (kills the routine's 49x49 hand-join); (c) per-day fame series = the parked periodLogs harvest; (d) clan-scoped coverage; (e) players_timeline season shortcut + boundary markers; (f) battles_cards deck-collinearity note; (g) partial-week flags on weekly series; (h) one-line claim_status semantics. Routine-readiness verdict: leader brief composable in 3 calls; war surface 'internally consistent to the point of exact reconstruction'.

**COLLECTOR ZERO-TRUST - DESIGN GATE (2026-09-06, docs/COLLECTOR-ZERO-TRUST.md, AWAITING JAMIE):** Jamie: operators cannot be assumed safe; collectors must have NO AWS connectivity - API clients only, launch-time contract, no IP at enrollment. Assessment CONFIRMED the worry in code: (1) provision-gateway.mjs grants cloudwatch:PutMetricData on Resource:* with no namespace condition - any collector can write metrics anywhere in the account; (2) ingest trusts msg.gateway_id from the message body - any collector can submit results AS any other collector; (3) request-queue recv+delete allows silent job black-holing. Design: three Bearer-token routes on web-api (/api/collector/config|lease|submit); server computes cr_path per job (collection changes = zero client changes); server stamps gateway_id/sha from the token (spoofing dies); leases ride SQS receipt handles so unsubmitted work self-redelivers; tokens hashed in gateway row (service-token pattern), lifecycle enforced at the door, one-time download IS provisioning (IAM minting script dies); static_ip dropped from enrollment (CR key IP-binding is operator<->Supercell business); per-gateway CloudWatch namespaces retired (DB heartbeats + bulk-age alarm). Migration: server first (both doors live) -> Go v2 HTTP-only client -> cabin starts zero-trust NEVER holding AWS creds -> Macs cut over (= the GO-PORT fleet cutover) -> IAM users deleted. NOT BUILT - awaiting ratification.

**SOL-6 ASSESSMENT: ALL FIXES SHIPPED + RDS ENCRYPTED (2026-09-06 03:00-03:50Z, Jamie: "proceed with all identified fixes, downtime acceptable"):** Verdicts first (all inspected against code + live reads): F1/F2/F4/F5/F6/F7-snapshots/F8/sessions/RDS-unencrypted/privacy-salted CONFIRMED; F3 half-confirmed (Retry-After real; the "no global budget" half missed live_reserve + pacing); "docs contradict universal reads" OVERSTATED (clan tools ARE gated, pinned by test - doc needed precision only). SHIPPED: (F1) war_period_anchor refreshes when observation >7d newer + readers select by first_observed_at desc - periodIndex RESETS each season (cr-docs), rollover Sept 7 was the deadline; rollover/replay regression tests. (F6) first_fetched_at stamps from collector fetched_at + 0037 backfills post-Sep-4 rows (export-era rows deliberately untouched - rewriting would orphan their twins). (F4) admit(endpoint, payload, entityKey) binds identity (player/clan/riverrace tag; battlelog observer-in-every-team). (F5) SendMessageBatch Failed entries logged + un-planned. (F7) snapshots observed_at (0038) + newer-only overwrite. (F8) truncated audited via MCP_RESULT_MAX_CHARS mirror; ingest catch logs cause; JobsErrorsAlarm. Sessions: token/cookie to 90d ABSOLUTE cap, DB row = 9d sliding truth (auth test re-pinned: 10d-old token verifies, 91d never). (F2) deploy.mjs migrates BEFORE the flip (UpdateFunctionCode on migrate + invoke, then stack update; create path unchanged); WORKFLOW "aborts the flip" overclaim fixed. Collector repo: 429 Retry-After pushes the pace anchor forward (Node + Go, parity tests). Tinylytics paths: pushState bridge beacons virtual page hits (Drop technique), paths coarsened to 2 segments. RDS ENCRYPTION (= RESTORE REHEARSAL, review item 2): pause (rules+ESM, queues drained) 03:26Z -> snapshot 5.5min -> encrypted copy 3.5min -> CFN replacement from snapshot ~10min (03:37-03:47Z) -> counts verified EXACT vs sol-6 baseline (40,647/37,880/109; snapshots +2 = pre-pause polls) -> pipeline resumed 03:47Z, first admission <4min, DLQs 0 -> old instance elixir-mcp DELETED (deletion protection off first), unencrypted snapshot deleted, ENCRYPTED snapshot elixir-mcp-pre-encryption-enc retained as rollback. Total user-facing window ~21min. Instance is now elixir-mcp-enc; DbSnapshotIdentifier param is PRESERVED FOREVER (reset = replacement; --param=K=V covers first-deploy sets). LESSON: my own gate slipped once tonight - a ';' after npm run verify let a red verify push; always '&&'. NEXT: multi-pass subagent MCP endpoint testing (Jamie's ask).

**REVIEW ITEMS 1+5+8 SHIPPED (2026-09-05, commits c8ca887/…/tools-split; Jamie picked from the 10-item holistic review):** (1) Tinylytics batch POST gets the 3s AbortSignal (a hung analytics endpoint could previously eat the relay's 60s and DELAY LOGIN EMAILS sharing the batch); invoker ping moved into a finally - the SQS send now rides AFTER the answer, ending the ~10-20ms pre-execution tax on every MCP call. (5) elixir-mcp-jobs Lambda (same VPC/VpcLambdaRole) now owns ALL timer-driven work - daily clan_pulse, MON payload+operational sweeps; EventBridge rules retargeted; migrate keeps deploy plumbing + break-glass ops ({probe},{stats},{feedback_*},{account_role},{gateway_provision},{export_payloads},{seed},{replay}). Verified live: jobs {clan_pulse} answered {clans:1,emitted:0,skipped:1} (correct same-day idempotent skip) and the rule targets elixir-mcp-jobs. My tick habit unchanged: feedback_pending stays on migrate. (8) tools.mjs 3,808 lines -> 73-line assembler + tools/{shared,elixir,players,battles,cards,clans,live,collections,war}.mjs; moved VERBATIM, all pins hold; the two manual fixes were function-REFERENCE imports the call-pattern regex missed (.map(normalizeTag), displayCard as value) - lesson: passed-as-value identifiers hide from call-site greps. UNPICKED review items still open for Jamie: 2 (restore rehearsal), 3 (cabin collector), 4 (user collections UI vs roles table), 6 (buttondown live proof), 7 (rotation runbook), 9 (tinylytics volume ceiling), 10 (admin polish batch).

**DETAIL-VIEWS SWEEP (2026-09-05, built + audit):** BUILT: /account/feedback/<id> (item as addressable page), /admin/feedback/<id> (adds status + maintainer RESPONSE composer - the API field existed, UI never exposed it), /admin/collections/<slug> open-and-edit (member rows w/ per-row remove, meta form, Explore link; create drops into editor; the odd inline add/remove column is GONE). Routing: legalRoute already passes /section/page/sub paths; App hands itemId to Admin/Dashboard. AUDIT - identified, NOT built (for Jamie's triage): (a) USER COLLECTIONS UI IS MISSING ENTIRELY - api.myCollections/myCollectionAction are dead client methods; family/partner tiers carry curation quota (5/20) with NO surface. The CollectionEditor pattern is ready to reuse. (b) admin account detail page (claims, usage, overrides per account); (c) activity row -> call detail (args/meta); (d) admin gateway detail (account-side collector detail exists); (e) access-request detail (inline approve/deny is probably enough). Gateways table has a pre-existing thead/td column mismatch (6 headers, 8 cells).

**LOOKUP RESOLVES NAMES + NICKNAMES (2026-09-06, commit b1260f6):** Jamie picked this as the next improvement (recorder swap explicitly HELD - "hold off on elixir-bot changes for a bit"). The Explore lookup falls through to players_search when a query isn't a recorded tag or isn't tag-shaped: 1 match -> player record; several -> pick-list (name, nickname chip, tag, source, clan); 0 -> honest miss (copy covers names now). Nicknames rank first so "tyler" lands on Jamie's Tyler. Resolution order now: collection slug -> player tag -> clan tag -> deck hash -> ISO week -> NAME/NICKNAME -> miss.

**PUBLIC STATUS DASHBOARD (2026-09-06, commits 002c8be+6e9e991):** Jamie: "operational dashboard, nothing confidential, desktop OR mobile... queues, collectors, an hour snapshot." GET /api/public/status (no auth, 60s cache): queue depth/in-flight across pipeline + 4 DLQs (VpcLambdaRole +GetQueueAttributes read-only; SQS VPC endpoint covers it), collectors by CARD NAME ONLY (no IPs/machine labels - privacy pinned by test), 75min of 5-minute capture buckets (rejects amber), data-derived health verdict (recent admission + empty DLQs = ok). Data > Status page, 60s auto-refresh, mobile-first. LESSONS: (1) ApproximateAgeOfOldestMessage is a CLOUDWATCH METRIC, not an SQS attribute - GetQueueAttributes rejects it (nulls were silent until the catch logged); (2) never Promise.all queries on the one-pg-client-per-invocation pattern. PWA was built then DROPPED same hour (Jamie: "may be a mistake - just make it a mobile view") - manifest deleted, apple-touch-icon kept site-wide for bookmarks. Also: inline NICKNAME cell on Account > Overview players table (Jamie's ask; blur/Enter saves via the elixir_nickname bridge call; /api/me claims carry nickname). LIVE STATUS SHOWS: Wall Breakers (= magic-pines' card identity) pending, awaiting the cabin NAS start.

**PRIVATE PLAYER NICKNAMES (2026-09-06, 0035, contract 0.21.0, commit 8e3583a):** Jamie: "To me Raquaza is Tyler, and King Levy is Levi... just for my user." player_nickname (account_id, player_tag, nickname<=40) - strictly ACCOUNT data, never public/cross-account (privacy pinned by test). elixir_nickname set/clear tool; players_search matches nicknames RANKED FIRST (source 'nickname' - the agent payoff: "what's Tyler's win rate" resolves); players_summary + clans_roster carry nickname beside real name; Explore player record shows the nickname chip + inline editor (same bridge call). 35 tools.

**COLLECTOR ARENAS DROPPED (2026-09-06):** Jamie: arena-climb = confusing flavor; QUOTA EARNING is the real benefit; keep card identity, NAMES DERIVED FROM THE CARD ("collectors have a specific name"). Removed GATEWAY_ARENAS/gatewayArena from contracts entirely; ladder + /api/me/gateways + elixir_collectors return credits = floor(points/10) and name = card_name (operator name demoted to 'machine' label). Web collector page: earned-quota line (+N daily calls - 10:1, 4x cap, +2 player/+1 clan slot bonus) replaces the arena meter; tool title 'Collectors'. Note: this overrides the design handoff's 'collector ladder' fixed-vocabulary item and its arena footnote - Jamie's call supersedes.

**DESIGN HANDOFF IMPLEMENTED (2026-09-06, commit 9f6f8ee):** Jamie ran the Claude Design session (project dfac157b, imported via DesignSync after /design-login); the handoff README was a complete screen-by-screen spec with a drop-in styles.css. HEADLINE DESIGN DECISION honored: Explore is NOT a reporting product - the 7-tab charts direction was cut by the designer ("the agent is the analyst; the website's job is verification"). Explore rebuilt as the RECORD BROWSER: "Do we have it?" lookup -> six record kinds (player/clan/battle/deck/week/collection) + list records, breadcrumb trail (truncate-on-revisit), reference fields as links, one-tool-call-per-page toolbar with raw JSON + copy link, real deep-link paths, honest miss state. SERVER: battles_query gained battle_id + corpus deck_hash addressing modes (0.20.2; deck_stats deliberately carries NO pooled win rate - who-plays-it confound, notes point to battles_meta_decks). IDENTITY: Clash Royale display face (Fan Kit, vendored from Drop + compliance note) for brand shell ONLY; gold = YOURS (star/arena/two CTA buttons), never a data value; three nav tiers three shapes; honesty components (.sample/.caveat/.freshness); UNOFFICIAL disclaimer footer; notify = switch; 70rem console column, 13px tabular tables. Old Clan/Collections/Meta/Collectors views DELETED (absorbed). Charts only in Data (shared hover, partial-today at 35%). legalRoute guards stale bookmarks (prototype's own bug, fixed in its logic class, ported). Live + smoke green. LESSON: the .dc.html prototypes carried the real logic (tool mappings, legalRoute) in their script block - extract from the prototype, don't re-derive.

**GO COLLECTOR SHIPPED + WEB PROVISIONING (2026-09-06, collector v0.1.1+, elixir-mcp 0034):** Jamie approved by proceeding (created cabin CR key, "walk me through... a test of how collector provisioning works since it is the first you cannot just do both sides of"). GO PORT LIVE: full parity worker (pinned constants, envelope wire-exact, job passes through as RawMessage - byte-identical), release pipeline publishes 4 binaries + SHA256SUMS from every green main (v0.1.1 = first release); self-update = release check + SHA256 + atomic self-replace; 'dev' builds never update; run-forever.sh now binary-aware (Go path needs NO git NO Node on the host). Node src/ stays until the Macs cut over (GO-PORT §5). WEB PROVISIONING (Jamie mid-build: "config parameters and tokens would come from the web app... exception of the CR token"): ARCHITECTURAL CONSTRAINT - NAT-free VPC lambdas cannot reach the IAM API (no VPC endpoint exists) and no public lambda should hold IAM powers -> owner mints LOCALLY (infra/scripts/provision-gateway.mjs: idempotent IAM user + key rotation + rendered .env with CR placeholder + __GATEWAY_ID__ substitution, staged via {gateway_provision} op; SECRET NEVER PRINTED/ENTERS CONTEXT) -> operator downloads ONE-TIME from Account > Collector (claim-and-null atomic; RETURNING-sees-new-values trap fixed with locked self-join; gateway_config_claimed event). CABIN NEXT: Jamie raises hand with cabin name+static IP -> owner runs provision script -> one-time download -> paste CR key -> NAS runs collector_linux_armv7 under Task Scheduler.

**GO COLLECTOR PORT - DESIGN GATE (2026-09-06, collector docs/GO-PORT.md):** Jamie: jump straight to Go, no Node on the NAS. Design doc written, AWAITING REVIEW. Key decisions proposed: (1) self-update moves from git-checkout to CI-BUILT GITHUB RELEASES (hourly check, SHA256-verify, atomic self-replace, exit; update failures NEVER stop collection; COLLECTOR_PIN_VERSION rollback; artifact signing DEFERRED with honest rationale - a key in GH secrets dies in the same compromise - trigger = first outside operator); (2) queue-contract mirror = hand-written Go structs + committed JSON fixtures, drift fails tests, server-first rule unchanged; (3) migration order protects the live fleet: additive Go alongside Node, CABIN DS416 SOAKS FIRST, then Macs one at a time, Node removed last; (4) parity checklist pins the learned constants (1.5s pace, 5x403 breaker, live-first + 4s bulk wait, dev-build never self-updates). Server-side changes: NONE.

**CABIN COLLECTOR (DS416) FEASIBILITY + NON-MAC RUNNER (2026-09-06, collector 0d9a119):** Jamie wants a collector on the cabin's Synology DS416 (DSM 7.1.1 = base armv7 model). Assessment: collector is fully portable (two pure-JS deps, no natives, global fetch; launchd is the ONLY Mac piece); the strategic win is real - first true GEOGRAPHIC redundancy (all 3 collectors share one house/WAN today); the friction is armv7 (unofficial Node builds, no Container Manager) and the REAL gate is the cabin's static IP (CR keys are IP-allowlisted). SHIPPED to collector repo: scripts/run-forever.sh (KeepAlive-as-shell-loop, NODE_BIN override for armv7 tarballs), systemd unit template, DSM 7 Task Scheduler docs, engines>=18 (honest floor; 24+ recommended). JAMIE-MANUAL for enrollment: confirm cabin static IP; raise hand from Account > Collector; create cabin CR key allowlisting that IP; create per-gateway IAM user; hand creds out of band; probation -> active.

**SITE SPLIT: STATIC + APP (2026-09-07, Jamie's ask - "the web interface is really trying to serve two things and not doing one of them well"):** the whole site was ONE Vite/React bundle, and the deploy patched a single crawlable block into the one shared shell. MEASURED FIRST: every one of the ten sitemap URLs returned byte-identical HTML - same `<title>`, same home-page meta description, same body. To a crawler or an agent fetching a URL the site was one page with ten aliases, and the six docs pages plus What's-new existed only inside JavaScript. NOW: `apps/site` (Eleventy 3, chosen because poapkings.com is Eleventy and already had llms.txt/sitemap patterns to read rather than invent) emits real documents for CONTENT - `/`, `/docs/*`, `/updates` (NEW page, was only a home panel), `/data/changelog` - plus `/llms.txt`, `/llms-full.txt`, `/tools.json`, `/feed.xml`, `/sitemap.xml`, `/robots.txt`. `apps/web` keeps everything behind a session or drawn live (Explore, Account, Admin, sign-in, Data dashboard/status) and is served from `/app.html`, `noindex`. ONE bucket, ONE hostname, no URL moved. THE TOOLS PAGE IS NOW GENERATED from `makeRegistry().declarations()` - the hand-written copy documented 31 tools while the server declared 36, exactly the drift the metadata-truth pass warned about; the registry imports with no DB or env because declarations live beside handlers and only handlers open a connection. Three places must agree on who owns a path (the CloudFront function, `STATIC_LINKS` in App.jsx, the pages Eleventy builds) and a test pins all three - drift there is a dead link or an app shell served where a document was expected. `infra/scripts/build-site.mjs` merges both dists and REFUSES a broken tree (dead sitemap entry, dangling llms.txt link, missing shell/asset, stylesheet that differs from `packages/design/styles.css`); CI runs it. Design tokens moved to `packages/design` so neither half copies the other. THE CATCH WORTH REMEMBERING: Eleventy's default markdown-it wrapped the architecture page's inline `<svg>` opening tag in a `<p>` and closed it before the diagram - the tag spans two lines - so the diagram collapsed into loose words while every other check stayed green. Switched the library to `marked` (what the app used), pinned by a test. SITEMAP SHRANK DELIBERATELY: `/data/dashboard` and `/data/status` left it, because they render nothing without JS and listing them points crawlers at an empty shell; both stay public and linked. The CloudFront Function keeps its logical id and Name (`elixir-mcp-spa-router`) on purpose - changing either replaces a function attached to the live distribution, which is not worth a better name. Deploy-time bake DELETED; the static build reads `/api/public/stats` itself, skip-not-fail. DEPLOYED + VERIFIED LIVE 2026-09-07: 21/21 smoke green (the smoke gate now TELLS THE HALVES APART - the old "site is the app" check would have passed throughout the defect); all 21 live URLs return the right shape on elixir.poapkings.com, six distinct <title>s where there was one, tools.json serves 36 tools, and the app still mounts from /app.html with its charts intact.

**SITE-IA BUILD-OUT COMPLETE (2026-09-06, commit 2455fa5):** the last piece - Explore intelligence. Player explorer gains the Pilot Score tab (score +/- SE, monthly trend table, full Level Curve with thin-sample honesty; curve is an ARRAY of {gap_range, n, win_rate}). Explore > Meta: segment picker (corpus / player collections via collections_browse / clan-tag input) over battles_meta_decks / battles_meta_cards / battles_trends. Clan & War: Standings (vs clan median), clan-wide Pilot Scores, Scouting Report (field names: mean_fame/max_fame/zero_fame_races - checked against the tool SQL, not guessed). EVERY SITE-IA SURFACE NOW EXISTS ON SCREEN - the parity debt from the IA audit is paid; the site is ready for the Claude Design session (handoff prompt delivered, docs/DESIGN-HANDOFF.md). Web/agent parity principle held throughout: every web view is an explorer-bridge call to the same registry tools agents use.

**ACTIVITY + COLLECTOR PAGES SHIPPED (2026-09-06 early, commit 114facf):** SITE-IA build-out continues. Account > Activity: three tabs - MCP requests (/api/me/requests, the account's OWN mcp_call_audit slice: tool/surface/ms/bytes/errors/args - Jamie's "see the MCP requests my user has made"), account events (existing /api/me/activity relocated off Usage), notifications (/api/me/events - READ-ONLY view of event_feed; NEVER advances events_seen_through, that cursor belongs to the agents - test pins it; unread rows past the cursor bolded). Account > Collector: run-a-gateway panel relocated from Overview + per-gateway detail via /api/me/gateway-detail (owner-scoped, ?id= query param - CloudFront forwards query strings, verified in template): 30-day fetch SVG series with admitted/rejected hover, 7-day endpoint mix, points, last success, running SHA. Column gotcha: gateway.last_seen_sha (0016), not gateway_sha. REMAINING from SITE-IA: Explore additions (Pilot Score tab, Meta page + segment picker, clan intel tabs).

**SITE IA RATIFIED + DATA SECTION SHIPPED (2026-09-05 night, commit 289106f):** Jamie's decisions: Data as tier-1 (love it); FULL history for dashboard series (retention later); Activity = three tabs; CR live leaderboards stay agent-only ("I don't want Elixir MCP to become a BI platform"); Home + Data must be CRAWLABLE. docs/SITE-IA.md is the nav spec of record. SHIPPED: GET /api/public/stats (no auth, 1h CloudFront cache, aggregates only - leak test pins it); Data tier-1 with Dashboard (full-history daily series as dependency-free inline SVG sparkbars + corpus totals) and Changelog (contract CHANGELOG rendered; SUBPATH import contracts/dist/changelog.js - the barrel pulls node:crypto via deck.js and breaks vite); Home touts live totals; CRAWLABILITY: deploy bakes stats+site text into index.html (#prerender div, app removes post-mount; skip-not-fail), robots.txt + sitemap.xml. Verified live: 37,719 battles / 40,484 players / 102 series days, baked text serving. STILL TO BUILD from SITE-IA: Activity 3 tabs (/api/me/requests + /api/me/events), Collector detail page, Explore additions (Pilot Score tab, Meta page, clan intel tabs). docs/DESIGN-HANDOFF.md written - the self-contained Claude Design prompt (POAP KINGS gold/crown + Drop purple-black lineage, landing as a DATA APPLICATION; deliverables: tokens, component language, chart language, 5 page layouts; IA and vocabulary locked).

**HEAT MODEL RETIRED (2026-09-05 evening, commit b55db49):** the A/B verdict authorized the planned retirement (heat was "kept one release as fallback"). Removed: ELIXIR_SCHED_MODE env knob, heat tiers + epoch decay, the pipeline re-heat write (heat=3), heat sort key. Flat cadences collapsed to {every, floor}; null-yield subjects keep explicit discovery defaults (battlelog 60m, profile 480m) - the "warm start" concern was already solved inside yieldCadenceMinutes. poll_state.heat column stays, DORMANT (no migration; operational table). Post-deploy probe: scheduler planning normally. The elixir-bot-imported heat model is now fully out of the system.

**META + TRENDS SHIPPED, FEEDBACK #8 FULL-CIRCLE (2026-09-05 late, 0.20.0+0.20.1+0033, commits 8baece9/ebdee85+):** Jamie greenlit features 2+4. SHIPPED 0.20.0: battles_meta_decks / battles_meta_cards / battles_trends over ONE shared segment abstraction (corpus | clan_tag | player_tag | collection) - EB-shrunk win rates (prior=segment mean, m=20), distinct-pilot counts (composition tell), usage shares, evolution forms never merge, SEGMENT_NOTE states the no-editorializing rule in-band. #4 needed NO Athena: battles/snapshots are lossless in Postgres (the deferred DATA-TOOLS lane was payload analytics); single-player weekly already existed (battles_performance group_by week) - the real gap was GROUP trends. FEEDBACK #8 (agent inference-error case study, filed at Jamie's request, actioned within the hour, 0.20.1): agent read the changelog's event-type enum + overnight gap as "Colosseum week just finished" - NAMED FAILURE CLASS: documentation of what CAN occur read as evidence that it DID. Affordances: war_current period block (period_index/war_day, started_observed_at anchor, period_end_nominal + week_end_nominal at ~10:00Z reset with drift note) so temporal claims CITE fields; event-type lists framed "schema, not news"; feedback category 'other' + unknown-category errors return the valid list (DB constraint had silently diverged from... actually matched the schema - the 400 was client/constraint-side; all three layers now in lockstep). BONUS: cr-agent-api-docs periodIndex corrected (season-monotonic, //7=sectionIndex - was described within-section). AWS creds expired mid-evening (~15:40Z, deploys blocked ~1.5h) - Jamie re-authed.

**YIELD A/B FULL-DAY VERDICT (2026-09-05 15Z, {ab_yield} op):** same-clock-hours comparison, Sep 3 15Z-Sep 4 15Z (heat model) vs Sep 4 15Z-Sep 5 15Z (yield scheduler), battles attributed to FIRST LIVE observation so archive replays contaminate neither side. RESULT: total fetch spend 1,976 vs 4,159 (47.5%); battlelog yield 2.788 vs 3.010 battles/fetch (92.6%) - and the baseline is INFLATED by one-time subject sweeps (240 battlelog subjects incl. first-poll 25-battle harvests vs 184 steady-state), so steady-state efficiency is at least equal. Raw battles captured (1,670 vs 8,991) is NOT a valid comparison for the same reason. VERDICT: the redesign's goal is met - half the spend at equal-or-better per-fetch efficiency; heat-path fallback can retire next release per the original plan. OPEN (not urgent): a true miss-rate audit (oldest-battle-already-stored check per poll) would upgrade "no evidence of loss" to "measured loss rate"; the KHAOS-era ~85% capture watch stands.

**METADATA TRUTH PASS (2026-09-05, contract 0.19.1):** Jamie: changelog stays (the pasted tree was a structure example); "verify all the metadata for the tools is accurate to the new structure." Audited all 31 declarations; fixed stale watch/claim/entitled-scope language across elixir_events, my_players (title "My players"), coverage, battles_compare, clans_roster/war_current/war_history (universal reads, defaults to YOURS), collectors (budget-vs-perks nuance); server instructions teach add+notify+events_pending. AUDIT CAUGHT A BEHAVIOR BUG: players_search still searched claims+clanmates only - now corpus-wide (universal reads) with claim>clanmate>corpus ranking. Pattern worth keeping: the stale-vocabulary sweep (regex over published declarations) - metadata drift is invisible until an agent trips on it.

**ADDED = RECORDED (2026-09-05, 0032, contract 0.19.0, commit 3c71ab5):** Jamie killed the whole add-vs-watch split hours after ratifying it: "all players and clans that are added are collected and recorded. Then the only setting is do you want them in your notification pipe." Model now: ADD (one act = claim/associate + record, tier slots are the only gate) / REMOVE (frees slot; clan recording stops when NO account has it added, else settles to widest remaining scope - ensureClanRecording/settleClanRecording shared by both doors) / NOTIFY on-off (the ONLY per-subject setting; feed fan-out honors it). clan_follow DROPPED (lived one migration - a do-nothing bookmark; Jamie's challenge "what would this do?" had no good answer for clans; for players the claim itself carries the association). account_clan = your added clans (scope+notify); claim.notify added; slots count ADDED things (claims / account_clan), not recordings. elixir_add_player/elixir_add_clan REPLACE elixir_watch_* (breaking rename); /api/recordings user route REMOVED (recording is not a user-facing toggle anymore); web panels: notify bell + Remove. STAR REFINEMENT (same session): primary claim = "me"; the primary player's clan auto-surfaces STARRED in Your clans as a suggestion with per-scope add buttons DIMMED when the tier lacks slots ("repopulate their clan for watching, dimmed if they don't have a role that can collect it"). Bot pin -> 0.19. LESSON: when Jamie challenges a state that exists only to hold UI together ("what would this do?"), the answer is usually to delete the state, not defend it.

**DECISIONS BATCH PROCESSED (2026-09-05, 0031, contract 0.18.0, commit 4caf069):** Jamie processed the review recommendations one by one. RATIFIED + BUILT: (1) ONE ENTITLEMENTS SYSTEM - ladder top rung role='owner' (super admin, exactly ONE; no admin can demote or affect the owner or another admin); admins SEE the console with day-to-day powers (requests, feedback, collections, roles up to partner via contracts canSetRole); owner keeps admin grants, service tokens, gateways, overrides; ALL gating derives from role now (is_owner = legacy column; resolvers compute isOwner/isAdmin from role; test seeds must stamp role explicitly). (2) CLAN WATCHING IS PURELY A USER FUNCTION - admin clan surface REMOVED ENTIRELY (page/routes/helpers); follow (add) is free, watch starts recording IMMEDIATELY within tier slots (no maintainer approval - the ladder is the gate); elixir_watch_clan action watch/follow/unwatch (BREAKING: no review requests); elixir_watch_player record:false; clan_follow table + backfill from existing watches; Your clans panel on Account > Overview. (3) PUBLISHED TOOL TREE - tools/list declares in canonical GROUP_ORDER (Elixir MCP, Collections, Battles, Cards, Clans, Live, Players, War) then title, so order-preserving clients render the domain structure, never a read/write split (note: elixir_changelog kept though absent from Jamie's pasted tree - flagged, not deleted). (4) /docs/<page> URL-addressable. (5) Terms-review doc updated IN PLACE to universal-reads posture (no revision history - no users yet). DECLINED/DEFERRED: RDS restore rehearsal (skip for alpha), DATA-TOOLS/Athena lane (defer entirely), fleet-headroom display (superseded by self-serve), service-token cap stays informational. LESSON: removing a route block by slicing between route names swallowed the unrelated routes sitting between them - slice between EXACT adjacent anchors or grep-verify what left the file.

**HOLISTIC REVIEW + P1 FIXES (2026-09-05, commit cf85e1e):** Jamie asked for a step-back review; three P1 items shipped same tick. (1) FEED EMITS MOVED POST-COMMIT: projectors collect feedEvents, pipeline flushes after commit - the inline emit's swallow-catch was FALSE SAFETY inside a txn (any insert error aborts the whole txn; the catch hides the cause while the commit then fails = ingest halts). CLASS LESSON: a .catch inside an open transaction protects nothing. (2) battles_recorded COALESCES per (account, tag): unread rows fold into the new event (counts summed, fresh event_id); read rows never touched - prevents heavy watchers (Jamie: 17 players + comprehensive clan) living with permanently-nonzero events_pending, which would train agents to ignore the hint. (3) DOOR ALARMS: mcp-errors, web-api-errors, mcp-latency-p95 - the pipeline was fully alarmed but the user-facing doors were not. (4) deploy.mjs now smoke-gated (exits nonzero unless smoke.mjs passes; first gated deploy ran 7/7 green). Also: elixir-bot PINNED_CONTRACT bumped 0.16 -> 0.17 (pin is warn-only, verified). REVIEW ITEMS DOCUMENTED, NOT YET DECIDED/BUILT: is_owner-vs-role='admin' seam (recommend: is_owner = UI/ops key, role = quotas only - needs Jamie ratification); RDS restore never rehearsed; DATA-TOOLS/Athena lane still the parked dependency for trend tooling; approval flow should show fleet headroom (~30 clans/collector); Docs pages not URL-addressable; terms-review doc still carries the pre-universal-reads stance; partner service-token quota informational only.

**ENTITLEMENT LADDER + SELF-SERVE + PUSH LANE (2026-09-05, 0029+0030, contract 0.17.0, commits 5546c2b+):** Jamie ratified the five-tier ladder (member 3p/1 activity clan; leader 5p/1 comprehensive; family 10p/3+3 + 5 collections; partner 25p/10+5 + service token + collector expected; admin unlimited) after asking for two rows between leader and admin. SPINE: roles NEVER gate visibility - they gate collection slots and call volume; contracts/src/roles.ts is the single source; nullable account columns (max_player_recordings, mcp_daily_quota, NEW live_daily_quota) stay as hand-tuned overrides; operator bonus +2p/+1 activity clan stacks below partner. Enforcement swapped at BOTH doors (the two-doors-never-disagree rule): recording caps, live cap (was flat 50/day), daily quota, clan-watch slots by scope (elixir_watch_clan refuses before filing), collection caps. SELF-SERVE: tier panel + upgrade request on Account > Overview (feedback lane, kind role_upgrade_request, dupe-guarded); Admin > Accounts page (role picker, pending-request flag); family+ curate own collections on the Collections page (ownership = governance). Docs > Roles is PUBLIC per Jamie. PUSH LANE: event_feed table, implicit subscriptions (claims/recordings/feedback ARE subscriptions), fan-out on write; topics battles_recorded (coalesced, backfill-guarded like heat), clan_war_week_finished (null->set + 7-day recency guard), feedback_responded (both respond doors), recording_started/stopped, role_changed; elixir_events tool (cursor/topics/mark_seen) + meta.events_pending; 30-day prune in sweep_operational; {account_role} ops-lane op (list/set, service-name lookup). Backfill verified live: owner -> admin; elixir-bot service token rides the owner account so the bot inherits admin. 31 tools. LESSON: db/schema.fingerprint must be re-pinned from a FRESH scratch DB (cli defaults to the stale dev DB). NEXT (Jamie's five-feature ask): recorder swap design doc, pro-corpus meta intelligence, trend/time-series tools.

**TWO-TIER WEB NAV + COLLECTIONS UI (2026-09-05, commit 45b17ed):** Jamie: "the web apps navigation is a real mess... revisit it entirely... two tier... leaves room to grow" + "I don't see collections anywhere in the web app." Rebuilt around four tier-1 areas with a tier-2 page row: EXPLORE (Player / Clan & War / Collections / Collectors - ladder promoted from the Dashboard basement), ACCOUNT (Overview / Agents / Usage / Feedback), DOCS (own ToC), ADMIN (Requests / Clans / Collections / Collectors / Tokens / Feedback / Usage). Old flat paths redirect (REDIRECTS map in App.jsx); SECTIONS config is where growth lands. Collections: public browse via the explorer bridge (Explore > Collections, players link into the player explorer) + owner curation panel (create/visibility-toggle/add-remove tags/delete) backed by GET/POST /api/admin/collections (owner-gated, tested). Dashboard/Admin panels page-scope via a `hidden` prop - components intact, effects still mount once. Deployed + deep links verified live.

**FEEDBACK #6 (unprompted!) -> LEADERBOARDS + LIVE-LANE FIX (2026-09-05, 0.16.1):** the 0.16.0 initiative instructions produced their first unprompted filing within hours. Shipped: rankings paths on the live_fetch allowlist (collector PATH_BY_ENDPOINT + admission validators + identity-accreting projector - every ranked tag lands a player row); verified live: PoL global top-100 in 3.5s (trophy rankings genuinely empty late-season per docs). BONUS ROOT CAUSE: live fetches raced the collector's 10s bulk long-poll against the 8s live window (the census live_unavailable watch) - bulk wait now 4s, live window 12s, lane ~3-5s. Closes that watch.

**FEEDBACK ROUND TWO (2026-09-05, 0028, contract 0.16.0) + STANDING LOOP DUTY:** feedback #4 (agent, on the feedback system itself) + #5 (Jamie +1s) actioned: elixir_changelog (what-shipped-since-contract-X, backfilled to 0.6.0 - APPEND AN ENTRY WITH EVERY CONTRACT BUMP, same-commit rule); shipped_in/related_tools on responses; meta.feedback_responses_pending until elixir_my_feedback marks seen; status/since filters; server instructions + elixir_feedback description make friction-filing the AGENT'S job. Jamie standing order: CHECK {feedback_pending} EVERY LOOP TICK and action/respond per the closed-loop rule. Parked: friction-pattern nudges (5c) until volume.

**UNIVERSAL READS RATIFIED + COLLECTIONS SHIPPED (2026-09-05, 0027, contract 0.15.0):** Jamie: "make all battles accessible to everyone... RoyaleAPI has done this for years... once we start adding a player they are in the data. No limitations." All recorded GAME data readable by every approved account (scope 'public'; clan tools accept any recorded clan); ACCOUNT data stays private; leadership gate stays. Supersedes claims/clanmate read gating AND the terms-review "own-context only" launch stance (public-API-data posture). Privacy page rewritten. COLLECTIONS: owned editorial groupings (never global tags - the governance answer is ownership); collections_browse/get; creation owner-only v1 via {collection} ops (clan families = the expansion; admin UI = follow-up). FIRST COLLECTION LIVE: 'pros' - 11 pro players (Mohamed Light + Light family, CAL Sub Kun, Ryley, Pedro, SandBox, Clown, SK Morten), all recording within the hour, Mohamed Light already at 30 battles captured. Pro-scoped corpus intelligence (pro Level Curve / meta) is the compounding follow-up. 29 tools.

**FIRST AGENT FEEDBACK - FULL CIRCLE (2026-09-05, contract 0.13-0.14):** Claude (as user, via Jamie's connector) filed 3 items - the first real send_feedback use. Per Jamie's rule the LOOP CLOSED FIRST (0026: response/responded_at; elixir_my_feedback tool + /api/me/feedback; admin respond + {feedback_respond} op) - feedback is never actioned invisibly. THEN actioned same night: #1 clans_pilot_scores (whole clan in one call, was 18; also the parked pairs-perf lever) + include_curve flag; #2 players_search (name->tag in entitled scope, honest empties) + mode taxonomy already shipped in 0.12.0; #3 praise for sample-sizes/SEs/load-bearing-notes -> cited here as evidence the charter works. All three responded; requester verified seeing responses through its own tool. 27 tools.

**MCP REQUEST EFFECTIVENESS REVIEW (2026-09-05, Jamie's ask; census op {audit_census} now standing):** 7 days / 220 calls: ZERO truncation, 6.4% errors all structured (no internals), all three surfaces real (mcp 180 / web 21 / svc:elixir-bot 19); after rename-accounting only elixir_feedback + watch tools never called (both brand-new). ACTIONS TAKEN: battles_levels was 5.4s avg (40x fleet) -> 0025 stamps deck_avg_level at ingest + backfill, clans_standings lateral -> grouped (now 0.3-0.8s warm). FOLLOW-UP PARKED: battles_levels still ~4.5-6s warm (cost = 66k-row pairs materialization per request; next lever = ingest-maintained level_pairs rollup). WATCHES: cr_api_live 2/6 live_unavailable; KHAOS-era event-burst capture ~85% of bot's (25-battle window vs yield cadence during sprees). Blind spot noted: unknown-tool calls are rejected pre-audit, so a post-rename broken client is invisible - audit them.

**EXPERIENCE COHORTS SHIPPED (2026-09-04 night, 0024):** tenure from the YearsPlayed badge (level=completed years, progress=account age in DAYS - verified live); stamps at profile projection, backfilled from hot payloads; ABSENT = unknown never zero (measured: usually sub-1-year per Jamie's rule, but 2/21 badge-less accounts carry 2024 event badges - docs patched). battles_levels player block gains experience + cohort (pilot percentile among same-tenure corpus players, floors 5/30); clans_standings rows carry years_played. Live: raquaza pilot +0.095 = 81st percentile of 37 in years_3_5 (median +0.029); King Thing tenure-unknown served honestly. Perf: pairs set materialized once per request (temp table, rollback-guarded) after a 4-scan timeout.

**LEVEL CURVE + PILOT SCORE + SCOUTING REPORT SHIPPED (2026-09-04 evening, contract 0.11.0):** battles_levels (66k-obs binned curve, floors, no extrapolation; pilot_score = actual - level-expected with monthly trend + experience/band confound notes) and war_rivals (observer-deduped rival fingerprints, bracket default). BRANDING (Jamie): metrics carry the brand - Pilot Score ("wins your card levels can't explain"), Level Curve, Scouting Report; tagline territory "numbers with receipts". Live-verified via svc:elixir-bot: King Thing pilot -0.102 +-0.035 (trend Jul -0.085 / Aug -0.118, both months n~100); bracket report incl. los claneros 14/14 zero-fame finished races. Preview op {preview_intel} remains as validation scaffolding. Experience cohorts (YearsPlayed badge projection) = queued v2.

**Intelligence roadmap REPRIORITIZED (Jamie, 2026-09-04 evening):** meta deck/card tools drop to third; the two tracks "more actionable for the player" lead: (1) LEVEL ECONOMICS (battles_levels - binned win-vs-level-gap curves; corpus has 98.4% both-sides levels; gap distribution TIGHT per census: median 0.00/p90 0.75 - matchmaking compresses; within-match slope vs positional effect separated); (2) WAR RIVALS (war_rivals v1 clan fingerprints from projected bracket standings - census: 124 rival clans, 88 with 3+ races, observer-deduped; rival rosters = v2 projection review). Designs in META-INTEL sections 9-10, REVIEW PENDING before build. Probe gained level_census + rival_census.

**ELIXIR-BOT PHASE 1 LIVE (2026-09-04):** the first real consumer. elixir-bot's member trend, war attendance, and NEW get_clan_standing (backed by clans_standings, contract 0.10.0) route DIRECTLY here via its service token (svc:elixir-bot) - no shadow mode (Jamie: not enough volume), local tables as error fallback only. Client elixir_mcp.py (None-on-failure), builders capabilities/mcp_stats.py (emit local shapes). Live-verified end to end: trend block, 5/5 season attendance, rank-15-of-48 standing. Battle-intel views stay bot-local by design. Deferred gaps ON RECORD: clan-relative tooling shipped (clans_standings); early-history hole (bot has Apr 18-May 7, we start May 7) ACCEPTED by Jamie ("doing its best to see everything; we may miss some things and that is okay"); player-adjusted lift stays bot-side for now.

**Clan recording SCOPES (Jamie, 2026-09-04, 0023):** clan_scope stamped on the recording row - 'activity' (clan only: roster/war/standings) vs 'comprehensive' (plus every open member's battles+profile, roster-following). ALL existing clans backfilled comprehensive (behavior preserved; POAP KINGS explicitly wants it - "ALL activity by EVERYONE regardless of connection" - and elixir-bot will need it). Scheduler member fan-out + eligibility filter on scope; scope changes take effect next tick (downgrade = member rows go dormant, upgrade = re-seed). Admin panel toggle; watch_clan requests carry scope.

**Domain-first tool names + service tools (Jamie, 2026-09-04, contract 0.9.0):** tool NAMES lead with the domain (players_*, battles_*, clans_roster, war_*, cards_catalog, live_fetch, elixir_*) - names are the one grouping mechanism every client honors. CLEAN BREAK, no legacy aliases (Jamie: alpha, no outside users) - any stale roster must reconnect. The Elixir MCP domain owns account/service actions: elixir_watch_player (claim+record via MCP, web caps enforced identically), elixir_watch_clan (reviewed request through feedback triage, context.kind=clan_watch_request), elixir_data_insights, elixir_collectors. 21 tools; write set = the service domain's three. Jamie's further domain ideas absorbed; future service-domain candidates: unwatch/stop-recording, usage/quota view (web has it, tool doesn't).

**Tool taxonomy SHIPPED 2026-09-04 (Jamie's ask, contract 0.8.0):** TOOL_GROUPS in contracts is the one classification source (Players/Battles/Clans/War/Cards/Live/Elixir MCP, mirroring cr-agent-api-docs; send_feedback lives in the service group). Registry throws on unclassified tools; annotations carry grouped titles + readOnlyHint (16 read-only) + openWorldHint (cr_api_live). /docs Tools page added. NOTE: connector UIs cache the roster - Jamie must reconnect /mcp to see the groups.

**Yield gap found+fixed by monitoring (2026-09-04 ~17:30Z):** battlelog fetches collapsed to ~3/hr mid-colosseum — yield cadences only re-tighten AT a poll, so members whose EWMA learned dormant stay stale exactly when play resumes. Fix: projectRiverRace raises signaled members' battlelog yield_bph from decks_used deltas (one shared 30m riverrace poll names every battler); raise-only, replay-guarded. Watch the battlelog rate recover during war hours. Also verified 0022 re-stamp working (day1 119->289 stamped, unstamped 808->451); (135,4) war_day-null rows persist at ~177 — some are honest boundary nulls, keep an eye.

**Yield scheduler A/B — FIRST HONEST READ (2026-09-04 18:45Z, delivered to Jamie):** heat (Sep 4 07-13Z, clean post-import): 133 fetches/hr, 94 battlelog/hr, 79 battles/hr, 0.84 battles/battlelog-fetch. Yield (15-18Z): 60 fetches/hr (-55%), 27 battlelog/hr, 28 battles/hr, 1.04 battles/fetch (+24%). War polling steady (~25/hr) so war_current freshness unaffected; battlelog's 25-battle window means lower cadence defers capture, never loses it. Delta-feedback visibly working: 18Z caught 25 battles in 10 targeted fetches (2.5/fetch). Caveat: windows are different clock hours — same-hours 24h A/B completes Sep 5 morning; direction already unambiguous (equal coverage, ~half the spend). Superseded:  probe op added to the migrate lambda ({probe: true}) — hourly battlelog/player/war fetch counts (live gateways only) vs battles harvested, 48h window. First reading right after the 14:45Z deploy shows the expected first-sweep burst (256 battlelog fetches/239 subjects in the deploy hour — every subject had null yield_bph) moderating within the next hour. Honest before/after needs the EWMAs settled; re-probe after several hours. NOTE the archive-import hours dominate any 48h battle counts until Sep 6 — compare fetch volume and battles-per-fetch on LIVE hours only.

**MCP testing round 1 COMPLETE (2026-09-04):** 3 personas, 10 fixes shipped (contract 0.3.0) — see commit 19e1e70. Wishlist added for Jamie's triage (beyond pilot round's): evolution facet on get_card_performance / with_evolution filter; per-card win_rate_delta vs baseline; slim get_collection (drop iconUrls) + owned_evolutions_only; suggested one-card-swap deck comparisons; query_battles total_count; per-era completeness gap map in get_coverage (auditor found a day where snapshot delta 10 vs 3 recorded battles — backfill-era days under-captured, only visible per-day); clarify lifetime battleCount vs recorded-battle mode sets; warnings array for future-window empties. BOTH testers hit the frozen-roster cursor-schema mismatch — reconnect /mcp (also unlocks war tools for round 2). Perf: R1 battlelog projector 408->274ms; riverrace still ~3.1s post-0015 — race/stamp timing split deployed to attribute it (in flight).

**Product direction (Jamie, 2026-09-04): Elixir MCP is a DATA PRODUCT.** Primary interfaces: account management, data exploration, product updates, activity logs + quota, data collection. Self-improvement loop is first-class: agent/user feedback via web AND an MCP write tool (agent feedback = that user's feedback); MCP activity logs are product signal; changes must reach users simply. Approved queue additions (all with Jamie's picks): (a) feedback loop + product-updates surface; (b) collector credits -> owner quota at 10:1, capped 4x base (500->2000); (c) collectors split to their own repo elixir-mcp-collector (small clone for operators; contracts stay canonical here); (d) service-token lane for elixir-bot NOW (Admin-issued long-lived tokens, per-token call visibility) + a note in elixir-bot's tooling that Elixir MCP exists — no functionality moves yet; (e) S3 payload archive per DATA-TOOLS.md: BUILD APPROVED; (f) fun: gateways named after CR cards with the card as profile picture. The scheduler yield redesign IS his priority-queue point (server orders one queue; collectors drain it). (g) WEB DATA EXPLORER (Jamie, 2026-09-04): a browser way to explore what the MCP serves. V1 interpretation (Jamie: open to interpretation): /explore page + ONE bridge endpoint POST /api/explore {tool, args} invoking the SAME tool registry (entitlements/honesty/shapes identical; session-authed; audited surface='web' — explorer usage is its own product signal). Views: subject picker, Summary, Battles table, weekly Trend charts (hand-rolled SVG), Decks, Collection grid, War (when entitled), Coverage. Every future tool becomes explorable for free; users see exactly what their agents see. (h) SITE DOCUMENTATION SECTION (Jamie, 2026-09-04): /docs on the web UI — index/ToC + About, Privacy, Terms, and Architecture (technical docs users can read and admins can reason from; distilled from DESIGN.md). Markdown in-repo, rendered at build time, ships with web deploys. MAINTENANCE RULE goes in AGENTS.md: any change altering architecture or user-facing behavior updates site docs in the same commit. Batched with the explorer (same surface). Work order after self-update gap fix: scheduler redesign -> data explorer + feedback loop -> credits->quota -> S3 archive -> collector repo split -> card avatars -> service tokens.

**Self-update gap (found live 2026-09-04):** comparing checkout vs origin never fires on a machine where the checkout advances by development (HEAD==origin always; running process stays old). Fix: restart whenever checkout HEAD differs from the SHA the process STARTED on — one rule covers dev and operator machines.

**Decision pass (Jamie, 2026-09-04 morning):** (1) scheduler yield-per-call redesign: GO, direct build (not shadow), heat path kept one release as fallback. (2) api_payload retention REFRAMED: payloads move to S3 tiering (Postgres keeps latest-per-entity hot set; every admitted payload archives to S3 content-addressed, lifecycle to IA) — design doc docs/DATA-TOOLS.md first, covering Athena/S3 Tables/DuckDB query tooling and what data tools Elixir MCP should use broadly; REVIEW BEFORE BUILD. (3) Wishlist: build weekly win-rate series + headline summary call + deck sort/share/total_count; per-era completeness map and evolution facet parked. (4) elixir-mcp-alarms -> projects-ops-alerts WIRED (raw delivery, queue policy updated, handoff note in sysadmin notes/). (5) Gateway self-update from green main: GO (hourly fetch/pull/exit + SHA in heartbeats, Admin shows fleet versions). Work order: gateway self-update -> wishlist batch -> DATA-TOOLS design doc -> scheduler redesign. Jamie still to reconnect /mcp for round-2 war-persona testing.

**Budget-efficiency redesign — DRAFT for Jamie's review (2026-09-04):** replace heat (immediacy) with EXPECTED YIELD per call. Every endpoint's value is measurable from our own receipts: battlelog yield = new battles admitted per fetch (the archive census shows it directly); profile yield = snapshot deltas; riverrace yield = points/standings changes (war days >> training days). Proposal: (1) score each (subject, endpoint) with an EWMA of recent yield-per-fetch from api_receipt + projection outcomes — the data already exists; (2) scheduler ranks eligible jobs by yield x staleness instead of heat tiers, with the existing starvation floors unchanged (they are correctness, not efficiency); (3) battlelog cadence adapts per player automatically (a 20-battles/day player polls hourly; a dormant one daily) — this is what hot-player heat approximated, derived instead of asserted; (4) war-day awareness moves from fixed 30-min riverrace cadence to yield-driven (training days decay to ~2h, war days tighten); (5) fleet: keep single global budget + SQS competition (observed working with 2 gateways); per-gateway budget splits only if a gateway misbehaves — the trust model already covers that via draining. Measurable target: battles-captured-per-API-call and snapshot-completeness at HALF today's call volume. Implementation is a scheduler-only change (plan.mjs) — no schema, no gateway changes. AWAITING JAMIE's go.

**Scheduler budget-efficiency redesign (Jamie, 2026-09-04, queued after the archive import + R1):** the heat model was imported from elixir-bot's "hot player" feature — which optimized IMMEDIACY and was of questionable value even there. The real objective is efficiency of API calls across the known gateway fleet: spend the global budget where it buys the most recorded value (battles captured per call, war-window coverage, snapshot completeness), not where activity happened most recently. Rethink CADENCE/heat from that frame; fleet-aware lease distribution belongs in the same design. A second production gateway (jamie-mac-2, second CR key, same host) is live specifically to make multi-gateway behavior observable before this design.

**V2 build order COMPLETE.** Open Jamie-manual / later items:
- ~~Flip clan scope to verified-claims-only~~ MOOT: verification retired 2026-09-03, claims are trust-based; the knob was removed with it.
- **Cards endpoint was NEVER SCHEDULED (found by post-deploy verification 2026-09-03):** ingest/tools/fixtures knew `cards`, but CADENCE, seedPollState, the eligibility SQL, and the gateway path map all missed it — prod had zero catalog, get_card_catalog 503'd, and the 0011 backfill silently stamped decks unconverted against an empty catalog. Fixed (daily cards/GLOBAL job + gateway path + GLOBAL freshness in pipeline); 0012 repaired the stamped-raw decks using the exact IngestFunction-update instant from CFN stack events as the raw/display discriminator, verified live (Jamie's maxed deck reads all 16s, norm=2). Lessons: (a) an endpoint exists only when the SCHEDULER knows it — fixtures make everything downstream pass; (b) a data backfill must fail loudly when its reference data is missing, never stamp-and-continue; (c) verify data migrations through the real door immediately after deploy.
- **Card levels normalized 2026-09-03 (Jamie's live bug report):** API levels are rarity-relative; everything we serve now uses the in-game 1-16 display scale via contracts displayLevel — THE one conversion (elixir-bot's five-call-site scar imported). Ingest stamps deck.norm=1; 0011 backfilled stored decks (norm-guarded, double-shift-proof); get_collection normalizes; cr_api_live stays RAW by design and says so; CONTRACT_VERSION 0.1.1. Any future field that carries a card level must pass displayLevel at the seam.
- **Visibility & quotas shipped 2026-09-03 (Jamie-approved plan):** 0009 account.mcp_daily_quota (default 500, row-update override) + quota headroom in every tool meta; /api/me/usage + Dashboard Usage panel; /api/admin/usage (per-account, per-tool, budget line vs 86,400/day capacity + heaviest subjects); Connected agents panel (family list + Disconnect); 0010 account_event activity log (signed_in, claim_added, recording_started/stopped, gateway_raised, connection_revoked, agent_connected) + Recent activity panel; recording cap (default 5 active player recordings/account, owner exempt); per-recording + per-gateway fetches/24h. Verification-attempt caps were dropped from the plan (verification retired). Retention note: prune mcp_call_audit.args after ~90 days when it matters.
- **Ratified 2026-09-03: being in a clan IS sharing your battles with it.** share_battles_with_clan toggle removed (UI, route, entitlement lane); rule 2 clanmate access covers full battle history of open members. The column stays in the ladder unused. Rationale: battlelogs are public in-game and via the API; the toggle gated convenience access to public data.
- Per new gateway operator: IP-allowlisted Supercell key + per-gateway IAM user (recv/delete request queues, send results, PutMetricData), handed over out of band; then "Begin probation" in Admin.
- Gateway auto-update (launchd runs from checkout; kickstart after gateway-code pulls) — a self-update or deploy reminder still wanted.
- Subscribe elixir-mcp-alarms to the sysadmin projects-ops-alerts queue.
- Delete the Fastmail draft that carried the JMAP token (Jamie).
- Capacity: ~30 clans saturates one gateway's practical throughput; volunteer fleet is the lever.
- Timeline 'season' granularity — contract-minor, when wanted.

2026-09-03 audit outcomes (DESIGN.md §11–§12 added): versioning designed (migration ladder + fingerprint test; contracts package + serverInfo cache-buster + deprecation rules; audit-row tuning loop). Build-blockers closed: DB access path (migrate Lambda + break-glass), SQS 256KB (gateway gzip, alarm on overflow), live request lane (second queue, gateways drain first), budget state in Postgres. Scope changes ratified: `get_player_timeline` into V1 (11 tools), season-roll watcher into V1, **repo public from day one** (fixture discipline from first commit).

**Scheduler metrics HANG fixed (issue #1, incident 2026-09-06, routed by Operations):** the 0040 ledger change added an awaited CloudWatch `PutMetricData` call to the scheduler tick. The scheduler's NAT-free VPC has only SQS + S3 endpoints (no CloudWatch interface endpoint), so the SDK call had no network path and hung every tick to the full 50s Lambda timeout; reserved-concurrency-1 then throttled following ticks (`elixir-mcp-scheduler-errors` ALARM at 10:03Z, 55/60 REPORTs `Status: timeout`). Core recording survived — the DB commit lands before the blocked call, so preflight stayed green. Fix: emit the three ledger metrics as CloudWatch Embedded Metric Format on stdout (`services/scheduler/src/metrics.mjs`) via `process.stdout.write` — NOT `console.log`, whose Text-log-format prefix would corrupt the EMF JSON — which rides the out-of-band Lambda log-delivery path and needs no VPC endpoint. The handler now fire-and-forgets metric emission (never awaits it), so no future sink can reintroduce the hang; regression test injects a never-resolving sink and asserts the tick still returns. Removed the now-unused `@aws-sdk/client-cloudwatch` dep and the `cloudwatch:PutMetricData` IAM grant (with a comment on why, so it isn't re-added). NAT-free VPC preserved per the issue's acceptance. **Lesson:** any awaited network call added to a VPC Lambda must have a matching interface/gateway endpoint, or it degrades to a silent full-timeout hang, not an error — EMF-on-stdout is the endpoint-free path for metrics.

**GPT-6 Astra bug sweep FIXED (issues #2-#7 + collector #1, 2026-09-06):** seven read-only-assessment bugs in the 0040 ledger / zero-trust door, all with regression tests. **#2** two leases for one subject expiring together raced the one-queued-per-subject index and threw, blocking fleet-wide leasing — settlement now picks at most ONE retry per subject (`distinct on`), folds the rest, and retries once on a residual 23505. **#6** missed-expiry attribution was charged only to whichever gateway happened to settle — now every expired lease charges its abandoning gateway inside the settlement transaction itself (`returning leased_by` → one `missed_streak` bump), exactly once regardless of actor; the door no longer keeps its own charge. **#5** the two-outstanding-leases cap was a check-then-grant race across connections — the door now does the count + grant under a per-gateway `pg_advisory_xact_lock` in a short transaction (held only across check-and-grant, never the poll wait). **#7** the door stamped `last_success_at` from the client's declared status — removed; ingest is the sole owner and stamps only on ADMISSION (re-deliveries don't advance it; content-identical refetches do). **#4** inline ingest gunzipped without an output bound (a 10 KB body could inflate past the 512 MB Lambda) — now `gunzipSync(..., {maxOutputLength: 2 MiB})` (largest legit CR payload ~300 KB), rejected as an observable `body:too_large` receipt. **#3** a live wait accepted the newest receipt for the subject, so a bulk collector's result could serve a user's live answer (the ratified channel boundary) — 0041 adds `api_receipt.job_id`, the door stamps it server-side from the lease row, and the waiter binds to ITS job id AND a live-channel gateway; job_id is stable across in-place requeue so a retried live job still completes its wait. **Collector #1** both clients applied the 250 KB overflow to the RAW body before gzip (discarding compressible battlelogs the transport would accept) and counted those as zero errors — overflow is now judged on the gzip+base64 encoded size (an explicit 8 MiB raw ceiling stays distinct), and overflows count as fetch errors in the activity summary. Fingerprint re-pinned from a fresh scratch DB for 0041.

**Live-wait same-second truncation FIXED (found by the #3 prod verification, 2026-09-06):** with two healthy live collectors, a prod `live_fetch` timed out twice while coverage showed the fetch was leased, submitted, and ADMITTED at 13:19:09.000Z - the exact request second. Cause: collectors stamp `fetched_at` at SECOND precision while the waiter's `since` guard was a millisecond timestamp, so a fetch completing in the same wall-clock second sorted before `since` and was dropped. Latent since the ledger cutover made the live path sub-second (under SQS the round trip took seconds, masking it); with one fetch per 12s window a miss is a timeout. Fix: floor `since` to the second with a 5s margin - safe because the #3 job_id binding is now the correctness guard and `since` only bounds the scan. Regression: a second-precision collector clock in the same second as the request must complete the wait. Lesson: **a millisecond-vs-second comparison across a clock boundary is a race that only appears when the path gets fast**; the collector-side ms-precision fix is optional now.

**Bug sweep #14-#20 FIXED AND DEPLOYED (2026-09-07, live 02:00:29Z):** seven reported bugs, two of them authorization. **#14 (P1)** `/api/admin/decide` checked only that the ACTOR was an admin, so an admin could read the owner's email hash off `/api/admin/accounts` and DENY the owner out of the service - a status move outranking the role move `canSetRole` already refuses, and `docs/roles.md` already promised ("No admin can change the owner's account - or another admin's"). `decideAccess` now takes `actorRole` and carries the hierarchy in the UPDATE's own predicate (owner untouchable by anyone including themselves - availability outranks it; admins only by the owner), then disambiguates zero-rows into 403 vs 404 so a refusal is never reported as a decision. **#16 (P1)** the self-service collection upsert checked ownership in a separate SELECT while its `ON CONFLICT` had no ownership predicate: racing another account's creation of the same slug let the LOSER rewrite the winner's row, including flipping it public and exposing curated members. Ownership moved into the conflict predicate (`where collection.owner_account = $6 returning ...`); zero rows = 403. The ADMIN upsert deliberately keeps no predicate - editing any collection is a console power - and both statements now say which they are. **#15 (P2)** the #13 fix protected page one and page TWO undid it: the gap query started at the request's `since`, which is already past the unread event page one stopped at, so following our own `next_cursor` acknowledged exactly what was protected. The fetch cursor and the acknowledgment boundary are now two numbers (`cursor` vs `ackFrom`); the gap search always starts from the account's persisted position. **#17 (P2)** all three call sites read membership, computed a whole desired set, and passed it in as a replacement, so two concurrent adds each deleted the other's member - the collection lock serialized writing STALE snapshots. `setCollectionMembers` now takes `{mode: set|add|remove}` and resolves the delta inside its own lock. **#18 (P2)** raising scope to comprehensive reconciled only added/removed tags (empty on an unchanged roster), so existing members kept activity depth and the promised battle history was never captured; new `reconcileCollection()` deepens what is already there, under the same subject locks. **#19 (P2)** deleting a collection cascaded membership away but reconciled nothing, leaving subjects scheduled forever for a list that no longer exists; new `deleteCollection()` settles former members after the delete, so `reconcileRecording` keeps owning the "somebody else still wants it" and ops-recording rules. **#20 (P2)** `CustomErrorResponses` is DISTRIBUTION-wide, so mapping 403/404 to a 200 app shell also rewrote the API's refusals - unauthenticated `/api/admin/requests` returned 200 `text/html` and `apps/web/src/api.js` (`res.json().catch(() => ({}))` + `res.ok`) read it as SUCCESS. Replaced by a CloudFront function (`elixir-mcp-spa-router`) on the DEFAULT behavior only, rewriting to `/index.html` only when the last path segment has no extension; the client now reads the body as text and treats a non-JSON body as a failure whatever its status. Every fix has a regression test verified to FAIL against the old code; `npm run verify` green (301 node + 19 web). **Lessons:** (a) a precheck in a separate statement authorizes nothing - the rule belongs in the predicate of the write; (b) a lock only protects what is READ inside it, so a caller that computes a desired set before the lock is racing no matter how well the helper locks; (c) two cursors that are usually equal are still two cursors, and the bug appears exactly when they diverge (#13 -> #15, same shape as the ms-vs-second race above); (d) an edge-wide error mapping is not a site feature, it is a distribution feature, and it will rewrite everything the distribution serves.
- **DEPLOYED 2026-09-07:** stack UPDATE_COMPLETE, all six Lambdas updated 02:00:29Z, no alarms, collectors unaffected. #20 verified live at the reported hostname: `GET https://elixir.poapkings.com/api/admin/requests` now returns 403 `application/json {"error":"not_entitled"}` where it returned `200 text/html`; API 404 stays 404; `/account/overview` still serves the shell at 200; a missing asset is 403, not the shell. `infra/scripts/smoke.mjs` gained those four edge checks and runs them on EVERY deploy, so a reintroduced distribution-wide error mapping fails the deploy rather than going unnoticed. All seven issues closed.

**CLAN RECORDING: THE COLLECTION WAS NOT A REASON (2026-09-07, FIRED IN PROD then FIXED + REPAIRED):** Jamie set out to remove the players and clans he had personally added now that they sit in collections, and asked for a watch that collection status kept them collecting. The pre-check found the bug instead: THREE functions each knew only SOME of the reasons a subject is recorded. `reconcileRecording` knew `claim` (players) and `collection_member` but NOT `account_clan`; `ensureClanRecording`/`settleClanRecording` counted `account_clan` and could not see collections. So removing the last account that had added a clan stopped a clan a collection still curated - and the mirror case, editing a clan out of a collection, could stop a clan an account still had added. `settleClanRecording` also lacked the `origin <> 'ops'` guard, so it could stop an operator-owned recording, and `ensureClanRecording` could DOWNGRADE a clan a comprehensive collection wanted when an account added it at activity scope. **It fired before the fix landed:** the five `clans-to-watch` clans read `recording:true` at 02:31Z and `false` at 02:43Z; the 11 `pros` and 13 `elixir-drop` players stayed `true` throughout, which is exactly the player-path/clan-path split. FIX: `reconcileRecording` counts every reason for both subject types (claims, account_clan, collections, each with its own depth) and the two clan helpers delegate to it; players keep upgrade-only scope, clans keep the ratified settle-to-widest-remaining now computed across collections too. THIRD blind spot found while repairing: the ops door `collectionOp` in the migrate lambda wrote `collection_member` directly and never reconciled, so an ops add started no recording and an ops remove stopped none - it now goes through `setCollectionMembers`, and gained `{collection:{op:"reconcile", slug}}` as the repair op. REPAIR: that op restored all five (`recordingsStarted:5`); `pros` and `elixir-drop` returned 0, confirming players were never touched. **Lessons:** (a) when two subject types have separate write paths, the REASONS they are recorded must still live in one function - "who wants this?" is not a per-path question; (b) an ops/break-glass door that bypasses the shared helper is a fourth code path nobody tests, and it will drift; (c) a pre-flight code read before a destructive user action is worth more than any amount of watching afterwards - the watch Jamie asked for would only have told him the data had already stopped.

**AGENT FEEDBACK #9 ANSWERED, contract 0.26.0 (2026-09-07):** two reported `war_current` bugs were ONE root cause. The ~10:00Z war reset drifts EARLY, so the recorder first saw war day 4 open at 09:57:37Z; `period_end_nominal` was computed as "the next 10:00Z strictly after the anchor", which landed 2.4 minutes after the period STARTED - ~24h early and already 10.5h in the past at read time. `decks_today` is gated on `Date.now() < period_end_nominal` (the stale-anchor guard), so the same wrong boundary silently removed the nudge list on a live war day. Both fields now anchor on the period's own nominal START (latest 10:00Z at or before the anchor, 1h drift tolerance) with the end a full day later. Verified live post-deploy: `period_end_nominal 2026-09-07T10:00:00Z`, `decks_today` untouched 34 / partial 6 / finished 8 over 48, reconciling exactly with `attendance_by_war_day` battled:14. **Why it reached production:** the existing `decks_today` test anchored the period at `now()`, where "the next 10:00Z" is ALWAYS in the future - the test could not fail. The new regression pins the production shape (observed minutes BEFORE the reset). Third item: `clans_pilot_scores` gains a `basis` block (curve_pairs, curve_bins, window_from, window_to) rather than the requested `model_version` - the curve is refit over a rolling window every request, so scores move with the corpus and no code revision exists to report; basis is the honest discriminator between a moved baseline and a moved player. Declined for now with reasons given: per-day `decks_by_war_day[]` on participants (a war_participation storage change, not a bolt-on). **Lesson:** a test that constructs its fixture from `now()` can encode the same wrong mental model as the code it guards - pin the shape the PRODUCTION data actually has.

**WAR CLOCK FOLLOWS THE POLICY GRID (Jamie, 2026-09-07, contract 0.27.0):** Jamie's call, and the right one. Supercell's reset is 10:00Z BY POLICY, but clans are matched into races of five as matchmaking fills, so each clan's real period start drifts off the hour by its own amount. elixir-bot measured POAP KINGS' drift and followed it - correct for ONE clan. Elixir MCP is fundamentally multi-clan and cannot honour every clan's start while `war_day` still means one comparable window, so `warClock` now takes its boundary from the policy grid for every clan; the anchor only picks WHICH policy day, and is reported raw as `started_observed_at` + new `observed_offset_minutes` so a single-clan consumer can still correct. EVIDENCE (new read-only `{war_drift: true}` op on the migrate lambda, 50 anchors / 10 clans): POAP KINGS median -19 min, range -124..+655; every other clan had 1-3 anchors and FIVE shared an identical +278 because one polling sweep first saw them all. That is the argument in miniature - `first_observed_at` is when the RECORDER looked, so across many clans on a fetch budget the anchor mostly measures OUR cadence, and the census note says so in its own output. Second reason: the policy grid is a pure function of time, so a replay assigns the war day the live run did (the stateless lesson from the phantom-season incident, one level down). COST, stated not hidden: battles between a clan's real start and the policy hour land on the previous policy day. **The canary was muted:** `decks_today` capped display at `least(...,4)`, so the first symptom (somebody counting FIVE decks in a policy day) was silently rounded away - it now surfaces as `over_cap`, which is the direct measurement of what this costs. Also collapsed the boundary arithmetic to ONE `nominalPeriodBoundsMs` in war-clock.mjs: it had been duplicated in war_current and the clan_pulse feeder, and only one copy got the feedback-#9 fix, so clan_pulse was still dropping its war block on live war days. **OPEN for Jamie:** seasons still anchor on 09:30Z (derived from observed riverracelog createdDate stamps) while periods now use 10:00Z policy - two nominal hours coexist. Flagged in war-clock.mjs, NOT changed: if the real policy is 10:00 for both, a season boundary observed in that 30-minute window on a first Monday rolls half an hour early, but moving it reshuffles season attribution for every historical week. **Lesson:** the right clock for a single-subject tool is the wrong clock for a corpus - per-subject accuracy and cross-subject comparability are in direct tension, and a multi-tenant service should pick comparability and report the per-subject correction rather than bake it in.

**OAUTH AUTHORITY MATCHES CONSENT (issue #21, 2026-09-07, contract 0.28.0):** The consent page promised only reads while the one `cr:read` bearer could invoke six state-changing tools. Fixed at the source: `packages/contracts` now owns the five-capability catalog and every tool's required scope; OAuth families persist their canonical grant; refresh rotation carries it unchanged; the MCP door checks it before hourly/daily quota; and RFC 8707 `resource=https://elixir.poapkings.com/mcp` binds the grant, code, family, and access-token validation to this one resource. The first connection is read-only (`cr:read`); tool-specific 403 challenges ask clients to step up with recordings, collections, account, or feedback write capability. Existing families migrate to `cr:read`, never retroactively gaining authority their user did not grant. Owner-issued service tokens remain deliberately full-capability because the owner mints them once for trusted headless integrations; Connected Agents now shows each OAuth family's scope so authority is visible after consent. Regression coverage pins the entire 36-tool map, consent text, read/write enforcement, refresh preservation, and audience refusal. **Lesson:** MCP annotations describe behavior to agents but authorize nothing; the required capability belongs beside that classification and must be checked before any quota or handler work.

**RDS RESERVED INSTANCE PURCHASED + THE SWAP FALSE ALARM (2026-09-07):** 1-year All Upfront reservation `elixir-mcp-pg-1yr-2026-09` on db.t4g.micro / postgresql / Single-AZ, $95 (offering 4393892f-6ba1-43e9-a176-d0c7e46ea183), state payment-pending at purchase. On-demand is $0.016/hr = $140.16/yr (Pricing API), so this saves $45.16/yr (32%). Options were No Upfront $105.12, Partial $49+$0.006/hr = $101.56, All Upfront $95; 3-year was a better RATE (All Upfront $199 = $66.33/yr, 53%) but rejected as over-committing a database that was one day old. RESERVATIONS DO NOT COVER storage/backups/IO (~$1-2/mo gp3 stays). **Size flexibility removes the upsize trap** (verified in AWS docs, applies to RDS for PostgreSQL): normalized units are micro 0.5 / small 1 / medium 2 / large 4, within the SAME instance class type only, so a micro RI covers half a db.t4g.small and TWO micro RIs exactly equal one small RI ($95x2 = $190 = the small RI price). Nothing is stranded as long as we stay in db.t4g; moving to a different family (m7g/r7g) would strand it. **THE SWAP FALSE ALARM, recorded because the failure mode will recur:** I read SwapUsage in 6-HOUR buckets (4 datapoints: 40 -> 73 -> 75 -> 77 MB), called it ongoing memory pressure, and recommended a resize to db.t4g.small (+$140/yr). Jamie pushed back on the premise ("do we have that much activity?"). At HOURLY resolution the same data is: 0 MB at instance creation -> ~70 MB over the first five hours -> FLAT 65-85 MB for the next 19 hours (+7 MB, noise), with within-hour min/max of 40-60 / 94-108 showing pages actively returning. FreeableMemory is stable at 104-110 MB (the "decline" was the initial cache warm-up 143 -> 110 read as a trend). Supporting evidence: ReadLatency 0.7ms, ReadIOPS single digits, CPU 5% with credits climbing, 2 connections, and 2 days of MCP traffic totalling ~180 calls (war_current 32, battles_query 24, war_history 22, live_fetch 19). Linux parks cold pages in swap with RAM free and RDS reports those bytes indefinitely; ~70MB on a 1 GiB Postgres box is unremarkable. NO RESIZE. **Lessons:** (a) a coarse CloudWatch Average can turn a one-time fill plus a plateau into a "trend" - always confirm a capacity claim at a resolution where the initial transient and the steady state are distinguishable; (b) the oscillation the average smoothed away (pages coming BACK) was the actual discriminator between churn and starvation; (c) CLI timestamps render in LOCAL time (CDT) even for a UTC-specified window, which made the series look shifted 5h - check tz before reasoning about a time series. **WATCH INSTEAD OF SWAP:** ReadLatency sustained above a few ms, ReadIOPS climbing persistently, FreeableMemory toward single-digit MB. **Performance Insights: SUPPORTED here** (the db.t4g.micro/small exclusion list applies to MariaDB and MySQL only; RDS for PostgreSQL's instance-class restriction column reads N/A) but NOT turned on - it measures DB load/waits/SQL and our load is ~2 connections at 5% CPU, so it would show an empty chart. Enhanced Monitoring (OS memory breakdown + swap in/out RATES) is the tool that would actually have settled this, and is the one to reach for next time; both run an agent on the box whose memory is the constrained resource. Neither enabled - Jamie's call.

**NEWSLETTER IS OPT-IN, PRIVACY PAGE CORRECTED (issue #27, 2026-09-07, 0049):** Two statements on `/docs/privacy` had drifted off the implementation, one of them false. (1) The page said email is stored "only as a one-way hash (never plaintext in the application tables)" while `0046` had added `account.email` the day before ON PURPOSE - Jamie's call, "an account we can never write to is an account we can never tell anything." The decision was right; nobody updated the notice, so the page stated a falsehood to every reader for a day. It now describes both roles honestly: the hash is identity and the only lookup key, the address is contact information, never a lookup key and never on a public surface. (2) Enrollment in the Buttondown list rode the login send - signing in subscribed you. Authenticating is not an affirmative marketing choice. `account.newsletter_opt_in` (default FALSE) now decides, set only by a switch on Account > Overview; the relay has no database, so the enqueuing VPC lambda stamps `msg.newsletter` and the relay enrolls only what is stamped (both login doors: web `/api/auth` and the OAuth shell). Transactional mail is explicitly NOT governed by it. **Buttondown 400 is now READ, not assumed:** it answers 400 both for "address already exists" (benign, and we still never override an unsubscribe) and for "your request is wrong", and treating the whole status as success meant a validation error or a renamed field looked like a healthy enrollment with nothing anywhere to say otherwise. Known exists-codes pass, everything else throws into the log; enrollment stays best-effort and never dead-letters, because a retry would RESEND the login email. Also fixed the "history, not new exposure" line (the honest half of #26): every observation was public when collected, but a season of somebody's battles in one query is a capability the official API does not offer. **JAMIE-MANUAL / DECISION QUEUED: addresses enrolled under the old behaviour are still on the Buttondown list.** The column governs future enrollment only and Buttondown holds its own unsubscribe state. Decide whether to leave them (they can unsubscribe from any issue), or prune the list to only accounts that have since opted in. **Lesson:** a ratified decision that changes what you store is not done until the page that describes what you store has changed too - the migration comment recorded the decision perfectly and the user-facing notice contradicted it for a day.

**ROLE CHANGES ARE ONE DECISION (issue #29, 2026-09-07):** The same shape as #14, one endpoint over. `POST /api/admin/accounts` SELECTed the target's role, asked `canSetRole`, then UPDATEd by `account_id` alone - so an account promoted between the select and the write could be demoted on stale authority, which is precisely the admin-touching-an-admin move the hierarchy forbids and `docs/roles.md` promises against. New `setAccountRole()` in services/auth/src/gate.mjs, sibling to `decideAccess`: `canSetRole` stays the single source of the hierarchy, but instead of restating it in SQL we ask it which CURRENT target roles this actor may move to the requested role and the UPDATE matches `role = any($3)` plus `is_owner is not true`. Zero rows disambiguates into 404 (no such account) vs 403 (owner_protected / not_entitled), so a refusal is never reported as a decision, and `logEvent`/`emitFeedEvent` now fire only for a change that actually committed. Also added a UUID guard: a malformed `account_id` was reaching Postgres as a uuid syntax error. **The regression test is a REAL interleaving, not a mock:** a second connection promotes the target to admin and holds the row lock; our change arrives, blocks on the lock, and under READ COMMITTED Postgres re-evaluates the predicate against the committed new row - zero rows, refused. Verified to fail against the restored select-then-update shape. **Lesson (third time now, #14 -> #16 -> #29):** a precheck in a separate statement authorizes nothing. When a rule is about the row you are writing, it belongs in that write's own predicate; the only safe SELECT-then-UPDATE is one where the SELECT's answer cannot change.

**AUDIT ARGUMENTS ARE ALWAYS VALID JSONB (issue #30, 2026-09-07):** `invoker.mjs` did `JSON.stringify(args).slice(0, 4000)` into a `jsonb` column. The cut lands wherever 4,000 characters happen to fall - usually mid-string or mid-object - Postgres refused the fragment, and the insert's catch swallowed it silently, so **a caller could choose an argument length that erased its own audit row.** An evidence bypass, not an authorization one, which is why it sat at P2. Now `boundedArgs()`: redact by key name first (nothing in the 36-tool surface is named token/secret/code/api_key, and nothing should be - an audit row is evidence, not a place for a future tool's credential to land), then measure in BYTES not characters, and on overflow keep top-level fields that still fit while naming `dropped_keys` and pinning the whole original with a sha256, so two identical oversized calls are still recognisable as the same call. Oversized arrays/scalars get the envelope alone. The swallowing catch now logs `audit_write_failed` with tool + surface: telemetry still never breaks serving, but a durable audit that failed is exactly the thing worth knowing. **The regression test is end-to-end against real Postgres** - an oversized `battles_query` through the real invoker, then `args -> '_audit' ->> 'truncated'` read back BY POSTGRES, which only works if the value is genuinely jsonb. Verified to fail against the restored `slice()`. Unit coverage walks 3900/3960/3980/4000/4001/4096/20000 bytes plus multibyte (2,000 emoji), because the old bug lived exactly at that boundary. **Lesson:** truncating a serialized structure is not the same as truncating a string - if a column has a type, bound the VALUE before serializing, never the serialization after.

**MACHINE LABELS ARE PRIVATE, AND THE CODE NOW MEANS IT (issue #28, 2026-09-07):** The ladder's own comment said the operator-chosen name "stays as the machine label" while the response handed `machine: g.name` to every approved account. Machine labels routinely carry hostnames, usernames and locations, so this broke an explicit privacy boundary the code had written down for itself. **The report only found the web ladder; `elixir_collectors` leaked the same field to every connected AGENT**, which is the wider surface of the two. Both now return the card as the only name, and an unnamed collector is "Collector" rather than falling back to the private label (`card_name ?? name` was a second leak path). The one legitimate client use - an operator picking their own row out of the ladder - is served by a server-computed `mine` boolean instead; deliberately NOT `gateway_id`, because #31 treats knowing a gateway identifier as an attack precondition. **Inventory of every gateway-bearing response (the issue asked for one):** `/api/admin/gateways` owner-gated and full-detail, fine; `/api/me/gateways` your own rows only, fine; public status publishes card + status + fetch counts + the operator's primary claimed PLAYER name/tag and no machine label, which is intended but was undisclosed - now documented in OPERATORS.md and the privacy page as "your CR identity becomes public, your machine name and IP do not." **TWO FINDINGS LEFT FOR JAMIE, deliberately not changed:** (1) `POST /api/gateways` answers 409 `name_taken` on a global uniqueness check, so any approved account can probe whether a machine label exists - a real enumeration oracle, but the dedupe serves a purpose and scoping uniqueness per-owner changes semantics (`name` is used as a key elsewhere, e.g. the `backfill-elixir-bot` filter); (2) the public operator player credit is a product decision, now disclosed rather than removed. **Lesson:** when a comment states a privacy boundary, the boundary needs a test - this one was documented in three places (code comment, OPERATORS.md, the ladder's own design note) and enforced in none.

**COLLECTOR ENROLLMENT IS TRUSTED-VOLUNTEER, SAID OUT LOUD (issue #22, 2026-09-07):** The finding is correct and the P1 defensible in principle: probation is a STATUS, not a control - a probation gateway leases real jobs and its admitted submissions project straight into canonical history, and admission proves shape and identity, never truthfulness. Black-hole quarantine catches a collector that returns nothing, not one that returns fiction. But the full remedy (shadow lane, trusted double-fetch comparison, activation thresholds, gateway-scoped purge/rebuild) is a multi-week project, and today there are ZERO third-party operators: every collector is one of Jamie's machines, and enrolling anyone requires him to hand-issue an IP-allowlisted CR key and per-gateway credentials out of band. So the blocking P1 becomes a scheduled prerequisite: `docs/COLLECTOR-ZERO-TRUST.md` now opens with what is true TODAY rather than where the design is going, and states **the activation gate - before the first gateway run by someone other than Jamie goes `active`, either the shadow lane ships or the decision to trust that operator anyway gets written down.** "Begin probation" currently means "these results count". Also corrected the doc's own overclaim: it described provenance quarantine ("revoke, purge that gateway's receipts, replay without them") in the present tense, but there is **no gateway-scoped purge procedure and none has ever been run** - the `{replay}` op exists and was exercised for the elixir-bot backfill, but excising one observer while KEEPING the same battles as seen by an honest clanmate's collector is the hard part, and it needs a disposable DB and archived fixtures before anyone relies on it. The cabin DS416 is Jamie's own machine and is not gated by this. **Lesson:** a design document named for its destination will be read as a description of the present; if the gap matters operationally, the gap belongs in the first section, not inferred from a v2 roadmap halfway down.

**[PARTLY SUPERSEDED 2026-09-08 — the headers half stands; the "app loads NO third-party script" half protected nothing and was reverted. See the analytics-proxy entry below before trusting this one.]** **THIRD-PARTY JS OFF THE PRIVILEGED ORIGIN + BROWSER SECURITY HEADERS (issue #25, 2026-09-07):** The Tinylytics embed loaded on every route except `/signin`, which meant it executed on `/account`, `/admin` and `/explore`. HttpOnly stops script from READING the session cookie; it does not stop same-origin script from ISSUING authenticated requests and reading the answers, so that code ran with the signed-in user's authority - including owner token minting and role changes. **The site split made the fix nearly free:** analytics belongs to `apps/site` (no session, no privileged API) and the app now loads NO third-party script at all. What had to survive the deletion was the `login_token` scrub - the magic token arrives in the query string and must not linger in history/bookmarks/Referer - so it moved to `apps/web/src/url-hygiene.js` as first-party hygiene. Product signals were already server-side through the relay (mcp.tool_call, explore.tool_call, site.signin, site.feedback); the only real loss is client-side hit counts on `/data/status` and `/data/dashboard`. **Headers:** new `SecurityHeadersPolicy` (CloudFront ResponseHeadersPolicy) on the default behaviour AND all four API/MCP/OAuth behaviours - CSP, HSTS 2y +includeSubDomains (NOT preload: irreversible, belongs to a domain decision), nosniff, frame-ancestors 'none' + X-Frame-Options DENY, strict-origin-when-cross-origin, and Permissions-Policy as a custom header (no first-class field in SecurityHeadersConfig). **`script-src` has no 'unsafe-inline'** - the home page's access-request handler moved to `/assets/request-form.js` to keep it that way. `style-src` DOES allow 'unsafe-inline', recorded as a deliberate trade: ~70 `style=""` attributes in the Eleventy templates plus the OAuth consent page's `<style>` block, and inline style cannot execute. **THE TRAP WORTH REMEMBERING: I wrote the first CSP from what I thought the pages loaded and it would have blocked the site's webfont.** The origin list came out of the BUILT tree in the end (`grep -hoE 'https://[a-z.]+' dist/site/**/*.html`) - fonts.googleapis.com and fonts.gstatic.com were invisible from the source. A CSP written from memory is a CSP that breaks the page. Coverage: a site test asserts app.html and its bundle reference no third-party origin and that NO built page carries an inline `<script>`; six new smoke checks assert the headers on a real response, because a policy that is defined but not ATTACHED to a behaviour looks identical in the template and does nothing in production.

**ONE IAM ROLE PER FUNCTION + THE ARCHIVE IS RECOVERABLE (issue #24 part 1, 2026-09-07):** Five Lambdas shared `elixir-mcp-vpc-lambda`, so each carried the UNION of what any of them needed. Concretely: the MCP door and the scheduler held `s3:GetObject`/`PutObject`/`ListBucket` on the payload archive and have no `ARCHIVE_BUCKET` env var at all - they never touched it. Now `WebApiRole` (email send, email-DLQ attrs, archive rw), `McpRole` (email send only), `SchedulerRole` (Postgres + stdout, nothing else), `MigrateRole` (archive rw), `JobsRole` (archive **read only** - the weekly sweep HEAD-verifies an S3 twin before deleting the Postgres row, and the job that deletes the database copy must not be able to write the copy it checks against). Each grant was derived from the env vars the function actually receives, not from guesswork. **THE ARCHIVE NOW HAS VERSIONING** - it is the system of record that canonical projections rebuild FROM, and a `PutObject` over a known key was silent and unrecoverable, which is exactly the right the internet-facing ingest path holds. Noncurrent versions expire at 365d (payloads are content-addressed, so an overwrite means something went WRONG rather than something changed - keep long enough to notice, bounded so a loop cannot grow the bill), plus abort-incomplete-multipart at 7d and explicit SSE-S3 rather than inheriting the account default. `s3:DeleteObject` is granted to nothing. **CFN TRAP: YAML anchors/aliases are rejected by CloudFormation** ("YAML aliases are not allowed in CloudFormation templates") - the five identical `AssumeRolePolicyDocument` blocks have to be written out five times. Caught by `validate-template` before deploying, which is why that step exists. **DELIBERATELY NOT DONE, needs Jamie:** (1) separate PostgreSQL roles - all five still share the `elixir` schema-owner credential; doing it properly means migration-managed GRANT/REVOKE per runtime role and it is a bigger, riskier change than an IAM rewrite; (2) `sslmode=no-verify` -> verified TLS: Node does not trust the Amazon RDS CA by default, so this means bundling `global-bundle.pem` into every Lambda and switching pg to `verify-full`, and getting it wrong takes the whole service off its database. The RDS instance is private and in-VPC, so the marginal risk it closes is an attacker who is already inside the VPC. Worth doing, not worth doing unattended on a live service.

**STAGED COLLECTOR CREDENTIALS EXPIRE, AND ARE CLAIMED BY POST (issue #31, 2026-09-07, 0050):** `provision_token` mints a bearer, stores its hash, and stages the RAW token in `gateway.provision_env` for the operator's one-time reveal. The post-claim design was already sound (hash only, nulled on read); the PRE-claim window had no end, so a token staged and never claimed sat in the database as plaintext indefinitely. Two fixes: (1) `provision_expires_at`, set to now()+72h at BOTH staging writers (the admin route and the migrate lambda's provisioning op), carried in the claim's own predicate - and because the predicate is `provision_expires_at > now()`, a NULL fails it, so a stage with no clock reads as EXPIRED rather than as unlimited. **Fail-closed by construction, not by a branch.** The migration backfills existing staged rows with the same 72h clock rather than exempting them, since leaving them NULL would silently break a pending handover. The weekly `sweep_operational` nulls expired stages so an unclaimed bearer stops being READABLE, not just unclaimable. (2) The claim moved from `GET /api/me/gateway-env?id=` to `POST` with the web contract header: consuming a credential is a state change, and as a GET it could be spent by a link scanner, a prefetch, or a cross-site top-level navigation under SameSite=Lax by anyone who knew the gateway id. Response carries `Cache-Control: no-store`. `provision_ready` now means CLAIMABLE rather than merely staged, on both the operator and admin surfaces. **Already correct, verified rather than changed:** re-provisioning sets `token_hash` and `provision_env` in ONE statement, so minting a replacement invalidates the previous bearer atomically - the issue asked for it and it was already there. **NOT done:** mint-at-claim / encrypted staging. Owner-only surface, three gateways, and the window is now 72h instead of forever; the redesign buys little over the expiry. **Lesson:** an expiry column expressed as `expires_at > now()` in the WHERE clause needs no null-handling branch and cannot be got wrong later - the same rule, in the predicate of the write, that #14/#16/#29 keep teaching.

**COLLECTOR PER-TOKEN REQUEST BUDGETS (issue #11, 2026-09-07):** `COLLECTOR-ZERO-TRUST.md` promised per-token limits on the existing `rate_limit` table and the door never applied any. The outstanding-lease cap bounds held WORK and says nothing about request volume, so config polls, empty lease polls and refused submissions all ran unmetered against the shared web API and database. Two buckets, both charged AFTER authentication so a caller can never pick the bucket it fills: `collector-work#<gateway_id>` at 10,000/hr (lease + submit) and `collector-config#<gateway_id>` at 120/hr. **The numbers come from what a collector does, not from a round number:** pacing floor 1500ms and bulk long-poll 2s put the busiest honest collector near 2,000 leases + 2,000 submits an hour, so the work ceiling is >2x unreachable even mid-backfill with a much larger fleet; config is a launch-time contract read on start and refreshed hourly (single digits), so 120 is ~30x headroom while still stopping a config-poll flood, which is the cheapest abuse of the three. Over budget returns 429 with `scope`, `limit_per_hour`, `retry_after_s` and a matching `Retry-After` header - the route wrappers now pass `r.headers` through, which they previously dropped. Window is FIXED not sliding (the existing helper's semantics), and `retry_after_s` is computed to the top of the hour so it is honest about that. **Scopes are separate on purpose: a spent config budget must not halt collection.** Tests pin isolation between tokens, isolation between scopes, the exact boundary (119 served, 120th refused), window recovery, and that an unauthenticated caller creates NO bucket. Verified to fail with the checks removed. **NOT done:** the collector client's own backoff handling lives in the elixir-mcp-collector repo; the budget is set high enough that no honest client should ever see a 429, but if we ever lower it, the client side wants checking first.

**DEV TOOLCHAIN UPGRADED, AND SOMETHING NOW WATCHES IT (issue #32, 2026-09-07):** Five dev-only advisories (1 critical Vitest dev-server arbitrary file read/execution, 1 high Vite, 3 moderate esbuild/vite-node/@vitest-mocker). Production has always audited clean and still does - the risk was a contributor running `vitest`/`vite dev` against a hostile page or network peer, which for a repo with no other contributors is close to theoretical, but the upgrade is mechanical so there is no reason to carry it. vite 5 -> 8, vitest 2 -> 5, @vitejs/plugin-react 4 -> 6, jsdom 25 -> 30, both majors. **Full `npm audit` is now 0 across all severities.** All 20 web tests pass unchanged, the Vite build produces a working bundle, and `npm run verify` is green - no config changes were needed for either major, which is worth recording because it will not always be true. Lockfile checked for what a dependency bump can smuggle: **zero new `hasInstallScript` packages and every new `resolved` URL is registry.npmjs.org.** ADDED: `.github/dependabot.yml` (weekly npm, monthly actions, GROUPED into one PR per ecosystem - this is a single-owner repo and five dependency PRs a week is how a person learns to ignore dependency PRs), and two CI steps - `npm audit --omit=dev --audit-level=low` which GATES, and a plain `npm audit || true` which only reports. That split is deliberate: a fresh advisory against a test runner should tell us, not block a deploy of unrelated work. **Lesson:** the thing that made this a real finding was not the advisory, it was that nothing in the repo was looking - the fix that lasts is the dependabot file, not the version bump.

**PUBLIC READS CACHE, AND THE INTERNET-FACING LAMBDAS HAVE A CEILING (issue #23 subset, 2026-09-07):** The real finding was narrow and worth fixing regardless of any attacker: `/api/*` used the managed CachingDisabled policy while `/api/public/status` and `/api/public/stats` already emit `Cache-Control` (60s and 3600s), so every hit recomputed several time-series aggregates and a full-corpus grouping, and `x-cache` said Miss every time. Both now have their own GET/HEAD behaviours on CachingOptimized, which HONOURS the origin's own Cache-Control. **Listed BEFORE `/api/*` because CloudFront takes the first pattern that matches, not the most specific one** - a more specific behaviour placed after a wildcard is dead config. No origin request policy on them, so no cookie/header/query reaches the origin and one cached object serves everyone; that is correct here exactly because these responses are identical for every caller. Also `ReservedConcurrentExecutions: 20` on web-api and mcp: each invocation opens its own PostgreSQL client against a db.t4g.micro, so unbounded Lambda concurrency makes the DATABASE fail first - the ceiling bounds the blast radius and the bill, and 20 is orders of magnitude above measured traffic. Smoke now fetches public status TWICE and requires the second to be a cloudfront HIT, because a cache behaviour that is present but ineffective looks identical to one that works. **DELIBERATELY DECLINED, with reasons:** WAF (~$8/mo+ of rules to defend a service with ~26 approved accounts) and RDS Proxy (~$15/mo on a $95/yr instance) - both cost more than the footprint they protect, and `~/Projects/CLAUDE.md` says not to add company-scale layers without an observed problem. **DEFERRED, needs care not money:** API Gateway stage throttles and access logs. Both APIs use the quick-create form (`Target:` on the Api resource), which auto-creates the `$default` stage, so there is no Stage resource to attach them to; adding them means converting to explicit Api + Integration + Route + Stage, which replaces the live stage. That is a real change to a live API and wants to be done attended, not as a subset. Disabling the default execute-api endpoints depends on the same refactor and on the custom-domain path being proven first, exactly as the issue sequences it.

**NEWSLETTER IS OPT-OUT, NOT OPT-IN (Jamie, 2026-09-07, 0051 — corrects 0049):** I read #27's consent finding as "make it opt-in" and shipped that. Jamie's actual product decision is the opposite and it is the right one for what this is: **a private, hand-approved beta, where taking part includes the product email.** You can unsubscribe from any issue afterwards; you cannot be in the beta and refuse to hear from it. 0051 flips the default to TRUE and backfills. **The column and the flag-on-the-queue-message survive on purpose** - the relay still has no database and still obeys only what it is told, so the policy lives in one place (the account row) and flipping it later never means editing relay code. **The Account toggle from 0049 is REMOVED, deliberately:** a switch that stops future enrollment but cannot unsubscribe you from Buttondown is a control that lies about what it does, which is a new version of the exact problem #27 was about. One mechanism now - the unsubscribe link in every issue - and Buttondown's own suppression is authoritative and never overridden (an existing address, subscribed or not, is left alone). **Scale check that made this easy:** two addresses have ever been on the list and no issue has ever been sent, so there was nothing to clean up. **What was actually wrong in #27 and stays fixed:** the privacy page claimed email was never stored in plaintext while 0046 stored it. That was the real defect; the opt-in/opt-out question was a product call I should have asked about rather than assumed. **Lesson:** a security review's remediation bullet is a suggestion about MECHANISM, not a decision about PRODUCT - "make enrollment opt-in" reads like a fix but is a policy choice, and the owner makes it.

**PRIVACY POSTURE RATIFIED (issue #26, Jamie, 2026-09-07):** Elixir MCP is a **private, individually approved beta, run by one person, and will never be revenue-generating.** The idea that it opens to others exists but is not the current state. Recorded on the privacy page as "What this is", together with the honest note that public CR data is already published broadly by third-party sites (RoyaleAPI being the obvious one) - so the record here is not a new KIND of exposure, though it is a genuine one. **Jamie's open question, not answered here: nobody knows what RoyaleAPI's relationship with Supercell actually is, so their existence is evidence of tolerance, not of permission.** The GDPR-shaped work (lawful basis, data-subject rights, minors) is gated on general signup and the page now says so in those words. Non-commercial matters for the Fan Content Policy posture and is now stated rather than implied.

**PRINCIPALS: USERS / AGENTS / INTEGRATIONS (2026-09-08, ratified by Jamie in session; Phase 1 shipped at contract 0.29.0):** Jamie: *"the service token is attached to my user so when calls are made it is around me. Service tokens for a clan agent break that."* Correct, and structural: `account` is unambiguously a PERSON (email_hash, timezone, newsletter_opt_in, a `claim` row with `is_primary` meaning literally "me"), and a service token was only ever a credential — `mcp_call_audit` recorded `account_id`, so everything past the door saw Jamie. The 0020 code comment said so out loud: *"Bound to the OWNER's account by default (elixir-bot acts with Jamie's entitlements); a different binding can come later."* Observed consequences before the fix: elixir-mcp-discord called `elixir_my_feedback` on first boot and posted EIGHT of Jamie's own answered feedback items into a public clan channel, because it read his correspondence as its own; `elixir_my_players` returns King Thing as primary, so a member could ask the clan channel "what players do you track?" and get Jamie's personal claimed-player list; the bot polls `elixir_events` with `mark_seen:false` and a local cursor because `events_seen_through` is one marker per ACCOUNT and it could not safely participate in the product's own push lane.

**THE RATIFIED MODEL — three principal kinds, one `account` table, `kind` + `owned_by_account_id`:** **Users** are persons, OAuth only (a "user API key" is precisely what made elixir-bot act as Jamie; `oauth_family` refresh tokens already cover long-running personal clients, so no exception is needed). **Agents** are owned by a user and act FOR a clan — their "me" is `account_clan`, they authenticate by OAuth (a human driving one) or service token (headless: elixir-bot, elixir-mcp-discord), and they are **open to every role, bounded by clan slots**: *you can create an agent for a clan you have added.* Jamie explicitly rejected a partner gate here — *"this is actually a feature all clan leaders should have available."* **Integrations** have NO "me", consume the corpus for their own userbase, are token-only, and ARE partner+ — an Integration is a capacity conversation, an Agent is a feature. Elixir Drop is the type specimen and it has **no connection to POAP KINGS**: it passes an explicit `player_tag` for whoever is playing and carries a configured `warClockClanTag` purely to borrow *some* clan's river race as a season clock.

**DECIDED, with reasons worth keeping.** Agent tier is its OWN, capped at the owner's — Jamie: *"I'm an admin but my agent for poap kings is not."* Agent slots and quota draw on the PARENT; Integration quota belongs to the KEY (its traffic scales with its own userbase, which has nothing to do with its owner's personal usage — that asymmetry is what justifies the partner gate). `roles.ts`'s `service_tokens: 1` at partner was declared and **read by nothing**; it was never about agents — rename to `integrations` and enforce it. Per-key `scope` on `service_token`, because today `validateServiceToken` hands every token `[...OAUTH_SCOPES]` — Drop's credential can edit collections and change account settings to read a clock. An agent's subject is `account_clan` (+ an `is_primary` flag), **not** a collection: collections are content an agent reads, and overloading them with identity invites "what happens to the agent when the collection is deleted". **URLs: `/a/<id>/mcp` and `/i/<id>/mcp` with SHORT RANDOM OPAQUE ids** — we talked ourselves through clan-tag slugs and GitHub-style `owner/agent` namespacing before Jamie called it (*"I think we are being too cute with the URLs"*) and he was right: legibility comes from `serverInfo.name` ("Elixir MCP · POAP KINGS"), the owner namespace existed ONLY to solve slug collisions, and opaque ids solve those for free — so the two-segment path collapses to one. Random, not monotonic: a sequence leaks how many agents exist. Keep the `/a/` vs `/i/` prefix — the URL declares intent and the credential proves it, so a mismatch is a 403 instead of a silently wrong answer.

**WHY DISTINCT URLS AT ALL (the dual-connection problem):** a clan leader running Claude may hold a personal connector AND an agent connector. Both would publish the same 36 tool names with identical descriptions; the model picks one arbitrarily and the two give DIFFERENT answers — different "me", different feed — silently. Writes are the sharp edge: `elixir_add_player`, `collections_edit`, `elixir_feedback` through the wrong connector land on the wrong account. Distinct resource URLs plus genuinely different tool surfaces (no `elixir_my_players`/`elixir_nickname` for agents or integrations) make a wrong choice visible. **The machinery already exists**: 0047 implements RFC 8707 resource indicators and pins the audience to one value by CHECK constraint — Phase 4 relaxes that to a tight pattern, and that relaxation is a SECURITY review, not a schema tidy-up. **Do not build principal switching** (an `act_as` tool or scope parameter): the server is stateless, so the model would carry the switch, flip mid-turn, and never show it in the audit.

**GAP FOUND, worth its own tool:** there is no clan-agnostic way to ask what season/war day it is. Drop borrows an arbitrary clan's river race to learn the date, which is why an MCP season bug reaches Drop through a third party's war race. A subject-free `game_clock` read fixes it for Users, Agents and Integrations alike. Note most reference data is ALREADY subject-free (`cards_catalog`, `battles_meta_*`) — the gap is specifically the calendar.

**PHASE 1 (contract 0.29.0, migration 0052):** `token_id` + `request_id` on `mcp_call_audit`, both nullable, pure expand. `validateServiceToken` had selected `t.token_id` since 0020 and dropped it. `request_id` is minted in the invoker (the one seam every call passes through), written to the row, and stamped onto `body.meta` after the tool returns — a body with no envelope is left ALONE rather than given a half-built one missing the fields the contract promises. Returned to callers as `meta.request_id` so a reported answer joins to its row; surfaced in Account > Activity with the token name. Five tests pin it, including that the id the caller quotes is the id on the row, and that an OAuth call audits with a null token rather than crashing.

**MIGRATION HAZARD THAT TURNED OUT NOT TO EXIST:** the first pass flagged that moving subjects between accounts could stop recordings (a recording stops when NO account has it added). It does not apply to any consumer here — elixir-bot's claims are genuinely Jamie's and stay put, and reads are universal, so an agent needs only its own `account_clan` row, which is purely ADDITIVE and settles to the widest scope. All three consumers migrate by creating an agent, adding the clan, and swapping one env var. Nothing moves.

**PRINCIPALS PHASES 1-3 SHIPPED (2026-09-08 overnight, contract 0.30.0, migrations 0052+0053, autonomous run with Jamie's explicit "deploy through Phase 3"):** Live and verified against production. **0052/0.29.0:** `token_id` + `request_id` on `mcp_call_audit`; `validateServiceToken` had selected `t.token_id` since 0020 and dropped it. `request_id` is minted in the INVOKER (the one seam every call passes through) before the tool runs, so the row and the caller's copy agree even when the tool throws, and stamped onto `body.meta` afterwards — a body with no envelope is left ALONE rather than given a half-built one missing the fields the contract promises. **0053:** `account.kind` (person|agent|integration) + `owned_by_account_id` + random `public_id`; `email_hash` NOT NULL relaxed (the constraint that forces the model change to be honest); `account_clan.is_primary`; per-key `scope`/`daily_quota`/`hourly_rate_limit` on `service_token` (NULL = inherit, so every pre-existing key behaves identically); `service_token.name` unique PER ACCOUNT rather than globally — survivable while only the owner could mint one, but with agents open to everyone the first person to pick 'poap-kings' would have taken it from every clan. `roles.ts`'s `service_tokens` is now `integrations` and finally ENFORCED. **0.30.0:** `game_clock` (subject-free calendar — Drop carries a `warClockClanTag` for a clan it has no relationship with, purely to borrow a river race as a date), per-kind tool surfaces, and per-kind server instructions.

**RULES THAT MADE A LIVE DEPLOY SAFE, worth reusing:** STRICTLY EXPAND with every default reproducing today's behaviour exactly — existing accounts are persons, existing keys keep every scope and inherit their account's quota, an unknown/absent `kind` is treated as a person. Under that constraint elixir-bot, Drop and elixir-mcp-discord could not notice the deploy, and all three were verified working after each one. **Hiding a tool is presentation, not enforcement:** clients cache tools/list forever (this server publishes a fingerprint precisely because they do), so a hidden tool is REFUSED on call as well as omitted from the list; the two surfaces produce different `serverInfo.version` fingerprints and a test pins that they cannot claim to be the same one.

**MIGRATION HAZARD THAT DISSOLVED ON INSPECTION:** the plan flagged that moving subjects between accounts could stop recordings (a recording stops when NO account has it added). It applies to nothing here — elixir-bot's claims are genuinely Jamie's and stay put, reads are universal, so an agent needs only its own `account_clan` row, which is ADDITIVE and settles to the widest scope. All three consumers migrate by creating an agent, adding the clan, swapping one env var. Nothing moves. **Enforced as a rule anyway:** an agent's clan must be one its owner already added, so no second recording starts and no slot is counted twice.

**PHASE 4 BUILT, NOT DEPLOYED (branch `principal-urls`, migration 0054):** per-principal resource URLs `/a/<public_id>/mcp` and `/i/<public_id>/mcp`. Held deliberately: it relaxes the 0047 CHECK that currently prevents a token being minted for an arbitrary audience, which is a SECURITY review and not a schema change. The constraint pattern and `resourceForPath` are ONE boundary in two halves — the constraint decides what audience may be stored, the parser decides what a request is for; disagreement yields either tokens no door accepts or doors accepting audiences the DB would never issue. Both are tested against one shared adversarial matrix (lookalike host `elixir.poapkings.com.evil.com`, embedded URL, query, fragment, trailing path, wrong prefix letter, ids over and under bounds). `principalMatchesResource` is what makes the URLs load-bearing rather than decorative: one agent's key at another agent's door is refused though both are agent doors — 403 not 404, because the resource exists and pretending otherwise turns the door into a probe for which agents exist.

**URL DESIGN, arrived at by discarding two better-sounding options.** Clan-tag URLs (`/c/<TAG>/mcp`) name the SUBJECT, not the principal — but the audience must name the principal, and a multi-clan family agent has no single clan URL. GitHub-style `owner/agent` namespacing solved slug collisions at the cost of an account-handle system. Jamie cut both: *"I think we are being too cute with the URLs."* Legibility comes from `serverInfo.name` ("Elixir MCP · POAP KINGS"), the owner namespace existed ONLY to solve name collisions, and opaque ids solve those for free — so the path collapses to one segment. Random, never monotonic: a sequence tells anyone holding one id how many principals exist.

**VOCABULARY COLLISION FOUND IN THE CONSOLE:** Account > Agents already existed and listed OAuth CONNECTIONS ("Connected agents") — your own clients signed in as you. Once "agent" became a principal kind, one page called two opposite things by one name. Now split into "Your agents" (principals you own) and "Connected clients" (you, via OAuth), with the difference stated rather than left to inference.

**DEPLOY GATE FLAKE FIXED EN ROUTE:** smoke asserted an edge-cache HIT on exactly the SECOND request, which assumes both land on the same CloudFront PoP — and the deploy invalidates the distribution immediately before smoke runs. A correct deploy failed its own gate after the stack had already updated and the migration had already run. Now six attempts; the intent ("caching is effective, not merely configured") is unchanged, since a disabled cache never hits however many times you ask.

**OPEN / NEXT.** (1) Phase 4 review + deploy. (2) Migrate the three consumers to agent accounts — additive, one env var each, elixir-bot deliberately last. (3) elixir-bot pins contract 0.19 and warns on every boot; its AGENTS.md still says "22 tools, contract 0.10" while the server is at 0.30 with 37. (4) A pre-existing `pg` DeprecationWarning ("client.query() while already executing") appears in the MCP lambda logs; predates this work (seen 2026-09-07 23:01), not investigated. (5) `live_fetch` remains a GLOBAL budget that per-account quotas do not bound — the one resource where "inside their quotas" is not actually a limit.


**PHASE 4 SHIPPED (2026-09-08, migration 0054, Jamie: "ship it now, add OAuth consent after"):** Per-principal resource URLs live. `/a/<public_id>/mcp` and `/i/<public_id>/mcp` alongside the unchanged `/mcp`. **KEY-ONLY for now, and that is a real limitation, not an oversight to forget:** `oauth-routes.mjs` pins the consent flow to the personal resource and refuses anything else as `invalid_target`, so a leader cannot yet sign in and consent to act AS their agent -- agent doors accept service tokens only. The "no member ever handles a raw token" story is not true yet; OAuth-into-agent is the agreed next piece.

**THE BUG POST-DEPLOY VERIFICATION CAUGHT, and the lesson:** the first Phase 4 deploy succeeded, smoke passed, and the feature was completely inert. CloudFront only routed `/mcp`, `/oauth/*` and `/.well-known/*` to the MCP origin, so `/a/*` got the edge's own 403 HTML and the Lambda never saw a request. The Lambda-side routing was correct the whole time and every test passed, because every test ran below the CDN. **A door is not open until something outside the building can walk through it** -- probe the real hostname, and read the CONTENT TYPE of a refusal: `403 <!DOCTYPE HTML` and `403 {"error":"wrong_resource"}` mean entirely different things and both look like "working" in a status-code-only check. Fixed by adding `/a/*` and `/i/*` cache behaviors with the same no-cookie origin request policy as `/mcp` (a session cookie must never reach a door); `SpaRouter` is bound to the default behaviour only, so these bypass it, and `/a/*` cannot collide with `/api/*` or `/account/*` since neither begins with the literal `/a/`.

**VERIFIED LIVE, the whole matrix:** `/mcp` + person key -> 200; `/a/<real>/mcp` + person key -> 403 `wrong_resource`; `/a/<unknown>/mcp` -> **byte-identical 403**, so the door is not an enumeration oracle; `/i/...` -> 403; malformed id -> 405 with no auth attempted; no credential -> 401. Per-resource metadata resolves (`/.well-known/oauth-protected-resource/a/<id>/mcp` returns that resource), a malformed one 404s, and the canonical document is unchanged.

**ROLLBACK WINDOW, now open:** 0054 is reversible today because no `oauth_family` row can hold an agent audience -- the consent flow cannot issue one. The moment OAuth-into-agent ships and somebody consents, re-adding the old literal CHECK would fail against real rows and rollback becomes fix-forward.

**OAUTH INTO AN AGENT + CONSUMER MIGRATION (2026-09-08 early morning, autonomous):** The piece Phase 4 shipped without. The consent flow now accepts any of the three audiences: the person still authenticates as themselves, but if the audience names a principal the GRANT is bound to that principal's account, so every token minted from it carries the agent's identity, budget and 34-tool surface and the owner's own players are unreachable through it. **Only the owner may consent, and only for a principal they own** — an ownership comparison against the authenticated account, not a role, because owning the agent IS the authority. A principal that does not exist and one that is not yours refuse IDENTICALLY, so the consent screen cannot enumerate agents. Service tokens remain for headless runtimes; the "no member ever handles a raw token" story is now true.

**CREATION HAS ONE HOME, REACHED FROM TWO DOORS.** `createPrincipal` moved to `@elixir-mcp/claims` — an account bound to a subject under slot rules is what that package is. The console mints and hands over once; the ops lane (`{principal: {...}}` on the migrate Lambda, mirroring `collectorTokenOp`) registers a key minted on an operator's machine. **createPrincipal never sees a raw token** — callers pass a sha256 and keep the secret, which is exactly what lets the ops path be the same code instead of a careful parallel copy. `mintServiceTokenValue` keeps token FORMAT in auth without tying it to where the row is written. A test asserts a raw token cannot be passed where a digest belongs.

**ELIXIR-MCP-DISCORD IS NOW AN AGENT** (`public_id 272bd891a21d`, role `leader`, own key, own cursor, own feedback, connecting at `/a/272bd891a21d/mcp`). Minted locally, digest sent via the ops lane, raw written file-to-file into its `.env` and never printed. Verified: **34 tools not 37**; `elixir_my_players`/`elixir_add_player`/`elixir_add_clan` absent AND refused on call; feedback inbox 0 items (was reading Jamie's 8); its key refused at `/mcp` and at another agent's door; a live answer that reached for `game_clock` then `war_current` without ever needing to know whose account it was on. Local state was RESET — the cursor and answered-feedback ledger were positions in someone else's feed, and carrying them would have silently skipped or replayed.

**MY OWN NEAR-MISS, worth keeping:** I wrote `.env.bak-preagent` as a safety backup and a `git add -A` swept it into a commit in the PUBLIC discord repo. GitHub push protection blocked it and nothing reached the remote; the commit was reset and `.gitignore` now covers `.env.*` with `!.env.example`. A backup of a secrets file is still a secrets file, and push protection is not a control to be relying on.

**ELIXIR-BOT: pin bumped 0.19 -> 0.30** after eleven versions of warning on every boot. A pin that is always wrong stops being a signal. Its four tools are unchanged across that span, so the pin was stale rather than the integration broken; its AGENTS.md said "22 tools, contract 0.10" and now points at `elixir_changelog` rather than a list that will go stale again. **It is now the only consumer still authenticating as Jamie's person account** — deliberate, unstarted, and purely additive when it happens. The running process keeps the old constant until its next natural restart; the pin is warn-only, and bouncing a live clan bot for a log line is not worth it.

**NOT DONE, and why: ELIXIR DROP -> integration.** Prepared but not executed. Drop's key is a CloudFormation parameter (`ElixirMcpKey`), so swapping it means deploying Drop — a different stack, a live game, and a deploy path I have not exercised. The migration is: create an integration owned by Jamie, mint locally, set the parameter, deploy, then narrow the key to `cr:read` (today every service token carries every scope, so Drop can edit collections and change account settings in order to read a war clock). While there, `game_clock` lets it delete `warClockClanTag` entirely and stop borrowing an arbitrary clan's river race as a calendar.

**CAPTURE-AUDIT SUBJECT CENSUS (2026-09-08):** The public status page correctly exposes aggregate 24-hour capture completeness, but 10 gaps in 1,000 eligible polls could not be attributed to a player or checked against that player's scheduler state without a break-glass database session. `migrate {capture_audit:{days?}}` is now the private read-only evidence path: aggregate polls/gaps plus only affected tags, their first/last gap, last audit, and battlelog planning/admission stamps. This is diagnostic evidence, not a cadence change; first polls remain excluded by the capture-audit writer. Regression coverage pins a gap subject, a non-gap poll for that same subject, and exclusion of an all-clean subject.


**THE PROMPT WAS THE COST (2026-09-08, contract 0.31.0, migration 0055):** Jamie: *"a user's MCP endpoint tells their connected agent to always start by enumerating my players. That is a holdover from when you could only see your players... I've seen it make three tool calls just to figure out the identity and context of the requester before it actually does the work."* **The capability to skip it already existed and was never documented:** omitting `player_tag` has ALWAYS resolved to your primary claim, and `clan_tag` to your clan, across ~12 tool call sites (`resolveSubject` / `resolveEntitledClan`). The only place it was written down was an error hint you reach by passing an empty string. So the brief now NAMES the caller at initialize — one query per connection, never per call — and `elixir_my_players` stops saying "Call this first".

**RELATIONSHIP (primary | alt | friend | watching)** replaces a boolean that could only say me / not-me. An alt is you under another tag, which makes "how am I playing" a different question from the same query over four unrelated players — a distinction the server could not previously make, and the thing that makes "how are my friends playing?" answerable at all. Exactly one primary, required. 0055 is the EXPAND half only: `is_primary` stays the read path (40+ readers and every fixture still write it), `relationship` carries the label, both written together. **That coupling found a real bug immediately** — demoting only the boolean left the old primary holding `relationship='primary'` and the new unique index refused the switch: issue #8's exact shape, one migration later.

**WHO IS ASKING.** An agent serves many humans through one connection and MCP carries no per-request end-user identity, so the agent supplies one — `discord:1234`, `signal:`, `telegram:`, anything. Opaque on purpose: the point is that any surface works. `elixir_identify` maps it once, `on_behalf_of` uses it, `elixir_my_identities` lists it. **It grants NOTHING** — recorded reads are universal, so a mapping only picks a default subject; a wrong one is a visibly wrong answer, not an escalation, which is why it needs no verification. Binding requires the player be a current member of a clan on the connection. Jamie: *"This last feature is truly what makes the agent an agent."* Precedence is strict: explicit tag > on_behalf_of > primary claim.

**50 PLAYER SLOTS AT EVERY TIER** (was 3/5/10/25). One comprehensive clan watch already records ~50, and **recordings are SHARED**, so adding a clanmate of an already-captured clan is free. Player slots stop being a rung; the ladder differentiates on clan watches, calls, the live lane, collections, integrations. **Named consequence:** a member can now assemble by hand roughly what comprehensive gives — what comprehensive still buys is that it FOLLOWS the roster. The distinction now rests on convenience, not capacity.

**THE AGENT BLOCK CARRIES LEADERSHIP, NOT THE ROSTER.** 48 members is ~325 tokens that would be wrong by evening: an agent holds its instructions until it reconnects (the Discord bot goes days), so an embedded roster would be confidently stale in the most authoritative place in its context. Leadership changes rarely and answers "who do I escalate to"; the block says "pull clans_roster ONCE and reuse it".

**CONSUMER-SIDE BUG FOUND WHILE VERIFYING, worth remembering:** elixir-mcp-discord rendered a FAILED tool call as a success. `is_error` is not sufficient — an unknown tool name fails at the PROTOCOL layer (no is_error on the block) and Elixir MCP reports its own refusals as a body carrying an `error` object. Both rendered as ordinary successes, so the model announced "linked!" having written nothing, twice. Fixed there; the lesson is general — **a consumer that only checks `is_error` will report confident success on a refused call.**

**THE #25 ANALYTICS PROXY WAS A FIX FOR NOTHING — REVERTED (2026-09-08, Jamie: "I DO NOT want to route that through our API as a proxy… yuck… you should have asked me before doing something that major"):** Issue #25 removed the Tinylytics embed from `apps/web` on the reasoning that third-party script on the session's origin can issue authenticated same-origin requests with the user's authority. The reasoning is sound; **the implementation never achieved it.** The session cookie is `Path=/` on `elixir.poapkings.com`, and the embed kept loading on `/`, `/docs/*` and `/updates` — the same origin. Verified live before touching anything: `curl https://elixir.poapkings.com/docs/about | grep tinylytics` returns the embed. A script on `/docs` could already `fetch('/api/...')` with the session. So keeping it off `/app.html` alone bought **zero** security and cost every app-side hit count; I then built a `POST /api/track/view` -> SQS -> relay -> Tinylytics hits API proxy to buy the counts back. That proxy preserved a property that did not exist. **RIPPED OUT:** route + `trackView` dep (web-api handler/index), `tinylytics_hit` message kind + `makeTinylyticsHitter` (relay handler/index), `api.trackView` + the App.jsx effect. **RESTORED:** `apps/web/src/analytics.js`, client-side, the same shape Drop and Thingy both use (embed + pushState bridge beaconing `tinylytics.app/collector/<site>` via `sendBeacon`). The `login_token` scrub stays where #25 correctly put it — `url-hygiene.js`, first-party hygiene — and `main.jsx` runs `takeLoginToken()` BEFORE `loadTinylytics()`, which is load-bearing ordering. **What actually holds the line** is the CSP (`script-src 'self' https://tinylytics.app`), already enforced on every behaviour, so no infra change was needed. Tests re-aimed accordingly: the site test no longer asserts "the app is clean" (a property that protected nothing) but "no THIRD origin appears on either half", and the smoke check asserts the CSP directive rather than the absence of a string. **DEPLOYED + VERIFIED LIVE 2026-09-08:** `POST /api/track/view` now 404s, the app bundle carries both `tinylytics.app/embed` and `tinylytics.app/collector` and zero `track/view` remnants, all 30 smoke checks green. (`public status caches at the edge` had failed the previous run — that was a COLD CACHE right after an invalidation, not a defect; it passes on a warm edge. Don't chase it.) **Deploy runs as `AWS_PROFILE=jamie`** — `deploy.mjs` line 3 says so and I ran it bare, got `CredentialsProviderError` off the expired `[default]` profile, and misread an auth failure as Jamie's session being stale. Read the script header. **LESSON (the one worth keeping): a defence that is only applied to part of a shared origin is not a partial defence, it is no defence — and I never checked whether the other part still had the script before building a whole pipeline to work around my own restriction.** Second lesson, Jamie's: routing a third-party analytics call through our own API is an architecture decision, not an implementation detail; ask.

**CR TAGS ARE NOT PII — POSTURE CORRECTION (2026-09-08, Jamie: "We keep hitting blockers where you're treating them like email addresses or PII. That is not the correct treatment."):** Audited where this posture is written down. **It is NOT in AGENTS.md** — there is no rule anywhere that says tags are confidential; AGENTS.md line 49 says only "CR tags are the only IDs for game entities", which is a normalization rule. The posture had no documented home. It lived instead in code comments and user-facing prose that each cited the *previous* instance as precedent: `App.jsx` ("/explore/#TAG is a player's tag and has no business in an analytics store"), the old `analytics.js` ("record ids and player tags never become analytics dimensions"), `privacy.md` ("no identifiers in analytics", "a player tag ... never leaves as part of a page view"), `updates.js` entry 2026-09-05 ("no identifiers"), and `relay/index.mjs` ("a path and a coarse visitor id, nothing else"). Its actual origin is `docs/archive/DESIGN-v2-2026-09-03.md` §"Non-registered players are data subjects" — a terms-review note about *historical battle records of people who never signed up*, which is a real and narrow concern, generalized by me into "tags are sensitive everywhere". **Corrected:** analytics now reports the tag as a query parameter, and privacy.md says plainly that CR tags are public game identifiers served by Supercell's own API, not personal information. **App routing unchanged (Jamie, 2026-09-08: "leave the path as it Is for now"):** an Explore record stays at `/explore/player/2ABC` as a path segment. The query parameter is an ANALYTICS decision only — it stops the report shredding into one row per player — not a URL-design one. Do not re-litigate the route.

**The standing rule going forward:** in this project a CR tag is a public game identifier. Ratified by Jamie earlier as well — "this is a game ecosystem and you are part of its system when you decide to play. There is no 'make my play private' option." Email addresses are PII and keep every protection they have; tags do not, and coarsening/withholding them is not a safety measure, it is lost data.

**NOTIFICATION FEED RESHAPED FOR THE PRINCIPAL KINDS (0.32.0, 2026-09-08):** Jamie's frame, and it decided everything: **the feed is a NOD, not a report** — "hey, a thing happened over here and you may want to look." Coalescing is therefore not a volume concession, it is the point; and payloads got SMALLER, not richer (I was about to put tenure/roster-full/rejoin evidence on `member_left` — wrong lane, the agent pulls that from `clans_roster`). **What I found first:** fan-out was blind to `account.kind` AND to `claim.relationship`; `emitToTagWatchers` was defined, tested, and **never called in production**, so no player-stream event had EVER reached anybody's feed; and `role_changed` (your account tier) sat one word from `member_role_changed` (a clan promotion) — elixir-bot uses `role_changed` for the CLAN one, so logic ported between projects landed backwards. **Jamie's answers narrowed it hard:** watching gets the SAME activity as primary (so relationship is NOT a feed dimension at all — that whole table collapsed); no per-topic mute, the receiving LLM filters; `member_left` stays raw, the agent judges leave-vs-kick; agents get the clan's players and nothing beyond. **SHIPPED:** `TOPIC_CONTRACTS` registry (stream, audience-by-kind, coalesce, payload floor) replacing the flat `FEED_TOPICS` array, which is now derived from it; one `emitToSubjectWatchers` entry point (the `emitToClanWatchers`/`emitBattlesRecorded` shims are gone, the hand-rolled battles fold generalized); player-stream fan-out now reaches claimants UNION agents-via-`clan_membership`, which is how an agent hears about 50 players without 50 claims; integrations excluded by `a.kind = any($2)` — the one audience rule with teeth today. **NEW EMITTERS (the actual gap):** `projectPlayerBadges` rewritten to a data-modifying CTE that reads prior state in the same statement and reports WHICH badge moved (the old upsert returned only a rowCount, which is why nothing could ever be notified); badge tier split at the emitter per elixir-bot's `badge_tier` rule — **no level = one-off = `legendary_badge_earned`, level = mastery grind = `badge_earned`** — as two topics, never one topic with a tier field, because a reader hardcoding one name silently drops the other half forever. Snapshot milestones (`arena_changed`, `best_trophies_peak`, `career_wins_milestone` at thousand-crossings, `collection_level_milestone`, `pol_promotion`) off a widened `prev` read. **FIRST OBSERVATION EMITS NOTHING** on both paths — a newly added player's whole badge shelf reads as new; that is elixir-bot's documented flood class and with the quota at 50 it is the difference between a feed and an outage. **Coverage:** 9 registry-invariant tests (pure) + 5 fan-out tests against real scratch Postgres proving the agent-via-clan route, the integration exclusion, the flood guard, the fold, and that a milestone payload is `{count}` and nothing else. **LESSON:** the dead `emitToTagWatchers` sat there for weeks looking like working code — an export with a test and no production caller is invisible to every check we run. When auditing a pipeline, grep for who CALLS the fan-out, not whether it exists.

**CAPTURE ARRIVED IN 8-HOUR SPIKES; SCHEDULER NOW JITTERS (2026-09-08, Jamie spotted it on the Status chart):** Confirmed from the live `/api/public/status` 24h buckets before touching anything — three spikes at **23:00, 07:00 and 15:00 UTC, exactly 8h apart, each ~3.4x the hourly baseline** (94/324/98/96/102/96/88/104/88/**337**/80/90/79/95/78/95/118/**321**/77/96/87/94/89/18; median 94.5). **MECHANISM:** every cadence in `plan.mjs` is a pure function of a subject's state and due-ness is `now - reference >= cadence`, with NO jitter anywhere — so two subjects seeded in the same moment with the same state are due in the same moment forever. Adding clans in a batch enrolls their members as one cohort, exactly Jamie's hypothesis. **WHY 8 HOURS:** `yieldCadenceMinutes` returns 480 for endpoint `player` when `bph === null`. **The only production write to `yield_bph` is `pipeline.mjs:95`, scoped `where endpoint = 'player_battlelog'` — the profile row's yield_bph is NEVER written, stays NULL forever, and always takes the 480 branch.** The other three `player` branches (4320 dormant / 120 hot / 1440) are UNREACHABLE DEAD CODE, and the docstring claiming "profile stretches with the same signal" describes behaviour that has never happened (the comments-describe-past-architecture trap again). **SHIPPED:** `jitterFactor(subject, endpoint)` — FNV-1a over the pair, ±15% (`JITTER_SPREAD = 0.3`), applied to CADENCE only and never to the fairness floor (a floor is a worst-case guarantee, not a schedule to nudge). STABLE, not re-rolled per tick: re-rolling leaves a cohort clustered on average and merely adds noise, and this file's deterministic tie-break needs reproducible planning. Because it is multiplicative the extremes separate by 0.3 x cadence per poll, so a cohort fully de-phases in **4 cycles (~32h on the 8h profile cadence)** — pinned by a test, along with a distribution check that would fail on a hash that clumped (a min/max check alone would pass and still reproduce the spike). One existing test probed the clan heartbeat at 16min against a 15min cadence, i.e. exactly inside the new ±15% band; moved to 18min so it tests the cadence rather than that tag's hash. **STILL OPEN (Jamie's call, NOT shipped):** wiring the profile row's cadence to the battlelog row's bph would move dormant profiles from 8h to 72h — a ~9x cut in profile polling and the actual root cause — but it is a data-freshness decision, not a bug fix.

**THE FIVE-TIER GAP LIST, ALL SHIPPED (0.33.0, 2026-09-08, migration 0056):** Jamie's review ask after the agent work. **TIER 1 — the agent lifecycle was a one-way door:** create + revoke-key was the ENTIRE surface, and revoke was a trap rather than a gap — with no way to issue a replacement the only path back was delete-and-recreate, which discards the account_id, the public_id in the agent's own MCP URL, and `events_seen_through` (so the replacement re-reads the whole feed or skips it). Added `rotateToken` (revoke + issue in ONE transaction, name and scope carried forward because a rotation is not a re-grant), `setPrincipalStatus` (suspend/resume), `GET /api/me/principals/events` (the agent's feed, read-only — it NEVER advances the cursor, which belongs to the agent's own elixir_events polling), and an `/account/agents/<id>` detail page. **Suspend looks exactly like an invalid token (Jamie's call)** and needed no new code at the door: both `validateAccessToken` and the service-token path already require `a.status = 'approved'`, so `disabled` travels the ordinary not-found path. **THE GAP JAMIE DIDN'T LIST AND I'D RANK ALONGSIDE:** `budgetFor` charges an agent's calls to `owned_by_account_id` and the counter bucket is `mcpday#<owner>` (enforcement was correct — I checked before claiming a bug), but `/api/me/usage` filters `where account_id = <owner's own>`, so the calls that exhausted your budget appeared NOWHERE. `calls_7d`/`last_call_at`/`unread_events` now ride the principal list. **TIER 2 — `claim.relationship` had a reader and no writer:** 0055 added the column and `describeIdentity` groups the MCP identity block by it, but no console control, no API action and no MCP tool could set it, so every non-primary player has been announced to every connected agent as "watching" since the day it shipped and the alt/friend vocabulary in the docs was unreachable. Added the `relationship` action on `POST /api/claims` (alt|friend|watching; `primary` deliberately NOT settable — exactly one claim is primary and promoting one must demote the other, which `make_primary` does atomically) plus a select in Your Players. **TIER 3:** `agent_identity` (the on_behalf_of map that makes an agent multi-user) had no surface at all — now listable and correctable, owner-only. **TIER 4:** the profile cadence never adapted — `yield_bph` is only ever written to the battlelog row, so the `player` row read NULL forever and three of four branches were dead code; profiles now borrow the battlelog row's bph via a subquery, moving dormant profiles from 8h to 72h (~9x fewer profile fetches) and de-phasing cohorts naturally on top of the jitter. **TIER 5:** `tools.json` now carries `contract_version` (an agent reading it to decide whether to re-fetch tools/list had nothing to compare against). **MIGRATION 0056:** rotation collided with `unique (account_id, name)` because it counts REVOKED rows — a rotation is "revoke the old, issue a new one with the same name". Now a partial unique index over `revoked_at is null`: a name identifies a LIVE credential, and history should not hold one hostage. Strictly weaker than the constraint it replaced, so no data can violate it. Fingerprint re-pinned from a FRESH scratch DB per the standing rule. **TWO LESSONS.** (1) `AgentDetail` shipped referencing a hook `Dashboard.jsx` never imported and EVERY check passed — `verify` green, route sweep green — because the sweep walked `SECTIONS` and detail pages are not in it. **A page nothing renders is a page nothing tests**; the sweep now includes four `:id` routes. (2) My first identities route returned `200 {identities: []}` to a stranger while the feed route next door returned 404 — not a leak, but it answers "that agent exists and has nobody mapped", which is more than a stranger is owed. Ownership is checked BEFORE the read now, and the two routes agree.

**HYGIENE PASS (2026-09-08):** Measured rather than eyeballed. **Coverage is NOT the debt:** node's `--experimental-test-coverage` puts services at 85-94% lines (ingest 94.4, scheduler 90.3, contracts 90.0, claims 88.8, email-relay 88.9, auth 87.9, mcp 85.6), web-api handler.mjs at 95.0/58.8 (lines/branches), test:source line ratio 0.57, and ZERO TODO/FIXME/HACK in the tree. The weak spot was **services/jobs at 64%** — and it is the module that DELETES things. **FIXED:** `sweepPayloads` retires an api_payload row only after confirming its S3 twin, and that guard — plus the `s3override` injection seam somebody added *for a test that was never written* — was entirely unexercised. Now 4 tests including the safety property (no twin ⇒ no delete) and an assertion on the EXACT key it asks for, because the key is rebuilt from `first_fetched_at` and disagreeing with the writer is the sol-6 finding 6 bug, which is fail-safe and therefore SILENT — `missing` climbs, `swept` stays flat, nothing alarms. `sweepOperational` (7 deletes + 2 redactions, all bare intervals against now()) was not even exported; now exported and pinned with inside/outside-the-window pairs per rule, so a changed interval fails in CI instead of quietly eating a month of data. jobs 62% → 78.9%. **THE REAL DEBT WAS DUPLICATION, and the code said so out loud:** war_current and the clan_pulse feeder each rebuilt "when does this period nominally end, is it still open" from the shared primitives, and the comment in jobs/index.mjs read *"this copy carried the 'next 10:00Z after the anchor' bug on its own ... because the arithmetic was duplicated instead of shared."* Sharing `periodInfo`/`nominalPeriodBoundsMs` was not enough — the PREDICATE on top of them is what drifted. New pure `anchoredPeriod(periodIndex, anchorMs, nowMs)` in war-clock.mjs owns startMs/endMs/weekEndMs/observedOffsetMinutes/**openNow**; both call sites collapsed onto it, war_current's output byte-identical. 5 tests, including the early-observation case that was the original bug and the negative offset (a race that opened before the policy hour). **LESSON: extracting shared PRIMITIVES does not prevent drift; the derivation on top of them drifts instead. Extract the predicate, not just its ingredients.** **STRUCTURAL DEBT REPORTED, NOT TOUCHED (needs Jamie's go — big diffs):** `services/web-api/src/handler.mjs` 2,237 lines / 55 routes in one file; `apps/web/src/views/Dashboard.jsx` 1,610 lines / 13 components; `services/migrate/src/lambda.mjs` 1,261 lines mixing the migration runner with ~10 break-glass ops. Precedent for all three is the tools.mjs split (3,808 → 73-line assembler + 9 modules, moved VERBATIM, all pins held). Also 12 schema columns are never referenced in non-test source; two are whole unshipped features (`claim.share_battles_with_clan`, `account.pending_email`) and two mean `timing: 'exact'` has never once been used (`clan_event.occurred_at`, `player_event.occurred_at`).


**ANSWER INTEGRITY (0.35.0, 2026-09-08, Jamie requested the review fixes):** Four failures reproduced before changing production code, using scratch PostgreSQL and the real registry/protocol. A 3,000-battle history (2,000 wins, 1,000 losses) returned 100% in the ordinary summary and 66.7% weekly because the former silently limited to 2,000. A fresh profile made a two-day-old battlelog look fresh. A complete three-day capture interval was labeled 33% complete against only its final calendar day. An oversized protocol response was sliced JSON with isError=false and no trailing request_id. Fixes: complete SQL aggregates with explicit last_n sampling retained; endpoint-specific source_polls and oldest relevant freshness, with recorded_since distinct from active-recording start; read-time coverage over actual snapshot observation instants, so late battles repair the estimate; valid bounded size errors preserving request_id. War-current now exposes its race-poll time separately from the first period anchor and flags elapsed nominal periods (issue #38). Migration 0057 nulls only unsupported derived day-level estimates; canonical records and capture counts remain unchanged, nullable columns retained for compatibility. Legacy average_ratio remains decimal text; incomplete_days remains nullable but always unknown, replaced by explicit interval counts. Unknown observation times and incompatible counters never assert coverage. Methodology now describes shipped pooled/shrunk rates, marks within-player lift as future work, and removes unsupported claims that Pilot Score proves improvement or spending independence. The old lease belonged to completed task 01a080db-3be3-75f1-842e-ec1f9cccdae3; cleared via clear-stale after verifying completion and clean synchronized HEAD, then this session claimed the checkout.

## 2026-09-08 — Guide the first useful answer from recorded evidence

Jamie selected the first-use group after the answer-integrity review. Keep the
account overview's guidance in a small component and its read model in a
separate web-API module, rather than growing the dashboard and router inline.
The read model derives current state; there is no stored onboarding state,
new migration, new MCP contract, or extra collector work.

The repeat-use signal is existing personal MCP audit history over seven days.
A successful data response, including an empty one, is a transport/use proxy;
it is not an assertion of a useful answer. Copying a question produces no event.
No synthetic member calls or accounts are created to establish acceptance.
Readiness rules and user-facing behavior are documented in the public quickstart.

## 2026-09-08 — Statistical and documentation alignment

Jamie selected statistical/documentation alignment after the first-use group.
Three scratch regressions reproduced denominator contamination by draws,
reversed card-meta windows accepted as empty successes, and different level
inputs between personal and clan scoring. The two score tools now share the
population query and ingest-stamped level precision. Method declarations drive
both the reader floors and the generated methodology table.

This is alignment of the descriptive estimators, not implementation of the
previously deferred within-player lift design. Existing standard_error remains
for compatibility with explicit method limitations. Aggregate basis counts are
volume context, not model identity: the earlier claim that unchanged counts
attribute a change to the player was unsupported. New metadata and the corrected
empty-rate semantics ship in contract 0.36.0. No migration or new collection work.

**TECHNICAL DEBT PASS (2026-09-08):** Completed the four follow-ups from the
review. Web API route assembly, account page dispatch, and migrate Lambda
entry points now delegate to focused modules; all existing route and function
bodies were preserved. Successful account/player and agent lifecycle journeys
exercise the real API against disposable Postgres databases. Metadata has
producer-side runtime validation (including the previously undeclared
`timezone_applied`); the public example is generated and checked from rendered
HTML. Contract 0.36.2, no schema change. CI calls the same `npm run verify` as
local work; Knip's 38 stale/redundant configuration hints are removed, and active
maintenance guides point to ENGINEERING/public docs rather than archived
DESIGN sections. Historical decision records remain historical. No change to
product direction, collector releases, quotas or statistical models.

**CONNECTION STARTER QUESTIONS (2026-09-08):** Jamie approved adding curated,
data-aware questions to Account → Connections as the next step after connecting
an AI client. The connection page and overview share the readiness hook, question
templates and copy controls. An added clan with a recorded war week can contribute
one clan-war question; this is a read from existing records, not a generated
answer or an MCP call. Copying is not counted as successful usage. Empty, waiting,
error and manual-copy states are covered. No migration or MCP contract change.


## 2026-09-08 — Platform integrations use REST

User-approved direction: integrations are admin-managed platform identities,
separate from users and clan agents. V1 builds only Drop's needs: policy clock,
recorded player context, asynchronous profile refreshes and automatic add-only
collection enrollment. The user explicitly included automatic membership;
supplied tags remain unverified. Collection grants preserve existing ownership,
manual members and recording depth. No scores, XP, identities or game facts are
written back by Drop. See the public integration guide and OpenAPI contract.


## 2026-09-09 — An agent's clan is not a claim

`resolveEntitledClan` derived a caller's clans from `claim` joined to
`clan_membership`. An agent holds no claims by design, so `ent.clans` was empty
for every agent and any clan tool called without `clan_tag` returned
`not_entitled: No recorded clan membership on this account` — on a connection
whose own `initialize` block reported the clan, its name and its member count,
and whose instructions tell the model to omit the tag. 0053 added the principal
kind and `account_clan`; every other agent-aware path (identity, feed fan-out,
OAuth consent) learned to read it and this resolver did not.

Fixed at the resolver: an agent's actively recorded `account_clan` rows join
its entitled clans, primary first so a tool's default is the clan `initialize`
announced. `roles` is deliberately untouched — whether a clan's agent inherits
leadership-scoped analytics without a claimed elder tag is a product decision,
not a side effect of fixing a default; `requireLeadership` has no tool callers
today.

Found by elixir-mcp-discord after it stopped interpolating a configured clan
tag into every prompt. The explicit tag had masked this since agents shipped,
and its owner's personal token had masked it before that (the instance owner
gets every recorded clan). No migration, no contract change.

The fixture in `identity-resolution.test.mjs` inserted its `recording` row
before any account existed, under `.catch(() => {})` — `requested_by` is NOT
NULL, so the write silently never happened and the fixture's clan was never
recorded. Moved after account creation and unswallowed.


## 2026-09-09 — Two auth flows shared one code, and one selector

`verifyMagicCode` took the newest unused, unexpired `magic_login` row for an
address REGARDLESS of purpose, while two flows write to that table: website
sign-in (`web`) and OAuth consent (`oauth`). So they consumed each other. A
console sign-in issued while a Claude authorization was open made the console's
code the newest row, after which the correct, newest, unexpired OAuth code from
the person's inbox could never be redeemed — identically, every retry. The
attempt counter made it worse: a wrong-flow guess incremented the OTHER flow's
row, so five console sign-ins could exhaust a pending authorization's five
attempts without the holder doing anything.

Reported by Jamie with a screen recording: "the code isn't being accepted", on
the newest code, which was true. Selection is scoped by purpose now, so each
flow sees only its own codes and spends only its own attempts.

The message was the second half of the problem. One sentence covered seven
causes — three fixed by retrying and four not — so somebody holding a correct
code was told to start over, did, and hit it again. Failures now name the
cause: wrong flow, superseded, already used, expired, attempts exhausted, no
live code, malformed, or a request-side mismatch (client, redirect_uri,
resource, code_challenge), each with the action that follows from it.

AUTH LOGGING. None of this was diagnosable from the server: the Lambdas emitted
only REPORT lines, and a refused credential never reached `mcp_call_audit`
because the invoker writes after auth. `services/auth/src/audit-log.mjs` adds
one JSON line per auth decision — codes issued, accepted and rejected with a
reason; OAuth token grants and every distinct invalid_grant fault; 401s and
wrong-resource 403s at the MCP door. Never a code, token, PKCE verifier or raw
address: an email is the first 8 hex of the hash already stored, a credential
is an 8-hex digest, both correlatable and neither reversible.

**DOUBLE SUBMIT (2026-09-09, same day):** with the logging in place the real
failure showed itself in one line pair — `oauth_code_accepted` at 08:54:42,
`oauth_code_rejected reason=no_live_code` at 08:54:43.8, and no token exchange
at all. The authorize form is posted TWICE on iOS (a one-time-code fill
submits, a tap submits again ~1.4s later). The first POST authorised and 303'd
to the client; the second found the code spent and rendered an error over the
top of the redirect, so the browser never reached the callback. From outside it
read as "the code was refused"; the server had accepted it and then
contradicted itself.

The code step is idempotent now: a duplicate within two minutes repeats the
first answer instead of denying it. Nothing is trusted from the replay -- the
client, redirect_uri, resource and PKCE challenge are re-checked against the
request exactly as on the first pass, and each auth code stays single-use, so
whichever navigation the browser wins with, exactly one code is redeemable.


**ELEVEN-ITEM FEEDBACK BATCH SHIPPED (2026-09-09, contract 0.39.0, feedback #14-#24, all from one Claude session on Jamie's connector):** Assessed in aggregate before building, because they share one shape: the corpus stores rich per-row detail and each tool exposed ONE grouping axis. NEW AXES: `battles_opponents` (#14), `badges_rarity` + `badges_holders` over `player_badge` (#18 pt 2), `cards_synergy` with co-occurrence/players-per-pair/lift and exact-name-only anchors (#19), `players_names` bulk corpus resolver with an explicit unknown list (#14/#15). NAMES: `name_known` on every opponent/teammate; MEASURED the null-name gap with reads - August+ battles carry names, June-July archive rows do not (elixir-bot's own record has them for all 282 pre-Sept King Thing battles), so a `{player_names}` migrate op fills NULL names only (coalesce, never overwrites) from elixir-bot's battle_events/players. QUOTA (#17): `meta.quota` declared in contracts meta.ts (calls + live used/max/remaining, resets_at; unlimited = null) and stamped for EVERY account after the tool runs; the counter is now read for admins too so `used` is truthful. SCOPE (#16): `elixir_events` is `cr:read` (readOnly stays false - it advances the caller's own bookmark); the 403 carries granted_scope + a reconnect hint. SIZING (#18 pt 1): data_insights reports recorded_players {direct, via_clans, total}, clans_by_scope, recorded_clans[], profiles {with_snapshot, with_badges}. STATISTICS (#21/#23): meta tools shrink toward the CORPUS mean over the same window/mode (own params - Postgres refuses an unused bound parameter, which is what "failed unexpectedly" was), flag `insufficient_sample` below segment_min_decided=30 and withhold shrunk rates, exclude boat battles (type_class), itemize `excluded` {duels, boat, draws, unresolved, no_deck}; battles_performance gains decided_battles/boat_battles/duel_battles and win_rate excludes boat attacks; mode_note names (game_mode, type) as the key and explains type 'unknown'. DUELS (#22): MEASURED on elixir-bot's 412 duel rows - crowns SUM across rounds (max 7), tower hp is final-round only; princess array: head-to-head rows OMIT destroyed towers and go null when none survive (never a 0), duel rows carry 0s AND length-1 arrays - so the tool pads length-1 to [hp, 0] (safe: 13,090/13,091 head-to-head length-1 rows are 1-crown-conceded) and the legend states the duel rules; `rounds_played` on duel rows. FORMS (#20/#24): `cardForms()` in contracts decodes the bit field (corroborated 123/123 against iconUrls via `npm run cr /cards`); players_collection description fixed + forms_available/forms_unlocked; cards_catalog serves maxLevel 16 with maxLevelRarityScale. DOCS REPO: cr-agent-api-docs d5c1ae5 records duel crowns/final-round hp, princess shapes with counts, Colosseum in the rounds conditional, the bit-field framing, and the one-rule `level + (16 - maxLevel)`; four new observed-claim guards. NOT DONE / JAMIE: bulk LIVE name resolution (N tags -> N live fetches in one call) is a rate-budget posture question and was deliberately not built; players_names covers the corpus half and lists misses so live spend is a choice. LESSON: the invoker's generic catch was silent server-side - it now logs tool_failed_unexpectedly with the request_id.

**BULK LIVE NAME RESOLUTION: DECLINED (Jamie, 2026-09-09):** The ask in feedback #14/#15 (one call, N tags, live fetches for the misses) was an artifact of the June-July archive rows lacking opponent names. The `{player_names}` backfill closed that gap (26,300 filled), `players_names` covers the corpus half, and a single `players_profile(live: true)` covers a brand-new opponent. Not needed; do not build. A fan-out live call would turn the one global CR budget into a drain for cosmetic strings.

**TWO DOOR BUGS FIXED (2026-09-09, commits bfa8b3f + 1d6b985, contract 0.39.1):** (1) A refused OAuth access token at POST /mcp returned 500: `describeRefusedCredential` selected `account_id`/`client_id` from `oauth_token`, which has neither (both live on `oauth_family`, 0005); the throw escaped the door. Joined through the family (revoked token/family -> revoked_key, then expired, then wrong_door) AND wrapped the call site so describing a refusal can never change it. Regression: unknown + expired `eat_` tokens -> 401 with the challenge, the expired one attributed through the family. (2) `spendLiveQuota` keyed `liveday#` on the caller's own id, so each agent carried a fresh live allowance its owner never had - N agents = N live budgets against the one CR budget. One definition now, `liveBudgetFor` (tools/shared.mjs), used by the spender and the meta.quota describer: an agent spends its OWNER's live lane (auth budgetFor's previously-unread `liveOverride`), an integration its own key. Plus a per-account agent count in roleQuotas (member 3 / leader 5 / family 10 / partner 25 / admin+owner unlimited) enforced in `createPrincipal` under the owner's row lock -> `not_entitled` reason `agent_limit`; console explains it; Roles page carries the row. LESSON: an unread field in a budget resolver (`liveOverride`) is a bug that has not happened yet - a resolver should have one consumer per field or none.
Deployed 2026-09-09 (`--skip-web`, exit 0, migrations 60/0). Live: the repro curl with an `eat_` garbage token answers 401 with `WWW-Authenticate: Bearer resource_metadata=..., scope="cr:read"`, same as `svt_`.

**DOOR HARDENING BATCH (2026-09-09, commits 452f57a..5df75fb, contract 0.39.2):** Six verified findings, each with a regression test. (1) `/api/explore` was an unmetered, uncapped, unscoped path to every tool: it now spends the SAME hourly (`mcp#`) and daily (`mcpday#`) buckets as the MCP door, renders oversized bodies through the same 48k bounded failure (renderToolResultText exported from protocol.mjs), and serves read-only tools - `elixir_nickname` is the one console-owned write, because the account UI's nickname editor calls it through the bridge (Overview.jsx/Explore.jsx); feedback via the bridge is refused (it has its own route). (2) Tool arguments are validated against the declared inputSchema at the registry boundary (`services/mcp/src/validate.mjs`, no dependency): unknown property, wrong type, enum/min/max/length/items/pattern/required -> bad_request naming the argument; an over-max `limit` is REFUSED now, not clamped (the 0.22.1 "clamp echoes" pin was updated). Refusals happen before the handler, so the test proves it with a null db. (3) `players_search` escapes `%`, `_`, `\`. (4) Usage page reads roleQuotas + per-account overrides; the session now carries mcpDailyQuota/liveDailyQuota. (5) UUID_RE/ID_RE guards on principals rotate/rename/status/events, admin token revoke, feedback triage. (6) Origin gate: CloudFront attaches `x-elixir-origin` (OriginCustomHeaders on both API origins) from a NoEcho `OriginSecret` stack parameter; both Lambdas refuse without it (constant-time; unset = off). parameters.mjs gains SECRET_PARAMETERS: minted by deploy.mjs on the parameter's first deploy (stack lacks the key) and UsePreviousValue thereafter - never in repo/file/log. LESSON: validation at the boundary changed messages three older tests pinned by prose; pin the code, not the sentence.
Deployed 2026-09-09 (full, exit 0, migrations 60/0) after one failed attempt: the template crossed the 51,200-byte inline cap (51,735) and deploy.mjs now passes it by TemplateURL through the code bucket (80a9bb9). The OriginSecret was minted on that deploy and is preserved. Live: elixir.poapkings.com serves (/mcp 401 on a bad bearer, /api/public/status 200, site 200, OAuth metadata 200); both execute-api hostnames answer `{"error":"forbidden_origin"}` 403 on /api/public/status and POST /mcp.

**JAMIE IS EMAILED WHEN BETA USERS SPEAK (2026-09-09):** Feedback (site form AND `elixir_feedback` at the MCP door) and tier-upgrade requests now enqueue an `owner_notify` alongside access requests and collector events. ONE message shape for every producer (`services/web-api/src/notify.mjs`, imported by the MCP entrypoint too): `notify_kind` picks the subject in the relay, `note` is a ~300-char excerpt, `detail` is labelled facts (category, surface, from = agent public id or 8-hex email-hash prefix - never an address), `link` = /admin. Recipient is `OWNER_NOTIFY_EMAIL` (template env on web-api + mcp: jamie@thingelstad.com). The owner's own feedback, and feedback from agents the owner runs, is NOT mailed (not news to the owner; Close the Loop still reads the table). RELAY: each notify_kind has its own subject (pinned in relay tests); the old "new access request" subject for everything is gone. LANE DECISION: owner_notify rides the BEST-EFFORT lane - one send attempt, `owner_notify_drop` log line on transport failure or malformed message, never a batchItemFailure - because the announced fact is durable in Postgres and visible in /admin, while the DLQ alarm is shared with sign-in mail, which must keep retrying hard. Producers also wrap the enqueue in try/catch so a queue outage never fails the filing. Contract: `notify_kind`/`link`/`detail` are additive and optional; in-flight pre-change messages still validate and render generically.
Deployed 2026-09-09 (`--skip-web`, exit 0, migrations 60/0; commits ca68c4c + 8656030). Read-only acceptance: OWNER_NOTIFY_EMAIL resolves to jamie@thingelstad.com on elixir-mcp-web-api and elixir-mcp-mcp; the relay function's LastModified is this deploy; the email DLQ holds 0 messages; the six subjects render from the shipped template. No synthetic feedback was filed. The What's-new entry publishes with the next web deploy.

**TEAM PLUMBING + PG SERIALIZATION (2026-09-09):** Four maintenance findings. (1) DANGLING SPEC REFS: all four Codex objectives told runs to read `docs/DESIGN.md`, archived to `docs/archive/` on 2026-09-03 — every run has been opening a missing file since the team went live. `automations.toml` now points at `docs/ENGINEERING.md` (invariants) plus `apps/site/src/docs/` (what the service does), and says outright that DESIGN.md is archived and not authoritative; same fix in `infra/template.yaml` (2 lines), `services/mcp/src/oauth-routes.mjs`, `services/auth/src/gate.mjs`. ~15 further `DESIGN §N` section pointers remain in `services/ingest/*` and `services/auth/oauth.mjs` — same rot, left alone as out of the named scope; worth a sweep. (2) BLOCKED RUNS NO LONGER HOLD THE CHECKOUT: Keep the Record True hit an ExpiredToken on `--profile jamie` (2026-09-08), held the `record` lease, and blocked Close the Loop the same morning. New `objective-lease.mjs abort <objective> --lease-id <id> --reason "<text>"` releases the lease AND queues a note for Jamie in `.git/agent-team-queued-notes.jsonl` (outside git on purpose: an aborting run cannot commit and must hand over a CLEAN tree); `notes [--clear]` reads/consumes it and preflight prints it, so the next run transcribes it here. **abort REFUSES a dirty worktree** — never abandon uncommitted work; report and leave the lease held. WORKFLOW.md step 6 is the rule. (3) `feedback_respond` passed `related_tools` straight to a text[] column and 500'd on the comma-separated string an operator naturally types (prod 2026-09-09 09:17Z); `normalizeRelatedTools` accepts either shape, null still means "unchanged", `[]` clears. (4) PG DEPRECATION: `Promise.all([db.query(a), db.query(b)])` on a `pg.Client` was in 8 places (~45 warnings/48h in mcp, ~6 in web-api). MEASURED 2026-09-09: one client is one connection and pg serializes anyway — 913 ms concurrent-looking vs 907 ms sequential for three `pg_sleep(0.3)` calls, i.e. the pattern bought ZERO parallelism and only produced warnings that pg 9 turns into errors. All 8 serialized; invariant documented in ENGINEERING.md; a source-guard test walks `services/` + `packages/` and fails on a `Promise.all` wrapping `db.query` (proved to fail on a planted violation). `npm run verify` deprecation warnings: 7 -> 0. Root `test` script now also runs `AGENT-TEAM/scripts/` so the lease tests are gated.

**JAMIE-MANUAL (2026-09-09):** (a) The mirrored Codex task copies at `~/.codex/automations/*/automation.toml` carry the same dead `docs/DESIGN.md` instruction and were deliberately NOT edited from here — apply the same repointing there, or re-register the four tasks from `AGENT-TEAM/automations.toml`, or every scheduled run keeps reading a file that does not exist. (b) RECOMMENDED BEFORE INVITES: trigger **Guard the Door** manually once. Its first scheduled run is Sunday 2026-09-13, which is after invites go out, and it is the only objective that audits entitlements, account isolation, OAuth/service-token boundaries and secret hygiene — the exact surface that first contact with strangers tests.
Deployed 2026-09-09 (`--skip-web`, exit 0, migrations 60/0; commits 38da7bc, ff84ff8, b857e8a, 5aaa4a6). Live acceptance: `feedback_respond` with `related_tools` as a comma string against an id matching NO row answers `{"updated":0}` where it used to 500 — the array parses, nothing mutates; the array shape still works; feedback queue still 0 pending, nothing written. Prod deprecation warnings, 48h before: ~45 in mcp, ~8 in web-api. First 10 minutes after the flip: 21 mcp invocations and 218 web-api invocations, ZERO warnings in either.

## 2026-09-09 — Fetch-loop audit: measured, modelled, not implemented

**FETCH-LOOP AUDIT (2026-09-09 14Z, `docs/FETCH-LOOP-AUDIT-2026-09-09.md`, read-only measurement while another session held the lease; IMPLEMENTED the same afternoon at Jamie's ask - deploy record follows):** Jamie asked for measure-then-model on the player and battlelog loops. MEASURED (six live days, not fourteen — receipts begin 09-03; replay hours and first polls excluded): budget at 127 fetches/h = 3.5% of the bucket; the target-batch rule intends the 24h clamp for the MEDIAN subject (3.4 battles/day) and under 8h only for the top 10%; 51-58% of battlelog receipts are byte-identical to the previous payload; capture loss is real and concentrated — 40 provable overflows in 1,814 yielding fetches, capture_audit 0.8% of all polls (6.6% of yielding fetches on the post-roll training days), battleCount-delta ratio 0.845 as an UPPER bound because `battleCount` counts modes the battlelog never shows (#PVLU2GQQ0 +30 with zero log entries — cr-agent-api-docs follow-up). Ten grinders carry 42% of the loss; they fill the 30-entry log in 1.8-2.5h and are polled every 1.5-6h. Three of Jamie's seven players were 23h stale at read time. Lockstep is GONE post-jitter (1-min QueuedJobs p50 9 / p90 17 / max 28 vs 234-270 spikes). The 09-08 profile fix changed shape, not volume (33/h -> 35/h): the 120m hot branch is 70% of profile spend. MODELLED (replay simulator calibrated to 920 vs 879 actual fetches/day): a loss-aware bound (cadence <= 0.5 x fastest 30-battle fill, from battle timestamps so an overflowed poll cannot hide the rate) cuts loss 3.7% -> 1.0% for +50% battlelog fetches; a 60m cap for subjects read in the last 24h takes reader staleness p90 17h -> 0.9h for ~20 fetches/day/subject; profile hot branch 120m -> 480m halves profile spend. War-day caps RAISE loss under the current EWMA (every zero-yield poll decays it 0.7x); time-of-day priors buy nothing and re-create lockstep. Scale: player loops are ~1,600/day at 300 players; the 15m clan heartbeat for auto-followed clans is the real scaling line (up to 26k/day). PLAN in the doc: build 1+2+3 as one scheduler change + additive 0061 (`burst_bph`, `burst_at`, `last_read_at`), A/B by subject hash via a `LossBoundArm` parameter with `ab_yield` extended per arm, EMF `ElixirMCP/Capture BattlelogGap`. Instrument gap to close regardless: a `{fetch_census}` op — the audit had to parse the S3 archive locally because no census op exposes per-subject receipts.

**FETCH-LOOP CHANGE SHIPPED (2026-09-09, commits b4bdb3a + c381c18, migration 0061, deployed 14:41Z then a full deploy for the site docs 14:56Z, exit 0 both):** Jamie said "implement this" once the lease freed. ONE contract-free change: (1) `poll_state.burst_bph/burst_at` stamped by the battlelog projector from battle timestamps (max in any 6h window over 14d, `stampBurst`); the planner bounds the battlelog cadence at 0.5 x 30/burst x 60 min, behind `ELIXIR_LOSS_BOUND=half` (the jitter-phase hash, so `ab_yield` splits receipts identically). (2) `poll_state.last_read_at` stamped in `subject()` (every tool that resolves a player); battlelog cadence capped at 60m for 24h after a read. (3) Profile hot branch 120m -> 480m — AND the 2026-09-08 borrow was INERT: `activity_bph` was computed in the CTE and never re-selected in the outer query, so `yieldCadenceMinutes` saw undefined and every profile kept the 480 branch (which is why the volume never moved, 33/h -> 35/h). Fixed; the test "the profile row really borrows the battlelog signal now" would have failed before. Scheduler EMF gains PlannedJobs / LossBoundedJobs / ReadCappedJobs; `ab_yield` gains per-arm `gaps`, `gap_rate`, `zero_yield_share`, `battles_per_battlelog_fetch`. LIVE ACCEPTANCE: first tick on the new code 14:42:36Z planned normally (ticks are every 5 min in prod — `SchedulerTickMinutes=5`); one `elixir_coverage` read of King Thing at 14:43:32Z -> the 14:47:36Z tick reported `ReadCappedJobs:1` and the battlelog's `freshness_seconds` went 40,255 -> 27; zero errors in scheduler, web-api or mcp logs. Site docs live: Architecture "Scheduling: how often a player is fetched, and why" + What's-new entry. **READ THE A/B RIGHT:** the two hash arms are NOT balanced at baseline — before the flip the control arm already held more grinders (09-08 gap rate 0.69% treated vs 2.17% control; 09-09 to 14:40Z 0.95% vs 4.9%). Compare each arm with ITSELF across the flip (difference-in-differences), not treated vs control on one day. Promotion to `all`: treated arm's gap rate falls to under a third of its own pre-flip rate over 72h while control's does not, and total fetches/h stays under 200. Expected cost: +~460 battlelog fetches/day at `all`. FIRST LOSS-BOUNDED PLAN observed on the 15:47:36Z tick, 66 minutes after the flip (`PlannedJobs:14, LossBoundedJobs:1, ReadCappedJobs:1`) — the lag is structural: a subject only acquires `burst_bph` at its next admission, then has to reach the bound before its yield cadence. Both new counters are now proven live. Profile fetches fell from ~35/h to ~11/h in the first hour (the inert borrow, now real), battlelog rose slightly (reader cap), total ~130/h unchanged.

## 2026-09-09 — Docs for people who build on this

**DOCS GAP AUDIT + REWRITE (2026-09-09 afternoon, commits e497508..b468df9):** Jamie asked for the public docs to serve client authors, agent builders and integrators from zero without reading source. Five parallel code inventories (door/contracts, OAuth, tools/feed/feedback, recording/roles/agents/limits, REST/machine surfaces) produced `docs/DOCS-GAP-2026-09-09.md`: every capability, the page that owned it, missing/partial/stale/wrong with file:line, 14 statements false or stale today, and 12 code inconsistencies filed for follow-up (step-up 403 names the personal resource_metadata at agent doors; `-32029` defined twice; `elixir_add_clan` defaults to a scope the member tier cannot hold; `POST /api/feedback` rewrites category `other`; no `Retry-After` on MCP 429s; `/oauth/token` unthrottled; `/i/<id>/mcp` OAuth unreachable; collection member limits only for integrations). Two brief premises corrected: no `CHANGELOG.md` exists (it is `packages/contracts/src/changelog.ts`) and there is no `live_quota_exhausted` code (seven codes; live exhaustion is `quota_exceeded`). SHIPPED: three new pages — `/docs/protocol` (transport, discovery docs verbatim, DCR rules, PKCE + RFC 8707 `resource`, token lifetimes 1h/30d/90d, five scopes with the -32003 challenge, per-kind tool counts 44/41/39, every error code at three layers, the 48,000-char cap, cache-buster, cursors, tag/deck_hash rules), `/docs/limits` (one table of 30 limits with bucket, window, status and body; retention table), `/docs/recording` (added-means-recorded, reasons and shared recordings, scope endpoints, relationships/nicknames/collections, the cadence table, freshness fields, `elixir_coverage` fields, `live_fetch` allowlist). Rewritten: quickstart (per-client: Claude.ai, Claude Desktop, Claude Code, generic client sequence, honest ChatGPT note), agents (builder guide with a full `on_behalf_of` exchange against real response shapes and cursor code with `mark_seen:false`), events (topic payload floors, `has_more`/`seen_through`, digest JSON). Corrected: roles (six rungs, `account_tier_changed`, changelog link, operator bonus tiers), about (~30, terms link), architecture (pulse recipe pointer, Go or Python), integrations (`week`/`notes`, admin defaults, `invalid_json` before auth, audit), responses (cap number, `timezone_applied` emitters, error pointer), privacy (90-day args/usage), operators (door routes, limits, quarantine, lifecycle), connections (owner-only consent, console map), methodology (link). Marked lost heading ids in v8 so anchors were dead; `eleventy.config.mjs` now renders `<hN id="slug">` (GitHub-style) and the docs use four fragment links. New pages sit under `/docs/`, which the edge router already routes by prefix, so only `STATIC_PAGES` in `apps/site/test/site.test.mjs` grew; `STATIC_LINKS` and the CloudFront function are unchanged. Generated `/docs/tools` untouched. `npm run verify` green; deployed with the full deploy (site included).

**STATUS CHARTS: THE BUCKET IN PROGRESS (2026-09-09 ~16:45Z, one commit, full deploy):** Jamie read the Status page three times today as "no data pulls" / "captures 0". Both capture charts gap-fill to now, so the right-most bucket is ALWAYS the one being filled and, with a 5-minute scheduler tick, reads as a zero bar (tooltip "nothing fetched") for the first minutes of every bucket; the current hour's bar in the 24h chart is short for the same reason. The rows and the charts count the same receipts (checked: the 12 completed 5-minute buckets summed to 117 = Ram Rider 57 + Tesla 55 + Wall Breakers 5). Fix, Jamie's suggestion: the in-progress bucket is shaded like the Dashboard's unfinished day (`bar--partial`), an empty one is a dashed outline (`.chart .pending`), its tooltip says "bucket in progress · N fetches so far", and each panel head states its total ("5-minute buckets · 117 fetches, last bucket in progress"). Tests pin all three. STILL OPEN (product call): the budget gauge's "expected by now" marker is elapsed-fraction of full capacity, so at the fleet's ~3.5% utilisation it always reads idle; a trailing-24h pace marker would say what "on pace" means. Also worth knowing when reading the shape: since the profile borrow became real this morning, profile fetches fell from ~35/h to ~7-9/h while battlelog fetches rose (reader cap), so the per-tick mix changed even though the hourly total did not.

**RUN ELIXIR MCP (2026-09-09 21:44Z, healthy no-op):** Public status: `ok=true`, last admission 242s, 182 battles in the last hour, all queues and DLQs empty; Ram Rider, Tesla and Wall Breakers were active with fresh heartbeats. Migrate `{probe:true}` showed continuous 80-186 fetches/hour in ordinary hours and `{stats:true}` reported 57,459 battles; all 13 stack alarms including estimated charges were `OK`, RDS had 17.6 GB free of its 20 GB floor, and estimated charges were $12.29. Monday's jobs log recorded the payload/operational sweep (`candidates=2000`, `swept=2000`, `missing=0`) and today's clan pulse emitted for one clan. The queued 19:20Z note saying contract 0.40.0 was undeployed was stale: public `/docs/recording` serves the last-seen surface and `/data/changelog` serves 0.40.0; the jobs Lambda was modified at 20:51Z. No production mutation was needed.


## 2026-09-10 — The console design handoff: tokens, rail, Overview

**HANDOFF 2026-09-09 IMPLEMENTED THROUGH STEP 4 (commits 4ef70f7, b523a48, d6bac37).** Jamie handed over a design package (`design_handoff_elixir_mcp/` in the "Elixir MCP" Claude Design project: START-HERE, README, SCREENS, VOCABULARY, a `styles.css`, and five `.dc.html` prototypes) and asked for START-HERE to be implemented. Its order of work is seven steps; steps 1-3 are done, step 4 landed with 3, and step 3 is the handoff's own review gate ("if Overview is right, the rest is repetition; if it is wrong, you will repeat the mistake thirty times"). **Steps 5-7 are NOT started:** the one log table behind Activity's three views and Admin's eight pages, then Records/Connections/Status/Collections/Feedback/Admin/Auth, then the site (Data page, Use cases, Family, docs rail split, Home transcript). `apps/site` is untouched apart from the docs corrections below.

**STEP 1, TOKENS — NOT A DROP-IN, AND THE HANDOFF'S OWN SHEET SAYS SO WRONGLY.** Every token is renamed to say its job (`--ground`/`--line`/`--ink-dim`/`--accent`) rather than its appearance (`--bg0`/`--edge`/`--muted`/`--purple`), and the palette moves with them; all 23 consumers across both builds were retargeted in one commit, because a screen built before this would hard-code colours that then have to be unpicked. START-HERE says "replace it with this folder's styles.css", which cannot be taken literally: the handoff sheet drops the `@font-face` for the display face **its own `--font-display` references**, the six validated collector series colours, the focus ring, the 4pt spacing scale, and ~80 component classes `apps/site` renders with. A literal copy leaves the static site unstyled. Those four are carried forward as jobs the sheet does not cover, not as palette values added back. Three classes changed MEANING: `.label` is the uppercase eyebrow now (form labels became `.field-label`), `.btn` is the secondary control with `.btn--primary` gold (`.btn--gold` folded in), and freshness "fresh" is "live". `.meter--gold` was deleted outright — a gold meter fill is the specific thing house rule 1 forbids — and nothing rendered one. The legacy chip geometry is scoped to its four modifiers instead of sitting on `.chip`, where its mono face and `::before` dot leaked into every new chip. Found in passing: `.outline` asked for `var(--s-5)`, which no ruleset defined, so the whole margin shorthand was dropped on every docs page.

**NARROW RAIL: THE PROSE BEATS THE PROTOTYPE.** START-HERE, the README and the handoff stylesheet all say the rail below 900px is a disclosure above the content and explicitly "not a drawer over the content"; the design-system prototype implements it that way. The **console prototype implements a fixed drawer with a scrim** (`position:fixed;z-index:30;transform:translateX(-104%)`). Built as a disclosure — three statements beat one — and flagged to Jamie. If he wants the drawer, it is a change to `.rail`'s narrow block and `Rail`'s `open` branch, nothing else.

**TWO DEVIATIONS TO KEEP THE DESIGN'S OWN HOUSE RULES.** (1) Admin's feedback sub-page is labelled "Feedback queue": Access ▸ Feedback is a top-level item that stays visible while Admin is open, so the design's bare "Feedback" would put two identical labels on screen at once — rule 17, the one that broke navigation twice during design. The design's own remedy for the same clash on Usage was "Across accounts", and its `DOC_LINKS` already calls this page the feedback queue. The same clash exists in the design for Collections and will need the same treatment at step 6. **A test now enforces the rule per open section** and is what caught this. (2) The rail's identity block says "Signed in", not an address: we never store one (`account.email_hash` is a sha256, the session carries only an id), and naming the account by its primary player instead would put game content in a rail that rule 16 says must not grow when the data does.

**STATUS IS NO LONGER PUBLIC, WHICH THE DOCS CLAIMED THREE TIMES.** The design's settled decision 1 is that the only rendered status screen lives in the console. Status therefore moved from `/data/status` (which was unauthenticated) to `/status/service` behind a session. `/api/public/status` is unchanged and keeps its machine consumers, so the three "the public Status page shows/reports/publishes…" statements in `architecture.md` and `recording.md` were repointed at the endpoint that actually stayed public. Old paths redirect.

**OVERVIEW DEVIATES FROM THE MOCK IN THREE PLACES, EACH BECAUSE THE DATA SAYS OTHERWISE.** Four slot meters, not three: `activity_clans` and `comprehensive_clans` are separate limits, and one combined "Clan slots 2/2" would show room that cannot be spent. Battle counts carry the windows we hold (30 days and 7) rather than the mock's "recorded · last 24h". The client line reports the connection count and when one last read, rather than naming clients, because a quiet agent and a broken one look identical from a count alone — and naming them costs a fourth request on the landing page. `first-answer` gained the clan's war-week COUNT (additive, no contract change): "POAP KINGS" says a clan is tracked, "POAP KINGS · 27 war weeks" says what it can answer with.

**A ZERO LIMIT IS NOT A FULL ONE (found by rendering the empty state).** `used >= limit` is true at 0/0, so a tier with no collections rendered its meter value in gold — the ink that means "at a limit you can spend" — making an absence read as an achievement. It says "none on this tier" now.

**ICONS: LUCIDE, THE PACKAGE (corrected 2026-09-10, Jamie: "you should use Lucide icons from their package").** The design draws the rail with Lucide and the app had no icon system at all (the only SVG in it was chart geometry, under a comment reading "inline SVG, no libraries"), so the first cut copied twenty glyphs out of `lucide-static` to avoid a dependency. Wrong trade: copies go stale the moment Lucide fixes a path, and a twenty-first icon becomes a chore instead of an import. `lucide-react` is a dependency of `apps/web` now. Measured cost of the swap: **+3.9 kB raw, +2.0 kB gzipped** for 21 icons — Vite tree-shakes the other ~1,600, which is the thing worth checking before believing an icon package is cheap. `components/Icon.jsx` stays as a thin wrapper for three reasons that are not about avoiding the package: it pins stroke and size so two icons in one row cannot disagree, it keeps every icon `aria-hidden` (they are decoration over a text label and never carry meaning alone), and it accepts the design's kebab-case names so `<Icon name="chart-column" />` reads like the file it came from.

**VERIFICATION.** `npm run verify` green at each of the three commits. A new `apps/web/test/rail.test.jsx` pins step 2's own acceptance — every rail destination is a real route, resolves back to the item that offered it, and has its own docs-strip entry; no two items visible at once share a label; the narrow rail is a disclosure that is a SIBLING of the content, not positioned over it. The first-answer tests were split to follow the components: readiness assertions on `FirstAnswer`, prompt assertions on `ConnectionQuestions`, and the scratch-Postgres account journey re-routed through Tracking and Connections. Screens were checked against the design at 1280px and narrow, signed-in and brand-new, with headless Chrome over a mock API (`/tmp` only, nothing committed).

**JAMIE-MANUAL / OPEN.** (a) The narrow-rail conflict above wants a ruling if the drawer was intended. (b) Steps 5-7 are unstarted and are the bulk of the work — roughly thirty screens plus the site. (c) The design's Collections screen (rail item, `not_entitled` panel below family tier) is deliberately NOT in the rail yet: it lands at step 6, and its rail label will collide with Admin ▸ Collections the same way Feedback did.

**HANDOFF STEPS 5-7 COMPLETE (2026-09-10, commits 26dcdde..39f00fb).** Jamie said "continue through the entire redesign" after the step-3 gate. All seven steps of START-HERE are now implemented and `npm run verify` is green at every commit.

**STEP 5, ONE LOG TABLE.** Activity's three views and Admin's six log pages were nine tables with nine sets of markup; they are one component (`apps/web/src/components/LogTable.jsx`) with columns, tones, mono columns, filters and a footnote as data. Filter options are built from the values the rows actually contain rather than an enum that can drift from them. `apps/web/test/log-table.test.jsx` renders all eight log pages and asserts the shape: a bare table (no card), no ragged row against its header, and a tone that puts dot and ink on ONE value. Admin became a router instead of eight panels rendered at once behind `hidden`, so each page now loads only what it needs — every one of them used to fetch all five admin endpoints. Collectors and Service keys keep bespoke screens, as the design draws them: a forward-only lifecycle and a one-time secret are not logs. **Notification ids finally go somewhere:** `/account/activity/n/<id>` renders the JSON body the connection receives.

**STEP 6, THE SCREENS.** Status split into three: the service (budget with pace marker, work waiting as a four-cell pipeline, recording health, and the capture-gap count that was collected, served and rendered nowhere), the fleet, and one collector carrying the TWO CLOCKS. Connections became one typed table of every caller — an OAuth client and an agent are different mechanisms with the same consequence, and splitting them meant "what can reach my data" had two answers, neither complete. Collections is a screen at last (`/api/me/collections` had been served since 0043 with nothing rendering it), and below the family tier it is a NAMED refusal carrying the API's own `not_entitled`, not a greyed-out copy of itself. Explore's four rail sub-pages were broken by the rail itself — `/explore/players` fell through to a record page with no id — and are ways into the lookup now. Feedback dropped `other`, which the API coerced to `general` anyway.

**THE SIGN-IN BUG THE DESIGN CAUGHT.** "That link is expired or already used" was what somebody saw whether their link had really expired OR their access request was still waiting — and the second sent them round a loop that could not work, however many times they tried it. `mintSessionResponse` answers `not_approved` for a valid credential on an unapproved account now. Safe to distinguish: holding the code already proves control of the address. The consent page keeps its best property untouched (unticked boxes for capabilities the client did not ask for) and stops wearing a palette that matched nothing else in the product; its hexes are literals because the door serves it under `default-src 'none'` and can load no stylesheet, not even ours.

**STEP 7, THE SITE.** `/data` is the corpus proof as a real document, baked at deploy, with the growth curve computed at BUILD time from the daily series (monthly buckets by when a battle was PLAYED; an empty month keeps its width). `/family`, three `/use-cases/*` pages carrying all eleven transcripts, and the home page leading with the typing transcript instead of a counter panel. The tool reference became eight family pages under a third rail level; the per-group body is one include so a family page and the index cannot drift, and the slug comes from the registry's own group name rather than a second taxonomy beside it.

**THREE SILENT FAILURES WORTH REMEMBERING, ALL FOUND BY LOOKING AT THE RENDERED PAGE.** (1) Nunjucks has no namespace assignment: a variable `set` inside a `{% for %}` does not survive the loop, and the first cut of the use-case pages rendered a complete shell — nav, footer, lede — around nothing at all. Use a filter. (2) The generator lifting design copy matched ONE brace pair, and `SCRIPTS` is a literal followed by an `Object.assign` adding eight more; three cases got a transcript and eight quietly did not. (3) A class that sets `display` beats the UA's `[hidden]` rule, so all three scripts' tools showed at once and the thinking dots sat under a finished answer — `:not([hidden])` fixes it. Eleventy pagination and the sitemap's collection walk also disagreed (three pages written, one listed), so grouped pages are explicit files over a shared include.

**DEVIATIONS, cumulative.** Admin's Feedback and Collections sub-pages are "Feedback queue" and "All collections" (rule 17: their bare labels are top-level items that stay visible while Admin is open). The rail's identity block says "Signed in" (we store no address, and a player name in the rail breaks rule 16). Overview has four slot meters, not three (activity and comprehensive clans are separate limits). The narrow rail is a disclosure, not the console prototype's drawer. **Nothing is deployed and nothing is pushed** — `main` is ahead of origin.

## 2026-09-10 — Feedback as a log table; the profile page

**FEEDBACK (Jamie).** The list is the console's one log table, like Notifications: id (opens the record), when, category, the first line of the note shortened to 72 characters, state, replied. The record renders the note and the maintainer's reply as **Markdown** through a new `components/Markdown.jsx` (marked, with raw HTML escaped before parsing and `javascript:` links rendered as text) — a note with paragraphs and a list used to arrive as one run of text. `LogTable` gained `actions` (a control beside the title) and `above` (a form the action opens) so a page can keep the design's single primary action without a second header idiom.

**PROFILE (Jamie: "we DO in fact have the email addresses").** `account.email` has held the address since migration 0046; the earlier note that only a hash was stored was wrong and the rail's identity block now shows the address, as the design drew it, falling back to "Signed in" for an account that predates 0046 until its next sign-in. `/api/me` returns `email`. New `/account/profile` (no design of its own yet): email read-only, tier, sign-in method, and the timezone select — which moved here from Settings & tier; that page's Account section is gone and a footnote points at the profile. The identity block is the link to the profile; the sign-out button inside it stops the click from following the link.

## 2026-09-10 — Feedback as a log table; Settings & tier folded into Profile

**FEEDBACK (Jamie).** The list is the console's one log table, like Notifications: id (opens the record), when, category, the first line of the note shortened to 72 characters, state, replied. The record renders the note and the maintainer's reply as **Markdown** through a new `components/Markdown.jsx` (marked, with raw HTML escaped before parsing and `javascript:` links rendered as text) — a note with paragraphs and a list used to arrive as one run of text. `LogTable` gained `actions` (a control beside the title) and `above` (a form the action opens).

**PROFILE (Jamie: "we DO in fact have the email addresses"; then "everything in Settings & tier should just get collapsed into the Profile").** `account.email` has held the address since migration 0046; the earlier note that only a hash was stored was wrong. The rail's identity block shows the address, as the design drew it (falling back to "Signed in" for an account that predates 0046 until its next sign-in), and is the link to `/account/profile`. That page is now the one account page: email (read-only — Jamie does not want to deal with changing it), sign-in method, the timezone control (editable), the tier panel with slot meters and the upgrade request, today's quota with the reset time and the run-a-collector row, and the docs card. **Settings & tier is gone**: the rail item is Profile (user-round glyph) in its place, `/account/settings` redirects, and every link, doc and setup-step target was repointed. `/api/me` returns `email`.

## 2026-09-10 — Claude's review of the docs over MCP, and two softened examples

**CLAUDE READ THE DOCS THROUGH elixir_docs AND FILED FIVE THINGS (via Jamie).** (1) **Unrendered template variables served over MCP** — the corpus was built from the Markdown SOURCE, so an agent got `{{ statistics.meta.prior_strength }}` where a person got the number; the response envelope on the Responses page was literally `{{ responses.example | json | safe }}`. llms-full.txt had the same bug. One renderer now (`apps/site/src/_lib/doc-render.mjs`, Nunjucks against the same `_data` files) feeds both the corpus and the text bundle, and tests assert no page carries `{{`. (2) **`/docs/tools` was a dead link in the corpus** — site-relative links now become absolute in the corpus, and the index note says the reference is tools/list and names the site page too. (3) **"41 tools" was hand-typed and stale** (the door served 44): `tools.agentCount` is generated from `toolsHiddenFrom("agent")`. (4) The Limits quota example named a `resets_at` already in the past: it renders the next UTC midnight from a `build` data file. (5) **Search was exact-substring** — "pilot score minimum battles" returned nothing and an agent would conclude the docs did not cover it, the worst failure a docs tool has. It is tokenized now: pages holding every word rank first, it degrades to any word and says `fallback: true`, the excerpt is cut around the densest cluster of hits rather than the first, and each match names the H2 it sits in (`in_section`). Plus his smaller ask: `page` + `section` returns one H2 section, and the index lists each page's sections, for the long pages.

**TWO EXAMPLES SOFTENED (Jamie).** The roster example names the three quiet members by the game's own last-seen date, drops the win-rate and the demotions, and ends on "a friendly check-in is the move, not a kick". The scouting example is rebuilt on the **Clans to Watch** collection — five recorded clans, twelve races each since Season 133 — via `collections_get`, `war_rivals` (with `rival_tags`) and `war_history` per clan, instead of this week's four unknown rivals.

## 2026-09-10 — The examples are real, and the site is live

**REAL TRANSCRIPTS (Jamie: "make sure those are real").** The design's transcripts were placeholders — invented clanmates, invented numbers, a citation chip under each answer. Every one of the eleven was re-run for real against the live door as King Thing (#20JJJ2CCRU), with POAP KINGS for every clan and Jamie's tracked friends for other players (his rule: any POAP KINGS member may be named). The tools under each window are exactly the ones called; every number in an answer came back from them on 2026-09-10 (S136 W1, war day 1). Answers are Markdown, rendered by the same `marked` the docs use and typed by revealing text nodes in order, so bold, lists and a quoted recap arrive with the words; the citation chip is gone and the provenance is in the prose, which is how a model actually writes. Questions were adjusted to what the record supports: "hit 8,000" became "get back to my best" (he is at 12,562 against a best of 12,622), "Kiwi" became Levi (King Levy, at the 14,000 cap). The three builder examples read the documentation through `elixir_docs` — the same corpus the deploy serves — so an agent asked how to set up a bot answers from the Agents page. The home page's transcript and its three cards are DERIVED from the examples data (`_data/transcript.js`), never a second copy. `elixir_events` was read with `mark_seen: false` so Jamie's cursor did not move.

**DEPLOYED (2026-09-10, two deploys, both exit 0).** First: contract 0.43.0 with the three documentation tools, the Examples/Family/Docs rails, the footer column. The door answered 0.43.0 mid-batch while the data above was being pulled. Second: the real transcripts. Pushed to the public repo after the key-shaped-string scan.

## 2026-09-10 — Site pass two: the rails, the docs shell, and the service documenting itself

**"LOOK HARDER AT THOSE WEBSITE PAGES AND HIT THE ACTUAL DESIGN" (Jamie).** Read from the Claude Design project this time (`Elixir MCP Site.dc.html`, `Elixir MCP Docs.dc.html`, the handoff README/SCREENS/VOCABULARY), not the two prototypes on disk. What the first pass got wrong: the multi-page sections were rendered as one page each with a plain link list; the rails had no head row and no glyphs; the docs rail was the old flat list.

**EXAMPLES, NOT USE CASES (Jamie).** The section is `/examples/<slug>`, tab "Examples", eleven pages by pagination (`examples.njk` over `_data/examplesFlat.js`; `addAllPagesToCollections` so the sitemap lists all eleven — without it Eleventy adds only the first page of a paginated set, which is the disagreement the earlier note blamed on the sitemap walk). Each page: rail of eleven under three headings with a Lucide glyph, head row "Examples · <this one> · 11", breadcrumb, display title, lede, the transcript typed live, what it reads, setup steps as buttons that go somewhere, gold Request access, and "More like this" (the rest of its group first). `/use-cases/*` is gone; the edge router prefix moved.

**FAMILY IS ONE PAGE PER PRODUCT** (`family.njk` over `_data/familyFlat.js`): `/family` is POAP KINGS (the design's default) and the seven others sit at `/family/<key>`. Byline per project, kind and state chips, links with the first external one gold, the 400px screenshot slot, Highlights / Why we like it, How it fits, and "Also in the family" / "More we like" siblings — never the hub, never across the ours/theirs line.

**THE DOCS SHELL** (`_includes/doc.njk`): the design's rail — search box that filters the rail by name and description with "/" to focus (`site-rail.js`), four groups (Start here, Using it, The record, Policy; front matter `section` regrouped and reordered on every page), a glyph per page (`icon:` front matter), "gen" on Tools, children under the current page (tool families under Tools, the page's own H2s elsewhere), the machine surfaces as chips at the foot. Article: breadcrumb, title in the display face, `lede:` front matter (the design's one-liners), the Markdown without its own H1, and the console cross-link card (`console:` front matter) where the design has one. Right: "On this page" marking the section being read, Edit this page (GitHub blob), Updated from `git log -1` at build.

**GLYPHS ON THE STATIC HALF** come from `lucide-static` through an Eleventy shortcode (`{% icon "rocket", 18 %}`) that inlines the SVG with the stroke and size pinned and aria-hidden — the package, not copies, same rule as the console. An unknown name fails the build (Lucide has no `github`; the edit link uses `square-pen`).

**RAIL HEAD ROWS** on Data, Updates and Family (section · where you are · count) with the narrow-width disclosure behind them: `.siterail__nav[data-open]` and the toggle in `site-rail.js`; `rail-anchors.js` and `updates-filter.js` write "where you are" into the head as you scroll or filter. Updates also merges the contract changelog as `kind: contract` entries with version chips and a link into the changelog, and hides a kind with nothing in it.

**THE FOOTER RAN EDGE TO EDGE** on a wide window while the bar stopped at 1280px (Jamie): `.disclaimer` now pads to the same centred column with `max(24px, calc((100% - --page-max) / 2 + 24px))`, no markup change on either half.

**THE SERVICE DOCUMENTS ITSELF OVER MCP (contract 0.43.0).** New workspace `packages/docs`: `build.mjs` turns `apps/site/src/docs/*.md`, `_data/examples.js` and `_data/updates.js` into `dist/corpus.json` (generated, ignored; the root build and `buildAll` run it first, since the MCP bundle imports it as JSON). Three read-only tools in the Elixir MCP group: `elixir_docs` (index / one page as Markdown / substring search with an excerpt), `elixir_examples` (index / one with transcript, reads, tools, setup), `elixir_updates` (newest first, `since`, `limit`). One source, two readers: an agent asked how to use the service answers from the same text the site renders. `corpus_built_at` rides in the body because the meta envelope is closed. The "For builders" examples name only real tools, so the site's test still passes.

**JAMIE'S QUESTION — a public, no-OAuth door for the self-documentation.** Answered in the reply; not built. Short version: worth doing, as a SEPARATE path rather than by making `/mcp` optionally authenticated — most MCP clients decide at add-time whether a server needs OAuth from the first 401, so a door that sometimes needs it confuses them; a second door (`/mcp/public` or `/docs/mcp`) serving only these three tools, no session, IP-rate-limited, and `initialize` instructions that say how to get a real account is clean and cheap.

## 2026-09-10 — Landing audit: the redesign checked screen by screen

**"ASSESS HOW WELL WE LANDED THE REDESIGN" (Jamie).** Four written comparisons of the console and site against the handoff's two prototypes on disk (`/tmp/elixir-handoff/console.dc.html`, `site.dc.html` — the START-HERE/SCREENS/VOCABULARY files are in the Claude Design project and were not readable from this login), one reviewer per screen group, each citing prototype line and implementation line. Sixty-odd findings; the ones below were fixed, the rest are listed as open. `npm run verify` green.

**FOUR BUGS THE DESIGN PASS HAD NOT CAUGHT, ALL FOUND BY COMPARING, NOT BY TESTING.** (1) The owner's collector page threw into the ErrorBoundary for anyone with endpoint data: `CollectorDetail` read `e.calls` and `/api/me/gateway-detail` returns `fetches`. (2) Explore's war-week lookup accepted only an ISO calendar week (`2026-W36`) while its own hint said "Type a week as S135 W3" — the advertised form fell through to a name search and a miss. It parses `S135 W3` / `s135w3` now and lands on the week record. (3) Every clan record said "never polled": `clans_roster` carried a bare `as_of` with no `freshness_seconds` (only the player and battle tools used `buildMeta`). It carries the clan's poll clock now, and a deck record says "derived" instead of borrowing a participant's clock. (4) **The site's transcripts named eight tools that do not exist** (`decks_performance`, `battles_search`, `war_participation`, `events`, `meta_decks`, `clans_timeline`, `gateways`, `api_receipt`) — the design's placeholders — while the home and use-case copy asserted every one was in the registry. Renamed to the real tools (`battles_decks`, `battles_query`, `war_history`, `elixir_events`, `battles_meta_decks`, `clans_standings`, `elixir_collectors · elixir_coverage`), the links now land on the tool's own heading, and a site test refuses a name the registry does not know. Also: Collections rows linked to `/explore/col/<slug>`, a kind Explore never handled; `/api/me/feedback` never returned `shipped_in` so the chip never rendered; `recording#scopes` in the docs strip pointed at an anchor that does not exist.

**SCREENS REDRAWN TO THE DESIGN.** Usage: quota meters with the reset time in the lede, fourteen days of bars with today dimmed, By connection and Busiest tools — `/api/me/usage` gained `by_caller` (client name, agent key name, `svc:` token, or the console) and fourteen days. Settings & tier: the design's three stacked sections and the docs card; slot meters are ONE component shared with Overview (`components/SlotMeter.jsx`) so the two pages can no longer name the same limit "Clan slots" and "clan watches"; the quota is ONE reading (`lib/quota.js`) shared with Usage. Feedback: bare rows that are links, a gold Send feedback control that opens the form in place, the design's status tones (new = accent, planned = warn, done = ok — it had new wearing amber and planned nothing), and the item page with the id as its h1 and "you cannot edit a filed note". Service tokens: the design's title and lede (it had Integrations' lede, twice), a STATE column, a footnote, `.table`. Admin collectors: `.table`, "Back to probation" for a draining collector, tones that follow the state (pending is new, probation and draining want an eye; it had them inverted), and the channel as plain text. Rail: counts on Collections, Connections and Feedback, the unread dot on Activity and the alert dot on Connections — `/api/me` gained `signals` for them. Docs strip: the design's per-record entries (tracked record, notification, collector, player, record) via a `doc` key from `railPosition`, plus the links it dropped (Live fetch, elixir_feedback, Scopes, Key lifecycle, Identity map, The job ledger, Integration API) with the anchors the docs actually have. Tracking: a kind glyph per row, clan rows on the same freshness clock as players (`/api/me` now includes the account's clan recordings), the † mark the footnote was already explaining. Overview rows go to the tracked record; the tracked record says the poll cadence and a clan's member count. Explore is titled Explore, the player record has "tracked by you" and a Manage tracking link. Agent record: "Acts for <clan>", a Key issued row, Revoke dressed as danger, Copy/Done on a minted key. Connections: a suspended agent reads "key reads as invalid"; the refused-credential callout says which kind. Collections: New collection opens a create form (the button went to Explore); private is amber. Vocabulary: "gateway" in a confirm, "subject" on Status and in the collections editor, the `IAM user` sentence in the collectors lede (collectors hold no AWS credentials since 2026-09-06).

**THE SITE.** Home carries the design's hero, the "recording now" pill, the three use-case cards with "All 11 use cases", the collector → record → agent pipeline (it had onboarding steps under that heading), the counters strip, What's new as links, and the four-path For machines list. The hero copy is the design's ("Give your agent a real Clash record.") — the previous "Point your AI at your Clash Royale world" and its three example questions are gone; easy to put back if Jamie prefers them. Use cases: the eleven-case rail with the three headings, breadcrumb, the transcript typed live per case (the include takes any scripts; the clock runs one instance per window), tool links, and "To set this up" as buttons that go somewhere real (`setupHref` in the Eleventy config maps each step to the page that does it; the design left them blank). Family: the links the data carried and the template never rendered (the first external link is the gold primary), per-project bylines, Highlights / Why we like it, a rail with here/beta/theirs, `#anchors`. Data: the On-this-page rail with `rail-anchors.js` marking the section being read; as-of to the minute. Updates: kind pills (derived: a version in the title is a contract change, a retirement says so — nobody types a second field), version chips, month headings, a By-kind filter (`updates-filter.js`, progressive) and the month archive in the rail. Transcript clock: the pause on the finished exchange (it set the hold BEFORE reset() zeroed it, so there never was one), the design's 26ms tick with per-role speed, an opening hold, and a caret.

**DEVIATIONS KEPT, WITH REASONS.** Provision token is still offered to a PENDING collector (the design hides it): our lifecycle needs the staged token claimed before the collector can heartbeat into probation, so hiding it would strand the flow. The Updates page still rotates to the next topic after the pause rather than replaying the same one — a visitor who leaves the tab open sees all three. Connections' first sub-item "Clients" shows every caller (the design's landing state), not clients only.

**STILL OPEN, IN ROUGH ORDER.** Status's "What was captured" buckets (battles / snapshots / clan reads / river-race reads, 24h) need per-endpoint counts `/api/public/status` does not carry; the Recording-health panel stands in. Fleet's VERSION column and the collector record's errors list and 24h hourly bars for non-owners need data the public status and detail endpoints do not expose. Admin ▸ Integrations is still a bespoke table rather than the one log table. The notification record has no Connection/Queue rows (the feed row does not know which connection will read it). Notifications lack the design's CONNECTION column and "expired — never read" state for the same reason. The player record does not inline recent battles or clanmates (one click away). Explore record facts still sit in panels. The agent record is one tall panel with scope ids rather than two panels with labelled capabilities (the capability set is a contract question, #21). The narrow-rail drawer-vs-disclosure ruling is still Jamie's.

**VERIFICATION.** `npm run verify` green; new tests: every transcript tool name is in the registry and links to a heading that exists; the family page links to each product; updates carry kinds, month anchors and the two scripts; `quotaReading`. Screens re-rendered against the mock API at 1280px. Nothing is deployed and nothing is pushed.

## 2026-09-10 — QA pass: eight findings against the design

**"MAKE SURE YOU LANDED IT AS DESIGNED" (Jamie).** Checked the redesign against the handoff's Definition of done and the 21 house rules, with four written checks kept in `/tmp` (rendered-route sweep, top-bar parity, VOCABULARY.md as a linter, table-in-a-card scan). Eight findings, all fixed; `npm run verify` green.

**1. THE TOP BARS WERE NOT THE SAME BAR** — the one thing the handoff calls a build constraint rather than a preference ("it must render identically in the Eleventy build and the app"). The site rendered `.nav1` with a **Sign in** control that `nav-session.js` swapped to **Account** once `/api/me` answered; the app rendered `.chrome` with a **Console** button. Two components, two controls, and the session-dependent swap is precisely the reshaping-nav clunk that script was written to end. Both halves render the same `.chrome` markup now, six tabs in the design's order, gold Console on the right; `nav-session.js` is deleted. Console leads somewhere real either way — signed in it is the console, signed out it is the wall that offers sign-in. A check compares the two as shipped.

**2. TRACKING WAS MOVED, NOT REDRAWN.** It was still the old Overview's two card-wrapped tables — one for players, one for clans — each row carrying a nickname field, a relationship select, a notify switch and a Remove. That is two idioms for one idea and a form pretending to be a list. Now one bare table over both kinds with a kind filter and slot chips, and **`/account/tracking/<tag>`**, the record SCREENS.md marked `[new]`, holding the controls plus a Capture panel. "Stop tracking" states its consequence beside itself: history already recorded is kept.

**3. FOURTEEN CHIPS STILL USED THE RETIRED VARIANTS** (`--active/--pending/--error/--live`) across Admin, Explore, Feedback and Tracking, held up by a compat block I wrote as a temporary bridge in step 1. Converted to the design's five tones; the compat block and the four rules are deleted.

**4. THREE CONSOLE PAGES HAD NO TITLE OR LEDE** — Usage, the Agents list and Admin ▸ Integrations were still wearing the panel-title shape the rail replaced.

**5. THE IN-APP CHARTS HAD NO RAIL AND NO DOCS STRIP.** `railPosition` knew nothing about `/data`, so a signed-in reader following the site's link to the daily charts lost the entire console navigation. They resolve to Explore now.

**6. A TABLE INSIDE A CARD ON THE EXPLORE RECORD** — house rule 5, on the console side where it applies. (The Data page's framed table is the design's own treatment for the marketing half and stays.)

**7. VOCABULARY VIOLATIONS**, from VOCABULARY.md read as a linter: `SUBJECT` as a column header on the Data page and the agent record, and "Dashboard" as the in-app charts' label and link text. **The design file itself writes SUBJECT in Tracking's header and "Track a subject" on its button** — VOCABULARY.md is described as mandatory and is the stricter of the two, so it wins: the column is TRACKED and the buttons say Track a player / Track a clan.

**8. A LITERAL HEX IN A COMPONENT** (`#150f36`, the floating auth card). It is the design's value and not in the token set, so it became `--panel-float` rather than being inlined or invented.

**FLAKE, NOT A REGRESSION:** `a second-precision collector clock in the same second as the request` failed once at 3,230 ms with headless Chrome and a mock server competing for CPU, and passes in ~413 ms on a quiet machine. Timing-sensitive by design (it exists because a millisecond fake once hid a second-precision race); worth knowing it is load-sensitive before believing it.

**STILL OPEN:** four tables keep the legacy `.tablewrap` idiom inside a panel (Agents ×3, Usage, Integrations) — they render correctly and are not card-wrapped in the rule-5 sense, but they have not been redrawn to `.table`. The narrow-rail drawer-vs-disclosure ruling is still Jamie's. Nothing is deployed and nothing is pushed.

**REDESIGN DEPLOYED (2026-09-10, commits 4ef70f7..3e8dab9, full deploy, exit 0).** Nineteen commits: the whole 2026-09-09 handoff, the Lucide package swap, the QA pass and the last five tables. Pushed to the public repo after checking `git ls-files` and scanning the whole diff for key-shaped strings. First deploy attempt died on `ExpiredToken` at `GetCallerIdentity` — before any build or upload, so nothing partial shipped; Jamie re-ran `aws login --profile jamie` and the second attempt went clean. No migrations in this batch; the only infra change is three lines of the CloudFront function's page list (`/data`, `/family`, and the `/use-cases/` prefix). Smoke gate all green. Live acceptance: `/data`, `/family`, `/use-cases/play`, `/use-cases/clan`, `/docs/tools` and `/docs/tools/players` all 200 and all REAL DOCUMENTS with their own titles rather than the app shell; the top bar serves the same six tabs and the gold Console button; `/api/public/status` reports ok with 3 collectors, 0 DLQ and a 155-second-old admission; the MCP door still answers 401 to a garbage bearer.

**NARROW TOP-BAR MENU (2026-09-10, Jamie: "on mobile the nav needs to collapse into a menu on the right ... keep the console button out of the menu always").** Six tabs plus a wordmark plus a Console button do not fit on a phone; the tabs previously vanished below 900px and reappeared at the foot of the console rail, which was two answers to one question and left the static half's tabs to wrap. Now: one button on the right opens a sheet that drops below the bar (six links is a menu, not a modal), and **the Console button is never in it, at any width**. Both halves render the SAME markup at every width and let ONE media query decide which is showing — teaching each half its own breakpoint in JavaScript is the easy way to break the identical-bar constraint, so the decision lives in the stylesheet. `nav-session.js` stayed deleted; the rail-foot link duplication is gone.

**TWO WAYS THIS FAILED SILENTLY, BOTH THE SAME SHAPE AS EARLIER ONES.** (1) The nav carried an inline `style="display: flex"`, and an inline style beats any stylesheet rule — so `@media (max-width: 900px) { .chrome__nav { display: none } }` did nothing and the bar kept all six tabs at 500px. Display belongs in the class. (2) The static half's menu button rendered its open AND close icon side by side: **the `hidden` attribute does not hide an `<svg>`**, because the UA rule implementing it lives in the HTML namespace and an svg element is not in it. `.chrome__menu svg[hidden] { display: none }` is explicit. The React half swaps components and never had the problem — which is exactly why the hand-written half needs its own test, and now has one (jsdom, in the site suite).

**THE SMOKE GATE COULD PASS ON STALE CONTENT (found 2026-09-10 by the menu deploy).** `home carries the live corpus` greps the home page for "battles recorded", which the redesign moved to `/data` when the counter panel became the transcript. The redesign's own deploy reported it **ok** — it had fetched a CloudFront-cached copy of the OLD home, so the check validated a page the deploy had just replaced. A gate that can pass on stale content is close to useless for its one job, which is catching a bad upload. Site pages are now fetched with `cache: "no-store"` and `cache-control: no-cache`, and the corpus assertion moved to the page that carries the numbers, alongside two new ones: home leads with the transcript, and the narrow menu is on the bar with Console NOT inside it.

## 2026-09-10 — Contract 1.0.0: the review actioned

**"MOVE FORWARD WITH ACTIONING ALL OF IT" (Jamie, on `docs/REVIEW-2026-09-10-DOCS-TOOLS-SEAM.md`).** Four calls were his and were asked up front: feedback under the default grant (his answer: clients should request every scope; the protected-resource document advertises all five, a client that names none is offered all five, and the consent page ticks the unrequested ones so "unless the user removes feedback:write it works"); the breaking batch ships now as **1.0.0** rather than waiting; prose on the wire becomes a one-line caveat plus a docs pointer, with the formulas on the pages; the compact war nudge list carries name + tag.

**RATIFIED, DO NOT RE-LITIGATE.** (1) Every windowed tool takes `from`/`to` plus an optional per-call `timezone`; `days`/`weeks`/`seasons` are sugar; every windowed response echoes `applied.window` with `source: argument | default | unbounded | fixed`. The two Pilot Score tools take `days` only because their curve is fit over a trailing window ending now. (2) `applied` is the one echo block; `filters_applied`, `window_from/to`, `window_days` and `limit_applied` are gone. (3) `verbosity: full | compact` is the only size control; `summary: true` and `include_curve` are gone. (4) The segment tools take a nested `segment` and default to the corpus, because the same flat `player_tag` meant "you" on eleven tools and "the corpus" on five. (5) Prose is `notes: string[]` (one sentence each) plus `docs: "page#section"`; the fifteen `*_note` keys and `card_legend` are gone; `completeness_note` stays in meta because it is per-call. (6) `elixir_add_*` is `elixir_track_*` (the console's word), `make_primary` is gone (`relationship: "primary"`), collections take `collection`, events rows carry `created_at`. (7) Groups: Account, Players, Battles, Cards, Clans, War, Collections, Live, Feed, Service, Help; `elixir_coverage` sits with Players. Group is title-only. (8) Error set gains `no_subject` and `result_too_large`. (9) `live: true` on `players_profile`, `clans_roster`, `war_current` and `battles_query`; `live_fetch` refuses `/players/{tag}/battlelog` before spending the lane (a raw log cannot fit the cap; 16% of live fetches were truncated). (10) `events_pending` and `feedback_responses_pending` are stamped by the invoker on EVERY response: the Discord bot re-read its feedback ledger every 300 s because the hint only rode `buildMeta` tools. (11) The door declares `resources` (the docs corpus, examples, changelog, updates, card catalog as `elixir://` URIs) and `prompts` (the eleven examples) beside tools, returns `structuredContent`, and ten tools declare an `outputSchema` the registry validates (throws under the test runner, logs in production). (12) Annotations follow rules: `destructiveHint` true when any action removes; `readOnlyHint` true on `elixir_events`; `openWorldHint` true when any path is live. (13) `docs/ENGINEERING.md` carries the conventions and `tool-conventions.test.mjs` enforces the mechanical half.

**MEASURED, NOT ASSUMED.** The call log (`audit_census`, 2,727 calls, one week, three accounts) drove five items: the pending-hint stamping (56% of traffic was polling), the level-pairs query rewrite (a correlated participant count per row put `clans_pilot_scores` at 11.6 s average, 22.6 s max), the battle-log refusal, `live: true` on the clan tools (this week's four bracket rivals each had exactly one raw clan read on 2026-09-08: an agent falling back after `clans_roster` refused an unrecorded clan), and the census views (per client, p95, `live_fetch` by path, feedback adoption) plus the `args_census` op, because the audit stored arguments from day one and nothing could read them.

**NOT DONE THE WAY THE REVIEW SAID, WITH THE REASON.** The review proposed a daily materialised level curve; the query rewrite was tried first because a materialised view would have made every existing test fixture stale (tests insert battles after migrations run). If the EMF `DurationMs` p95 stays high after deploy, the materialisation is the next step, not a replacement for the rewrite.

**PART 5 (capture) and the docs pages** landed in the same pass by two parallel agents in this checkout; their reports are folded into the commits that follow.

**PART 5 LANDED: every call is captured (2026-09-10).** `mcp_call_audit` gains `captured`, `db_ms`, `db_queries`, `live_wait_ms`, `serialize_ms`, `cold_start`, `principal_kind`, `on_behalf_of` and `rpc_error_code` (0063); the request (redacted like the bounded args) and the response the client received (quota stamped through the invoker's `finalizeMeta` hook before capture) go to the archive bucket at `calls/dt=<date>/request_id=<id>.json.gz`, written in the invoker's `finally` behind the answer, never in front of it; a failed write logs `call_capture_failed` and the row says `captured=false`. JSON-RPC-layer refusals (-32602, -32601, -32029, -32003) audit as rows with `rpc_error_code` through `context.auditRefusal`. One EMF line per call (`ElixirMCP/Tools`: DurationMs, DbMs, ResultBytes, Errors, by Tool and undimensioned) and `ToolLatencyP95Alarm` at p95 > 10 s. Console: Activity request ids open `/account/activity/c/<request_id>` (request, response with meta folded, metrics strip, prev/next by credential, feedback prefilled with the id); `/api/admin/calls/<id>` for admins. Retention 90 days (S3 lifecycle `calls-expire`; `sweep_operational` flips `captured`). Only the MCP door captures; the explorer bridge gets the timing columns and nothing else, deliberately.

**1.0.0 IS PUSHED, NOT DEPLOYED (2026-09-10, queued for Jamie).** `main` is at 77656e1 with the full verify gate green; both consumer repos are pushed (elixir-mcp-discord 3d93b97, elixir-bot 8f5062c9). `AWS_PROFILE=jamie node infra/scripts/deploy.mjs` died on `ExpiredToken` at `GetCallerIdentity`, before any build or upload, so nothing partial shipped and the door still serves the pre-1.0.0 contract. The steps left, in order: (1) Jamie runs `aws login --profile jamie`. (2) The deploy (migration 0063 adds the capture columns; the smoke gate runs inside it). (3) Live checks, reads only: `initialize` reports 1.0.0 and declares resources and prompts; `tools/list` is 47 for a person; one windowed call carries `applied`, `notes` and `docs`; `resources/read` on `elixir://docs/choosing-a-tool` returns the page; `audit_census` shows `captured` rows; `ElixirMCP/Tools DurationMs` has datapoints and `clans_pilot_scores` p95 is measured. (4) Only then restart the consumers, `launchctl kickstart -k gui/$(id -u)/com.poapkings.elixir-mcp-discord` and the elixir-bot service per its AGENTS.md, because both were written to the 1.0.0 shapes and the Discord bot retired a workaround the new server makes unnecessary; the processes running now are the pre-1.0.0 code and match the pre-1.0.0 server. Known leftovers, none blocking: the explorer bridge is deliberately not capture-wired; `dist/site/docs/tools/elixir-mcp/` is a stale build leftover; the lede fallback in the corpus builder is dormant; the 32 `invalid_tag` refusals in the census came from something other than the running elixir-bot process.

**1.0.0 DEPLOYED (2026-09-10, two deploys, exit 0 both, smoke gate all green).** Jamie re-ran `aws login --profile jamie`; the full deploy applied migration 0063 (`applied: 63, ran: 1`) and the smoke gate passed 38 checks. Live, reads only, as the Discord agent principal: `initialize` answers `1.0.0+tools.e1a1628b6073` with `tools`, `resources` and `prompts` capabilities and the agent `_meta`; `tools/list` is 44 for an agent (47 minus the three self tools), ten with `outputSchema`; `clans_standings({days: 7})` carries `applied.window.source: argument`, `notes`, `docs: "recording#completeness"` and `meta.events_pending` on a non-feed tool; 36 resources and 11 prompts list; `live_fetch` on `/battlelog` refuses with `result_too_large` and the `battles_query({live: true})` hint; the call's request and response are in the archive bucket under `calls/dt=2026-09-10/`; EMF `ElixirMCP/Tools` carries `DurationMs`, `DbMs`, `ResultBytes` and `Errors` per tool. FIRST MEASUREMENT of the level-pairs rewrite: `clans_pilot_scores` at the 90-day default is 8.8 s max, 4.4 s average over two post-deploy calls, against 22.5 s p95 before; better, still the slowest tool, and the materialised curve stays the next step if the alarm fires. One defect found by the live read and fixed in the second deploy (ad50cef): every corpus page opened with its own H1, which `resources/read` and `elixir_docs` print again from `title`, so each page read back with the heading twice; the corpus builder now drops it. Consumers restarted after the door answered 1.0.0: `com.poapkings.elixir-mcp-discord` logged `contract_version_changed from=0.43.0 to=1.0.0` and loaded every routine; elixir-bot via `scripts/admin.sh restart` (backup taken), no drift warning since its pin is the major. Today's census before the restarts still shows six `war_history` `invalid_tag` refusals from the elixir-bot surface, which the `#`-name hardening in 8f5062c9 should end; tomorrow's `audit_census` is the check. The queued-for-Jamie paragraph above is done.

**TINYLYTICS COVERAGE AUDITED AFTER THE REDESIGN, ONE REAL HOLE FIXED AND DEPLOYED (2026-09-10, commit f07cb33, exit 0, 41 smoke checks green):** Jamie asked whether the redesign lost analytics coverage on either half. THE STATIC HALF NEVER LOST ANY - the embed rides `base.njk`, all 60 content pages of a fresh build carry it, verified live on the new `/examples/*` and `/family/*` pages too, and no site asset uses the history API, so every static view is a real document load. THE CONSOLE HAD A HOLE, AND IT WAS THE ENTRY POINT: `loadTinylytics()` returned before installing the pushState bridge whenever the first document was `/signin`. Skipping the EMBED there is correct (the magic token is in that URL and the embed reads the address bar as it runs) but the bridge is how a SPA reports anything after its first document, and a magic link lands on `/signin` and pushStates into the console without ever loading another one - so a session that entered the console the ORDINARY way reported not one page view, and every screen the 2026-09-09 redesign added was invisible to exactly the people who use it. The two jobs are separate now: embed = the document load, and the only part `/signin` skips; bridge = navigation, installed always. Nothing double-counts because a `/signin`-start session beacons its first page instead of loading an embed to record it, and the beacon is built from `analyticsLocation()` - the ROUTE, never the address bar - which is what keeps the token out of it. `/signin` also ends the view before it, so signing out and back in counts the landing page again. LIVE PROOF: the new bundle's minified `Sa()` reads `if (localhost || (wa(), pathname.startsWith("/signin"))) return` - the bridge call folded INSIDE the guard, before the signin return, which is the ordering. COVERAGE GUARD: the site test now asserts the embed over the BUILT TREE rather than one page, so a page that quietly leaves `base.njk` fails instead of going silently uncounted; four new web tests pin the magic-link session and the token-never-in-the-beacon property. **THE LESSON: two jobs behind one flag.** The `/signin` carve-out was written when the embed was the only thing being loaded, and the bridge was added inside it later; nothing was wrong at either moment, and the combination reported nothing. A guard that protects one thing must not be the roof over a second thing that needs no protecting. PROSE ALSO CORRECTED, both still describing the world issue #25 briefly created and 2026-09-08 reverted: the CSP comment in `infra/template.yaml` claimed the app loads no third-party script (it is the boundary, not a backstop, and `connect-src` names tinylytics for the beacon), and the privacy page said the sign-in page "loads no analytics at all" - now precise about what is skipped and explicit that pages after sign-in are counted by route. TWO THINGS OBSERVED, NOT TOUCHED: (1) Eleventy does not clean `apps/site/dist`, so the pre-redesign `/use-cases/*` pages are still in it and get merged into `dist/site` by `build-site.mjs` (they are not reachable live - the edge router hands unknown paths to the app shell - but a one-line `rmSync` would stop ghost pages being uploaded at all); (2) `REDIRECTS` renders the new page without rewriting the address bar, so a bookmark to `/data/status` or `/account/collector` still credits the OLD path in the report and `/status/service` looks unvisited when it is not.

**CI WAS RED FROM A CLEAN CLONE FOR SIX COMMITS, AND `npm run verify` COULD NOT SEE IT (2026-09-10, commit 578c764, validate green):** Found while wrapping up the Tinylytics audit - the gate had been failing since 77656e1 (15:36) with `Cannot find module packages/docs/dist/corpus.json imported from packages/docs/src/index.mjs`, and every run since said the same. THE CYCLE: `packages/docs/build.mjs` renders a doc, which needs `apps/site/src/_data/tools.js`, which reads the MCP registry, which imports `@elixir-mcp/docs`, whose `src/index.mjs` imports the corpus AT MODULE SCOPE - the file this build exists to write. **It never failed on a developer machine because a previous build always left one behind.** A stale generated artifact was standing in for a missing step, which is the same shape as the `use-cases` leftovers and worth naming as a class: an untracked build output that is always present locally and never present in CI turns a hard failure into a silent local pass. Reproduce any suspected instance with `rm -rf <dist> && npm run build`, which is all it took here. FIX (Jamie chose the stub over the lazy read, told plainly that it makes the cycle permanent): `build.mjs` writes `{docs:[],examples:[],updates:[],built_at:null}` when the file is ABSENT and overwrites it with the real corpus at the foot of the same run. Only when absent, so a crash mid-build cannot replace a good corpus with an empty one. Contents cannot leak into the output because every `DOCS`/`EXAMPLES`/`UPDATES` use in `services/mcp` sits inside a HANDLER BODY, so only the import has to resolve - proved by building cold and warm and diffing the corpus, identical but for `built_at`, 20 pages / 11 examples / 95 updates either way. **THE REAL FIX IS STILL OPEN and the comment in `build.mjs` carries it:** read the corpus lazily in `src/index.mjs` and the package stops needing to exist before it is built. Cost measured, not guessed - ~20 mechanical call sites in `resources.mjs` and `tools/elixir.mjs` plus one test, none of them at module scope, plus `CORPUS_BUILT_AT` becoming a call because a Proxy cannot fake a scalar. Take it the next time that file is open.

**THE ACCESS-REQUEST FORM WAS RECORDING REQUESTS AND SHOWING NOTHING; IT NOW LIVES ON THE SIGN-IN CARD (2026-09-10, commit a1691e6, deployed, exit 0):** Jamie: "I just tried to Request access and the form submission literally goes nowhere. Massive issue in user acquisition." IT WAS NOT THE API. Probed live with a refusal (`invalid_tag` -> 400 through CloudFront), and his own attempt is in the record: `signup.requested` fired once at 17:59:09Z, so `requestAccess` committed the row and `notifyOwner` was enqueued; the relay logs carry no `owner_notify_drop`. **THE BUG WAS THAT THE PAGE COULD NOT MOVE.** `request-form.js` ended a successful submit with `form.hidden = true; sent.hidden = false` - but the form carried inline `style="display: flex"` and the confirmation is `class="notice"`, which `styles.css:1677` gives `display: flex`. **A class or inline `display` beats the UA's `[hidden] { display: none }`,** so the form never hid and the notice had been visible all along; nothing on screen changed at any point. The codebase already knew this trap - `styles.css:1003` documents it against `.transcript__thinking` - which is the lesson: a rule you have already been bitten by is worth grepping for when a "nothing happens" bug arrives, and `[hidden]` is only as strong as the weakest `display` rule pointed at the same element. Only the error path worked, because `.field-error` sets no `display`. FIX (Jamie: "Can just be an option on login - either logging in or requesting access"): the form is a `request` step of `SignIn.jsx`, the footnote that linked out to `/#request` switches to it in place, and `/signin?request` deep-links it for the home hero, the home panel, the examples CTA and the tool reference. The confirmation REUSES the `pending` card instead of a second copy, which is also the honest wording - the API answers 200 identically for new, repeat, denied and already-approved, and "Your request is in" is true for all four. The static half now holds no form, no fetch and no error strings; `request-form.js` is deleted and its passthrough with it. **A SECOND DEFECT SURFACED WHILE DELETING IT:** the asset kept being deployed, because Eleventy does not clean its own output and `build-site.mjs` copied `apps/site/dist` wholesale - the same reason `/use-cases/*` outlived the redesign that renamed the section. `rmSync(siteDist)` before the build; 85 files -> 79, and `/assets/request-form.js` is a 403 live. WATCH: `/signin` is deliberately untracked in analytics, so the request FORM VIEW is not counted - the conversion still is, server-side, as `signup.requested`. If the funnel needs the view, the answer is a server-side `site.request_view` ping, never loosening the `/signin` rule. Jamie's pending request from 17:59Z is in `/admin/requests` and was never lost; rate limit for his manual test is 5/hour per IP.

**THE 22-ITEM PUNCH LIST, DONE (2026-09-10, commits a44d793 / 51b22e5 / 49552bb / 3e9d888 / e70739d; NOT DEPLOYED — three migrations wait for Jamie):** Four questions asked before starting; two answers changed the work. (1) `/data/changelog` was NOT a duplicate of `/updates` — it was the tool CONTRACT changelog generated from `packages/contracts`, and `updatesView` was already merging both into one stream, so the page was a second rendering of half the list. Jamie: fold it in, keep the detail. Its extra facts (tools added, what broke) ride on the version's own update page now instead of being flattened into a summary. (2) Service tokens are NOT user connections — owner-issued headless credentials bound to one account — so Jamie's "see everyone's connections and revoke on their behalf" did not exist; built it as a new admin page, and the tokens page now says what a token is and whose entitlements it spends. (3) THE CLAN-SCOPE CORRECTION IS THE ONE TO REMEMBER: I offered to track a clan off-quota; Jamie's answer was that **every tier already has one activity clan slot for exactly this**, and that we are not clear anywhere about what activity vs comprehensive MEANS — activity follows the clan itself, comprehensive also records every member's battles. The docs' scope table said what each POLLS and never what it is FOR, which is why the console's own nudge posted `comprehensive` (a slot the member tier has none of), had the refusal swallowed, and read as a dead button. Fixed in the nudge, in approval, and in `recording.md`. (4) Updates filter by kind and/or month with the list in the body and a click through to the full entry.

**FIVE THINGS WORTH KEEPING FROM THE WORK ITSELF.** (a) A RECORDING BELONGS TO THE SUBJECT: two readers scoped by `requested_by`, so the second account to claim a player — who joins the existing recording and never gets a row — was told "never polled" and "no data" with the battles in the same query. The clan half had already been fixed for this; the player half had not. Grep for `requested_by` before writing any "do I have this" query. (b) TAGS IN URLS ARE NOW ONE SEAM (`apps/web/src/lib/tag-url.js`): links carry no `#`, readers put it back, `%23` still accepted for old bookmarks. Three call sites had each solved it privately and disagreed — Explore stripped and restored, Tracking encoded the canonical tag, Overview stripped and the reader never restored, so the FRONT PAGE's links 404'd. (c) `[hidden]` LOSES TO `display` — see the access-request entry above; same class of bug, and `styles.css` had already documented it once. (d) ELEVENTY PAGINATION NEEDS `addAllPagesToCollections: true` or the sitemap lists ONE of 148 pages. (e) The edge router's Data pages are LISTED, not prefixed, because `/data/dashboard` is the app's charts — a `/data/` prefix would have taken the console's chart page away. **JAMIE-MANUAL: deploy.** Migrations 0064 (refusal dismissal), 0065 (`account.onboarded_at`), 0066 (`feedback.request_id`) and contract 1.1.0 ship with it; the schema fingerprint is repinned. Nothing is live until then — the console still shows the old Data page and the old tier meters.

**PUNCH LIST DEPLOYED (2026-09-10 21:18Z, exit 0, 40 smoke checks green, `{"applied":63,"ran":3}`):** Migrations 0064 (refusal dismissal), 0065 (`account.onboarded_at`) and 0066 (`feedback.request_id`) applied in one flip. VERIFIED LIVE, reads only: `/tools.json` publishes `contract_version: 1.1.0` with 47 tools and `elixir_feedback` declaring `request_id` as an optional arg; the five Data pages and the per-update pages all serve real documents (`/updates/2026-09-10-contract-1-1-0` titles itself); the docs carry the new scope wording ("activity follows the clan itself") and the `request_id` argument; the app bundle is new. **THE MINOR BUMP PROVED ITSELF:** `com.poapkings.elixir-mcp-discord` logged `contract_version_changed from=1.0.0 to=1.1.0` at 21:19:45Z with NO restart — which is the difference between a minor and a major in this contract, and the reason the version question is worth getting right: a patch would have told a cached client there was nothing new to call, when the whole point is that agents should start attaching `request_id`. Versioning rule confirmed against practice while answering Jamie: additive = minor (ENGINEERING.md), and the 51 shipped versions use PATCH only for behaviour corrected with no new surface (0.39.2 validation enforced, 0.39.1 quota re-attributed, 0.36.1 precision wording, 0.22.1 refusals replacing silent empties). **ONE LOOSE END, deliberately not fixed in the deploy:** an old `/data/changelog` bookmark now resolves through `legalRoute` to `/data/dashboard` (the console's charts) rather than to `/updates`, because a static path handed back to the app leaves for the HOME page, not for an arbitrary static one. Nothing links there any more; the fix is a special case in App.jsx that does `window.location.replace("/updates")`, and it needs a deploy of its own.

**ACCOUNTS IS A LIST OF PEOPLE, AND TWO NAV LABELS SHORTENED (2026-09-10, commits 5a86325 + 99887bb, both deployed, exit 0, 40 smoke checks each):** Jamie's three follow-ups after the punch list. (1) An agent or an integration is an ACCOUNT in the schema (0053) with its own door, connections and public id, but it is not a SIGNUP — no email, no tier of its own, created by somebody — so listing them beside their owners made the queue of humans hard to read. `/admin/accounts` filters to `owned_by_account_id is null`, carries a count of what each account runs, and the record page names the children with a link to each; opening a child says whose it is and leads back. One query still: the record finds its children in the list it already loaded. (2)+(3) "Connections across accounts" -> "Connections" and "All collections" -> "Collections" in the rail. **THE RAIL TEST WAS NARROWED, NOT DELETED:** the house rule "no two visible items share a label" (navigation broke twice on it during design) is right about AMBIGUITY, and a sub-item is not ambiguous with a top-level one — it renders indented under its section's row, one line above it, and its page carries that section as a crumb. What still fails is a collision at ONE level. **THE MISS WORTH REMEMBERING:** the first deploy shipped the rename in the nav and left three links in prose still saying the old names (the service-tokens lede pointing at "Connections across accounts", the collection editor's two crumbs reading "All collections"). Caught by grepping the LIVE BUNDLE for the old strings after deploying, not by the tests — a label lives in more places than the array it is declared in, and a rename that stops at the nav leaves the product arguing with itself about what its own pages are called. Grep the built artifact for the old string before calling a rename done.

**THREE FEEDBACK REPORTS CLOSED AT THE RECORDING AND CONTRACT SEAMS (2026-09-10, contract 1.2.0):** live current-race evidence confirmed that `periodPoints` is the score in the day being fought while `fame` is cumulative boat progress banked at the day close; storing only fame made a correct zero look stale, so both are now retained and named. `war_history({season_id, section_index})` is the exact-week path and returns every recorded participant with the `war_days` indices already added in 1.0.0, replacing one call per member. A direct claim now caps profile cadence at eight hours even for a dormant player; incidental members of comprehensive clan capture keep the yield schedule, which bounds the global-rate cost to the explicit tracking cohort. `elixir_coverage.completeness_last_7_days.unmeasured_tail_hours` turns the post-snapshot blind spot from prose into a number. The operator feedback queue also returns the attached `request_id`, so future reports can be tied back to the exact audited call.

**CONTRACT 1.2.0 DEPLOYED AND THE THREE REPORTERS ANSWERED (2026-09-10 19:08 CDT, commit 5340006):** migration 0067 applied (`applied: 66, ran: 1`), CloudFormation reached `UPDATE_COMPLETE`, the CloudFront invalidation completed and all 40 smoke checks passed. Read-only live calls all carried 1.2.0: the exact-week path returned one week and 57 identified members; coverage exposed a 19.24-hour unmeasured tail beside 77.66 measured hours; and the fresh race returned POAP KINGS at `fame: 0`, `period_points: 5875`. Feedback 29, 30 and 31 were answered `done` with their shipped version and affected tools, and the final pending queue was empty. Friday synthesis was not due on Wednesday.


**SCHEDULED OWNER CONTRACTS RECONCILED (2026-09-10):** Jamie approved the definition-audit recommendations. Guard now judges the current public/private principal contract, preflight separates observation from mutation eligibility, credential blockers have a verified-source handoff, and Run/Loop explicitly own preview operations and friction. Calendar definitions retain the September 9 installed schedules; generated tables and receipt-based due work prevent prose/retry drift. The reading map points to current public contracts rather than duplicating them. Eleven decision cases are a pilot with no independent model-quality score claimed. Evidence and the next natural acceptance checks: `AGENT-TEAM/notes/2026-09-10-definition-repair.md`.

**LEADERBOARD COLLECTIONS ARE A CLIENT, NOT A FEATURE (2026-09-10/11, commits b93d1ef + bf86434; `clients/boards/`):** Jamie: keep programmatic collections (pol-global-top-100, then pol-us-top-100 and pol-jp-top-100) via "an Integration like Drop", not special code in the service. FOUR THINGS THE EXPLORATION SETTLED. (1) The integration lane is ADD-ONLY by design (`collections:members:add`, mode `add`) — right for Drop, where enrollment accumulates, wrong for a board, which is a REPLACEMENT; widening it was declined. MCP `collections_edit action:"set"` already does the job, and the door is stateless JSON-RPC — no initialize, no session, no SDK — so the "simple REST client" Jamie wanted IS the MCP client: one `fetch` with a Bearer token, one file, zero dependencies. (2) `live_fetch` reaches `/locations/{id}/pathoflegend/players` and `/rankings/players` only; the game-mode boards under `/leaderboards` need an allowlist change. (3) COLLECTION MEMBERSHIP IS A RECORDING REASON (`reconcileRecording`), so a board ≈ up to 100 recorded players; three boards against 130 recorded today is roughly +390 fetches/hr on a ceiling of 3600 with mean use 170/hr — fits, and the rate review (below) is why that number is known. (4) TROPHY-ROAD BOARDS ARE EMPTY: `/rankings/players` returned 0 items for global and US on day 3 of S136 while PoL returned 836 and 98; recorded in cr-agent-api-docs (4df27d6). **THE GUARD THAT WAS WRONG:** first draft refused any board under half its cap. PoL lists only players above a rating floor and a season resets everyone below it — Japan had 34, India 4 — so a SMALL board is the truth, not a fault; what is a fault is a board that COLLAPSED against what the collection holds (100 → 5 = the season boundary), which is held rather than written so ninety-five recordings are not stopped tonight and restarted over a fortnight. **THE TOKEN:** a `svt_` service token in `clients/boards/.env` as `ELIXIR_MCP_TOKEN` (same name as the Discord consumer); the Discord bot's own token was refused at `/mcp` with `wrong_resource` because it is bound to its `/a/<id>/mcp` door — the principal binding working, and the reason the client is tested against a fake door. Verified live with the issued token in DRY RUN only: global 25 in / 25 out within hours of creation (the floor churns fast), US 98 in, Japan 34 in. NOTHING WRITTEN YET; the first real run and the launchd schedule are Jamie's. **PROCESS MISS, mine:** I did the boards work without holding the checkout lease while a parallel session (Jamie's, the Support page) held it; the other session then could not release because MY uncommitted files made the tree dirty. Contained — disjoint files — and resolved by stashing, releasing with the sanctioned command, claiming, popping. Claim the lease at the start of EVERY mutating session, not just the ones that feel big.

**RATE-LIMIT REVIEW (2026-09-10, Jamie asked "3600/hr × collectors?"):** One `budget_state` row, token bucket settled per 5-min tick, `BUCKET_CAP_SECONDS=300` ("never a quota multiplier"), 10% live reserve, per-gateway 403 breakers; collectors LEASE, never choose. Supercell publishes NO number; the standalone reference records observed behaviour only (no headers, 403 accessDenied on overage, ~2s spacing safe, limits "appear" per-IP). Over 49 hours: mean 170 fetches/hr (4.7% of ceiling), median 133, peak 829 (23%), `due_starved: 0` — the ceiling is not the binding constraint, cadence is, so multiplying it would change no fetch today and would trade a documented ToS posture (ENGINEERING.md, golden rule 3) for a bet on an observed, unpublished per-IP behaviour. Not changed. NIT: `/api/public/status` names tokens-minted-so-far `expected_hour`, which reads as unmet demand beside `used_hour`; worth renaming.

**THE LEADERBOARDS ARE RECORDED (2026-09-11, commits 2ef87d6 / 8840983 / 4a36f72 / 30795c5, deployed 03:0xZ, `{"applied":67,"ran":1}`, contract 1.3.0, 49 tools):** Jamie: "the leaderboards are themselves meaningful data"; then the season-story video — the #1's rank through a season with battles and opponents overlaid. WHAT SHIPPED. 0068: `ranking_board` (global + all 262 locations; global `pol` hourly with `record_top 200`, everything else daily), `ranking_snapshot`/`ranking_entry` (a snapshot per fetch that CHANGED — identical fetches bump `last_confirmed_at` instead of writing a twin — `truncated` when the API offered a cursor), `ranking_presence` (a top-N appearance records the player at comprehensive scope until the next roll + 3 days; a NEW recording reason `ranked`, origin `ranking`, owner as requester like ops). `crPathForJob` asks for 1000 places, not 100. `overflow_bytes` 250,000 → 5,000,000 (the SQS fossil; the door is API Gateway 10 MB / Lambda 6 MB). Two tools, `rankings_players` (as_of, paged, compact) and `rankings_clans` (whole-field aggregate, ties by best rank), both `live: true` capable and therefore open-world. The boards client reads the record instead of live_fetch and spends nothing. A fifth AGENT-TEAM owner, Keep the Boards (`boards` lease objective, daily 04:20 Chicago) — Codex enablement is Jamie's. LIVE PROOF: first global snapshot 03:09:55Z, **869 entries, not truncated** (847 an hour earlier: the floor is filling); the rank-150 player's `meta.recording_active_since` equals the snapshot timestamp exactly, so presence records. ONE-TIME BURST: all 263 seeded boards were due on the first tick — 273 rankings receipts in the first hour, `used_hour` 295 against 3600 — expected and not repeating (daily from here). **WHAT THE BUILD TAUGHT:** (a) the collector never owned the ranking path — both fetch the hub's `cr_path`; the Go `crapi.Path()` was DEAD CODE with a stale `limit=100`, and its existence is what made a hub limit look like a collector bug for a day; (b) `requireEnum` validates and returns nothing — assign first; (c) a fixture tag with a Z in it was dropped by the normalizer, correctly, for the second time in two days — the CR alphabet is `0289PYLQGRJCUV`; (d) the other session shipped 1.2.0 mid-build, so this is 1.3.0 — a version is a fact the changelog records, read it before choosing one; (e) the planner tests park the 263 seeded boards exactly as they park the GLOBAL cards row.

**COLLECTOR v2.0.26 RELEASED, AND THE RUNBOOK'S SOAK STEP IS BROKEN FOR GO (2026-09-11 03:11–03:18Z):** Two fixes from the audit: `crapi.Path()` deleted (dead, drift trap), User-Agent `Elixir-MCP-Gateway/0.1` and `py2` → `Elixir-MCP-Collector/<stamped version>` in both twins — Supercell sees that string on every request and had been seeing the old name with no version since the rename. Validate + release green, v2.0.26 candidate at 03:11Z. **THE DEFECT:** staged on Ram Rider (Go, this Mac) as the runbook says; it came up as v2.0.26, asked the authority, was told v2.0.22, and DOWNGRADED ITSELF in one second (`update authority names v2.0.22; self-updating`). A Go canary cannot soak a candidate while an older version is named — the self-update is doing its job — which is also why v2.0.23–25 were never soaked or named. Soaked the PYTHON twin instead (Tesla never self-updates, by design): `py-v2.0.26`, 91 jobs, 0 fetch errors in its first 5 minutes. Named v2.0.26 at 03:18Z; Ram Rider self-updated to it on restart (`update authority names v2.0.26; self-updating` → `version=v2.0.26`); Wall Breakers (cabin) picks it up on its hourly config poll — Run Elixir MCP confirms the Version column. Runbook §2 now says so. **QUEUED FOR THE NEXT COLLECTOR CHANGE:** an `ELIXIR_MCP_PIN_VERSION` env for Go canaries so §2 is true for both twins again; it cannot itself be soaked on Go until a build containing it is named, which is a one-time exception worth taking deliberately.

**0069 BUILT, PUSHED, NOT DEPLOYED — AND A LIVE BUDGET LEAK IS WAITING ON `aws login` (2026-09-11 ~04:40Z, commit f14a264):** THE LEAK: from 0068's deploy (~03:0xZ) every one of the 263 boards has been re-planned as STARVED on every tick outside the 15-minute in-flight window — `used_hour` 513 at 04:19Z nineteen minutes in, i.e. ~1,500/hr against a recorder that does 170. Cause: admission stamped poll_state freshness under `normalizeTag(entity_key)`, null for `global` and for numeric locations, so `last_admitted_at` never advanced and the fairness floor fired forever. Seen from `source_polls.observed_at: null` on a board with two snapshots. FIXED in `pipeline.mjs` (board endpoints stamp under the board's own key) with a test that reads the stamp back. Still inside the 3600 ceiling, so nothing is refused; it is waste, not an outage — but it is 40% of the ceiling and it ends only on deploy. **JAMIE-MANUAL: `aws login --profile jamie`, then `AWS_PROFILE=jamie node infra/scripts/deploy.mjs`** — migration 0069 rides along. ALSO IN 0069: the 39 season finals (S97–S135, 9,999 places each, one-shot planner rows, each ~400 KB encoded — the 5 MB overflow ceiling earned its keep before we knew why), the clan ladders (global/US/JP daily), the game-mode boards enumerated from /leaderboards (30 today), /events as daily sightings, /globaltournaments; three tools (`rankings_clan_ladder`, `rankings_timeline`, `game_events`), `pol_final`/`mode` boards on the two existing ones; contract 1.4.0, 52 tools. EXPECT ON DEPLOY: a one-time burst — 39 finals + ~30 mode boards + 6 clan ladders + 3 GLOBAL reads ≈ 80 fetches on the first tick, then daily. WATCH AFTER: `used_hour` should fall back toward ~200-300 within the hour; `rankings_players({location:'global'})` meta.source_polls.rankings_pol.observed_at must be non-null; `rankings_players({board:'pol_final', season:135})` returns 9,999 places. /challenges returns notFound and is not recorded; /seasonsV2 is still all nulls (both in cr-agent-api-docs).

**0069 DEPLOYED, THE LEAK CLOSED, THE FLEET QUARANTINED AND RECOVERED, THE FINALS LANDED (2026-09-11 04:39–05:07Z; commits f14a264 / d3ea587 / d444ceb):** THREE THINGS WENT WRONG IN A CHAIN, and each was a copy of a number that should have had one owner. (1) THE LEAK (fixed in f14a264, deployed 04:39Z): admission stamped board freshness under `normalizeTag(entity_key)`, null for `global`/numeric locations, so every board was starved on every tick from 0068's deploy — `used_hour` 1394 by 04:49Z. Board endpoints stamp under the board's own key now; PROOF: `source_polls.rankings_pol.observed_at` 05:04:36Z, freshness 16s. (2) THE SECOND CEILING (d3ea587, 04:54Z): the door had its OWN `MAX_BODY_GZ_B64 = 400_000` beside the 5 MB `overflow_bytes` it serves, plus ingest's 2 MiB decompressed bound — a season final (~400 KB encoded, ~1.5 MB raw) was refused `bad_body`, the lease sat unsubmitted 90s, `lease_cap` followed. Door reads `CONFIG.overflow_bytes` now; ingest bound 16 MiB; the bomb test grew past it. (3) THE QUARANTINE (d444ceb, 04:58Z): ten unsubmitted leases is the quarantine threshold → both collectors `draining` → lease 401 → the fleet answered nothing from 04:54Z. Underneath, an OLDER defect: `missed_streak` resets only on a successful submit, which needs a lease, which the streak refuses — a quarantined collector could never recover, and the console's "Back to probation → Activate" would re-quarantine on the next lease. Probation now clears the streak; `{gateway_recover:{name}}` on the migrate lambda is the night lever, used at 04:59Z on Ram Rider, Tesla and Wall Breakers; all three `active` and pulling by 05:04Z. Total recorder outage ~5 min; degraded from ~04:49Z. **THE LESSON, once more:** the transport ceiling lived in four places (served config, door, ingest, and the SQS-era comment), and each one I found was presented as "the" limit. Grep for the NUMBER, not the name, before declaring a limit removed. LANDED: S97–S135 finals (9,999 each; S97 TRB Lucas 2977, S120 Light Work 4100, S135 Tsujihu 3914) in ten minutes; 30 game-mode boards enumerated (Merge Tactics 1,000 places); clan ladders (the global clan-score board is capped at 140,000 — three clans tie at #1); 8 events sighted today; timeline 7 points, floor 1189, field 916. Post-recovery burst: `used_hour` 659 at 05:07Z draining the backlog; expect ~200-300/hr from the next hour — Run Elixir MCP and Keep the Boards confirm. **THE NUMBER THE BACKFILL WAS FOR:** across 31 season pairs (six finals still landing at the time), the season's #1 had been in the previous season's top 200 in **81%** and top 500 in **94%**; two #1s (S116, S135's Tsujihu) came from outside the top 500. `record_top` 200 catches the eventual #1 from the roll four times in five; 500 makes it nineteen in twenty at roughly +300 recorded players (~+400 fetches/hr, ~25% of the ceiling all told). A one-row change in `ranking_board`, no deploy — JAMIE'S CALL. Lease released; tree clean.

**DEADLOCKS AFTER THE RECOVERY, AND THE QUARANTINE NOTICES JAMIE GOT (2026-09-11 05:01–05:17Z, commit 3db3632, deployed 05:09:48Z):** The three `gateway_quarantined` emails are the 04:54Z event (recovered 04:59Z, above). What followed was new: nine `submit_ingest_error … deadlock detected` in seven minutes from 05:01Z — two boards each upserting thousands of `player` rows in payload order while a battlelog admitted a few, taking the same locks in different orders. Each deadlock is a submit 500, each 500 an unsubmitted lease, and ten of those is the quarantine the fleet had just come out of; Tesla was at five. Every bulk upsert in the ranking projectors (players, presence, clans, events) is ordered by its lock key now — the rule `packages/claims` has followed since it was written, and the one the new code did not copy. VERIFIED: zero 500s and zero deadlocks in the eight minutes after the deploy, all three collectors `active`, queue drained to 0, `used_hour` 726 at 05:17Z (the backlog, not a leak — `due` is 27). LEFT FOR DAYLIGHT: a server-side 5xx on submit should not count against the collector at all — the quarantine exists for collectors that vanish, and both quarantines tonight were the door's fault. Lease released.

**THE FINALS WERE FILED UNDER THE WRONG SEASONS — THREE SEASON NAMESPACES, AND THE API'S NUMERIC ONE IS A LIST POSITION (2026-09-11 05:40–06:1xZ, commit 17f1ac3, migration 0070, contract 1.5.0, deployed `{"applied":69,"ran":1}`):** Jamie forwarded a ChatGPT note that the game shows "Season 87 · Minion Academy" on the Pass while clan war is Season 136, asking if there was anything to do. There was, but not what the note said. PROBED: `/locations/global/pathoflegend/{id}/rankings/players` takes `YYYY-MM` (`2026-08` → Flash Light 4121, the latest final) and bare numbers; `136` returned the 2026-01 final (Asaf 3697), `135` = `2025-12` (Tsujihu 3914), `97` = `2022-10` (TRB Lucas 2977), `87` → notFound. The V1 `/locations/global/seasons` list has 143 entries from 2016-02 (with duplicates early), so the bare number is the 1-BASED POSITION IN THAT LIST, and it has been strictly monthly since position 97. Clan-war S136 running on the same day was a coincidence that made 0069's `generate_series(97, seasonId-1)` look right. CONSEQUENCE: the 39 finals held as S97..S135 were the 2022-10..2025-12 finals (S89..S127 as the clock counts), and 2026-01..2026-08 were never fetched; the "season #1 was in the previous season's top 200 = 81%" analysis was computed over correctly-consecutive pairs, just mislabelled, so it stands. FIX: finals are keyed by the API's own name (`YYYY-MM`) — `settledPolMonths(now)` seeds `poll_state` from `2022-10` through the month that last rolled; `polSeasonMonth(key)` also resolves a numeric key by list position so a hand live_fetch lands under the right season; `ranking_snapshot.season_month` holds the API's name beside the clock's `season_id`; 0070 relabelled the 39 in place (position−8, month = 2022-10 + (position−97) months; verified on scratch and live: S89 = TRB Lucas, S127 = Tsujihu) and dropped the numeric `poll_state` rows; the planner's next tick seeded 47 months and the eight missing finals were due at once. Tool: `season` accepts `2026-08` or `135`; `game_clock` reports `season_month`. NOT DONE, DELIBERATELY: the Pass number ("Season 87") is not in the API anywhere — adding `game_season: 87` to game_clock as ChatGPT suggested would be a hand-maintained number with nothing to verify it against, the kind of field that is right the month it is written and wrong the month after. The clock's `season_id` (the riverrace `seasonId` namespace) stays the one season number the record files under; the note on game_clock now says so. `cr-agent-api-docs` corrected in bd71be4 (5bb8e10 had written the wrong claim two hours earlier). KEEP-THE-BOARDS: the season-story video for a season's #1 reads `rankings_players(board: pol_final, season: <the month>)` — by month is the unambiguous form.

**COLLECTOR DOCTOR AND THE RECIPES (2026-09-11 06:0x–06:3xZ; hub commits 25ed8aa + 9fff26b deployed; collector 816f10f + 1cad60a → candidate v2.0.27, NOT NAMED):** Jamie is adding two collectors on Oracle Always Free and wants that documented so others can; and a `doctor` preflight. CONTRACT FIRST: `/api/collector/config` already returned identity/channel/status read-only, so doctor implements against it; four additions landed server-side before any client pinned a shape — config now answers a `pending` token (lease/submit still refuse; the commonest new-operator state was a bare 401 identical to a typo), returns 403 `revoked` for a known-dead hash, echoes `observed_ip` (API Gateway `sourceIp` — the collector must not call ipify; "three endpoints and nothing else" is a stated property), names `doctor.cr_path` (`/locations?limit=1`, server-designated like every path), and carries `gateway.card`. NOT PROVIDED, ON PURPOSE: "probation day N of 3" (activation is a click, not a timer) and "key allowlist: X" (the CR 403 names the caller's IP, never the allowlist). Doctor: five checks, all run, exit 0/1/2 (2 = pending/draining: nothing on the box to fix), last-four for both secrets in text and `--json`, never leases; Go `collector doctor`, Python `collector.py --check`, same wording, both test suites pin it; verified live against Tesla's `.env` (read-only). RECIPES: one `cloud-init.yaml` (Go binary by default because it self-updates and nobody logs into a cloud box; Python swap documented), Oracle (PAYG conversion is the load-bearing step — the idle reclaimer would stop a collector; E2.1.Micro; reserved IP; 1:1 NAT), Hetzner, DSM/macOS/Windows hand recipes, template. Jamie: no "last verified" headers — ceremony. The YAML pins `TAG=v2.0.27` and greps the installed binary for the doctor banner before calling it, because on a pre-doctor binary `collector doctor` would START the collector in cloud-init's foreground. QUEUED: soak v2.0.27 (Python twin on Tesla per the runbook) and name it — until then `releases/latest` is 2.0.26 and a fresh install has no doctor; `apps/site/src/docs/operators.md` could link the recipes once named.
