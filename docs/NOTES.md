# Elixir MCP

Purpose: _One-line purpose statement TBD — rethinking elixir-bot as an MCP offering._

## Context: current Elixir

- Elixir is the clan agent for the POAP KINGS Clash Royale clan: a deterministic data engine (Clash Royale API → receipts → event streams → projections) with an hourly LLM awareness loop that is the sole proactive poster.
- It speaks through destination-specific Discord lanes (#elixir, #announcements, #actions, #ask-elixir, #leaders, etc.) with scoped memory, plus `/elixir` member commands and `/clanops` leadership operations.
- Clan management is deterministic (state + leader action cards, no model-decided promotions/kicks); runs on Python 3.14 / uv / SQLite, operated via launchd and `scripts/admin.sh`.

## Brainstorm

**Core idea:** Decompose the Elixir agent and simultaneously create a new offering: give Clash Royale players an MCP interface to explore their Clash data.

**Two components:**

1. **Elixir MCP** — the service/website.
   - Player registers with email via magic link (same pattern as other projects).
   - Registers their Clash Royale tag.
   - Possibly sets up billing/payment.
   - Possibly provisions their own Elixir MCP Gateway.
   - Service then collects data for that user and provides an MCP interface they can bring to Claude or any agent.
   - Can be focused on a player OR a clan.
2. **Elixir MCP Gateway** — solves the IP restriction issue of the Clash Royale API. Could be a multitude of gateways distributing load to the CR API across dozens of egress points.

**Future direction:** The Elixir agent sheds most of its data-collecting capabilities, relies on Elixir MCP for data, and becomes a lightweight layer on top providing clan agent services and clan management functions.

**Precedent:** This split already exists with Elixir Drop / Elixir Drop Gateway — the difference is many Elixir MCP Gateways instead of one.

**Scope clarifications:**

- Brand-new project. Does NOT reuse anything from elixir-bot. (May at some point backfill historical data for Jamie personally or for POAP KINGS.)
- Multiplayer and multi-clan from day one.
- The service stands alone without the Elixir agent — you could use Elixir MCP and never attach the agent. The agent is a V2 consideration, one possible consumer later.

**Core problem/value:** The Clash Royale API only provides current state, no history. Elixir MCP's job is to record history and expose a really robust MCP interface over Clash Royale data. A player says "record all my games"; a clan setup says "record all battles for all clan members." Substantial data collection; hundreds of players could attach, each making MCP requests via their favorite agent.

**Patterns already proven in-house:**

- MCP side (auth'd): very similar to the thingy MCP just built — auth patterns all exist.
- Gateway: looks a lot like the Clash Royale Drop bridge running on Jamie's home machine (queue-connected). Queue is the right model because there are two constraints: IP restriction AND rate limiting.

**Web interface scope:** Deliberately small. Register (magic link), identify your player tag, opt in to recording, and maybe a small dashboard showing what data is being collected for you. No chat interface — the agent experience lives entirely in the user's own MCP client.

**Monetization (later, not launch):** Launch is free, just player registration. But the shape of future monetization is clear: a player might pay a couple bucks ($20/year range) to have their data recorded; a clan could pay for collection across all members as a provisioned service for the clan.

**Key data-model insight:** Recording is shared, not per-user. If a clan is recording all members' battles and a member also signs up for their own data, that's the same data — record once, both parties get access. So the data model separates the recorded facts (battles/observations, stored once, keyed by player) from access entitlements (who subscribed to what). "A pretty robust data model backs the MCP."

## Open Questions

- Supercell CR API terms — commercial use, key ownership. Does a user-provisioned gateway mean the user brings their own API key?
- Multi-tenancy: what does per-user/per-clan data look like? (Greenfield — no elixir-bot code reuse.)
- MCP auth model — magic link to token for remote MCP connection (OAuth?).
- Data retention/collection start point for new users.
- Polling cadence vs CR battle-log window — the battle log endpoint only returns recent battles, so recorder scheduling determines whether games get missed.
- Queue/scheduler design for fan-out across many gateways with per-gateway rate budgets.
- MCP tool surface — raw queries vs aggregates (deck stats, win rates, trophy history, clan war history)?
- Canonical battle identity/dedup — the same battle appears in both participants' battle logs (and via clan recording), so battles need a canonical ID (player pair + battle time?) to store once regardless of how many recording paths observe it.

### Resolved / deferred

- ~~Migration path for elixir-bot to consume Elixir MCP~~ — agent is V2, one possible consumer later; no code reuse.

## Decisions

From the CR API terms review (full doc: [elixir-mcp-terms-review.md](../elixir-mcp-terms-review.md)):

- Launch free; any paid tier requires express Supercell approval per Fan Content Policy (fees prohibited; donations can't gate benefits).
- Gateway fleet is for redundancy/egress resilience, NOT quota multiplication; conservative global rate budget (~one key's limit) to avoid circumvention reading.
- No blockchain/POAP/ENS anything in this product (prohibited fan content).
- No Supercell marks in domain/branding; include unofficiality disclaimer.
- Non-subscriber data exposed only within a subscriber's own context; no arbitrary-tag historical lookup.

Jamie, 2026-09-03 (design session inputs):

- **Gated access:** account creation is "request access" with owner approval, not open signup. Expecting demand; approval queue in the web UI.
- **User-attached gateways:** gateway instances (elixir-mcp-gw) belong to users. Launch with Jamie's only, but the website must let a user raise their hand to run a gateway and walk them through setup. (Implies per-user gateway enrollment, key/IP registration, and a trust/ops story — still one global rate budget.)
- **Domains (revised same session):** web UI at `elixir.poapkings.com`, MCP/OAuth door at `mcp.poapkings.com` (two-hostname cookie separation, librarian pattern).
- **Stack:** TypeScript/Node 24 everywhere; repo `elixir-mcp`, monorepo in Drop's workspace shape.
- **Database:** RDS Postgres, single-AZ db.t4g.micro (24/7 recorder keeps serverless engines awake — fixed small instance wins).
- **Recorder compute:** fully serverless — EventBridge scheduler Lambda + SQS request/results queues + ingest Lambda; NAT-free VPC, email via queue-fed non-VPC relay Lambda.
- **Email:** Fastmail JMAP from `elixir@poapkings.com` (already configured for elixir-bot). Re-evaluated vs SES 2026-09-03 and kept: the relay-queue weight is a VPC/RDS consequence (librarian's non-VPC Lambdas send JMAP directly), SES's VPC endpoint costs more than the relay, and warm sender reputation matters most on the magic-link path. Relay transport is swappable to SES later behind the same queue.
- **Local time (2026-09-03):** `account.timezone` (IANA, migration 0002) — tools render timestamps in the player's local zone alongside UTC and resolve relative windows locally; storage stays UTC; UTC-day rollups labeled honestly. Set in web UI.
- **DNS workflow (2026-09-03):** poapkings.com DNS stays at Namecheap, Jamie applies records by hand. When infra reaches the front-door step, relay the exact records (CNAMEs for `elixir.` + `mcp.`, ACM validation records) to Jamie — never assume programmatic DNS.
- **Identity discipline:** CR tags (player + clan) are the primary keys for all game entities, one canonical normalized form, no surrogate IDs; game-data tables never reference accounts (`claim` is the sole join). Battles write participant rows symmetrically for claimed and unclaimed tags; a new user's claim *unlocks* pre-existing history recorded about them as opponents — entitlement change, never data migration. DESIGN.md §4.1.
- **Approvals:** owner-scoped admin page on the site (request queue, approve/deny).
- **Gateway phasing:** self-serve enrollment ships V2; V1 schema models gateways first-class and Jamie's gateway enrolls through the real path. Keys issued from Jamie's Supercell account (`key_source` column keeps operator-BYO open).
- **Visual design:** web UI follows the Elixir Drop / poapkings.com conventions — Clash Royale-themed look, dark background; a sibling of drop.poapkings.com, not a new brand.
- **Infrastructure:** design must include a mapped-out AWS architecture. Data volume exceeds prior projects (DynamoDB/SQLite territory ends here); a real SQL server — managed Postgres — is the working assumption. Engine/sizing choice (RDS vs Aurora Serverless vs external managed Postgres) to be decided in the design pass.

## Next Steps

- [x] Review Clash Royale API terms of service for compliance with the design (recording/retention, multi-key gateways, commercial use, other-players' data) — done; see Decisions and [elixir-mcp-terms-review.md](../elixir-mcp-terms-review.md).
- [x] Deep design work — elixir-bot data model study, CR API surface analysis, #ask-elixir question mining — producing [DESIGN.md](DESIGN.md).
- [x] Design v2 (2026-09-03): prior-art surveys (librarian auth/OAuth/MCP, Drop bridge, elixir-bot recorder), Jamie's decisions folded in, AWS infrastructure map (§7), prior-art map (§10).
- [ ] Create the `elixir-mcp` repo (monorepo skeleton, Drop workspace shape) and move these docs into it.
- [ ] Verify Fastmail JMAP send-as for `elixir@poapkings.com` works from a non-elixir-bot credential, or mint a dedicated API token for the relay Lambda.
- [x] CR token landed in `elixir-mcp/.env` (2026-09-03, mode 0600, canonical var name **`CR_API_TOKEN`** — note: Drop uses `CR_API_KEY`; our gateway config uses ours). If this is the borrowed Drop key, still swap in the dedicated key tonight; either way, delete the Fastmail draft that temporarily held a token. Do NOT borrow Drop's production key — decided against 2026-09-03 (shared 403 blast radius, and nothing needs a key before the gateway code exists).
- [ ] Confirm poapkings.com DNS/ACM path for `elixir.` + `mcp.` CNAMEs (Namecheap; certs in us-east-1).
- [ ] Privacy policy page for the site (we store emails + behavioral history incl. non-registered players; GDPR/deletion path per terms review §2/§4) — plain-language, one page.
- [ ] Apply to Supercell for the `verifytoken` scope (parallel track; swap into the claim ladder if granted).
- [ ] Postgres schema draft (DDL for §4) — first build artifact.

## V1 build order (the standing work order — check off as chunks land green)

- [x] Repo, gitignore-first, AGENTS.md/CLAUDE.md, workspace root
- [x] packages/contracts core (tags, errors, deck_hash, meta, version)
- [x] Migration runner + fingerprint + 0001 recorder core + 0002 timezone
- [x] Fixtures (12 real payloads + cross-observer pair) + admission + battle/roster ingest
- [x] Pipeline glue: results-message → txn(payload/receipt → admission → projections); poll_state freshness advance; ingest Lambda handler shape (queue contract v1 in packages/contracts; receipt redelivery dedup = 0003)
- [x] Scheduler: heat model, fairness floors, budget settle, job planning; EventBridge handler shape (0004: epoch-anchored decay + last_known_clan_tag auto-follow stamp; bulk share only, live reserve untouched)
- [x] Snapshots + events + rollups projectors (profile → snapshot + donation_reset; roster → clan events w/ evidence, first-sight silent; battles → daily rollups; completeness = day-level estimate from bracketing snapshots — refine if live gaps show)
- [x] Gateway (services/gateway): lease loop (live-first), gzip results + loud overflow, 403 breaker w/ half-open probe, split heartbeats, queue-name indirection, launchd installer, .env.example
- [x] Auth core: 0005 auth tables + magic-link/code + sessions (gate enforced in resolveSession) + rate limit (services/auth)
- [x] OAuth 2.1 logic (services/auth/oauth.mjs): DCR, PKCE, single-use codes, rotation + family replay revocation, 90d absolute lifetime, gate-enforcing validateAccessToken. HTTP shell (routes/HTML/discovery docs) rides the MCP-server chunk; /authorize state = magic_login purpose='oauth'.
- [x] MCP server: protocol layer (compact-JSON results, version cache-buster) + first tools (list_my_players, get_coverage, get_player, query_battles w/ local-time windows) + fail-open quota + audited invoker + bearer handler
- [x] Remaining V1 tools — all 11 declared and tested. Open threads: live lane wiring (returns live_unavailable until infra), upgrade-gap fields need packages/game-data reference table (no invented constants), timeline granularity 'season' deferred to V2 war work.
- [x] OAuth 2.1 HTTP shell + discovery on the MCP door (two-step authorize over the magic core; consent = entering the code; oracle-resistant; RFC 9207/8414/9728)
- [x] Web-api service: full journey tested (request → approve → sign in → claim → record); CSRF contract header; neutral gate responses everywhere
- [x] Web UI (apps/web): landing/sign-in/dashboard/admin/connect in Drop's token palette; 4 RTL tests; 49KB gzipped
- [x] Email relay (JMAP sender + templates + SQS consumer; EmailMessage contract) — transport swappable behind the queue
- [x] Season-roll watcher (shared pre-reset window fn; scheduler forces in-window profile polls; projector pins season_roll rows)
- [x] infra/ DEPLOYED 2026-09-03 (Jamie's go): stack live, migrations 5/5, smoke 7/7. Fixed en route: RDS force-SSL (sslmode=no-verify) and gateway .env→process.env export for launchd. Owner account + jamie-mac gateway seeded via the migrate Lambda's explicit seed payload; gateway running under launchd (com.poapkings.elixir-mcp-gw).
- **Hostname consolidation (Jamie, 2026-09-03 post-launch):** connect URL is `https://elixir.poapkings.com/mcp` — ONE hostname, path-split CloudFront behaviors (/mcp, /oauth/*, /.well-known/* forward no cookies). The mcp. distribution deleted before DNS existed; its issued cert sits unused (free). OAuth issuer = https://elixir.poapkings.com.
- **Owner identity fixed (2026-09-03):** owner login is jamie@thingelstad.com; the AWS-only address was purged from prod DB, local dev, repo, and git history (filter-repo + force push).
- [ ] DNS at Namecheap (Jamie): ONE record once I confirm aliases live — elixir → d3nauaye5ioxmf.cloudfront.net.
- [ ] Jamie: sign in at https://d3nauaye5ioxmf.cloudfront.net, claim #20JJJ2CCRU, opt into recording (the real product flow — also live-tests the JMAP relay).
- [ ] projects-sysadmin: subscribe elixir-mcp-alarms to projects-ops-alerts; add the gateway launchd service + backup verification to the rotations.

**Launch-day incidents (2026-09-03, all resolved same hour):** (1) RDS force-SSL → sslmode=no-verify; (2) gateway .env not exported to process.env under launchd; (3) stale CloudFront cache after web sync → deploys now invalidate; (4) API GW v2 base64 form bodies → 'unknown client_id' on first OAuth connect → rawBody() decode everywhere; (5) gateway had no instantaneous pacing → 1.5s fetch floor added; (6) first CR token was endpoint-limited (clans 200, players 403 accessDenied — the 'insufficient token scope' 403 flavor is REAL, watch for it) → replaced with a correct key. **STEADY STATE reached 18:30Z:** 92-job clan fan-out drained to 0, 177 fetches/6min, breaker silent, ingest clean; ~46 POAP KINGS members + Jamie recording.
- [ ] End-to-end on real recording (Jamie's tag via his gateway) — **GATE: dedicated CR key + DNS records from Jamie**

## V2 build order (the standing work order — check off as chunks land green)

Start every iteration with a recorder health glance (gateway FetchSucceeded/BreakerOpen, DLQs, a get_coverage call) — we babysit while we build. Deploys are routine (`deploy.mjs --skip-web` unless web changed); no billable gates in V2. Jamie-manual steps get queued in NOTES, never blocked on.

**Multi-tenant discipline (Jamie, 2026-09-03): elixir-bot's war/clan code was single-tenant — the pattern, never the assumption, transfers.** Every clan-scoped table leads its PK with the observing clan_tag; the war clock/season inference is per-clan; standings are view-scoped per observer (duplication across recorded clans in one race is by design); entitlements scope clan reads to the member's OWN clan. Any new clan-activity feature gets this checked explicitly.

- [x] War clock (services/ingest/war-clock.mjs): period grid + season inference + observed-anchor-with-fallback + resolveWarKeys from battle time (cross-section = honest nulls); verified against the three real captured riverrace payloads
- [x] Migration 0006 (deployed): war_period_anchor / war_week / war_week_clan / war_participation (**points**) / war_attendance_day + claim.share_battles_with_clan
- [x] War projector (deployed): anchors first-observation-wins; honest anchor_only genesis pending backfill; MAX-merge throughout; war keys stamped from battle time via per-clan clock; stale-payload-at-roll edge documented
- [x] riverracelog backfill (deployed): the log is a daily recorded endpoint; enrollment backfill = first poll; items carry seasonId (genesis unlock); 0007 adds rank/trophy_change; participation clan-scoped to own members
- [x] Entitlements module (deployed): rules 2-4; INTERIM soft claims count for clan scope until liveness ships (CLAN_SCOPE_REQUIRES flips then); owner administers all recorded clans, member consent still applies to owner
- [x] Clan enrollment UI (Jamie's ask, deployed): owner-only /api/admin/clans + Recorded Clans admin panel; leader self-serve stays a later item
- [x] Tools (deployed): get_war, get_war_history, get_clan, compare_players; all 15 tools entitlement-aware (clanmate=summary, war-only battles without consent + scope_note); timeline 'season' granularity deferred to a contract-minor later. Ops stats read added to the migrate Lambda ({stats:true}).
- **OPS GAP found live: the gateway runs launchd-from-source and does NOT pick up deploys — a gateway-code change needs `launchctl kickstart -k gui/$UID/com.poapkings.elixir-mcp-gw` (bit us: riverracelog jobs unleasable by the old process, 1 DLQ'd). Queue a proper fix (gateway self-update or a deploy-script reminder). Session MCP tool rosters are also start-frozen — new tools appear on reconnect (the serverInfo.version story working as designed).**
- Prod observation 20:40Z: 7 active recordings (Jamie appears to be enrolling clans via the new panel), 1,572 players, 1,533 battles.
- [x] Live lane (deployed + verified with a REAL end-to-end fetch from Claude Code: live colosseum data through the gateway in one tool call): enqueue → receipt poll; opportunistic recording confirmed; 50/day per-account live cap (owner exempt)
- [x] ~~Liveness-proof verification~~ RETIRED same day (Jamie, 2026-09-03): currentFavouriteCard is not reliably player-settable in-game, so the challenge could never be completed (cr-agent-api-docs patched). **Claims are trust-based**: owner approval is the gate, a claim is taken at its word. claim.status + verification_challenge (0008) stay in the ladder unused; never rebuild verification on favourite-card.
- [x] Web clan page (deployed): /clan — war standings (final rank order, our row highlighted), roster with trophies/donations/last-battle recency, share_battles toggle per claim. Recency policy mirrors get_clan: activity recency is roster-level; consent gates battle DETAIL only. Members reach it via open membership of a claimed tag in a recorded clan; owner falls back to first recorded clan. Leader self-serve clan-enroll folds into the gateway self-serve item below (same raise-hand pattern).
- [x] Gateway self-serve enrollment (deployed): POST /api/gateways raise-hand (name+static IP, owner notified) + Dashboard panel; owner lifecycle actions (pending→probation→activate→drain→revoke, forward-only) in the Admin gateways panel; ingest now ENFORCES the lifecycle (refuses revoked/unknown gateway_ids, cleanly — no FK-throw retry loop) and stamps last_heartbeat_at on any valid message / last_success_at on admission, so the health panel is real; docs/OPERATORS.md is the public guided install (linked from README). **Jamie-manual per operator: create the IP-allowlisted Supercell key + a per-gateway IAM user (recv/delete on request queues, send on results, PutMetricData) and hand both over out of band; then click "Begin probation".**
- [x] V2 smoke on live data (2026-09-03 21:16Z): smoke.mjs all green; ops stats 8,008 players / 7,442 battles / 64 war weeks / 2,975 participation rows / 5 war anchors across 12 recordings; live MCP get_coverage answered with 31s freshness and pre-claim battle capture. DESIGN/NOTES/AGENTS + memory updated.

**CI note (2026-09-03):** validate had NEVER passed — `node --test test/` needs the directory-positional support local Node 26 has and CI/Lambda Node 24 lacks, so every workspace failed on every run. Fixed with no-arg `node --test` (9a298ee); first green run confirmed. Lesson: dev machine runs Node 26, prod runs Node 24 — CI is the version referee, so check it after the first push, not days later.

**MCP agent-in-the-loop testing phase (Jamie, 2026-09-04, replaces the manual Claude<->Claude Code markdown relay):** fresh-context subagents ARE the naive users — they inherit the session's authed MCP connection but none of the implementation knowledge. Each round: spawn 4-5 persona agents (casual "how am I doing", deck tinkerer, clan leader, data-honesty nerd, adversarial edge-poker), each with a bounded mission (max ~12 tool calls, cr_api_live at most once) returning STRUCTURED feedback (task outcome / friction with exact tool+field names / delights / wishlist). Triage into three lanes: (1) fix now — descriptions, error messages, response shapes (contract-versioned); (2) feature wishlist -> this file for Jamie; (3) rejected with reason. Deploy fixes, spawn a FRESH round (never reuse a contaminated agent), repeat until a round is dry. Measure rounds objectively from mcp_call_audit error/truncation rates during test windows. Runs after the archive import + R1 land. **Jamie-manual: reconnect /mcp in the session when convenient — the session tool roster is start-frozen at the 11 V1 tools, so war tools (get_war, get_clan, get_war_history, compare_players) are invisible to testers until reconnect.**

**Archive import COMPLETE (2026-09-04 06:40Z):** all 13,809 elixir-bot raw payloads replayed through the real pipeline — final state 37,725 players / 35,120 battles / 2,730 snapshots / 134 war weeks / 5,109 participation rows; Jamie's own record now May 14 -> present (235 appearances). Verified via MCP door post-import: played-time ordering correct with interleaved inserts, display-scale levels, coverage honest. One mid-run FK crash (war projector vs never-seen clan) fixed forward. Perf census final in docs/DB-AUDIT.md: projector = 93-94% of cost, career-length scaling confirmed -> R1 next (batch projector writes + player/time index).

**MCP tester wishlist (pilot round 1, for Jamie's triage):** per-week win-rate series on get_performance (group_by: week — the tester approximated trend with one before/after split); mode filter or PoL-medal series on get_player_timeline; gap disclosure on timeline (gaps array or per-point flag); get_deck_performance sort/min_battles/share_of_battles; a single headline summary call (current trophies + 30d W/L + top deck). Pilot verdict: harness works — 6 tool calls, task answered, feedback citable; six friction fixes shipped same night (contract 0.2.1).

**PHANTOM-SEASON INCIDENT (found + fixed 2026-09-04, round-2 agent playtest):** the war-nerd tester cross-checked member_weeks against the battle log and caught colosseum rows frozen at day-1. Root cause: inferSeasonId's stateful roll (+1 when live section < logged section) ran away during the archive replay — logged state was FUTURE relative to replayed payloads, scattering 9 real weeks (true S134-135) across phantom seasons 136-144, then hijacking live polls into "145". Fix: seasons derive STATELESSLY from the CR calendar (first Monday -> first Monday, ~09:30Z; war-clock seasonFromDate, anchored + verified against riverracelog createdDate). 0021 purged season>135 rows; 132-135 history pristine; current week self-heals via MAX-merge. CLASS LESSON: time-derived identity must come from the calendar, never from a state machine that assumes in-order processing — same family as the insert-order/battle-time cursor bug. Round-2 honesty batch SHIPPED 2026-09-04 (contract 0.6.0, verified live): war_days_battled null-when-no-attendance-coverage (unknown never zero), seasons arg scopes member_weeks, get_war participants carry in_clan, standings payload-mirror note (zero-fame opponents can be real), member_since_observed -> first_observed_in_clan, compare_players recorded-window note. Still parked for later: periodLogs harvest (the proper fix for null week ranks/fame — daily endOfDayRank/pointsEarned).

**S3 payload archive BUILT 2026-09-04** (DATA-TOOLS.md has status + crib sheet): archive-at-admission live-verified (first live object 16:05Z), weekly sweep scheduled (MON 08:15Z). Free S3 gateway endpoint added (new private route table — the VPC lambdas had NO S3 route before this). **Migration COMPLETE same day:** history export 15,367/15,367 rows -> S3 (1:1 reconciled, 216MB gz), first sweep drove Postgres api_payload down to the 545-row hot set (14,822 superseded rows retired, 0 missing twins). Athena verified live (partition-projected query over Jamie's profile versions). Disk note: the 405MB table file shrinks only via vacuum full — pages are reused instead; RDS allocation floor is 20GB so reclaiming buys nothing. dt partition for replayed archive content = replay date (first time WE held the content), original fetch times live in receipts.

**Collector repo split DONE 2026-09-04:** github jthingelstad/elixir-mcp-collector (public); services/gateway removed from this repo; both local instances migrated in place (installer re-run from the new checkout, same labels/env files) and verified up; self-update now tracks the collector repo's main — deploying collector code = push THERE. Queue contract canonical here; the collector test pins the produced shape so drift fails collector-side first.

**MCP testing round 3 COMPLETE (2026-09-04, contract 0.7.0 + 0022):** three personas (war auditor / casual / adversarial), 13 fixes shipped same day — see commit ce5986e. The auditor's attendance cross-check exposed a REAL bug chain: attendance now unions decksUsedToday polls with recorded war battles, which exposed that (a) 0021 never purged phantom season stamps from battle ROWS (136-145 lived on) and (b) early-section battles stamped with null war_day are never revisited since stampWarKeys only selects season_id-is-null rows -> 0022 nulls both classes for re-stamping; boatBattle joined the stampable types. CLASS LESSON: a purge must sweep EVERY table carrying the poisoned key (0021 got the war tables, missed battle), and any stamp-once field needs a revisit story for rows stamped wrong. Adversarial: validation now symmetric (empty tag, forged cursor, >4 compare tags, seasons>12 all refuse loudly); full-verbosity limit capped at 25 since opponents now carry decks+tower_hp. Casual: week_of = ISO Monday, trophy_battles per weekly row, draws + best_deck + inclusion note on summary, names stamped from battlelogs (fill-null, forward-only — pre-existing opponents stay null until re-observed; S3 archive replay could backfill if wanted). cr-agent-api-docs patched (tower HP can be null).

**Round-3 wishlist parked for Jamie:** per-war-day per-member breakdown in get_war; decks_possible alongside decks_used; opponent tower HP / margin / close-losses filter on query_battles; roster tenure distinct from first_observed_in_clan; per-block as_of stamps; participants filter (in_clan_only/min_points); coverage hint inline in every summary tool; mode split inside weekly rows.

**ARCHITECTURE ASSESSMENT delivered (2026-09-04 night, Jamie's multi-clan thesis):** elixir-mcp = system of record for GAME facts; elixir-bot = system of record for COMMUNITY facts (voice/Discord/memory/policy verdicts). Unique-to-POAP stays in each fork (SOUL/CLAN/lanes, policy constants, spend); generic ALGORITHMS (archetype classifier, band math, awards mechanics) belong in the bot template as shared code, NOT in MCP; recording/fleet/corpus-intelligence/history belong in MCP and compound per clan (each clan brings a collector = capacity flywheel). Path: (1) stats reads DONE; (2) intelligence into bot voice (rivals->war narration, standings/pilot->answers, meta tools replace web-scraped meta_decks); (3) THE RECORDER SWAP - bot keeps emitters/baselines, sources observations from MCP recorded reads; when proven equivalent, bot's CR key/poller/raw layer retire (this makes clan #2 cheap; needs its own design doc before code); (4) productized fork + enrollment + service token + collector handshake. Risks on record: shared fate (fallbacks must survive phase 3), contract stability (no more alias-free renames once clan #2 connects), per-clan quota/budget policy.

**CLAN NOTIFICATIONS FIXED + CAPTURE AUDIT (2026-09-06, 0036, contract 0.21.1, commit 06f1072):** Jamie: "clan notifications aren't working - what triggers one?" INVESTIGATION: working as designed, design covered ~nothing - the ONLY clan topic was clan_war_week_finished (fires WEEKLY at the section boundary; none had passed since the lane shipped, so zero clan events ever). FIX: roster projector bridges observed diffs into the feed - member_joined / member_left / member_role_changed to accounts that ADDED the clan (firstSight silent, post-commit flush, fan-out pinned by test). CAPTURE AUDIT (Jamie approved): capture_audit table - one row per fresh battlelog poll with prior coverage; gap=true when the payload's OLDEST battle was previously unseen (log rolled past unseen battles). First polls = history arriving, never gaps. 24h gaps/polls PUBLIC on Data > Status - "no gaps" is now a measurement. QUEUED (Jamie, this session, in order): (1) DETAIL-VIEWS AUDIT - "click into an item" pages across the site: feedback items deserve dedicated pages; Collections editing should happen INSIDE an opened collection (current add/remove UI "very odd"); sweep the whole interface for detail-view opportunities. (2) POAP KINGS WEBSITE: add Elixir MCP alongside Elixir Drop + Elixir Agent. HELD: elixir-bot recorder swap.

**CLAN PULSE SHIPPED (2026-09-06, contract 0.22.0, commit 545799b):** Jamie ratified the design ("a good v1") and said to CLEAR THE QUEUE autonomously. Shipped same day: clan_pulse daily digest (EventBridge 07:00Z rule elixir-mcp-clan-pulse -> migrate {clan_pulse}; idempotent per clan per UTC day; notify-gated fan-out; facts only - battles_24h, active/total, top 3, quiet >=5d + never_recorded, war decks counts, roster changes), war_day_open on first sight of a war-day period anchor (recency-guarded, in projectRiverRace's new feedEvents), war_current decks_today (named untouched/partial/finished, attendance UNION battles, nominal-period-end staleness guard - a stale anchor says nothing). First live pulse verified: {clans:1, emitted:1}. Routine recipe in Docs > Tools. LESSON: warClock cross-checks periodIndex vs sectionIndex - mutated fixtures must keep them consistent. DEPLOY NOTE: deploy.mjs needs AWS_PROFILE=jamie in env (CLI profile flag isn't enough for the SDK's provider chain).

**CLAN PULSE - DESIGN (2026-09-06, docs/CLAN-PULSE.md, ratified):** Jamie: clan notifications should summarize player battle activity too; "design the solution first by looking at the kinds of things elixir-bot does" - intent is a scheduled Claude routine doing clan management against Elixir MCP. Design doc written, NOT built. Proposes: (1) `clan_pulse` daily digest topic (one per added clan per UTC day ~07:00Z via EventBridge -> migrate op; battles_24h, active/total members, top 3, quiet list >=5d recorded-quiet, war-day decks untouched/partial/finished, roster change counts - FACTS never judgments); (2) `war_day_open` real-time topic from the war_period_anchor first-insert; (3) war_current gains `decks_today` named lists (elixir-bot's _remaining_decks, computed attendance-union style); (4) routine recipe ships in site docs. No schema change; contract 0.22.0 when built.

**TINYLYTICS + BUTTONDOWN SHIPPED (2026-09-05, commits d351b24 + 5f91c13):** Jamie provisioned both tokens in .env and said clear the queue. TINYLYTICS (site Yzx8dUUvUPn9AEJpTMeU / numeric 3762, NO kudos): client embed loader (hits+countries+events+beacon; skips /signin entirely - magic tokens ride that URL; scrubs login_token; localhost never tracks) + SERVER-SIDE events - VPC lambdas enqueue {v:1,kind:"tinylytics_event",event,value} on the EMAIL queue, the non-VPC relay batch-posts to api/v1/sites/3762/events/batch (Bearer, mandatory User-Agent for Cloudflare, 1000 req/hr limit). Taxonomy: mcp.tool_call / explore.tool_call (tool name only), site.access_request, site.signin (magic_link|code), site.feedback (category). VERIFIED LIVE: one tool call -> mcp.tool_call visible via Tinylytics API seconds later. BUTTONDOWN (Drop's model, adapted): NO plaintext email exists in any table, so enrollment happens RELAY-SIDE on kind=login sends (only approved accounts ever get login email); idempotent POST, 400=exists=untouched (unsubscribes never overridden), failures log-and-drop (retry would RESEND the login email). RELAY SEMANTICS SPLIT (pinned by tests): email retries hard -> DLQ; analytics + enrollment are best-effort and NEVER dead-letter. Both tokens merged into elixir-mcp/app secret via new infra/scripts/secret-add-keys.mjs (file->AWS in-process, values never printed). Privacy page discloses both; newsletter pixel-free per ratified policy. JAMIE-MANUAL: (1) confirm tracking pixels are OFF in the Buttondown newsletter settings; (2) if the Buttondown account hosts multiple newsletters, tell me the newsletter id and I'll set BUTTONDOWN_NEWSLETTER_ID (currently unset - token-scoped assumed). WATCH: first real sign-in should appear as a Buttondown subscriber.

**QUEUE (Jamie, 2026-09-05, clear autonomously):** (1) DONE clan pulse; (2) DONE tinylytics; (3) DONE buttondown; (4) DONE docs refresh (architecture depth: collectors/pulse/feedback-loop/relay + repo family links; README repo-shape fixed); (5) DONE detail-views v1; (6) POAP KINGS website: add Elixir MCP alongside Drop + Agent.

**DETAIL-VIEWS SWEEP (2026-09-05, built + audit):** BUILT: /account/feedback/<id> (item as addressable page), /admin/feedback/<id> (adds status + maintainer RESPONSE composer - the API field existed, UI never exposed it), /admin/collections/<slug> open-and-edit (member rows w/ per-row remove, meta form, Explore link; create drops into editor; the odd inline add/remove column is GONE). Routing: legalRoute already passes /section/page/sub paths; App hands itemId to Admin/Dashboard. AUDIT - identified, NOT built (for Jamie's triage): (a) USER COLLECTIONS UI IS MISSING ENTIRELY - api.myCollections/myCollectionAction are dead client methods; family/partner tiers carry curation quota (5/20) with NO surface. The CollectionEditor pattern is ready to reuse. (b) admin account detail page (claims, usage, overrides per account); (c) activity row -> call detail (args/meta); (d) admin gateway detail (account-side collector detail exists); (e) access-request detail (inline approve/deny is probably enough). Gateways table has a pre-existing thead/td column mismatch (6 headers, 8 cells).

**LOOKUP RESOLVES NAMES + NICKNAMES (2026-09-06, commit b1260f6):** Jamie picked this as the next improvement (recorder swap explicitly HELD - "hold off on elixir-bot changes for a bit"). The Explore lookup falls through to players_search when a query isn't a recorded tag or isn't tag-shaped: 1 match -> player record; several -> pick-list (name, nickname chip, tag, source, clan); 0 -> honest miss (copy covers names now). Nicknames rank first so "tyler" lands on Jamie's Tyler. Resolution order now: collection slug -> player tag -> clan tag -> deck hash -> ISO week -> NAME/NICKNAME -> miss.

**PUBLIC STATUS DASHBOARD (2026-09-06, commits 002c8be+6e9e991):** Jamie: "operational dashboard, nothing confidential, desktop OR mobile... queues, collectors, an hour snapshot." GET /api/public/status (no auth, 60s cache): queue depth/in-flight across pipeline + 4 DLQs (VpcLambdaRole +GetQueueAttributes read-only; SQS VPC endpoint covers it), collectors by CARD NAME ONLY (no IPs/machine labels - privacy pinned by test), 75min of 5-minute capture buckets (rejects amber), data-derived health verdict (recent admission + empty DLQs = ok). Data > Status page, 60s auto-refresh, mobile-first. LESSONS: (1) ApproximateAgeOfOldestMessage is a CLOUDWATCH METRIC, not an SQS attribute - GetQueueAttributes rejects it (nulls were silent until the catch logged); (2) never Promise.all queries on the one-pg-client-per-invocation pattern. PWA was built then DROPPED same hour (Jamie: "may be a mistake - just make it a mobile view") - manifest deleted, apple-touch-icon kept site-wide for bookmarks. Also: inline NICKNAME cell on Account > Overview players table (Jamie's ask; blur/Enter saves via the elixir_nickname bridge call; /api/me claims carry nickname). LIVE STATUS SHOWS: Wall Breakers (= magic-pines' card identity) pending, awaiting the cabin NAS start.

**PRIVATE PLAYER NICKNAMES (2026-09-06, 0035, contract 0.21.0, commit 8e3583a):** Jamie: "To me Raquaza is Tyler, and King Levy is Levi... just for my user." player_nickname (account_id, player_tag, nickname<=40) - strictly ACCOUNT data, never public/cross-account (privacy pinned by test). elixir_nickname set/clear tool; players_search matches nicknames RANKED FIRST (source 'nickname' - the agent payoff: "what's Tyler's win rate" resolves); players_summary + clans_roster carry nickname beside real name; Explore player record shows the nickname chip + inline editor (same bridge call). 35 tools.

**COLLECTOR ARENAS DROPPED (2026-09-06):** Jamie: arena-climb = confusing flavor; QUOTA EARNING is the real benefit; keep card identity, NAMES DERIVED FROM THE CARD ("collectors have a specific name"). Removed GATEWAY_ARENAS/gatewayArena from contracts entirely; ladder + /api/me/gateways + elixir_collectors return credits = floor(points/10) and name = card_name (operator name demoted to 'machine' label). Web collector page: earned-quota line (+N daily calls - 10:1, 4x cap, +2 player/+1 clan slot bonus) replaces the arena meter; tool title 'Collectors'. Note: this overrides the design handoff's 'collector ladder' fixed-vocabulary item and its arena footnote - Jamie's call supersedes.

**DESIGN HANDOFF IMPLEMENTED (2026-09-06, commit 9f6f8ee):** Jamie ran the Claude Design session (project dfac157b, imported via DesignSync after /design-login); the handoff README was a complete screen-by-screen spec with a drop-in styles.css. HEADLINE DESIGN DECISION honored: Explore is NOT a reporting product - the 7-tab charts direction was cut by the designer ("the agent is the analyst; the website's job is verification"). Explore rebuilt as the RECORD BROWSER: "Do we have it?" lookup -> six record kinds (player/clan/battle/deck/week/collection) + list records, breadcrumb trail (truncate-on-revisit), reference fields as links, one-tool-call-per-page toolbar with raw JSON + copy link, real deep-link paths, honest miss state. SERVER: battles_query gained battle_id + corpus deck_hash addressing modes (0.20.2; deck_stats deliberately carries NO pooled win rate - who-plays-it confound, notes point to battles_meta_decks). IDENTITY: Clash Royale display face (Fan Kit, vendored from Drop + compliance note) for brand shell ONLY; gold = YOURS (star/arena/two CTA buttons), never a data value; three nav tiers three shapes; honesty components (.sample/.caveat/.freshness); UNOFFICIAL disclaimer footer; notify = switch; 70rem console column, 13px tabular tables. Old Clan/Collections/Meta/Collectors views DELETED (absorbed). Charts only in Data (shared hover, partial-today at 35%). legalRoute guards stale bookmarks (prototype's own bug, fixed in its logic class, ported). Live + smoke green. LESSON: the .dc.html prototypes carried the real logic (tool mappings, legalRoute) in their script block - extract from the prototype, don't re-derive.

**GO COLLECTOR SHIPPED + WEB PROVISIONING (2026-09-06, collector v0.1.1+, elixir-mcp 0034):** Jamie approved by proceeding (created cabin CR key, "walk me through... a test of how collector provisioning works since it is the first you cannot just do both sides of"). GO PORT LIVE: full parity worker (pinned constants, envelope wire-exact, job passes through as RawMessage - byte-identical), release pipeline publishes 4 binaries + SHA256SUMS from every green main (v0.1.1 = first release); self-update = release check + SHA256 + atomic self-replace; 'dev' builds never update; run-forever.sh now binary-aware (Go path needs NO git NO Node on the host). Node src/ stays until the Macs cut over (GO-PORT §5). WEB PROVISIONING (Jamie mid-build: "config parameters and tokens would come from the web app... exception of the CR token"): ARCHITECTURAL CONSTRAINT - NAT-free VPC lambdas cannot reach the IAM API (no VPC endpoint exists) and no public lambda should hold IAM powers -> owner mints LOCALLY (infra/scripts/provision-gateway.mjs: idempotent IAM user + key rotation + rendered .env with CR placeholder + __GATEWAY_ID__ substitution, staged via {gateway_provision} op; SECRET NEVER PRINTED/ENTERS CONTEXT) -> operator downloads ONE-TIME from Account > Collector (claim-and-null atomic; RETURNING-sees-new-values trap fixed with locked self-join; gateway_config_claimed event). CABIN NEXT: Jamie raises hand with cabin name+static IP -> owner runs provision script -> one-time download -> paste CR key -> NAS runs collector_linux_armv7 under Task Scheduler.

**GO COLLECTOR PORT - DESIGN GATE (2026-09-06, collector docs/GO-PORT.md):** Jamie: jump straight to Go, no Node on the NAS. Design doc written, AWAITING REVIEW. Key decisions proposed: (1) self-update moves from git-checkout to CI-BUILT GITHUB RELEASES (hourly check, SHA256-verify, atomic self-replace, exit; update failures NEVER stop collection; COLLECTOR_PIN_VERSION rollback; artifact signing DEFERRED with honest rationale - a key in GH secrets dies in the same compromise - trigger = first outside operator); (2) queue-contract mirror = hand-written Go structs + committed JSON fixtures, drift fails tests, server-first rule unchanged; (3) migration order protects the live fleet: additive Go alongside Node, CABIN DS416 SOAKS FIRST, then Macs one at a time, Node removed last; (4) parity checklist pins the learned constants (1.5s pace, 5x403 breaker, live-first + 4s bulk wait, dev-build never self-updates). Server-side changes: NONE.

**CABIN COLLECTOR (DS416) FEASIBILITY + NON-MAC RUNNER (2026-09-06, collector 0d9a119):** Jamie wants a collector on the cabin's Synology DS416 (DSM 7.1.1 = base armv7 model). Assessment: collector is fully portable (two pure-JS deps, no natives, global fetch; launchd is the ONLY Mac piece); the strategic win is real - first true GEOGRAPHIC redundancy (all 3 collectors share one house/WAN today); the friction is armv7 (unofficial Node builds, no Container Manager) and the REAL gate is the cabin's static IP (CR keys are IP-allowlisted). SHIPPED to collector repo: scripts/run-forever.sh (KeepAlive-as-shell-loop, NODE_BIN override for armv7 tarballs), systemd unit template, DSM 7 Task Scheduler docs, engines>=18 (honest floor; 24+ recommended). JAMIE-MANUAL for enrollment: confirm cabin static IP; raise hand from Account > Collector; create cabin CR key allowlisting that IP; create per-gateway IAM user; hand creds out of band; probation -> active.

**SITE-IA BUILD-OUT COMPLETE (2026-09-06, commit 2455fa5):** the last piece - Explore intelligence. Player explorer gains the Pilot Score tab (score +/- SE, monthly trend table, full Level Curve with thin-sample honesty; curve is an ARRAY of {gap_range, n, win_rate}). Explore > Meta: segment picker (corpus / player collections via collections_browse / clan-tag input) over battles_meta_decks / battles_meta_cards / battles_trends. Clan & War: Standings (vs clan median), clan-wide Pilot Scores, Scouting Report (field names: mean_fame/max_fame/zero_fame_races - checked against the tool SQL, not guessed). EVERY SITE-IA SURFACE NOW EXISTS ON SCREEN - the parity debt from the IA audit is paid; the site is ready for the Claude Design session (handoff prompt delivered, docs/DESIGN-HANDOFF.md). Web/agent parity principle held throughout: every web view is an explorer-bridge call to the same registry tools agents use.

**ACTIVITY + COLLECTOR PAGES SHIPPED (2026-09-06 early, commit 114facf):** SITE-IA build-out continues. Account > Activity: three tabs - MCP requests (/api/me/requests, the account's OWN mcp_call_audit slice: tool/surface/ms/bytes/errors/args - Jamie's "see the MCP requests my user has made"), account events (existing /api/me/activity relocated off Usage), notifications (/api/me/events - READ-ONLY view of event_feed; NEVER advances events_seen_through, that cursor belongs to the agents - test pins it; unread rows past the cursor bolded). Account > Collector: run-a-gateway panel relocated from Overview + per-gateway detail via /api/me/gateway-detail (owner-scoped, ?id= query param - CloudFront forwards query strings, verified in template): 30-day fetch SVG series with admitted/rejected hover, 7-day endpoint mix, points, last success, running SHA. Column gotcha: gateway.last_seen_sha (0016), not gateway_sha. REMAINING from SITE-IA: Explore additions (Pilot Score tab, Meta page + segment picker, clan intel tabs).

**SITE IA RATIFIED + DATA SECTION SHIPPED (2026-09-05 night, commit 289106f):** Jamie's decisions: Data as tier-1 (love it); FULL history for dashboard series (retention later); Activity = three tabs; CR live leaderboards stay agent-only ("I don't want Elixir MCP to become a BI platform"); Home + Data must be CRAWLABLE. docs/SITE-IA.md is the nav spec of record. SHIPPED: GET /api/public/stats (no auth, 1h CloudFront cache, aggregates only - leak test pins it); Data tier-1 with Dashboard (full-history daily series as dependency-free inline SVG sparkbars + corpus totals) and Changelog (contract CHANGELOG rendered; SUBPATH import contracts/dist/changelog.js - the barrel pulls node:crypto via deck.js and breaks vite); Home touts live totals; CRAWLABILITY: deploy bakes stats+site text into index.html (#prerender div, app removes post-mount; skip-not-fail), robots.txt + sitemap.xml. Verified live: 37,719 battles / 40,484 players / 102 series days, baked text serving. STILL TO BUILD from SITE-IA: Activity 3 tabs (/api/me/requests + /api/me/events), Collector detail page, Explore additions (Pilot Score tab, Meta page, clan intel tabs). docs/DESIGN-HANDOFF.md written - the self-contained Claude Design prompt (POAP KINGS gold/crown + Drop purple-black lineage, landing as a DATA APPLICATION; deliverables: tokens, component language, chart language, 5 page layouts; IA and vocabulary locked).

**HEAT MODEL RETIRED (2026-09-05 evening, commit b55db49):** the A/B verdict authorized the planned retirement (heat was "kept one release as fallback"). Removed: ELIXIR_SCHED_MODE env knob, heat tiers + epoch decay, the pipeline re-heat write (heat=3), heat sort key. Flat cadences collapsed to {every, floor}; null-yield subjects keep explicit discovery defaults (battlelog 60m, profile 480m) - the "warm start" concern was already solved inside yieldCadenceMinutes. poll_state.heat column stays, DORMANT (no migration; operational table). Post-deploy probe: scheduler planning normally. The elixir-bot-imported heat model is now fully out of the system.

**META + TRENDS SHIPPED, FEEDBACK #8 FULL-CIRCLE (2026-09-05 late, 0.20.0+0.20.1+0033, commits 8baece9/ebdee85+):** Jamie greenlit features 2+4. SHIPPED 0.20.0: battles_meta_decks / battles_meta_cards / battles_trends over ONE shared segment abstraction (corpus | clan_tag | player_tag | collection) - EB-shrunk win rates (prior=segment mean, m=20), distinct-pilot counts (composition tell), usage shares, evolution forms never merge, SEGMENT_NOTE states the no-editorializing rule in-band. #4 needed NO Athena: battles/snapshots are lossless in Postgres (the deferred DATA-TOOLS lane was payload analytics); single-player weekly already existed (battles_performance group_by week) - the real gap was GROUP trends. FEEDBACK #8 (agent inference-error case study, filed at Jamie's request, actioned within the hour, 0.20.1): agent read the changelog's event-type enum + overnight gap as "Colosseum week just finished" - NAMED FAILURE CLASS: documentation of what CAN occur read as evidence that it DID. Affordances: war_current period block (period_index/war_day, started_observed_at anchor, period_end_nominal + week_end_nominal at ~10:00Z reset with drift note) so temporal claims CITE fields; event-type lists framed "schema, not news"; feedback category 'other' + unknown-category errors return the valid list (DB constraint had silently diverged from... actually matched the schema - the 400 was client/constraint-side; all three layers now in lockstep). BONUS: cr-agent-api-docs periodIndex corrected (season-monotonic, //7=sectionIndex - was described within-section). AWS creds expired mid-evening (~15:40Z, deploys blocked ~1.5h) - Jamie re-authed.

**YIELD A/B FULL-DAY VERDICT (2026-09-05 15Z, {ab_yield} op):** same-clock-hours comparison, Sep 3 15Z-Sep 4 15Z (heat model) vs Sep 4 15Z-Sep 5 15Z (yield scheduler), battles attributed to FIRST LIVE observation so archive replays contaminate neither side. RESULT: total fetch spend 1,976 vs 4,159 (47.5%); battlelog yield 2.788 vs 3.010 battles/fetch (92.6%) - and the baseline is INFLATED by one-time subject sweeps (240 battlelog subjects incl. first-poll 25-battle harvests vs 184 steady-state), so steady-state efficiency is at least equal. Raw battles captured (1,670 vs 8,991) is NOT a valid comparison for the same reason. VERDICT: the redesign's goal is met - half the spend at equal-or-better per-fetch efficiency; heat-path fallback can retire next release per the original plan. OPEN (not urgent): a true miss-rate audit (oldest-battle-already-stored check per poll) would upgrade "no evidence of loss" to "measured loss rate"; the KHAOS-era ~85% capture watch stands.

**METADATA TRUTH PASS (2026-09-05, contract 0.19.1):** Jamie: changelog stays (the pasted tree was a structure example); "verify all the metadata for the tools is accurate to the new structure." Audited all 31 declarations; fixed stale watch/claim/entitled-scope language across elixir_events, my_players (title "My players"), coverage, battles_compare, clans_roster/war_current/war_history (universal reads, defaults to YOURS), collectors (budget-vs-perks nuance); server instructions teach add+notify+events_pending. AUDIT CAUGHT A BEHAVIOR BUG: players_search still searched claims+clanmates only - now corpus-wide (universal reads) with claim>clanmate>corpus ranking. Pattern worth keeping: the stale-vocabulary sweep (regex over published declarations) - metadata drift is invisible until an agent trips on it.

**ADDED = RECORDED (2026-09-05, 0032, contract 0.19.0, commit 3c71ab5):** Jamie killed the whole add-vs-watch split hours after ratifying it: "all players and clans that are added are collected and recorded. Then the only setting is do you want them in your notification pipe." Model now: ADD (one act = claim/associate + record, tier slots are the only gate) / REMOVE (frees slot; clan recording stops when NO account has it added, else settles to widest remaining scope - ensureClanRecording/settleClanRecording shared by both doors) / NOTIFY on-off (the ONLY per-subject setting; feed fan-out honors it). clan_follow DROPPED (lived one migration - a do-nothing bookmark; Jamie's challenge "what would this do?" had no good answer for clans; for players the claim itself carries the association). account_clan = your added clans (scope+notify); claim.notify added; slots count ADDED things (claims / account_clan), not recordings. elixir_add_player/elixir_add_clan REPLACE elixir_watch_* (breaking rename); /api/recordings user route REMOVED (recording is not a user-facing toggle anymore); web panels: notify bell + Remove. STAR REFINEMENT (same session): primary claim = "me"; the primary player's clan auto-surfaces STARRED in Your clans as a suggestion with per-scope add buttons DIMMED when the tier lacks slots ("repopulate their clan for watching, dimmed if they don't have a role that can collect it"). Bot pin -> 0.19. LESSON: when Jamie challenges a state that exists only to hold UI together ("what would this do?"), the answer is usually to delete the state, not defend it.

**DECISIONS BATCH PROCESSED (2026-09-05, 0031, contract 0.18.0, commit 4caf069):** Jamie processed the review recommendations one by one. RATIFIED + BUILT: (1) ONE ENTITLEMENTS SYSTEM - ladder top rung role='owner' (super admin, exactly ONE; no admin can demote or affect the owner or another admin); admins SEE the console with day-to-day powers (requests, feedback, collections, roles up to partner via contracts canSetRole); owner keeps admin grants, service tokens, gateways, overrides; ALL gating derives from role now (is_owner = legacy column; resolvers compute isOwner/isAdmin from role; test seeds must stamp role explicitly). (2) CLAN WATCHING IS PURELY A USER FUNCTION - admin clan surface REMOVED ENTIRELY (page/routes/helpers); follow (add) is free, watch starts recording IMMEDIATELY within tier slots (no maintainer approval - the ladder is the gate); elixir_watch_clan action watch/follow/unwatch (BREAKING: no review requests); elixir_watch_player record:false; clan_follow table + backfill from existing watches; Your clans panel on Account > Overview. (3) PUBLISHED TOOL TREE - tools/list declares in canonical GROUP_ORDER (Elixir MCP, Collections, Battles, Cards, Clans, Live, Players, War) then title, so order-preserving clients render the domain structure, never a read/write split (note: elixir_changelog kept though absent from Jamie's pasted tree - flagged, not deleted). (4) /docs/<page> URL-addressable. (5) Terms-review doc updated IN PLACE to universal-reads posture (no revision history - no users yet). DECLINED/DEFERRED: RDS restore rehearsal (skip for alpha), DATA-TOOLS/Athena lane (defer entirely), fleet-headroom display (superseded by self-serve), service-token cap stays informational. LESSON: removing a route block by slicing between route names swallowed the unrelated routes sitting between them - slice between EXACT adjacent anchors or grep-verify what left the file.

**HOLISTIC REVIEW + P1 FIXES (2026-09-05, commit cf85e1e):** Jamie asked for a step-back review; three P1 items shipped same tick. (1) FEED EMITS MOVED POST-COMMIT: projectors collect feedEvents, pipeline flushes after commit - the inline emit's swallow-catch was FALSE SAFETY inside a txn (any insert error aborts the whole txn; the catch hides the cause while the commit then fails = ingest halts). CLASS LESSON: a .catch inside an open transaction protects nothing. (2) battles_recorded COALESCES per (account, tag): unread rows fold into the new event (counts summed, fresh event_id); read rows never touched - prevents heavy watchers (Jamie: 17 players + comprehensive clan) living with permanently-nonzero events_pending, which would train agents to ignore the hint. (3) DOOR ALARMS: mcp-errors, web-api-errors, mcp-latency-p95 - the pipeline was fully alarmed but the user-facing doors were not. (4) deploy.mjs now smoke-gated (exits nonzero unless smoke.mjs passes; first gated deploy ran 7/7 green). Also: elixir-bot PINNED_CONTRACT bumped 0.16 -> 0.17 (pin is warn-only, verified). REVIEW ITEMS DOCUMENTED, NOT YET DECIDED/BUILT: is_owner-vs-role='admin' seam (recommend: is_owner = UI/ops key, role = quotas only - needs Jamie ratification); RDS restore never rehearsed; DATA-TOOLS/Athena lane still the parked dependency for trend tooling; approval flow should show fleet headroom (~30 clans/collector); Docs pages not URL-addressable; terms-review doc still carries the pre-universal-reads stance; partner service-token quota informational only.

**ENTITLEMENT LADDER + SELF-SERVE + PUSH LANE (2026-09-05, 0029+0030, contract 0.17.0, commits 5546c2b+):** Jamie ratified the five-tier ladder (member 3p/1 activity clan; leader 5p/1 comprehensive; family 10p/3+3 + 5 collections; partner 25p/10+5 + service token + collector expected; admin unlimited) after asking for two rows between leader and admin. SPINE: roles NEVER gate visibility - they gate collection slots and call volume; contracts/src/roles.ts is the single source; nullable account columns (max_player_recordings, mcp_daily_quota, NEW live_daily_quota) stay as hand-tuned overrides; operator bonus +2p/+1 activity clan stacks below partner. Enforcement swapped at BOTH doors (the two-doors-never-disagree rule): recording caps, live cap (was flat 50/day), daily quota, clan-watch slots by scope (elixir_watch_clan refuses before filing), collection caps. SELF-SERVE: tier panel + upgrade request on Account > Overview (feedback lane, kind role_upgrade_request, dupe-guarded); Admin > Accounts page (role picker, pending-request flag); family+ curate own collections on the Collections page (ownership = governance). Docs > Roles is PUBLIC per Jamie. PUSH LANE: event_feed table, implicit subscriptions (claims/recordings/feedback ARE subscriptions), fan-out on write; topics battles_recorded (coalesced, backfill-guarded like heat), clan_war_week_finished (null->set + 7-day recency guard), feedback_responded (both respond doors), recording_started/stopped, role_changed; elixir_events tool (cursor/topics/mark_seen) + meta.events_pending; 30-day prune in sweep_operational; {account_role} ops-lane op (list/set, service-name lookup). Backfill verified live: owner -> admin; elixir-bot service token rides the owner account so the bot inherits admin. 31 tools. LESSON: db/schema.fingerprint must be re-pinned from a FRESH scratch DB (cli defaults to the stale dev DB). NEXT (Jamie's five-feature ask): recorder swap design doc, pro-corpus meta intelligence, trend/time-series tools.

**TWO-TIER WEB NAV + COLLECTIONS UI (2026-09-05, commit 45b17ed):** Jamie: "the web apps navigation is a real mess... revisit it entirely... two tier... leaves room to grow" + "I don't see collections anywhere in the web app." Rebuilt around four tier-1 areas with a tier-2 page row: EXPLORE (Player / Clan & War / Collections / Collectors - ladder promoted from the Dashboard basement), ACCOUNT (Overview / Agents / Usage / Feedback), DOCS (own ToC), ADMIN (Requests / Clans / Collections / Collectors / Tokens / Feedback / Usage). Old flat paths redirect (REDIRECTS map in App.jsx); SECTIONS config is where growth lands. Collections: public browse via the explorer bridge (Explore > Collections, players link into the player explorer) + owner curation panel (create/visibility-toggle/add-remove tags/delete) backed by GET/POST /api/admin/collections (owner-gated, tested). Dashboard/Admin panels page-scope via a `hidden` prop - components intact, effects still mount once. Deployed + deep links verified live.

**FEEDBACK #6 (unprompted!) -> LEADERBOARDS + LIVE-LANE FIX (2026-09-05, 0.16.1):** the 0.16.0 initiative instructions produced their first unprompted filing within hours. Shipped: rankings paths on the live_fetch allowlist (collector PATH_BY_ENDPOINT + admission validators + identity-accreting projector - every ranked tag lands a player row); verified live: PoL global top-100 in 3.5s (trophy rankings genuinely empty late-season per docs). BONUS ROOT CAUSE: live fetches raced the collector's 10s bulk long-poll against the 8s live window (the census live_unavailable watch) - bulk wait now 4s, live window 12s, lane ~3-5s. Closes that watch.

**FEEDBACK ROUND TWO (2026-09-05, 0028, contract 0.16.0) + STANDING LOOP DUTY:** feedback #4 (agent, on the feedback system itself) + #5 (Jamie +1s) actioned: elixir_changelog (what-shipped-since-contract-X, backfilled to 0.6.0 - APPEND AN ENTRY WITH EVERY CONTRACT BUMP, same-commit rule); shipped_in/related_tools on responses; meta.feedback_responses_pending until elixir_my_feedback marks seen; status/since filters; server instructions + elixir_feedback description make friction-filing the AGENT'S job. Jamie standing order: CHECK {feedback_pending} EVERY LOOP TICK and action/respond per the closed-loop rule. Parked: friction-pattern nudges (5c) until volume.

**UNIVERSAL READS RATIFIED + COLLECTIONS SHIPPED (2026-09-05, 0027, contract 0.15.0):** Jamie: "make all battles accessible to everyone... RoyaleAPI has done this for years... once we start adding a player they are in the data. No limitations." All recorded GAME data readable by every approved account (scope 'public'; clan tools accept any recorded clan); ACCOUNT data stays private; leadership gate stays. Supersedes claims/clanmate read gating AND the terms-review "own-context only" launch stance (public-API-data posture). Privacy page rewritten. COLLECTIONS: owned editorial groupings (never global tags - the governance answer is ownership); collections_browse/get; creation owner-only v1 via {collection} ops (clan families = the expansion; admin UI = follow-up). FIRST COLLECTION LIVE: 'pros' - 11 pro players (Mohamed Light + Light family, CAL Sub Kun, Ryley, Pedro, SandBox, Clown, SK Morten), all recording within the hour, Mohamed Light already at 30 battles captured. Pro-scoped corpus intelligence (pro Level Curve / meta) is the compounding follow-up. 29 tools.

**FIRST AGENT FEEDBACK - FULL CIRCLE (2026-09-05, contract 0.13-0.14):** Claude (as user, via Jamie's connector) filed 3 items - the first real send_feedback use. Per Jamie's rule the LOOP CLOSED FIRST (0026: response/responded_at; elixir_my_feedback tool + /api/me/feedback; admin respond + {feedback_respond} op) - feedback is never actioned invisibly. THEN actioned same night: #1 clans_pilot_scores (whole clan in one call, was 18; also the parked pairs-perf lever) + include_curve flag; #2 players_search (name->tag in entitled scope, honest empties) + mode taxonomy already shipped in 0.12.0; #3 praise for sample-sizes/SEs/load-bearing-notes -> cited here as evidence the charter works. All three responded; requester verified seeing responses through its own tool. 27 tools.

**MCP REQUEST EFFECTIVENESS REVIEW (2026-09-05, Jamie's ask; census op {audit_census} now standing):** 7 days / 220 calls: ZERO truncation, 6.4% errors all structured (no internals), all three surfaces real (mcp 180 / web 21 / svc:elixir-bot 19); after rename-accounting only elixir_feedback + watch tools never called (both brand-new). ACTIONS TAKEN: battles_levels was 5.4s avg (40x fleet) -> 0025 stamps deck_avg_level at ingest + backfill, clans_standings lateral -> grouped (now 0.3-0.8s warm). FOLLOW-UP PARKED: battles_levels still ~4.5-6s warm (cost = 66k-row pairs materialization per request; next lever = ingest-maintained level_pairs rollup). WATCHES: cr_api_live 2/6 live_unavailable; KHAOS-era event-burst capture ~85% of bot's (25-battle window vs yield cadence during sprees). Blind spot noted: unknown-tool calls are rejected pre-audit, so a post-rename broken client is invisible - audit them.

**EXPERIENCE COHORTS SHIPPED (2026-09-04 night, 0024):** tenure from the YearsPlayed badge (level=completed years, progress=account age in DAYS - verified live); stamps at profile projection, backfilled from hot payloads; ABSENT = unknown never zero (measured: usually sub-1-year per Jamie's rule, but 2/21 badge-less accounts carry 2024 event badges - docs patched). battles_levels player block gains experience + cohort (pilot percentile among same-tenure corpus players, floors 5/30); clans_standings rows carry years_played. Live: raquaza pilot +0.095 = 81st percentile of 37 in years_3_5 (median +0.029); King Thing tenure-unknown served honestly. Perf: pairs set materialized once per request (temp table, rollback-guarded) after a 4-scan timeout.

**LEVEL CURVE + PILOT SCORE + SCOUTING REPORT SHIPPED (2026-09-04 evening, contract 0.11.0):** battles_levels (66k-obs binned curve, floors, no extrapolation; pilot_score = actual - level-expected with monthly trend + experience/band confound notes) and war_rivals (observer-deduped rival fingerprints, bracket default). BRANDING (Jamie): metrics carry the brand - Pilot Score ("wins your card levels can't explain"), Level Curve, Scouting Report; tagline territory "numbers with receipts". Live-verified via svc:elixir-bot: King Thing pilot -0.102 +-0.035 (trend Jul -0.085 / Aug -0.118, both months n~100); bracket report incl. los claneros 14/14 zero-fame finished races. Preview op {preview_intel} remains as validation scaffolding. Experience cohorts (YearsPlayed badge projection) = queued v2.

**Intelligence roadmap REPRIORITIZED (Jamie, 2026-09-04 evening):** meta deck/card tools drop to third; the two tracks "more actionable for the player" lead: (1) LEVEL ECONOMICS (battles_levels - binned win-vs-level-gap curves; corpus has 98.4% both-sides levels; gap distribution TIGHT per census: median 0.00/p90 0.75 - matchmaking compresses; within-match slope vs positional effect separated); (2) WAR RIVALS (war_rivals v1 clan fingerprints from projected bracket standings - census: 124 rival clans, 88 with 3+ races, observer-deduped; rival rosters = v2 projection review). Designs in META-INTEL sections 9-10, REVIEW PENDING before build. Probe gained level_census + rival_census.

**ELIXIR-BOT PHASE 1 LIVE (2026-09-04):** the first real consumer. elixir-bot's member trend, war attendance, and NEW get_clan_standing (backed by clans_standings, contract 0.10.0) route DIRECTLY here via its service token (svc:elixir-bot) - no shadow mode (Jamie: not enough volume), local tables as error fallback only. Client elixir_mcp.py (None-on-failure), builders capabilities/mcp_stats.py (emit local shapes). Live-verified end to end: trend block, 5/5 season attendance, rank-15-of-48 standing. Battle-intel views stay bot-local by design. Deferred gaps ON RECORD: clan-relative tooling shipped (clans_standings); early-history hole (bot has Apr 18-May 7, we start May 7) ACCEPTED by Jamie ("doing its best to see everything; we may miss some things and that is okay"); player-adjusted lift stays bot-side for now.

**Clan recording SCOPES (Jamie, 2026-09-04, 0023):** clan_scope stamped on the recording row - 'activity' (clan only: roster/war/standings) vs 'comprehensive' (plus every open member's battles+profile, roster-following). ALL existing clans backfilled comprehensive (behavior preserved; POAP KINGS explicitly wants it - "ALL activity by EVERYONE regardless of connection" - and elixir-bot will need it). Scheduler member fan-out + eligibility filter on scope; scope changes take effect next tick (downgrade = member rows go dormant, upgrade = re-seed). Admin panel toggle; watch_clan requests carry scope.

**Domain-first tool names + service tools (Jamie, 2026-09-04, contract 0.9.0):** tool NAMES lead with the domain (players_*, battles_*, clans_roster, war_*, cards_catalog, live_fetch, elixir_*) - names are the one grouping mechanism every client honors. CLEAN BREAK, no legacy aliases (Jamie: alpha, no outside users) - any stale roster must reconnect. The Elixir MCP domain owns account/service actions: elixir_watch_player (claim+record via MCP, web caps enforced identically), elixir_watch_clan (reviewed request through feedback triage, context.kind=clan_watch_request), elixir_data_insights, elixir_collectors. 21 tools; write set = the service domain's three. Jamie's further domain ideas absorbed; future service-domain candidates: unwatch/stop-recording, usage/quota view (web has it, tool doesn't).

**Tool taxonomy SHIPPED 2026-09-04 (Jamie's ask, contract 0.8.0):** TOOL_GROUPS in contracts is the one classification source (Players/Battles/Clans/War/Cards/Live/Elixir MCP, mirroring cr-agent-api-docs; send_feedback lives in the service group). Registry throws on unclassified tools; annotations carry grouped titles + readOnlyHint (16 read-only) + openWorldHint (cr_api_live). /docs Tools page added. NOTE: connector UIs cache the roster - Jamie must reconnect /mcp to see the groups.

**Yield gap found+fixed by monitoring (2026-09-04 ~17:30Z):** battlelog fetches collapsed to ~3/hr mid-colosseum — yield cadences only re-tighten AT a poll, so members whose EWMA learned dormant stay stale exactly when play resumes. Fix: projectRiverRace raises signaled members' battlelog yield_bph from decks_used deltas (one shared 30m riverrace poll names every battler); raise-only, replay-guarded. Watch the battlelog rate recover during war hours. Also verified 0022 re-stamp working (day1 119->289 stamped, unstamped 808->451); (135,4) war_day-null rows persist at ~177 — some are honest boundary nulls, keep an eye.

**Yield scheduler A/B — FIRST HONEST READ (2026-09-04 18:45Z, delivered to Jamie):** heat (Sep 4 07-13Z, clean post-import): 133 fetches/hr, 94 battlelog/hr, 79 battles/hr, 0.84 battles/battlelog-fetch. Yield (15-18Z): 60 fetches/hr (-55%), 27 battlelog/hr, 28 battles/hr, 1.04 battles/fetch (+24%). War polling steady (~25/hr) so war_current freshness unaffected; battlelog's 25-battle window means lower cadence defers capture, never loses it. Delta-feedback visibly working: 18Z caught 25 battles in 10 targeted fetches (2.5/fetch). Caveat: windows are different clock hours — same-hours 24h A/B completes Sep 5 morning; direction already unambiguous (equal coverage, ~half the spend). Superseded:  probe op added to the migrate lambda ({probe: true}) — hourly battlelog/player/war fetch counts (live gateways only) vs battles harvested, 48h window. First reading right after the 14:45Z deploy shows the expected first-sweep burst (256 battlelog fetches/239 subjects in the deploy hour — every subject had null yield_bph) moderating within the next hour. Honest before/after needs the EWMAs settled; re-probe after several hours. NOTE the archive-import hours dominate any 48h battle counts until Sep 6 — compare fetch volume and battles-per-fetch on LIVE hours only.

**MCP testing round 1 COMPLETE (2026-09-04):** 3 personas, 10 fixes shipped (contract 0.3.0) — see commit 19e1e70. Wishlist added for Jamie's triage (beyond pilot round's): evolution facet on get_card_performance / with_evolution filter; per-card win_rate_delta vs baseline; slim get_collection (drop iconUrls) + owned_evolutions_only; suggested one-card-swap deck comparisons; query_battles total_count; per-era completeness gap map in get_coverage (auditor found a day where snapshot delta 10 vs 3 recorded battles — backfill-era days under-captured, only visible per-day); clarify lifetime battleCount vs recorded-battle mode sets; warnings array for future-window empties. BOTH testers hit the frozen-roster cursor-schema mismatch — reconnect /mcp (also unlocks war tools for round 2). Perf: R1 battlelog projector 408->274ms; riverrace still ~3.1s post-0015 — race/stamp timing split deployed to attribute it (in flight).

**Product direction (Jamie, 2026-09-04): Elixir MCP is a DATA PRODUCT.** Primary interfaces: account management, data exploration, product updates, activity logs + quota, data collection. Self-improvement loop is first-class: agent/user feedback via web AND an MCP write tool (agent feedback = that user's feedback); MCP activity logs are product signal; changes must reach users simply. Approved queue additions (all with Jamie's picks): (a) feedback loop + product-updates surface; (b) collector credits -> owner quota at 10:1, capped 4x base (500->2000); (c) collectors split to their own repo elixir-mcp-collector (small clone for operators; contracts stay canonical here); (d) service-token lane for elixir-bot NOW (Admin-issued long-lived tokens, per-token call visibility) + a note in elixir-bot's tooling that Elixir MCP exists — no functionality moves yet; (e) S3 payload archive per DATA-TOOLS.md: BUILD APPROVED; (f) fun: gateways named after CR cards with the card as profile picture. The scheduler yield redesign IS his priority-queue point (server orders one queue; collectors drain it). (g) WEB DATA EXPLORER (Jamie, 2026-09-04): a browser way to explore what the MCP serves. V1 interpretation (Jamie: open to interpretation): /explore page + ONE bridge endpoint POST /api/explore {tool, args} invoking the SAME tool registry (entitlements/honesty/shapes identical; session-authed; audited surface='web' — explorer usage is its own product signal). Views: subject picker, Summary, Battles table, weekly Trend charts (hand-rolled SVG), Decks, Collection grid, War (when entitled), Coverage. Every future tool becomes explorable for free; users see exactly what their agents see. (h) SITE DOCUMENTATION SECTION (Jamie, 2026-09-04): /docs on the web UI — index/ToC + About, Privacy, Terms, and Architecture (technical docs users can read and admins can reason from; distilled from DESIGN.md). Markdown in-repo, rendered at build time, ships with web deploys. MAINTENANCE RULE goes in AGENTS.md: any change altering architecture or user-facing behavior updates site docs in the same commit. Batched with the explorer (same surface). Work order after self-update gap fix: scheduler redesign -> data explorer + feedback loop -> credits->quota -> S3 archive -> collector repo split -> card avatars -> service tokens.

**Self-update gap (found live 2026-09-04):** comparing checkout vs origin never fires on a machine where the checkout advances by development (HEAD==origin always; running process stays old). Fix: restart whenever checkout HEAD differs from the SHA the process STARTED on — one rule covers dev and operator machines.

**Decision pass (Jamie, 2026-09-04 morning):** (1) scheduler yield-per-call redesign: GO, direct build (not shadow), heat path kept one release as fallback. (2) api_payload retention REFRAMED: payloads move to S3 tiering (Postgres keeps latest-per-entity hot set; every admitted payload archives to S3 content-addressed, lifecycle to IA) — design doc docs/DATA-TOOLS.md first, covering Athena/S3 Tables/DuckDB query tooling and what data tools Elixir MCP should use broadly; REVIEW BEFORE BUILD. (3) Wishlist: build weekly win-rate series + headline summary call + deck sort/share/total_count; per-era completeness map and evolution facet parked. (4) elixir-mcp-alarms -> projects-ops-alerts WIRED (raw delivery, queue policy updated, handoff note in sysadmin notes/). (5) Gateway self-update from green main: GO (hourly fetch/pull/exit + SHA in heartbeats, Admin shows fleet versions). Work order: gateway self-update -> wishlist batch -> DATA-TOOLS design doc -> scheduler redesign. Jamie still to reconnect /mcp for round-2 war-persona testing.

**Budget-efficiency redesign — DRAFT for Jamie's review (2026-09-04):** replace heat (immediacy) with EXPECTED YIELD per call. Every endpoint's value is measurable from our own receipts: battlelog yield = new battles admitted per fetch (the archive census shows it directly); profile yield = snapshot deltas; riverrace yield = points/standings changes (war days >> training days). Proposal: (1) score each (subject, endpoint) with an EWMA of recent yield-per-fetch from api_receipt + projection outcomes — the data already exists; (2) scheduler ranks eligible jobs by yield x staleness instead of heat tiers, with the existing starvation floors unchanged (they are correctness, not efficiency); (3) battlelog cadence adapts per player automatically (a 20-battles/day player polls hourly; a dormant one daily) — this is what hot-player heat approximated, derived instead of asserted; (4) war-day awareness moves from fixed 30-min riverrace cadence to yield-driven (training days decay to ~2h, war days tighten); (5) fleet: keep single global budget + SQS competition (observed working with 2 gateways); per-gateway budget splits only if a gateway misbehaves — the trust model already covers that via draining. Measurable target: battles-captured-per-API-call and snapshot-completeness at HALF today's call volume. Implementation is a scheduler-only change (plan.mjs) — no schema, no gateway changes. AWAITING JAMIE's go.

**Scheduler budget-efficiency redesign (Jamie, 2026-09-04, queued after the archive import + R1):** the heat model was imported from elixir-bot's "hot player" feature — which optimized IMMEDIACY and was of questionable value even there. The real objective is efficiency of API calls across the known gateway fleet: spend the global budget where it buys the most recorded value (battles captured per call, war-window coverage, snapshot completeness), not where activity happened most recently. Rethink CADENCE/heat from that frame; fleet-aware lease distribution belongs in the same design. A second production gateway (jamie-mac-2, second CR key, same host) is live specifically to make multi-gateway behavior observable before this design.

**V2 build order COMPLETE.** Open Jamie-manual / later items:
- ~~Flip clan scope to verified-claims-only~~ MOOT: verification retired 2026-09-03, claims are trust-based; the knob was removed with it.
- **Cards endpoint was NEVER SCHEDULED (found by post-deploy verification 2026-09-03):** ingest/tools/fixtures knew `cards`, but CADENCE, seedPollState, the eligibility SQL, and the gateway path map all missed it — prod had zero catalog, get_card_catalog 503'd, and the 0011 backfill silently stamped decks unconverted against an empty catalog. Fixed (daily cards/GLOBAL job + gateway path + GLOBAL freshness in pipeline); 0012 repaired the stamped-raw decks using the exact IngestFunction-update instant from CFN stack events as the raw/display discriminator, verified live (Jamie's maxed deck reads all 16s, norm=2). Lessons: (a) an endpoint exists only when the SCHEDULER knows it — fixtures make everything downstream pass; (b) a data backfill must fail loudly when its reference data is missing, never stamp-and-continue; (c) verify data migrations through the real door immediately after deploy.
- **Card levels normalized 2026-09-03 (Jamie's live bug report):** API levels are rarity-relative; everything we serve now uses the in-game 1-16 display scale via contracts displayLevel — THE one conversion (elixir-bot's five-call-site scar imported). Ingest stamps deck.norm=1; 0011 backfilled stored decks (norm-guarded, double-shift-proof); get_collection normalizes; cr_api_live stays RAW by design and says so; CONTRACT_VERSION 0.1.1. Any future field that carries a card level must pass displayLevel at the seam.
- **Visibility & quotas shipped 2026-09-03 (Jamie-approved plan):** 0009 account.mcp_daily_quota (default 500, row-update override) + quota headroom in every tool meta; /api/me/usage + Dashboard Usage panel; /api/admin/usage (per-account, per-tool, budget line vs 86,400/day capacity + heaviest subjects); Connected agents panel (family list + Disconnect); 0010 account_event activity log (signed_in, claim_added, recording_started/stopped, gateway_raised, connection_revoked, agent_connected) + Recent activity panel; recording cap (default 5 active player recordings/account, owner exempt); per-recording + per-gateway fetches/24h. Verification-attempt caps were dropped from the plan (verification retired). Retention note: prune mcp_call_audit.args after ~90 days when it matters.
- **Ratified 2026-09-03: being in a clan IS sharing your battles with it.** share_battles_with_clan toggle removed (UI, route, entitlement lane); rule 2 clanmate access covers full battle history of open members. The column stays in the ladder unused. Rationale: battlelogs are public in-game and via the API; the toggle gated convenience access to public data.
- Per new gateway operator: IP-allowlisted Supercell key + per-gateway IAM user (recv/delete request queues, send results, PutMetricData), handed over out of band; then "Begin probation" in Admin.
- Gateway auto-update (launchd runs from checkout; kickstart after gateway-code pulls) — a self-update or deploy reminder still wanted.
- Subscribe elixir-mcp-alarms to the sysadmin projects-ops-alerts queue.
- Delete the Fastmail draft that carried the JMAP token (Jamie).
- Capacity: ~30 clans saturates one gateway's practical throughput; volunteer fleet is the lever.
- Timeline 'season' granularity — contract-minor, when wanted.

2026-09-03 audit outcomes (DESIGN.md §11–§12 added): versioning designed (migration ladder + fingerprint test; contracts package + serverInfo cache-buster + deprecation rules; audit-row tuning loop). Build-blockers closed: DB access path (migrate Lambda + break-glass), SQS 256KB (gateway gzip, alarm on overflow), live request lane (second queue, gateways drain first), budget state in Postgres. Scope changes ratified: `get_player_timeline` into V1 (11 tools), season-roll watcher into V1, **repo public from day one** (fixture discipline from first commit).
