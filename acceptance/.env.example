# Copy to .env (mode 0600). The .env is ignored by git; this example is not.
# An AGENT principal named "acceptance", clan #J2RGCRVG, scope cr:read only,
# minted locally and registered by hash through the migrate lambda's
# {principal} op (acceptance/README.md has the recipe). Its door is its own.
ELIXIR_MCP_URL=https://elixir.poapkings.com/a/<public_id>/mcp
ELIXIR_MCP_TOKEN=svt_replace_me
