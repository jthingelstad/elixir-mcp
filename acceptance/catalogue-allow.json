{
  "*": {
    "on_behalf_of": "an argument on agent connections, named in notes about the caller",
    "display_name": "same",
    "request_id": "meta, named by hints",
    "contract_version": "meta",
    "trophy_change": "a loss on the arena's trophy floor carries trophy_change null: the note says the game omits the field",
    "pre_reset": "on a series row only at a season roll",
    "season_roll": "same",
    "pol_trophies": "the profile's name for the board's rating, pointed at by rankings notes"
  },
  "elixir_changelog": {
    "tools_added": "on an entry only when a version added tools"
  },
  "players_names": {
    "in_corpus": "on unknown[] rows, present only when a tag is unknown"
  },
  "clans_roster": {
    "members_not_in_race": "war_current's field, pointed at"
  },
  "war_current": {
    "points_earned": "days_closed's per-day fields, absent until a war day closes",
    "end_of_day_rank": "same"
  },
  "docs": {
    "_": "tokens the docs use as prose or as another surface's fields; each with its reason",
    "live_status": "rides only on live: true reads, which the suite never makes",
    "observed_ip": "operators page: owner-only console fields, never on the MCP surface",
    "silent_since": "same",
    "nominal_period_elapsed": "documented as removed at 4.0.0",
    "filters_applied": "documented as a key that does NOT exist",
    "limit_applied": "same",
    "notify_off": "an account preference the timeline docs name, not a response field",
    "pol_league": "the glossary's name for the profile's league field",
    "towers_before": "on a battle row only when the record holds the boat/tower counters for that battle",
    "towers_after": "same",
    "modal_arena": "on trophy_floor only for a player with a floor in the window",
    "last_battle_at": "on a `returned` timeline item, a rare kind",
    "returned_after_days": "same",
    "player_activity": "a timeline entry kind for a player subject; the acceptance principal is a clan agent and follows no player",
    "training_today": "deprecated at 7.2.x (decks_today covers every race-week day); war_current serves it only on a training day, so a war-day run never sees it",
    "roster_and_war_only": "an activity basis served only for an activity-scope clan (members' logs not recorded); a run over recorded clans never sees it",
    "account_role_changed": "timeline: an account item written only when an admin changes the account's role; the suite's account has had none"
  },
  "battles_query": {
    "king_tower_fell": "a VALUE of inferred.duration.basis, not a field; the note explains the three rules",
    "regulation_ran": "same",
    "overtime_expired": "same"
  }
}
