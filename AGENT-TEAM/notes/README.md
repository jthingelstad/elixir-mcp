# Run notes

One file per finding, watch, or proposal worth keeping beyond the run —
named `<topic>.md` (or `<year>-W<week>-<topic>.md` when time-bound).
Durable decisions still land in `docs/NOTES.md`; this directory is the
team's working memory. A healthy no-op run needs no file here.
