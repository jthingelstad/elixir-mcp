{
  "name": "@elixir-mcp/contracts",
  "version": "0.1.0",
  "description": "Single source of truth for the Elixir MCP tool contract: tag normalization, error taxonomy, deck identity, meta envelope.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.6.0"
  }
}
