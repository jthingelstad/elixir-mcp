{
  "name": "@elixir-mcp/design",
  "version": "0.2.0",
  "description": "The Elixir design system: tokens, component styles and Tailwind utilities, compiled once and shared by the static site and the console. One compiled file, two consumers - never a copy. Verticals import the sources into their own Tailwind entry.",
  "type": "module",
  "exports": {
    "./styles.css": "./dist/styles.css",
    "./tokens.css": "./src/tokens.css",
    "./components.css": "./src/components.css"
  },
  "scripts": {
    "build": "tailwindcss -i src/index.css -o dist/styles.css --minify=false",
    "dev": "tailwindcss -i src/index.css -o dist/styles.css --watch",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
