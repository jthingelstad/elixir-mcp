{
  "name": "@elixir-mcp/scheduler",
  "version": "0.1.0",
  "description": "The planning tick: heat decay, fairness floors, global budget settle, job selection (DESIGN \u00a75.1-5.3). Runs as the EventBridge-scheduled Lambda, reserved concurrency 1.",
  "type": "module",
  "main": "src/plan.mjs",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@elixir-mcp/contracts": "0.1.0",
    "pg": "^8.13.0",
    "@aws-sdk/client-cloudwatch": "^3.600.0"
  }
}
