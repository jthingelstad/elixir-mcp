{
  "name": "@elixir-mcp/scheduler",
  "version": "0.1.0",
  "description": "The planning tick: the session clock, fairness floors, the global budget and job selection into the ledger. Runs as the EventBridge-scheduled Lambda, reserved concurrency 1.",
  "type": "module",
  "main": "src/plan.mjs",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@elixir-mcp/contracts": "0.1.0",
    "pg": "^8.13.0"
  }
}
