{
  "name": "@elixir-mcp/web-api",
  "version": "0.1.0",
  "description": "Site API for elixir.poapkings.com (DESIGN §2, §6.1): request access, magic-link sign-in + __Host- session cookie, claims, recordings, timezone, owner admin (approvals, gateway roster).",
  "type": "module",
  "main": "src/handler.mjs",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.600.0",
    "@elixir-mcp/auth": "0.1.0",
    "@elixir-mcp/claims": "0.1.0",
    "@elixir-mcp/contracts": "0.1.0",
    "pg": "^8.13.0"
  }
}
