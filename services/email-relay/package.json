{
  "name": "@elixir-mcp/email-relay",
  "version": "0.1.0",
  "description": "The non-VPC email sender: S3 invokes it for each message in the outbox, and it sends from elixir@poapkings.com over SES (or Fastmail JMAP).",
  "type": "module",
  "main": "src/handler.mjs",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1137.0",
    "@aws-sdk/client-sesv2": "^3.1137.0",
    "@elixir-mcp/contracts": "0.1.0",
    "@elixir-mcp/mail": "0.1.0"
  }
}
