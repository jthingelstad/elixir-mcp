{
  "name": "@elixir-mcp/jobs",
  "version": "0.1.0",
  "description": "Scheduled product jobs (clan pulse, sweeps) - split from the migrate Lambda so schema-capable code never runs on a timer.",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "@aws-sdk/client-s3": "^3.600.0"
  }
}
