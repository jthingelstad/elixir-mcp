{
  "name": "@elixir-mcp/ingest",
  "version": "0.1.0",
  "description": "Admission boundary + canonical projections: raw CR payloads in, receipts/battles/roster observations out. A library: the collector door's submit (web-api) runs it inline; there is no ingest Lambda.",
  "type": "module",
  "main": "src/handler.mjs",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1126.0",
    "@elixir-mcp/claims": "0.1.0",
    "@elixir-mcp/contracts": "0.1.0",
    "pg": "^8.13.0"
  }
}
