# The migrate ops

Every payload key `services/migrate/src/lambda.mjs` dispatches, one row
each. `services/migrate/test/ops-catalogue.test.mjs` compares the two and
fails `npm run verify` when a dispatched op has no row here, or a row names
an op the dispatcher no longer has. How to run an op, and the rules for
doing it on production, are in `SKILL.md` beside this file.

**The convention the test parses.** An op's row starts with its name in
braces, in backticks: `` | `{stats}` | ``. Nothing else in this file
starts a table row that way.

**The columns.**

- **Payload** is JSON to paste into the invoke. `true` means the op takes
  no arguments; otherwise the keys shown are the ones the op reads, with
  its defaults where they matter.
- **Does** says Read, or **WRITE** and exactly what it writes. A write op
  on production needs its owner's authority, named in the row: an
  objective runbook that grants it, or Jamie. **Heavy** means the op scans
  a large table or the whole corpus on a db.t4g.small: run it once, and
  expect `elixir-mcp-migrate-duration` if it passes 90 s.
- **Named by** is the runbook, skill, doc or script that already uses the
  op. "none" means only the source and the dated notes know it.

**Only `{}` runs the migrations.** That is the call `deploy.mjs` makes. A
payload whose key the dispatcher does not know, a misspelt op name or a
falsy value such as `{"stats": false}`, is refused with `"error":
"unknown_op"` and runs nothing (since 2026-09-25; before, it fell through
to the migration runner and applied anything pending). Copy the key from
this file and pass `true` or an object.

## Census and audits

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{stats}` | `{"stats": true}` | Counts of the record's main tables (accounts, active recordings, players, open memberships, battles, snapshots, war rows, receipts by endpoint, audit calls); board health (`ranking_health`: enabled, fresh and stale PoL locations, today's global tick receipts, ranking recordings, the global snapshot); the last hour's battle-log edge filter; the last 24 h of fetch errors by endpoint and status. | Read. Several `count(*)` over large tables. | run-elixir-mcp.md (yield and budget), keep-the-boards.md (the snapshots arrived), AGENT-TEAM/README.md |
| `{tables}` | `{"tables": true}` | Where the database's weight and churn are: each table's heap, index and TOAST bytes, live and dead tuples, inserts, updates and deletes since the stats reset, scans, last autovacuum; the memory settings and `TimeZone`; the database size. | Read (catalog statistics). | AGENT-TEAM/README.md |
| `{inspect}` | `{"inspect": true}` | The older storage census: table sizes, dead tuples, scan and autovacuum counts, and the 25 largest indexes with their scan counts. `{tables}` answers more; this one adds index sizes. | Read (catalog statistics). | none |
| `{probe}` | `{"probe": true}` | Hourly fetches against battles harvested over 48 h (live collectors only); the week's war battles by war day (`UNRESOLVED` is a war battle outside every war period); the last day's card-level gap; rival clans fingerprinted; the event ledger's last day by type; the `pros` collection's capture. | Read. **Heavy**: Run Elixir MCP forbids it as a routine check. Run it once and never retry on a 429. | run-elixir-mcp.md (on demand only), AGENT-TEAM/README.md |
| `{sessions}` | `{"sessions": true}` | Every sign-in session of the last 30 days by account (email-hash prefix and role): minted, last seen, both expiries, and whether it is live, idle-expired, capped or revoked. The factual answer to "why do I keep signing in?". | Read. No tokens, no addresses. | none |
| `{war_drift}` | `{"war_drift": true}` | Each clan's observed war-period start offset from the 10:00Z policy reset (min, max, median) and the overall spread. An upper bound: every offset includes polling latency, so read the spread, not one clan. | Read. | none |
| `{capture_audit}` | `{"capture_audit": {"days": 1}}` (1 to 30) | Which subjects are behind the battle-log capture gaps, with their gap times and their `poll_state` planned and admitted times, so a subject's cadence can be told from fleet pressure. The public status carries only the total. | Read. | none |
| `{poll_replay}` | `{"poll_replay": {"days": 7}}`, also `to`, `capacity`, `followups`, `ceilings`, `active_min`, `cold_ceiling`, `cutoff` | The session clock replayed over up to 14 days of battle-log polls for a grid of follow-up and ceiling values, beside the actual polls; and the battles lost, from the profile's lifetime counter against captured rows, split by audited gap (`cutoff` splits before and after a deploy). | Read. **Heavy**: every poll and snapshot interval in the window. | none |
| `{enum_census}` | `{"enum_census": true}` | The distinct values, with counts, of the columns the schema review wanted CHECKed (event types, mode group, poll hint and period type, snapshot kind, type class, deck selection, battle type, deck card count) and the orphan counts the NOT VALID foreign keys are validated against. A CHECK is written from this. | Read. **Heavy**: group-bys over the battle tables. | none |
| `{battle_fidelity_census}` | `{"battle_fidelity_census": true}` | For every column of `battle` and `battle_participant`, how many rows carry a value, by type class and by type: the column the manifest promises and the projector never fills. | Read. **Heavy**: a full scan per grouping. | none |
| `{mode_shape_census}` | `{"mode_shape_census": true}` | What each battle type contains: game mode and deck selection, trophy stakes and the Ladder loss rule, Ranked `trophy_change`/`starting_trophies` by league and outcome, monthly and daily calendars, deck levels, event tags and their reuse across types, the meta population by type, duel rounds, global ranks. | Read. **Heavy**: many full scans. | none |
| `{deck_census}` | `{"deck_census": true}` | Card-row integrity: participants whose deck has no `deck` row or no played rows, collection rows with no catalog card, stub cards, and the totals. All zero was the gate for the closing foreign keys. | Read. **Heavy**: two anti-joins over `battle_participant`. | ENGINEERING.md ("`{deck_census}` proves nothing is missing") |
| `{name_census}` | `{"name_census": true}` or `{"name_census": {"min_players": 50}}` | Whether the shape of a display name goes with performance, over battles, ranked boards and profiles: counts and means per name feature. No name or tag leaves the database. | Read. **Heavy**: the whole pvp corpus. | DECISIONS.md (name-pattern studies go through it) |
| `{archetype_census}` | `{"archetype_census": true}`, or `{"season": "2026-09", "top": 40}` | The archetype grammar over every deck: family distribution, top labels by players, the average-elixir histogram per win condition (where `CYCLE_MAX` was measured), and the unattested cards that define fallback decks (the Understand Clash Royale queue). | Read. **Heavy**: every deck and its cards into the Lambda. | apps/site/src/docs/archetypes.md (the cycle bound) |
| `{archetype_sample}` | `{"archetype_sample": true}`, or `season`, `labels`, `per_label`, `fallback`, `random` | Decks with their cards beside the stamped label, for a debug read: the top decks of the most-played labels, the top fallback decks, and a battle-weighted random draw, this season. | Read. | none |
| `{audit_census}` | `{"audit_census": {"days": 7}}` or `{"audit_census": {"from": "<ISO>", "to": "<ISO>"}}` | The product-signal read of `mcp_call_audit`: per tool volume, errors, truncation, latency and size; per surface, client and principal kind; cold starts; where the wall time goes; top errors; `live_fetch` by path; resources and prompts read; calls since ship for each shipped feedback item; declared tools nobody called. | Read. | none |
| `{args_census}` | `{"args_census": {"days": 7, "tool": "battles_query"}}` (`tool` optional) | Which argument keys each tool was called with, by outcome, and the key sets: what trips validation, what a client sends. Never values. | Read. | none |
| `{call_sequence_census}` | `{"call_sequence_census": {"days": 30, "gap_s": 120, "min_run": 5}}` | What agents call around a call: turns (one account's calls with no gap over `gap_s`), their collapsed shapes, and long paged runs of one tool, the sign of a missing tool. | Read. Loads every audit row in the window into the Lambda; keep `days` short. | none |
| `{acceptance_catalogue}` | `{"acceptance_catalogue": {"days": 7, "per_tool": 3}}` | The most frequent answered argument sets per read-only MCP tool (caller identity and live reads dropped) and each tool's duration percentiles: the input to the acceptance catalogue. | Read. | acceptance/catalogue.mjs (`--refresh`) |

## Explain and diagnostics

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{profile_tool}` | `{"profile_tool": {"tool": "clans_standings", "args": {"clan_tag": "#J2RGCRVG"}, "principal": "acceptance", "explain": true}}` | One real call of a read-only, recorded-only tool through the registry's own handler, as the principal a service token name belongs to (default `acceptance`, an agent with no player of its own, so name the player in `args`): every query timed, and with `explain` each read's plan summary, slowest first. The drift-proof way to see where a tool spends its time. | Read. The session is `default_transaction_read_only`; write tools and the live lane are refused. Explaining stops after 40 s. | none |
| `{explain_participation}` | `{"explain_participation": {"clan_tag": "#J2RGCRVG", "weeks": 8}}` | EXPLAIN (ANALYZE, BUFFERS) of every `clans_participation` query for one clan, with the tool's own SQL (`participation-sql.mjs`); the visibility map of `battle_participant` and `battle`; the first read again with only index paths open. | Read. | ENGINEERING.md (`{explain_*}`) |
| `{explain_standings}` | `{"explain_standings": {"clan_tag": "#J2RGCRVG", "days": 30, "mode": null}}` | EXPLAIN (ANALYZE, BUFFERS) of the one `clans_standings` query as the tool builds it (`standings-sql.mjs`). | Read. | ENGINEERING.md (`{explain_*}`) |
| `{explain_timeline}` | `{"explain_timeline": {"request_id": "<prefix>"}}`, or `{"account_id": "<uuid>", "from": "<ISO>", "to": "<ISO>"}`, or `{"as_agent_of": "#CLAN"}` | One `elixir_timeline` read replayed with the timeline's own builder: time per query, counts (never items), and the plans of the two clan queries an empty window's cost lives in. A request id takes the account and arguments from the audit row. | Read. Marks nothing read. | ENGINEERING.md (`{explain_*}`) |
| `{explain_meta}` | `{"explain_meta": {"clan_tag": "#J2RGCRVG", "days": 28}}` | EXPLAIN (ANALYZE, BUFFERS) of the pieces of a clan-scoped `battles_meta_decks` or `battles_meta_cards` call, and of the corpus aggregates on the population table. | Read. Carries its own copies of the SQL, and they have drifted: its excluded breakdown still joins `battle`, which the tool dropped. Use `{profile_tool}` for the served SQL. | ENGINEERING.md (`{explain_*}`) |
| `{explain_series}` | `{"explain_series": {"clan_tag": "#J2RGCRVG", "player_tag": "#TAG", "days": 180}}` | EXPLAIN (ANALYZE, BUFFERS) of the shapes `clans_timeline`, `clans_members_timeline` and `players_timeline` run. | Read (read-only transaction). Carries its own copies of the SQL; check them against the tool, or use `{profile_tool}`. | ENGINEERING.md (`{explain_*}`) |
| `{activity_preview}` | `{"activity_preview": {"owner": true}}`, or `account_id`, `as_agent_of` or `subjects`, with `from`, `to`, `timezone` | The timeline synthesized from the live record for one reader, as that person would see it. Written before the 2.0.0 timeline shipped so Jamie could read entries as a person. | Read. Returns that account's timeline items. | none |

## Captures

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{refusal_census}` | `{"refusal_census": {"code": "invalid_tag", "days": 14, "limit": 300}}` | The argument values behind a refusal code, per tool and surface, read from the captured requests (tag-shaped arguments only), with one example message. | Read: the audit and S3 `calls/`. A missing capture is counted, never guessed. | none |
| `{controls_census}` | `{"controls_census": {"days": 7, "limit": 400}}` | For each control a response can carry (comparable false, floored, partial bucket, season crossed, completeness note, segment omitted, pooled modes, one player), how often it fired in the captured calls of ten tools, and what the same account called next within ten minutes. | Read: the audit and S3 `calls/`. | none |

## Series

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{series_status}` | `{"series_status": {"hours": 1}}` | The series tables' rows and day spans; the snapshot table by writer (roster, profile, both); clan and war state fill; what the last N hours of admitted clan, player and race receipts were worth; the last day's arena moments. | Read (read-only transaction). | none |
| `{series_census_self}` | `{"series_census_self": {"since": "2026-03-12"}}` | Self-consistency: every admitted clan receipt's game day has its clan row and member rows (misses named and explained), every admitted profile receipt's day has its profile row, battles still missing the ten backfilled columns, and each backfill lane's cursor. | Read (read-only transaction). **Heavy**: its statement timeout is 250 s. | none |
| `{series_backfill}` | `{"series_backfill": {"lane": "clan", "budget_s": 45, "batch": 50}}`; `lane` is `clan`, `player`, `race` or `battle`; `"reset": true` walks the lane again | Fills the series tables from the S3 payload archive: walks the lane's admitted receipts in order, runs the series half of the projector on each archived payload, commits a batch at a time until the budget is spent, and returns `remaining`. | **WRITE**: clan rows and members' roster columns; snapshot profile columns, progress buckets, frozen counters and PoL finals; rivals' columns and period logs; the ten battle columns on `battle` (battle lane); `series_backfill_state`. Reads S3 `payloads/`. Holds the function for the whole run. Authority: Keep the Record True, for a repair the archive can replay, by the `migration` skill; finish with `{vacuum}`. | infra/scripts/series-backfill.mjs (the driver loop) |

## Record repairs and imports

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{archetype_stamp}` | `{"archetype_stamp": true}` | Re-stamps every deck behind the current grammar and vocabulary version, then counts decks by `archetype_version`. The nightly jobs run does the same after its rollup. | **WRITE**: the archetype columns of `deck`, the whole table (about 205k rows) after a vocabulary import or a grammar bump. Authority: leave it to the nightly; by hand only when Keep the Record True or Jamie asks for the repair. | run-elixir-mcp.md reads the nightly's `archetype_stamp` log field, not this op |
| `{duel_round_decks}` | `{"duel_round_decks": true}`; then `{"duel_round_decks": {"after": ["<battle_id>", "<player_tag>", <round>]}}` with the `after` it answered, until `done`; `budget_s` (45, up to 280), `batch` (200) | Fills each duel round recorded before 0182 with its deck (`deck_hash`: its eight cards, no tower troop) and its result (`outcome` by its crowns against the other side's same round); `remaining` ahead of the cursor, `still_null` overall (a round without eight cards or a side's crowns stays null). | **WRITE**: `battle_participant_round.deck_hash` and `outcome` where null; new `deck` and `deck_card` rows for the round decks, named at once (`archetype_*`); widens a deck's `first_seen_at`/`last_seen_at`. Rows only, no event. Authority: Keep the Record True, feedback #363, by the `migration` skill; finish with `{vacuum}` on `battle_participant_round`, `deck_card` and `deck`. | services/migrate/src/ops-duel-rounds.mjs |
| `{card_roles_import}` | `{"card_roles_import": {"roles": [], "aliases": [], "roles_version": "<commit time>", "source_commit": "<sha>"}}` | Replaces the archetype vocabulary with cr-agent-api-docs' `card-roles.json` and `deck-aliases.json`, validated the way the docs build validates them; refuses a card the catalog has not seen. | **WRITE**: deletes and reinserts `card_role` and `deck_alias`, upserts `card_role_version`, in one transaction. Authority: the deploy runs it every time; never by hand. | infra/scripts/import-card-roles.mjs, run by infra/scripts/deploy.mjs; ENGINEERING.md ("Grammar in code, vocabulary in data") |
| `{collection}` | `{"collection": {"op": "upsert", "slug": "<slug>", "title": "<title>", "kind": "player", "description": null, "visibility": "public"}}`; `{"collection": {"op": "add", "slug": "<slug>", "tags": ["#TAG"]}}` (or `"remove"`); `{"collection": {"op": "reconcile", "slug": "<slug>"}}` | Collections by ops: create or update one under the owner; add or remove members through the helper every door uses; or re-apply a collection's settings to the members it holds, starting any recording that is missing. | **WRITE**, every sub-op: `collection`, `collection_member`, and recordings started or stopped (added means recorded). Authority: Jamie. The board collections are Keep the Boards' through `clients/boards` over MCP, not this op. | none |

## Accounts, principals and grants

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{account_role}` | `{"account_role": {"list": true}}`; `{"account_role": {"account_id": "<uuid>", "role": "<role>"}}` or `{"service": "<token name>", "role": "<role>"}` | `list`: every account (email-hash prefix, status, role, owner flag, live service tokens, players and clans tracked) and every gateway. Otherwise sets one account's role. | `list` reads. Otherwise **WRITE**: `account.role` and a `role_changed` account event, which shows in that person's timeline. Authority: Jamie. | none |
| `{account_email}` | `{"account_email": {"email": "<address>"}}` | Sets an account's contact address, for an account made before 0046 or one with a wrong address. The hash comes from the address, so it can only ever set an account's own. | **WRITE**: `account.email` and an `email_recorded` account event. The payload carries a person's address. Authority: Jamie. | none |
| `{account_enroll}` | `{"account_enroll": {"dry_run": true, "source": "ops", "fill_empty": false, "accounts": [{"email": "<address>", "player_tag": "#TAG", "clan_tag": "#TAG"}]}}` | Enrolls people who never asked (built for elixir-bot's mail recipients, 2026-09-18): an approved account each, the player claimed as primary, the clan tracked at activity scope, no welcome mail. An existing account is skipped unless `fill_empty` and it tracks nothing. | **WRITE** unless a dry run, and `dry_run` defaults to true: `account`, an `enrolled` or `filled` account event, the claim and recording (`addPlayer`), `clan`, `account_clan`, `onboarded_at`. Authority: Jamie. | none |
| `{account_track}` | `{"account_track": {"primary_tag": "#TAG", "player_tag": "#TAG", "relationship": "alt", "dry_run": true}}` | Tracks a player on someone else's account at its owner's word. The account is named by its primary tag, so nothing personal is in the payload. | **WRITE** unless `"dry_run": true`, and `dry_run` defaults to false: a claim through `addPlayer` (quota, recording start, `claim_added`) and a `tracked_by_ops` account event. Authority: Jamie. | none |
| `{principal}` | `{"principal": {"kind": "agent", "name": "<name>", "clan_tag": "#J2RGCRVG", "token_hash": "<sha256 hex>", "scope": "cr:read", "owner_service": "<token name>"}}` | Registers an agent whose key was minted locally; only the sha256 is sent. The owner is the `owner` account unless `owner_service` names another. Integrations are refused (`use_integration_api`). | **WRITE**: `account` (the agent), `account_clan`, `service_token`. Authority: re-minting the `acceptance` or `gym` principal by the acceptance recipe; any other principal is Jamie's. | acceptance/README.md (the credential), gym SKILL.md (re-minting) |
| `{service_token_limits}` | `{"service_token_limits": {"name": "gym", "hourly_rate_limit": 900, "daily_quota": null}}` | Sets a live key's own ceilings by token name: an hourly limit gives the key its own bucket; null clears it back to the owner's. | **WRITE**: `service_token.hourly_rate_limit` and `daily_quota`. Authority: Jamie. | gym SKILL.md (queued for Jamie), acceptance/README.md |
| `{integration}` | `{"integration": {"action": "list"}}`; or `create`, `configure`, `rotate`, `revoke`, `suspend`, `resume` or `retire_legacy` with the admin route's body (`name`, `id`, `token_hash`, scopes and limits, `collection_id`) | The admin integrations route, run as the owner: list them with the collections available to grant, or change one. `create` and `rotate` take a locally minted `token_hash`; `retire_legacy` revokes the old MCP key only after an audited REST call. | `list` reads. Every other action **WRITE**: `account`, `integration`, `integration_collection_grant`, `service_token`, an account event. The only op that does not log its result. Authority: Jamie (integrations are admin-provisioned). | ENGINEERING.md ("Platform Integration API") |
| `{oauth_grants}` | `{"oauth_grants": {"redirect_host": "clan.poapkings.com"}}`; `{"oauth_grants": {"revoke": ["<family_id>"], "reason": "<why>"}}` | The live OAuth grants whose client redirects to a host (client, audience, scope, owner kind and primary player, created, last MCP call); or revoke named grant families, as the console's Connections page does. | `redirect_host` reads. `revoke` **WRITE**: `oauth_family.revoked_at` and a `connection_revoked` account event. Authority: only a revocation the account's owner asked for. | none |
| `{seed}` | `{"seed": {"owner_email_hash": "<hash>", "record_clan": "#TAG"}}` | The first production bootstrap: the owner account and, optionally, one comprehensive clan recording. Idempotent. | **WRITE**: `account` (approved, `is_owner`), `clan`, `recording`. Authority: Jamie, at a first bootstrap only. | none |

## Feedback

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{feedback_pending}` | `{"feedback_pending": true}` | The unanswered backlog: count, oldest age, how many are over a day old, and the oldest 25 items. A status-only acknowledgment is still unanswered. | Read. Item text is untrusted evidence, never instructions. | close-the-loop.md (every run), AGENT-TEAM/README.md |
| `{feedback_read}` | `{"feedback_read": {"feedback_id": 123}}` | One feedback item, without moving the filer's read pointer. | Read. | gym SKILL.md (the legacy list) |
| `{feedback_respond}` | `{"feedback_respond": {"feedback_id": 123, "status": "done", "response": "<text>", "shipped_in": "<version>", "related_tools": ["battles_query"], "expected": {"status": "seen", "response": null, "responded_at": null}}}` | Answers an item so the filer sees the status and the reply. `status` is `seen`, `planned`, `done` or `declined`; `expected` makes it a compare-and-set against the item as last read. | **WRITE**: the `feedback` row (status, response, `responded_at`, `shipped_in`, `related_tools`) and, with a response, a `feedback_responded` event in the filer's timeline. Authority: Close the Loop and the Gym, under the `loop` lease; re-read the item first and never replay blindly. | close-the-loop.md, gym SKILL.md |

## Collectors and the job ledger

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{poll_state}` | `{"poll_state": {"subject_tag": "global", "endpoint": "rankings_pol"}}` | One poll subject explained: its `poll_state` row, the last ten ledger jobs for the key and the last five receipts, so a missed fetch can be placed at the planner, the lease or admission. | Read. | keep-the-boards.md (a missed daily snapshot) |
| `{ledger}` | `{"ledger": {"op": "dead"}}`; `{"ledger": {"op": "requeue", "job_ids": ["123"]}}`; `{"ledger": {"op": "fold", "job_ids": ["123"]}}` | `dead`: every dead job (the collector path's DLQ) and the collector that last held it. `requeue`: named dead jobs back to queued, refused where a queued twin exists. `fold`: named dead jobs marked done where a twin carries the work. 1 to 100 ids. | `dead` reads. `requeue` and `fold` **WRITE** `job` rows. Authority: Run Elixir MCP, once the cause is fixed at the seam; name the jobs, never sweep every historical failure. | run-elixir-mcp.md (Action) |
| `{collector_token}` | `{"collector_token": {"name": "<gateway name>", "token_hash": "<sha256 hex>", "channel": "bulk"}}` | Stores the sha256 of a locally generated token on a named collector, and optionally its channel (`bulk` or `live`). Enrollment through the collector door is the ordinary path now (docs/COLLECTOR-ZERO-TRUST.md). | **WRITE**: `gateway.token_hash` and `channel`; the old token stops working. Authority: Jamie. | none |
| `{collector_release}` | `{"collector_release": {"platform": "<platform>", "version": "v2.0.NN", "sha256": "<hex>", "url": "<artifact url>"}}` | Sets the update authority the collector `config` endpoint serves for one platform. Collectors update to it within the hour. | **WRITE**: the platform's `collector_release` row. Authority: the maintainer, through `name-collector-release.mjs` by docs/RELEASING-COLLECTOR.md; never by hand. | docs/RELEASING-COLLECTOR.md, infra/scripts/name-collector-release.mjs |
| `{gateway_drain}` | `{"gateway_drain": {"name": "<machine or card name>"}}` | Moves an active or probation collector to draining, so a machine that is off on purpose reads as stopped, not silent. | **WRITE**: `gateway.status`. Authority: Run Elixir MCP for a collector submitting errors or bad data (its Action section), else Jamie. | `AGENT-TEAM/run-elixir-mcp.md` (incident authority) |
| `{gateway_recover}` | `{"gateway_recover": {"name": "<machine or card name>"}}` | Un-quarantines a draining or probation collector: back to active, missed-lease streak cleared. For the night nobody is at the console. | **WRITE**: `gateway.status` and `missed_streak`. Authority: Run Elixir MCP once a drained collector's fix is confirmed (its Action section), else Jamie. | `AGENT-TEAM/run-elixir-mcp.md` (incident authority) |

## Maintenance

| Op | Payload | Answers | Does | Named by |
| --- | --- | --- | --- | --- |
| `{backends}` | `{"backends": true}` | What every connection to the database is doing (pid, user, application, state, wait, age, the head of its query) and the connection count against `max_connections`. It retries its own connect for about 200 s, so it answers when the pool is exhausted. | Read. | none |
| `{terminate_backends}` | `{"terminate_backends": {"like": "%<query text>%", "older_than_s": 120}}` | Ends this database user's own backends whose query matches and has run longer than the threshold (at least 30 s): the orphan a killed Lambda leaves holding its locks. | **WRITE**: terminates backends. Every service connects as the same database user, so a loose pattern ends live door queries too, and `true` means `%battle_participant%`. Read `{backends}` first and name the query. Authority: Run Elixir MCP during an incident, on a migration or backfill backend holding its query past five minutes (its Action section), else Jamie. | `AGENT-TEAM/run-elixir-mcp.md` (incident authority) |
| `{vacuum}` | `{"vacuum": {"table": "battle_participant"}}`; the table is `battle`, `battle_participant`, `battle_participant_card`, `battle_participant_round`, `player_snapshot_daily`, `deck`, `deck_card` or `meta_season_pop` | VACUUM (ANALYZE) one table so its visibility map is rebuilt and index-only scans work again; returns `relpages` and `relallvisible` before and after. VACUUM cannot run inside a migration's transaction, hence an op. | **WRITE** (maintenance: the visibility map and planner statistics; no row changes). **Heavy** on the battle tables. Authority: Keep the Record True and the `migration` skill, to finish a backfill. | keep-the-record-true.md (Action), ENGINEERING.md |
| `{rewrite_table}` | `{"rewrite_table": {"table": "battle_participant"}}`; the table is `battle_participant`, `battle_participant_card` or `deck_card` | VACUUM FULL one table, to return the space a dropped column held. | **WRITE**: rewrites the whole table under an ACCESS EXCLUSIVE lock for its duration; collector submits and every read of the table wait. If it outlives the 300 s Lambda, the backend keeps the lock after the Lambda dies (the shape of the 2026-09-15 outage). Authority: Jamie, in a window he chooses. | none |
